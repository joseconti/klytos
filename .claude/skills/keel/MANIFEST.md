# Keel Manifest — v5.19.2

One file, three tables, one purpose: looking ONLY at this file, a session can tell (1) whether a project contains everything Keel requires at its current phase, (2) which skill files changed in which Keel version — so after an update it knows exactly what to re-read, without interpreting the changelog — and (3) what concrete actions each version asks of an existing project (the reconciliation delta).

**When to read this file:** after EVERY skill update (it is the post-update reconciliation's first input — see `references/project-state.md`), and any time a full parity audit is wanted. **Authority rule:** the phase references are the contract; this manifest is the verifiable index of them. If they ever disagree, the reference wins and the mismatch is a release bug — fix the manifest.

## Table 1 — What a Keel project must contain

Verification is phase-aware and condition-aware: read the project card and phase status in `docs/PROGRESS.md`, then check that every row required AT OR BEFORE the current position (whose condition the project meets) exists. Anything missing joins the reconciliation's batched plan — whatever version introduced it. Rows marked "conditional" depend on a recorded choice (project card) or a project trait; their absence is only a gap when the condition holds.

| Path (in the project) | What it is / template source | Required from | Condition |
|---|---|---|---|
| `docs/PROGRESS.md` | Living state: project card, phase status, position, deferred items (template: `references/project-state.md`) | Phase 1 step 0a | Always |
| `docs/decisions.md` | Append-only decision log (same template source) | Phase 1 step 0a | Always |
| `docs/lessons-learned.md` | Append-only problem→solution log (same) | Phase 1 step 0a | Always |
| Off-machine durability — a Git remote, or a tree that replicates off this computer | The work must survive this machine (SKILL.md "Work never lives only on this machine"): checked with commands (`git rev-parse --git-dir`, `git remote -v`, the tree's absolute path), recorded on the project card's `Durability:` line, and — where the user declined — carrying its accepted-risk D-entry with the consequence stated. `scripts/keel-verify` fails a remoteless repo that has no such record | Phase 1 step 0a (before anything is created); adoption step 2 | Always |
| A clean working tree at every block close | Nothing left uncommitted: commits land on `develop` or on a work branch bound for it, and a repo with only `main`/`master` gets `develop` created first (SKILL.md "Git flow") | Phase 1 step 0a | Always (where the project is a Git repository) |
| `CLAUDE.md` + `AGENTS.md` (repo root) | The portability lock: the same Keel block between `KEEL:BEGIN/END` in both files, stamped `— vX.Y.Z` | Phase 1 step 0a | Always |
| `GEMINI.md` or `context.fileName` in `.gemini/settings.json` | Gemini CLI's lock mirror (the recorded pick) | Phase 1 step 0a | Only if the user works with Gemini CLI |
| `.claude/skills/keel/` + `.agents/skills/keel/` | Embedded copy of the whole skill, version-synced, both trees identical | Phase 1 step 0a | Only if embed accepted (card: `Keel portability:`) |
| `docs/00-competitive-landscape.md` | Competitive scan artifacts | Phase 1 step 0 | Unless the scan was skipped on record |
| `docs/01-discovery.md` | Discovery document (verdict + user decision recorded), including `## Environment & test drivers` — the §5a preflight: what is present, what is missing, what is impossible on this machine, the screen-stealing verdict per platform, whether this session can run commands where the repo lives, what this session's environment cannot do at all (network, deletion, `localhost`, screen — or "none found"), and the `claude`-on-PATH verdict where chaining is wanted | Phase 1 | Always |
| `docs/estimate.md` | Estimate v1 preliminary → firm (per `references/estimation-budget.md`) | Phase 1 close | Always |
| `docs/token-ledger.md` | Actual token usage, one row per session | Phase 1 close | Always |
| `docs/02-functional-spec.md` | Functional contract (incl. design split with foreseen assets, per-screen a11y, breakpoints); every acceptance criterion carries its stable `AC-nn` ID | Phase 2 | Always |
| `docs/03-technical-plan.md` | Stack, architecture, code map with `[E]`/`[A]`/`[G]` markers, change map, conventions, testing plan (frameworks + commands + playground recipe + **driver per surface with its headless verdict, element-addressability convention, division of labour with tags, static-analysis commands, accessibility automation, read-back duty**) and **`## Environment requirements`** (the source `scripts/keel-doctor` compiles from) | Phase 2 | Always |
| `docs/threat-model.md` | Assumptions, defended controls each carrying a delivery state (`IN PLACE`/`TO BUILD`/`MANUAL`/`VERIFY`), and the "Not defended" table of deliberate omissions with their consequences (`references/phase-2-functional-spec.md` §4c) | Phase 2 | Always |
| `docs/flows/` | One file per multi-step/branching journey | Phase 2 | Always |
| `docs/budget.md` | Client-facing budget, approved | Phase 2 close | Only if `Client budget: yes` (card line, asked at Phase 1 step 10) |
| `docs/spec-references/` | Reference artifacts carrying a requirement (tests-as-spec, ported code with its provenance/license) | Phase 2 | Only if the spec records any (`## Reference artifacts`) |
| `docs/rubrics/` | Recorded judgment criteria per domain, applied at the Phase 2 §6a review | Phase 2 | Only if a rubric domain was accepted at §6a |
| `docs/design/references/` | Rich visual references (HTML mockups, prototypes, artifacts) shipped with the Phase 3 brief — input to Design, never a build source | Phase 2 | Only if the user holds any |
| Assistant rules — one container per accepted tool (`.claude/rules/`, `.cursor/rules/`, `.github/instructions/`, `.windsurf/rules/`, nested context files for Codex/Gemini) | Path-scoped rules from the plan + security profile (`references/assistant-config.md`) | Phase 2 close | Only if accepted (card: `Assistant config:`) |
| Assistant subagents — per capable tool (`.claude/agents/`, `.github/agents/`, `.cursor/agents/`, `.gemini/agents/`) | Reviewer/verifier subagents (same source) | Phase 2 close | Only if accepted (card: `Assistant config:`) |
| `docs/design/DESIGN-BRIEF.md` | Brief handed to Design (user-approved, bracket-clean) | Phase 3 | UI projects only |
| `docs/design/design-handoff/` | The returned handoff per `references/handoff-contract.md` (Design's delivery, or the recorded no-Design branch) — holds NOTHING else, ever (contract rule 10: wholesale-replaceable) | Phase 4 start | UI projects only |
| `docs/BUILD-SPEC.md` | Consolidated faithful-build contract (§1 is the evidence table) | Phase 4 | UI projects only |
| `docs/design/design-requests/` | Numbered DR register | Phase 4 | When the first Design Request appears |
| `.gitignore` + `.gitattributes` | Hygiene boundaries (full rules at Phase 7); `.gitignore` ALWAYS includes `CLAUDE.local.md`, `.claude/settings.local.json`, `.keel-update-check` (the machine-local update-check throttle stamp), and `docs/continuation-prompt.md` (ephemeral session hand-off, never committed), plus the accepted tools' personal files (`AGENTS.override.md`, `.gemini/.env`, `.gemini/tmp/`) | Phase 5 scaffold | Always |
| `docs/sprints/` | One file per sprint: YAML frontmatter (`schema: keel.sprint/1`, sprint, goal, status, and per slice `id`/`title`/`status`/`hours`/`depends_on`/`criteria`) as the DATA, markdown body as the prose (`references/project-state.md`, "The sprint plan") | Phase 5 | Always |
| `docs/sprints/deferred.md` | ONE backlog file: everything wanted and not in this version, same item schema plus `target:` (proposed version or `null`) and `reason:`. Ids share one namespace with the sprint slices, so promoting an item is a MOVE that keeps its id. Its hours are what the next version would cost and are NOT in the Phase 5 total | Phase 5 | Always |
| `docs/.keel/plan.json` | The whole plan derived into one machine-readable file — totals and every percentage computed from the hours, never stored in the sources; regenerated, never hand-edited, and drift from its sources fails `scripts/keel-verify` (`references/project-state.md`) | Phase 5 | Always |
| `docs/05-test-points.md` | Test-point log, all columns including evidence (commands + output + commit), the two coverage columns — `Criterion` (the `AC-nn` ID) and `Coverage` (`driven` or one of the eight delegation tags — never free text) — and `Red first` (one of the five fixed values, per the project's `Test-first policy:`) | Phase 5 | Always |
| `docs/api/INDEX.md` | One line per public surface | Phase 5 first slice | Always |
| `docs/keel-conformance.md` | The conformance sweep: one row per applicable MANIFEST Table 1 requirement with its state (`present` / `missing` / `declined` + D-entry / `n/a` + condition) — derived from this manifest, never from recollection (SKILL.md "Applying Keel completely") | Phase 1 step 0a; always at adoption step 4a and at every post-update reconciliation | Always |
| `docs/playground.md` | Playground access + try-it instructions + seed/reset commands + `last verified:` stamp | Phase 5 scaffold — or the first maintenance/adoption touch on a project that never had one | If the project can be run |
| `scripts/keel-verify` | The project's own release linter (`references/phase-5-development.md`) | Phase 5 scaffold — or the first maintenance/adoption touch on a project that never had one | If the project can be run |
| `scripts/keel-doctor` | The environment doctor compiled from the technical plan's `## Environment requirements`: `--check` / `--plan` / `--fix` / `--json`, four states, nothing global installed without the user's OK on an itemized list (`references/test-automation.md`) | Phase 5 scaffold — or the first maintenance/adoption touch on a project that never had one | If the project can be run |
| `scripts/` build/minify script (name per the technical plan) | Regenerates every `*.min.*` from its committed unminified source (`references/phase-5-development.md`); run locally by the working assistant, never CI — the source-first assets contract | Phase 5 scaffold | Only if the project ships front-end JS/CSS (source + minified pair) |
| `scripts/keel-handoff-verify` | Runs the five mechanical courier checks over `docs/continuation-prompt.md` (containment, `Repo`, `Commit`, `Generated` as epoch, `Tree`) and prints one line each plus `VERDICT: CONTINUE\|STOP` — the session NEVER composes these checks itself (`references/project-state.md`). Needs one allow-list entry: `Bash(./scripts/keel-handoff-verify:*)` On a card that says `Chaining: start`, the same script also TAKES the single lane before the session acts — as a BATON when the live holder is the PID recorded in this hand-off's launch receipt — and gives it back under `--release`, which the close-out runs as its last act on every path (`references/phase-5-development.md` §1 and §5 step 11). | Phase 5 scaffold | Always (it is what makes the courier checks runnable) |
| Single-lane lock — taken by `scripts/keel-handoff-verify` when the card is `Chaining: start` | One file OUTSIDE the repository (the user's state dir), keyed by the real path of `git rev-parse --show-toplevel` — never the root-commit hash, which is wrong in a shallow clone and changes on `--unshallow`. Holds the owning PID + start time, so a lock left by a crashed session is detectable. Taken by the ARRIVING session, never the launcher, which cannot know who else is in flight. Released at the holder's CLOSE-OUT — never "on exit", a moment a session never reaches (`references/project-state.md`) | Phase 5 scaffold — before `start` is offered | Only if the card is `Chaining: start` |
| `scripts/keel-continue` | Detects the tool, looks up its row in the registry, and fires the VERIFIED action recorded for it — passing this repository's ABSOLUTE hand-off path — or prints the prompt when no action is recorded. **Never fires a different tool's action as a fallback or a default: the detected tool's own row, or the printed prompt — nothing else** (measured incident, `references/anti-patterns.md`, 12q). Records its own PID in the launch receipt and releases the lane immediately BEFORE firing (`references/project-state.md`, which carries its template) | Phase 5 scaffold | Only if the card is not `Chaining: off` and not `Chaining: supervised` |
| `scripts/keel-close` | The close-out as an executable instead of a sequence a session walks by hand: commit, `keel-verify`, merge and push, write `docs/continuation-prompt.md` with `Commit:`/`Tree:` read from git AT THE INSTANT OF WRITING, `keel-chain-check`, `keel-continue`, `keel-handoff-verify --release` — then print every step with its evidence. Decides nothing; stops only on a failing `keel-verify` (`references/project-state.md`). Needs one allow-list entry: `Bash(./scripts/keel-close:*)` | Phase 5 scaffold | Always |
| `.githooks/post-commit` + `core.hooksPath` set | Deletes `docs/continuation-prompt.md` if it exists, and nothing else — so a hand-off that exists is newer than the last commit BY CONSTRUCTION and cannot point at a commit the work moved past. Unconditional, unlike the `pre-commit` confidential-data gate: `core.hooksPath` is therefore set on every project, package or no package (`references/project-state.md`) | Phase 5 scaffold, verified firing on a real commit | Always |
| `scripts/keel-stop-hook` + its `Stop` hook registration | Covers what `keel-close` structurally cannot: a session that never DECIDES it is finished and simply ends its turn, reaching neither the close-out, nor the launcher, nor the lane, nor the receipts. Blocks the stop on the three UNBREAKABLE-broken states (unpushed, hand-off not describing `HEAD`, and uncommitted work — that last one scoped to the SESSION: where another session is live in the checkout it CEDES, allows and names to whom, since the only remedy it could otherwise offer is `git add -A` in a tree it did not author), blocks once more on a non-empty queue — that block cedes on the same condition, is DISCHARGED by a completed close-out (receipt claimed for this hand-off with this session's `launcher-pid`, lane released, `keel-handoff-verify` returning `CONTINUE`) and counts only `docs/PROGRESS.md`'s `## Open items`, excluding items parked on the user — never spins (block log keyed by repository AND session; each rule's no-change fingerprint contains that rule's own inputs and nothing else), hands the queue on via `scripts/keel-continue` and notifies; any internal error exits 0 (`references/project-state.md`). **Registered only where the target tool's own `Stop`-hook output contract is CONFIRMED — today Claude Code alone; every other accepted tool is the container matrix's `—` cell (`references/assistant-config.md`), never a guessed schema wired into a config file it was never verified against** (measured incident, `references/anti-patterns.md`, 12p). Needs one allow-list entry: `Bash(./scripts/keel-stop-hook:*)` | Phase 5 scaffold, verified firing on a real end of turn, both directions | Always |
| `scripts/keel-session-pid.sh` | One sourced file, one function (`keel_session_pid`): this session's identity, derived once from its own process (PID + that process's start time, so a reused number cannot impersonate a dead session) and unchanged for the life of the process. The single source of "who am I" for the lane's owner, the receipt's `launcher-pid`, the baton, and the stop hook's concurrency probe and block-log key (`references/project-state.md`). Sourced, never executed — no allow-list entry of its own | Phase 5 scaffold | Always |
| `scripts/keel-chain-check` | The executable form of the whole chaining contract: thirteen structural rows (launcher and verifier present and executable, both allow-list entries, permission mode `auto`, sane `env.PATH`, hand-off gitignored, `claude` resolvable under the DECLARED PATH, lane reachable, the four measured launch bugs absent from the launcher, the launcher passing an explicit `--model` with a real `Chaining model:` behind it, the post-commit hook installed and active with any hand-off on disk matching `HEAD`, and the `Chain verified:` checksum still matching the launcher on disk) plus `--smoke`, which fires the real launch with a nonce prompt, reads the new window's terminal history back and closes it. Prints one line per row plus `VERDICT: READY\|NOT READY\|N/A`, `--json` available. Runs at every session start and immediately before every close-out fire; **never vetoes a fire** (`references/project-state.md`). Needs one allow-list entry: `Bash(./scripts/keel-chain-check:*)` | Phase 5 scaffold, with `--smoke` run before the scaffold is called done | Only if the card is not `Chaining: off` and not `Chaining: supervised` |
| `Chaining model:` line on the project card | The model every chained chat launches with, passed as `claude --model <value>`. Asked at Phase 1 step 0a in the same breath as `Chaining:`, never inferred — a new `claude` process inherits nothing from the one that launched it, so an unstated model means the chain silently resolves one from settings (`references/project-state.md`) | Phase 1 step 0a | Only if the card is not `Chaining: off` and not `Chaining: supervised` |
| `Chain verified:` line on the project card | Date, tier, Keel version and the checksum of `scripts/keel-continue` at the moment `--smoke` observed the launch actually run. Written ONLY by `scripts/keel-chain-check --smoke`, never by hand. A checksum that no longer matches means the launcher is unproven again (`references/project-state.md`) | Phase 5 scaffold | Only if the card is not `Chaining: off` and not `Chaining: supervised` |
| `.githooks/pre-commit` | Confidential-data gate (+ `core.hooksPath` set) — one per project, tool-agnostic | Phase 5 scaffold | Only if accepted (card: `Assistant config:`) |
| Permission allow-lists — per capable tool (`.claude/settings.json`, `.codex/rules/`, `tools.allowed` in `.gemini/settings.json`, `.cursor/cli.json`) | Minimal confirmed allow-list from the plan's verified commands | Phase 5 scaffold | Only if accepted (card: `Assistant config:`) |
| CI workflow (e.g. `.github/workflows/ci.yml`) | The plan's verified commands + secret scan + keel-verify for WHAT it runs; the card's `CI runs on:` for WHEN — default `main` (push to `main`, version tags, PRs targeting `main`), never every push by default. **Where the forge is GitHub and the repo is private, the default is argued with the account-wide metered-minutes risk named explicitly, not folded into "less noise"** (`references/assistant-config.md`, `references/anti-patterns.md` 12r) | Phase 5 scaffold | Only if accepted (card: `Assistant config:`) and the forge has CI |
| MCP registration — per capable tool (`.mcp.json`, `.cursor/mcp.json`, `.vscode/mcp.json`, `[mcp_servers]` in `.codex/config.toml`, `mcpServers` in `.gemini/settings.json`) | Development MCP servers, env expansion only | Phase 5 scaffold | Only if the technical plan defines dev MCP servers |
| `docs/architecture.md` | Consolidated architecture | Phase 6 | Always |
| `docs/api/`, `docs/usage/`, `docs/reference/` | Full documentation layout (api/ grows from Phase 5) | Phase 6 | Always (reference/ per project type) |
| `docs/security.md` | Consolidated security posture per the loaded profile(s) | Phase 6 | Always |
| `docs/accessibility.md` | Accessibility record: automated results + guided assistive-technology pass, per item | Phase 6 | Always |
| `README.md` (repo root) | The repository's front door | Phase 6 | Always |
| `guide/` (repo root) | End-user HTML guide built on the canonical docs theme (`references/guide-theme.md`): navigable index + one page per topic, one dir per locale, full capability coverage; optionally the developer portal (`guide/{locale}/dev/`) | Phase 6 | Unless declined (card: `User guide:`) |
| `guide/_theme/` + `guide/brand/` + per-page `<meta name="keel-docs-theme">` | The vendored theme unit (checksum-intact vs the theme release), the project's brand layer (contrast contract verified), and the mechanical version marker — per `references/guide-theme.md` | Phase 6 | Unless declined (card: `User guide:`); version on the `Docs theme:` card line |
| `docs/07-release.md` | Release record (incl. full-suite re-run on the candidate + keel-verify output) | Phase 7 | Always |
| `<site-docs>/` (`docs/site/` or the site's own repo, per the recorded decision) | Phase 8 site set: `PRODUCT-BRIEF.md`, site discovery, spec, design docs | Phase 8 | Website intent only |
| `<site-docs>/design/design-handoff/SPEC/art-direction.md` | The site's decided visual language per `references/phase-8-art-direction.md` (read, four dials with reasons, chosen direction + rejected alternatives + its decision entry, signature elements with recurrence points and CSS, blacklist overrides, both self-critique answers) | Phase 8 design handoff | Website intent only (since v5.12.0) |
| `~/.keel/art-ledger.md` | Machine-local, cross-project, NEVER committed and never in any repository: one appended entry per site (archetype, dials, type, palette, hero paradigm, grid, signature elements, date). Its last three entries constrain the next site. Absence is an absent constraint, not a gap | Phase 8 art direction close | Website intent only (since v5.12.0) |
| `<site-docs>/launch-report.md` | One row per launch check: command/tool, result, date | Phase 8 launch | Website intent only |
| `<site-docs>/operations.md` | Renewal dates, uptime/backup decisions, freshness duty | Phase 8 launch | Website intent only |
| `docs/.keel/e2e-status.json` | The newest end-to-end run, published in the standard shape (`schema: keel.e2e-status/1`; `result` one of `pass`/`fail`/`error`/`cancelled`; `commit` mandatory, and a `commit` that is not `HEAD` is history rather than a result); written atomically, never hand-edited, gitignored by default (`references/project-state.md`, "The end-to-end verification contract") | Phase 5 onward, and gated at Phase 7 | Only if the project card carries an `E2E:` line |
| `docs/.keel/e2e-history.jsonl` | Append-only, one JSON object per run, so "it has been red for three days" is answerable with no server — same spirit as `docs/decisions.md` | Alongside the status file | Optional even where `E2E:` exists; every reader copes with its absence |
| `docs/.keel/slices/<n>.json` | One report per worker slice (`slice`, `status`, `branch`, `commit`, `needs_user`), written and committed by the WORKER on its own branch — never `docs/PROGRESS.md`, which only the main tree's session writes (dispatched at the Phase 5 sprint kickoff — `references/phase-5-development.md` §0; rule in `references/project-state.md`, "Fan-out over worktrees") | Phase 5 — the sprint kickoff that dispatches the fan-out | Only if the project fans work out over git worktrees |
| `docs/issues.md` | Forge issue log — inventory, one entry per issue worked, and (since v5.8.0) the `Last inbound sweep:` header line the sprint kickoff reads against the card's `Issue sweep interval:` | First forge contact | Only if the forge's issues were ever accessed |
| `docs/old/` | Archive (move, never delete) | First sprint close | When archiving starts |
| `docs/04-adoption-audit.md` | Gap audit vs Keel standards | Adoption step 5 | Adopted projects only |

Project-card lines that must exist (inside `docs/PROGRESS.md`): the full card per the `references/project-state.md` template, including `Keel portability:`, `Assistant config:` (introduced v1.10.0 as `Claude config:`; renamed with the tools list in v3.0.0), `Keel baseline:` (since v1.10.0), `Client budget:` and `User guide:` (both since v2.0.0), `Docs theme:` (since v3.2.0 — set when the theme is vendored at Phase 6), `Models:` (since v3.3.0 — the role→model map, set when the assistant-config package includes agents; `n/a` otherwise), `Chaining:` (since v5.3.0 — `off`, `prefill`, or `start`: what a clean close-out does with `docs/continuation-prompt.md` beyond writing it; since v5.10.0 its RECOMMENDED value follows `Autonomy:` — automatic recommends the maximum tier the gates allow, everything else recommends `off`), and `Issue sweep interval:` (since v5.8.0 — on the `Autonomy:` line; `n/a` unless the after-sprint issue duty was accepted), and `Test-first policy:` (since v5.11.0 — `pure-logic` / `pure-logic + acceptance` / `none` with its D-entry, or `n/a` where the project ships no executable product; asked once at Phase 2 §4e, or in the adoption batch on an adopted project).

## Table 2 — Skill files and the Keel version that last changed them

After an update, re-read `SKILL.md`, the current phase's reference, and THIS file always; beyond that, the files to re-read are exactly the rows whose version is NEWER than the project's `Keel baseline:`. Rows are the `keel/` tree — what travels in the embedded copy.

| Skill file | Last changed in |
|---|---|
| `SKILL.md` | v5.19.2 |
| `MANIFEST.md` | v5.19.2 |
| `CHANGELOG.md` | v5.19.2 |
| `references/keel-maintenance.md` | v5.10.3 |
| `references/notifications.md` | v5.14.0 |
| `references/playground-recipes.md` | v5.1.0 |
| `references/test-automation.md` | v5.11.0 |
| `references/maintenance.md` | v5.17.0 |
| `references/guide-theme.md` | v3.2.1 |
| `references/assistant-config.md` | v5.18.0 |
| `references/phase-5-development.md` | v5.19.2 |
| `references/phase-7-release.md` | v5.17.0 |
| `references/project-state.md` | v5.19.2 |
| `references/phase-1-discovery.md` | v5.19.1 |
| `references/phase-2-functional-spec.md` | v5.11.0 |
| `references/adoption.md` | v5.11.0 |
| `references/estimation-budget.md` | v5.17.0 |
| `references/phase-6-documentation.md` | v5.2.0 |
| `references/phase-3-design-handoff.md` | v3.5.0 |
| `references/phase-4-faithful-build.md` | v5.2.0 |
| `references/handoff-contract.md` | v5.12.0 |
| `references/design-brief-template.md` | v5.12.0 |
| `references/design-request-template.md` | v2.0.0 |
| `references/build-spec-template.md` | v2.0.0 |
| `references/phase-8-website.md` | v5.12.0 |
| `references/phase-8-site-discovery.md` | v2.0.0 |
| `references/phase-8-section-catalogue.md` | v2.0.0 |
| `references/phase-8-domain-decision.md` | v2.0.0 |
| `references/phase-8-art-direction.md` | v5.12.0 |
| `references/phase-8-design-direction.md` | v5.12.0 |
| `references/phase-8-launch-checklist.md` | v5.12.0 |
| `references/phase-8-technical-seo.md` | v2.0.0 |
| `references/accessibility.md` | v5.2.0 |
| `references/anti-patterns.md` | v5.19.2 |
| `references/security/wordpress.md` | v4.0.0 |
| `references/security/web-app.md` | v4.0.0 |
| `references/security/mcp-server.md` | v4.0.0 |
| `references/security/library-component.md` | v4.0.0 |
| `references/security/website.md` | v4.0.0 |
| `LICENSE` | v1.0.0 |
| `NOTICE` | v2.0.0 |

## Table 3 — Per-version actions on an existing project (the reconciliation delta)

What the reconciliation APPLIES, version by version, for every version newer than the project's `Keel baseline:`. "None structural" means the version changed skill behavior only — re-reading per Table 2 is enough. Versions before v1.10.0 predate the baseline mechanism; for those, the Table 1 parity audit covers everything.

| Version | Actions for an existing project |
|---|---|
| v1.10.0 | Add `Claude config:` and `Keel baseline:` card lines; offer the optional Claude Code config package once. |
| v1.11.0 | Lock block gains its version stamp — refreshed by the normal lock-freshness check. |
| v1.12.0 | None structural (the manifest itself was introduced, skill-side). |
| v1.12.1 | Ensure `.keel-update-check` is an unconditional `.gitignore` entry. |
| v1.13.0 | None structural. |
| v2.0.0 | Ask the `Client budget:` question if it was never asked and add the card line. On runnable projects, generate `scripts/keel-verify` and add seed/reset to the playground at (or after) the Phase 5 scaffold. If the Claude config package is accepted and the forge has CI, offer the CI workflow. If the project has UI, the next handoff audit or re-audit uses the evidence-table `docs/BUILD-SPEC.md` §1. If Phase 7 is done, set the PROGRESS.md position to "maintenance" and work per `references/maintenance.md`. If website intent is recorded, plan `launch-report.md` and `operations.md` at the next launch or freshness pass. On runnable projects, the debug-log system with its on/off switch (Phase 5 scaffold spec) is added at the next sprint kickoff or maintenance change. The reconciliation itself ASKS whether to create the full end-user guide now (`guide/` — the Phase 6 section, with its language and packaging questions) and records the answer on the new `User guide:` card line; creating it on a released project is a normal maintenance change. The lock-block stamp refreshes through the normal freshness check. |
| v2.1.0 | None structural. Behavioral: nothing is ever written into `docs/design/design-handoff/` again — user-generated assets and acquired fonts go to the PROJECT's tree (contract rule 10). If the current handoff already contains foreign files, move each to its correct home (project tree or `docs/`) at the next Phase 4 touch, restoring the wholesale-replaceable state. |
| v3.0.0 | The multi-assistant generalization. Ask once which assistants work on this repo (if never asked) and rename the project card line `Claude config:` → `Assistant config: [none / rules / rules+agents / full] (tools: ...)`, keeping the recorded acceptance level. Create the missing `AGENTS.md` lock mirror (same block, same stamp) through the normal lock-freshness refresh; if the user works with Gemini CLI, ask its mirror question (a `GEMINI.md` copy, or `context.fileName` in `.gemini/settings.json`) and record the pick. If the skill is embedded, add the second tree so `.claude/skills/keel/` + `.agents/skills/keel/` both exist (verified full-copy protocol; they sync together from now on). If the assistant config package is accepted, offer materializing the containers for the newly named tools from the same recorded sources (per `references/assistant-config.md`); add the accepted tools' personal files to `.gitignore` (`AGENTS.override.md`, `.gemini/.env`, `.gemini/tmp/`); if the pre-commit gate is installed, update its embedded-skill exemption to cover `.agents/skills/*` (a recorded gate change). At the next Phase 7 touch, extend the export-ignore set to every generated assistant config tree. |
| v3.1.0 | If the assistant config package is accepted: regenerate `code-reviewer` (new comments check) and the `code-style` rule (new comments line) in every capable container from the same recorded sources. Where the `User guide:` card line is yes and `guide/` exists, generate the `guide-qa` subagent in each capable container at the next Phase 6 or maintenance touch. Nothing else structural. |
| v3.2.0 | Add the `Docs theme:` card line (n/a until a theme is vendored). Where `User guide:` is yes and `guide/` predates the canonical theme (improvised HTML): at the next Phase 6 or maintenance touch, OFFER rebuilding `guide/` on the theme per `references/guide-theme.md` and ask the never-asked developer-portal questions (render `docs/`? ships or repo-only?), recording the answers — regenerating a released project's guide is a normal maintenance change. Regenerate `guide-qa` (new check 6 + inputs) in every capable container where it exists. |
| v3.2.1 | None structural — release-asset verification (Phase 7) and the theme fallback warning (`guide-theme.md`) are behavioral; re-reading per Table 2 is enough. |
| v3.3.0 | Add the `Models:` card line. If the assistant config package is accepted WITH agents (`rules+agents` or `full`): settle the role→model map (orchestrator / reviewer / mechanical) with the user per `references/assistant-config.md` ("Model binding"), record it as a D-entry and on the card line, and materialize each capable container's native model field at the next Phase 2-close or maintenance touch. If no agents (or the package is not accepted): set the card line to `n/a` and do nothing else. Nothing else structural. |
| v3.4.0 | The build-assets source-first contract. On projects that ship front-end JS/CSS: at the next sprint kickoff or maintenance touch, ensure every shipped `*.min.*` has its unminified source committed beside it and a local build/minify script exists (`scripts/…`, run by the working assistant, never CI/forge); never hand-edit a minified file. If a project currently hand-maintains its minified assets, regenerate them once from source and adopt the script. Projects with no front-end assets, or that record a different pipeline in `docs/decisions.md`, set this n/a. Nothing else structural. |
| v3.5.0 | Two actions. **(1) Docs follow the code, all three operations.** From the next slice or maintenance touch, a public surface that is *changed* has its `docs/api/` or `docs/reference/` entry and its example updated in the same slice, and one that is *removed* is resolved rather than deleted in silence: entry and INDEX row deleted if it was never released, or marked deprecated/removed with its replacement (plus a D-entry) if it was. No retroactive sweep of existing docs is required — but any stale entry found while working is fixed then, not noted for later. **(2) The rubric question (Phase 2 §6a) is asked once if it never was**, and its answer recorded — a rubric in `docs/rubrics/` or an explicit "none for this project"; for a released project, adopting one later is a normal maintenance change. Reference artifacts (Phase 2 `## Reference artifacts`, `docs/spec-references/`, `docs/design/references/`) are opt-in and create no obligation for a project that holds none. Nothing else structural. |
| v4.0.0 | Documentation and reality stop drifting by discipline and start drifting by failing a check. Five actions, all applicable at the next natural touch — none requires a retroactive sweep. **(1) Load `references/anti-patterns.md`** for this project's type from the next session (it joins the security profile and the accessibility reference as a Phase 1 selection), and run its self-audit once against the current tree, every answer evidenced by a command or an artifact; each failing answer becomes a normal work item (an adopted or released project usually surfaces several). **(2) Mark the code map.** Add the `[E]`/`[A]`/`[G]` state marker to every row of `docs/03-technical-plan.md`; an existing project is almost entirely `[E]`, so this is a cheap pass, and from then on a path that is not `[E]` is treated as absent. **(3) Build the change map** (`references/phase-2-functional-spec.md` §4b): one row per recurring change type in this project, each naming real paths and real commands. Build it from the change types the project actually has — on a mature project this is the highest-value artifact of the whole version, because that knowledge currently lives only in the maintainer's head. From then on every slice reads its row first and a new change type adds its row in the slice that discovered it. **(4) Create `docs/threat-model.md`** (§4c) from the loaded security profile's new "Deliberate omissions" section plus the project's reality, with every control at its HONEST delivery state — a released project typically has more `TO BUILD` than expected, and recording that accurately is the point; the "Not defended" table turns silent omissions into decisions. Any control currently described in the present tense that is not `IN PLACE` with evidence is corrected in the docs at the same time. **(5) Extend `scripts/keel-verify`** with the new checks (cited commands exist, internal doc links resolve, no orphan documents, `[E]` markers match disk, code fences balanced, suppression count reported) — on projects that have the script; on those that do not, it is created at the next scaffold or maintenance touch as usual. The `docs/lessons-learned.md` template gains symptom/cause/fix and a "Check added" field; existing entries are not rewritten, new ones use the new shape. **(6) The Phase 1 competitive confrontation (§3a) is offered once** on any project whose scope was set before it existed: present every competitor functionality and external demand against what the project actually has, with per-feature AI-time cost and demand evidence, and let the user decide row by row — on a released product this is a roadmap exercise rather than a scope one, and its "never" decisions are recorded so they stop being re-proposed. Skip it only where the scan itself was skipped and the user declines to run it. **(7) Every duration the assistant states is AI development time** (AI working hours + vibe coder supervision hours), named as such every time — no human-team figure, in any doc, table or conversation, unless explicitly labelled and placed beside the AI figure. Nothing to migrate; it governs from the next session. |
| v5.0.0 | The assistant stops asking the user to be the test runner, and applying Keel stops depending on anyone's memory. Eleven actions, applicable at the next natural touch. **(1) Run the conformance sweep and create `docs/keel-conformance.md`** — walk EVERY applicable row of Table 1 above plus every Table 3 action newer than the project's baseline, give each one a state (`present` / `missing` / `declined` with its D-entry / `n/a` with its condition), present every `missing` row to the user in one uncurated batch, and report the finished table in full. From now on this sweep runs at adoption, at every post-update reconciliation and at the Phase 7 gate, and `scripts/keel-verify` fails on a `missing` row with no decision. This is the answer to "Keel was applied, but only partly, and nobody said so". **(2) Record the environment requirements and generate `scripts/keel-doctor`** from them (`references/phase-2-functional-spec.md` §4d, `references/test-automation.md`): the technical plan gains `## Environment requirements`, the doctor is committed, and `--check` runs before gate zero and at the first test point of every session. On a released project this is a cheap pass that pays for itself the first time a machine is missing a driver. **(3) Name a driver per surface in the technical plan's `## Testing`**, each with its headless verdict and — where a driver takes over the user's screen (macOS and native Windows UI tests) — the agreed mitigation, plus the element-addressability convention and the division of labour with its delegation tags. **(4) Convert "the user will check it" into driven tests.** No retroactive sweep is required, but from the next slice every user-visible acceptance criterion gets a test that fills the real fields (valid, empty, invalid) and asserts what the interface shows, with console/network/log read-back failing the test; existing criteria whose only evidence is a human verdict are converted as their area is next touched. **(5) Sprint closes change shape:** the user receives verified results with evidence plus the short tagged list of what only they can do — never a numbered list of screens to visit. **(6) Accessibility rebalances:** the keyboard-only and focus-order pass moves from the manual list to the driven list (the assistant runs it on every UI slice), and the real assistive-technology pass batches per flow at sprint closes under `ASSISTIVE-TECH` instead of being demanded slice by slice. **(7) Maintenance and adoption join the contract:** every maintenance session opens with `keel-doctor --check`, reproductions and hotfix verifications are driven rather than delegated, and a released project that never had a playground or drivers gets them as its first maintenance change; the adoption audit gains a Testability row. **(8) Phase 4 stops being the manual phase:** `SPEC/external-setup.md` is triaged into drivable (scripted into the seed) and guided (tagged), fonts with a deterministic source are fetched by script rather than by the user, the environment and drivers stand up as soon as the first screen renders, and fidelity is verified from rendered output at the declared breakpoints instead of by reading code. **(9) Product screenshots for the guide** are captured by the assistant from the playground at a fixed viewport against seed data. **(10) Acceptance criteria gain stable `AC-nn` IDs** and `docs/05-test-points.md` gains the `Criterion` and `Coverage` columns — the pair that makes the coverage rule checkable by a script instead of self-declared; existing projects add IDs as each area is next touched, with no retroactive renumbering. **(11) If the assistant config package is accepted:** generate the `test-driver` subagent in every capable container, and extend the permission allow-lists to cover the playground, the drivers, the sniffers, `keel-verify` and `keel-doctor --check`/`--plan` — never `--fix`. A driver command missing from the allow-list is how this whole mechanism quietly reverts to asking the user. |
| v5.1.0 | Browser tests stop being invisible. Three actions, all cheap. **(1) Turn on the recording.** Every browser surface sets `video` and `trace` on in its Playwright config, with the artifacts under the ignored artifacts directory; from the next test point the test-point row carries the trace path, and the sprint-close report gives the command that opens it. A driven check on a browser surface with no trace path is now incomplete. **(2) Add the headed script.** A documented npm script (`test:e2e:watch` or the project's equivalent) runs the same suite with `headless: false` and a `slowMo` a person can follow, so "I want to see the browser do it" is a flag rather than a rewrite. It is offered at sprint closes as a courtesy, never as a substitute for the recording, and the technical plan records that headed runs take the screen while they last. **(3) Record the run mode** in the technical plan's `## Testing` block: headless as the default, the headed script, and the recording settings. Projects with no browser surface set this n/a. Nothing else structural. |
| v5.2.0 | Verification stops running in a queue. Nothing to migrate and no new artifact — this is a dispatch rule, applicable from the next gate. **(1) Send every READING verifier a gate calls for in ONE parallel block**, at most one EXECUTING verifier per environment alongside them, and judge the gate on their merged findings rather than the first to answer: the Phase 7 gate reads with `security-auditor` + `guide-qa`; a Phase 5 sprint close reads with `docs-verifier` while `playground-qa` holds the playground; the Phase 8 launch checklist runs `launch-verifier` + `a11y-auditor`; a Phase 5 test point sends `code-reviewer` out with whatever else that slice's gate reads for; the hotfix gate (`references/maintenance.md`) fans out too — fast means fanned out, never trimmed. Dispatch only what the project has: six of the nine agents are conditional. **(2) Where one verifier repeats the same check over independent units, send one agent per unit** instead of one agent walking the list — screens in the Phase 4 fidelity walk, locales in the Phase 6 guide check, audit dimensions in the adoption gap audit, rubric domains in a Phase 2 §6a review where the rubric already exists (Phase 1's one-agent-per-competitor scan was already this). Add one whole-artifact agent for whatever the split would drop: the guide's secondary-locale mirroring and its theme/dev-portal checks are the worked example. Where the per-unit check needs a running environment, capture once and compare in parallel — ONE pass (the session's own, or whichever executing agent already holds the environment) captures every screen at the declared breakpoints, then the per-screen auditors read those captures concurrently as readers, so twelve screens cost one round without twelve agents on one browser. **(3) Serial survives in exactly four cases**: one check's input is another's output; two executing verifiers would share one environment (playground, test machine, seed database, deployed origin — they queue, and two crawlers on one origin re-verify any `429`/`5xx` single-threaded before it becomes a finding); an agent can change what the readers are reading (only `test-driver`, on a run that adapts selectors, waits or fixtures — it goes first and alone); or concurrency is capped. The canonical rule is `references/assistant-config.md` ("Parallel fan-out"). The lock block changed, so `CLAUDE.md` and `AGENTS.md` (and any `GEMINI.md` mirror) refresh at the next lock-freshness check. Environments with no subagents run the checks inline and therefore serially, exactly as the standing fallback already says — that is not a licence to drop any of them. **(4) If the assistant config package is accepted:** regenerate `test-driver` in every capable container with `Edit` in its `tools:` line — which its recorded job (mechanically adapting selectors, waits and fixtures) always required and never had — with the test-scaffolding-only scope stated in the agent's body, since no container's `tools:` line can express a path scope, and enforced in the permission allow-list where the tool supports one. Nothing else structural. |
| v5.3.0 | Chats hand over through a file instead of a paste. Eight actions, none retroactive. **(1) Add `docs/continuation-prompt.md` to `.gitignore`** — ephemeral session state, regenerated at every close, never in history. **(2) From the next session that ends mid-work, write the continuation prompt to that path** as well as showing it, with the freshness header (`Repo` / `Generated` / `Keel` / `Commit` / `Tree` / `Position` / `Handover`). Showing the prompt in the chat does not stop; the file is in addition. **(3) When READING one, verify it before acting** — five mechanical checks, containment FIRST: the file's real path is inside `git rev-parse --show-toplevel`; `Repo` against `git rev-list --max-parents=0 HEAD \| sort \| head -1` (write `unavailable (shallow)` when `git rev-parse --is-shallow-repository` is true, since the command otherwise returns the grafted commit with exit 0); `Commit` against `git rev-parse HEAD`; `Generated` against now and the file's own mtime; `Tree` against `git status --porcelain`. `Position` is a human-readable corroborator and is NOT claimed as a check, because prose against prose is not executable. Any mismatch stops the session. **(4) Containment, `Repo` and an ABSOLUTE launch path are all three required, and no one of them suffices.** The filename is identical in every Keel project, so a chat opened in the wrong window reads a DIFFERENT project's hand-off whose commit and position both legitimately match, and continues the wrong project in perfect internal consistency. The absolute path makes the right file get read; `Repo` makes a session in a different repository stop; containment makes a session in a different CHECKOUT of the same repository stop — a worktree, a second clone or a copied folder shares both root commit and `HEAD`, so identity checks pass while the working directory is wrong. **(5) Generate `scripts/keel-handoff-verify` at the next scaffold or maintenance touch, and add its ONE allow-list entry** (`Bash(./scripts/keel-handoff-verify:*)`). The session NEVER composes the courier checks itself: written inline they become a line of nested `$(...)`, the permission matcher returns `Parse error`, and the allow-list is bypassed even though every git command in it is allowed — at every link of a chain, measured. Timestamps compare as epoch, never as strings (`+02:00` vs `+0200` reports tampering on every run). **(6) `scripts/keel-continue` runs that script BEFORE firing** — under `start` there is no human between the launch and the work, so discovering the mismatch after launching is the wrong order. **The lock block changed** — the continuation duty is now its own UNBREAKABLE numbered item instead of a trailing clause on the commit item — so `CLAUDE.md` and `AGENTS.md` (and any `GEMINI.md` mirror) refresh at the next lock-freshness check.**(7) Ask the `Chaining:` question once and add the card line** — `off` (the recommended default), `prefill` (opens pre-filled, the user presses Enter) or `start` (launches and submits). Only if the answer is not `off` does the project gain `scripts/keel-continue` at the next scaffold or maintenance touch. Chaining depends on a VERIFIED action for the detected tool; documented-but-untested is a lead, not a permission, and everything else prints. **Two rules hold on every project regardless of the answer:** wherever a chat cannot be opened — for any reason, including a failure to fire — the full prompt is printed to be copied, so no session ever ends leaving the user with neither an open chat nor a copyable prompt; and a session that DOES chain closes itself in one short message (chat closed, continuation launching, where the hand-off lives, what is left to press) written in the CONVERSATION's language, while the file stays in English like every other artifact. Ask it at Phase 1 step 0a with its warning visible — that `start` moves development to CLI sessions rather than the editor, and that it removes the only person who supervises — never as a silent card field. **`start` is GATED**: not offered until the project has the single-lane lock, and verified on macOS only (opening a VISIBLE CLI session is `osascript`+Terminal.app there; Linux and Windows are unverified, and reaching for the plain open command ships the headless variant, which runs correctly and opens no window). **(8) The single-lane lock**, required before `start`: taken by the ARRIVING session — a launcher cannot know who else is in flight, which is exactly what failed under test, four live sessions in sixteen seconds from a three-link chain — with the lock file OUTSIDE the repository, because a brake the chain can `git clean` is not a brake. A blocked close-out never chains, but it still PRINTS the prompt: refusing to chain and refusing to print are different things, and only the first is intended. |
| v5.3.1 | Two artifacts that v5.3.0 required and never told anyone to build. Both are scaffold-time, both cheap, and a project that already applied v5.3.0 needs them at its next touch. **(1) Generate `scripts/keel-handoff-verify` at the scaffold, on EVERY project** — v5.3.0 put it in this table and in `references/project-state.md` but never in `references/phase-5-development.md`, the reference a session actually executes the scaffold from, so by this manifest's own authority rule (the reference wins) it was never created on a new project; only the reconciliation created it, which meant existing projects had it and new ones did not. It is unconditional: every project writes `docs/continuation-prompt.md` at every session end whatever `Chaining:` says, so every project needs the script that verifies one. Add its ONE allow-list entry, `Bash(./scripts/keel-handoff-verify:*)`, to every capable tool's container. **(2) The single-lane lock is taken BY that script**, at the Phase 5 scaffold, on cards that say `Chaining: start`. v5.3.0 specified the lock, gated `start` on it, and named no one to build it and no moment to build it in — a gate with no key. It is claimed at the moment the arriving session verifies, so there is no window between verifying and starting work; one file outside the repository, keyed by the real path of `git rev-parse --show-toplevel`, holding PID and start time. No separate script, no step for the user to remember. **The lock block's content did not change** — only its stamp — so the refresh at the next freshness check is a stamp-only rewrite. |
| v5.3.2 | Nothing to migrate and no new artifact. `start` always needed the `claude` command on PATH and v5.3.0 never said so, so the gate now names it as its fourth requirement: neither the desktop app nor the VS Code extension puts `claude` there — the app runs Claude Code graphically and the extension bundles a private copy for its own panel — so a user can have both installed, use Claude Code daily, and have no `claude` command. A project whose card is `Chaining: start` on a machine without it is offered `prefill` instead, naming the requirement that failed. Projects on `off` or `prefill` are unaffected. |
| v5.3.3 | Two behavioural fixes, nothing to migrate and no new artifact. **(1) The conformance sweep never reads the previous `docs/keel-conformance.md`** — its authorised sources are the disk, `MANIFEST.md` and `docs/decisions.md`, and it overwrites that file rather than consulting it. A sweep that cites the last sweep is self-confirming: a wrong row reproduces itself with the air of evidence, and the artifact that decides whether Keel is applied grows more confident with every run instead of more correct. Applies in every modality — fan-out, subagent block, or one session walking the list inline. From the next sweep on any project; no retroactive re-run is required, though a project whose sweep has run several times may want one. **(2) The single-lane lock is released by its holder on every exit path, and an orphan is recovered rather than reported.** v5.3.1 said only that a stale lock was *detectable*, which is a property and not a policy: a lane nobody leaves is the same outage as a lane nobody can enter. A dead PID — or a live one whose start time does not match the recorded value, because numbers get reused — is taken by the arriving session, which says in one line that it recovered an orphan and from which PID, and continues without asking. Only projects on `Chaining: start` hold a lane at all. |
| v5.4.0 | Keel learns what its environment can actually do, and orchestrates accordingly. One new artifact, conditional; everything else is behaviour applicable at the next natural touch. **(1) The Phase 1 environment preflight gains two questions and the discovery doc gains two lines.** Where the card is `Chaining: prefill` or `start`, `command -v claude` is run, the platform's install command is OFFERED if it is missing, and the answer is recorded either way — absent and declined means `start` is not offered and `prefill` is the maximum, naming the requirement that failed. And whatever this session's environment cannot do is stated up front rather than discovered on failure: no network where the files are, no file deletion, no `localhost`, no screen control, execution and files on separate filesystems. Both lines go under the existing `## Environment & test drivers` in `docs/01-discovery.md`; a project already past Phase 1 adds them at its next session rather than re-running the phase. **(2) `NO-EXECUTION` gains its partial case:** a session that can execute AND can reach the files, but not both at once or not with a network, says which half is missing and tags per LEG — it never hands the whole suite back and never hands the clicking to the user. **(3) Where a Phase 5 sprint is fanned out over git worktrees** (new, conditional; dispatched at the sprint kickoff — `references/phase-5-development.md` §0): only the session owning the main tree writes `docs/PROGRESS.md`; each worker commits `docs/.keel/slices/<n>.json` on its own branch (`slice`, `status`, `branch`, `commit`, `needs_user`) and closes out in the fixed order commit → report → atomic done-signal, so an existing signal always implies committed work. A `blocked` report is not merged; its question goes to the user. This INVERTS the "update the state at the moment of change" rule on purpose — with the workers writing `PROGRESS.md` the conflict rate was 100% of merges, N−1 times for N workers, while the code merged clean; `merge=union` in `.gitattributes` is not the fix and must not be used, because a rewritten state file then merges cleanly into a corrupt one. A project that never fans out over worktrees does nothing here. **(4) The verifier dispatch rule gains a three-step fallback chain** — workflow (where one exists and the user opted in) → parallel subagent block → inline and serial — plus, where a workflow tool is available, the conformance sweep as its canonical shape, dedup and the cross-unit check as code rather than discipline, and the honest boundary of "no post-processing" (no MODEL between the data and the file; a deterministic template and a way to reach disk are still code someone writes). Nothing to migrate: an environment without the first two steps runs exactly as before and says so. **(5) File isolation is not environment isolation:** a per-agent worktree isolates FILES, so it never relaxes "one executing verifier per environment", which stays a serial stage. **(6) Three designs are recorded as measured and rejected** so no future session re-proposes them: a second "director" chat orchestrating workers (it works, and costs 15–20k of context per slice on a real project, degrading between the 6th and 9th, for parallelism the native primitives already give with cost accounting, cancellation and a concurrency cap); `claude --resume` as a mailbox into a live session (there is none — it forks the transcript, fabricates a turn, and loses the message silently); and closing another session's window from a script (40–78 s with no confirmation, no cleanup hooks fire, orphaned lane guaranteed). `claude agents --json` is recorded as the supported way to see live sessions, with the caveat that `-p` sessions do not appear in it. **The lock block's content did not change**, only its stamp, so the refresh at the next freshness check is a stamp-only rewrite. |
| v5.4.1 | Three fixes to v5.4.0's own material. Nothing to migrate and no new artifact. **(1) The fan-out gains its dispatch command** (`references/project-state.md`, "Dispatching a worker"): v5.4.0 specified the worker's report, its close-out order and the blocking wait, and never wrote `git worktree add` or the launch, so a session had a contract to satisfy and no way to begin. Phase 5 §0 now points at the command and states its precondition. **(2) The CLI probe stops being gated on `Chaining:`.** Phase 1 §5a probed the CLI only where the card said `prefill` or `start`, so a project on the default `off` could plan a fanned-out sprint and meet the requirement at dispatch — the v5.3.2 shape recurring inside the release that introduced the fan-out. The step now asks both halves and is not applicable only when BOTH are false; no working CLI means `prefill` is the maximum AND the sprint is built serially. A project already past Phase 1 answers the fan-out half at its next session rather than re-running the phase, recording it under the existing `## Environment & test drivers` in `docs/01-discovery.md`. **(3) A probe's answer is not the machine's state, in both directions** (`references/test-automation.md`): a negative `command -v` is corroborated against the login shell, the platform's install locations and any variable the tool exports (`CLAUDE_CODE_EXECPATH`, `CLAUDE_CODE_VERSION`) before it may be written `MISSING` — an assistant's restricted `PATH` hides binaries that work in the user's shell, and the resulting row offers to install what is already there; and a positive is followed by the tool's own version probe against the requirement, recording `TOO OLD` or `NOT OPERATIONAL` rather than `OK`, because an `npm -g` install onto a runtime older than the package's declared engine only warns. Applies to every doctor row, not only the CLI. |
| v5.5.0 | Keel settles its own operating conditions, stops working in the dark, and stops leaving work on the machine. Eight actions, none retroactive. **(1) Run the session-start setup batch once** and add the project-card lines `Autonomy:` (automatic mode yes/no — everything hangs off it — plus the after-sprint issue duty), `Branches:` and `Notify:` (out-of-band channel and recipient, or `none`/`declined`), with a D-entry for what was answered — three questions in ONE batch, then never asked again. **(2) Repair `env.PATH` wherever it carries an unexpanded `${PATH}` or `$HOME`** — including the user-level `~/.claude/settings.json` — writing every directory literally and never omitting `/usr/bin` and `/bin`; the unexpanded form silently removes the system directories and breaks `git`, `ls` and `grep` machine-wide. Then **resolve `manual` permission mode** before the next working session: create or MERGE `.claude/settings.local.json` (`defaultMode: "auto"`, minimal `deny`, `env.PATH`), never removing a rule already there, and verify the file is in `.gitignore` (it was already an unconditional entry). The per-session route (`--permission-mode auto`) is equivalent and vanishes with the session. **(3) Drop every `export PATH=... && ` prefix** from generated `scripts/`, project notes and habitual tool calls at the next touch — the prefix alone forces a permission dialog on every call, which is the problem it appeared to solve. **(4) Probe the notification channel and record the verdict** in `docs/01-discovery.md` under `## Environment & test drivers`, classifying each candidate as delivering / compose-only / none — a draft-only mail connector is never counted as a notification. **(5) Adopt the `develop` git flow:** create and publish the integration branch if it does not exist, start work branches from it, and — in automatic mode — merge them back and push without asking; never merge to `main`, never tag, never release without an explicit instruction, and name anything ready for `main` under "Ready for `main`" in PROGRESS.md. Add the `Branches:` card line. A production hotfix still branches from the release tag. In an ADOPTED repo the shape is settled with the user against what the repo already does (observed, not imposed) — only "the assistant never merges to the release branch, never tags, never releases" is non-negotiable. `scripts/keel-verify` gains the check that nothing is stranded unpushed and that the current branch is not the release branch. **(6) Regenerate `scripts/keel-continue` on any project whose card is not `Chaining: off`**, adding the atomic launch receipt (`mkdir`, keyed by the hand-off's `Generated:`+`Commit:`, stored outside the repo beside the lane), the never-retry rule and the three-per-hour circuit breaker — without it one close-out can open several windows, which is measured, not theoretical. Invoke it from exactly one place. **(7) Stop asking whether to continue:** while the queue holds work independent of what is unfinished, carry on, handing off through `docs/continuation-prompt.md` when context runs out. **(8) From the next sprint close or maintenance touch, answer forge issues in three beats and never close one:** comment fix-landed (status `awaiting deploy`), notify the developer that a deploy is needed and WAIT, comment testable on confirmation (status `awaiting reporter`), and let the reporter's answer close it or reopen the work. Existing `docs/issues.md` entries gain `Replies:`, `Deploy:` and `Closed by:` as they are next touched — no retroactive backfill. |
| v5.5.1 | One flag, and it was wrong twice over. **(1) Change every fan-out dispatch to `--permission-mode auto`** — `bypassPermissions` asks for confirmation EVERY time (measured), so it guarantees the prompt it was chosen to remove and an unattended worker hangs waiting for an answer nobody gives; it also evaluates no rules at all, handing a `deny`-free exemption to the only sessions that are parallel, unattended and writing to real branches. If workers stall under `auto`, extend the project's allow-list — never go back to bypassing. **(2) Copy `.claude/settings.local.json` into every worker's worktree before launching.** It is gitignored, so `git worktree add` produces a checkout without it and the worker silently falls back to user-level config: no project `deny`, no `env.PATH`, no allow-list. Without this step the flag is right and the directory is wrong, which looks fixed and is not. **(3) Nothing else** — no new artifact, no card line, no migration. |
| v5.5.2 | The launch of the next chat, fixed where a real close-out broke it: five windows opened and four killed by hand. No new artifact, no card line, no migration. **(1) A session NEVER composes the launch itself — it runs `scripts/keel-continue`, or it PRINTS the prompt** (UNBREAKABLE, the same rule the courier checks already carried). A project without that script is not a project to improvise around: no script means no chaining yet, which is the `off` behaviour, and regenerating it is a later maintenance touch and never an improvisation at the end of a close-out. **(2) The `start` action carries `cd '<absolute repo root>' &&` and it is mandatory.** The registry claimed the CLI "inherits `cwd`" — true of a direct exec, FALSE of the action actually verified for `start`, because `osascript` asking Terminal.app to `do script` opens a new LOGIN shell that starts in `$HOME` and inherits nothing. Containment, `Repo:` and the absolute hand-off path do not save this: they are checks the arriving session runs on a file it was told to read, and a session that lands in `$HOME` reads no hand-off at all. **(3) A launch that fired WRONG is spent, exactly like one that fired unobserved** — the never-retry rule said "I cannot tell whether it opened" and was read as not covering "it opened, and it is wrong", so the close-out killed and re-fired four times, each launch technically successful. Once fired, this session's chaining is over: report it in one line, print the prompt. Killing another session's process to make room for a better attempt stays forbidden, including for the session that launched it. **(4) `references/phase-5-development.md` §1 described the script as "fire the constant URI"** — the `prefill` mechanism — for a tier that needs a visible CLI session, contradicting `project-state.md`. The two tiers do not share a command, and the scaffold line now says so. **Regenerate `scripts/keel-continue` at the next maintenance touch on any project whose card is not `Chaining: off`** — the `cd` is the one change that matters, and a script generated before v5.5.2 does not have it. **The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite. |
| v5.6.0 | The work stops being able to exist in only one place. **One new card line, two new `keel-verify` checks, one real lock-block change.** **(1) Answer Question 0 once on every existing project and add the `Durability:` card line** — the session-start setup batch is now FOUR questions, and this one is asked first because it decides whether everything the session produces exists in one place or two. Verify it with commands rather than memory (`git rev-parse --git-dir`, `git remote -v`, the tree's absolute path against the known synced-folder roots): a repository whose remote was removed months ago is exactly what this finds, and a folder is never counted as synced because its NAME looks like it — the user confirms it. Two things satisfy the requirement, a Git repository with a remote or a tree that replicates off the machine; **a repo with no remote is a partial answer, not a pass**, since local commits protect against a bad edit and against nothing that happens to the disk. Whatever is missing is offered concretely — `git init` plus the first commit, publishing to the forge the user names, or moving the tree into a synced folder — and a refusal is recorded as `Durability: NONE — accepted risk (D-0XX)` with its consequence written out, never as a silence. The ANSWER is then never re-asked; the two facts behind it are re-verified every session, in one line, so a removed remote surfaces the day it happens. **(2) Add the two checks to `scripts/keel-verify` at the next touch** on any project that has one: a remoteless repository with no recorded decision now FAILS instead of being skipped (the worst case had been the one case nothing reported), and a dirty working tree fails naming the files — the fix is the commit, never a `.gitignore` entry chosen to make the check green. **(3) Nothing stays uncommitted, on every project, from the next working block**: commits land on `develop` or on a work branch bound for it, and a repo carrying only `main`/`master` gets `develop` created first rather than product work landing on the release branch. Outside automatic mode the PUSH still waits for the user; the COMMIT never does. **(4) Adoption runs the same check at step 2** and additionally inventories whatever arrived uncommitted in the working tree, committing it after the confidential-data check instead of leaving it stranded — an adopted repo carries years of work, so "it exists only on this laptop" is a far larger exposure there than on a new project. **Automatic mode is untouched by design:** Keel performs `git init`, the first commit, the creation of `develop` and every commit and push itself without asking; only choosing WHERE the work is published stays the person's. **The lock block CHANGED** — item 4 gains the never-leave-work-uncommitted duty and the no-remote warning — so `CLAUDE.md` and `AGENTS.md` (and any `GEMINI.md` mirror) need a real content refresh at the next lock-freshness check, not a stamp-only rewrite. |
| v5.7.0 | The hand-off stops depending on a session that happens to stop. Nothing to migrate and no new artifact — pure behaviour, applicable at the next sprint close and the next session start. **(1) Write `docs/continuation-prompt.md` at EVERY sprint close, unconditionally**, whatever the card's `Autonomy:` and `Chaining:` lines say and whether or not the session carries on afterwards. The duty existed but was framed everywhere as a session-END artifact, so in automatic mode — where a session normally continues straight into the next sprint — the close was exactly the case nothing compelled, and the close is where a person actually walks away. Chaining decides whether a next chat is OPENED, never whether the hand-off EXISTS. **(2) Regenerate it as the work advances**, at every commit point (in Phase 5, every test point that commits) and unconditionally before the session ends: the courier checks compare `Commit` and `Tree` against the repository, so a file written at a close and left behind by three more slices returns `VERDICT: STOP` — it looks like insurance right up to the moment it is needed, which makes a stale hand-off worse than none. **(3) Read it FIRST at session start, before the state files** — this is what makes a bare "continue" restart the work with no recap. Run `scripts/keel-handoff-verify` and obey the verdict; a missing file is ordinary (it is gitignored, so a fresh clone has none) and the normal reading order proceeds. **(4) `VERDICT: STOP` costs the FILE its authority, not the SESSION its work:** discard the hand-off, say so in one line, resume from the committed state. The courier table always said this; `SKILL.md`'s "stops on a `VERDICT: STOP`" read alone said the opposite, and that is the sentence a session meets first. **(5) The hand-off is a COURIER, never an authority** — where it and `docs/PROGRESS.md` disagree, the committed state wins and the divergence is stated. **The lock block CHANGED for the second release running** — item 2 gains the read-it-first duty, item 5 the sprint-close duty — so `CLAUDE.md` and `AGENTS.md` (and any `GEMINI.md` mirror) need a real content refresh at the next lock-freshness check, not a stamp-only rewrite. |
| v5.8.0 | Forge issues stop waiting for the next sprint to END. Nothing to migrate, no new file, and the LOCK BLOCK IS UNCHANGED (a stamp-only refresh at the next lock-freshness check). **(1) Add `Issue sweep interval:` to the project card's `Autonomy:` line** — asked with the after-sprint issue duty in the session-start setup batch, default 24h, `n/a` on any project that did not accept the duty. **(2) Add the `Last inbound sweep:` header line to `docs/issues.md`** (`never` on an existing log until the first sweep stamps it), and stamp it the moment ANY inbound sweep finishes — sprint close or maintenance touch — found something or not. **(3) Sweep at the sprint KICKOFF too, before the sprint is planned**, whenever that line is missing or older than the interval: the after-sprint duty guarded only the closing edge, so a gap of days between sprints left a fresh report — or an `awaiting reporter` reply that decides an issue — unread until the NEXT close, and the sprint got planned without it. Within the interval the kickoff says so in one line and moves on, which is why a sprint chained seconds after a close pays nothing. A fresh issue the sweep turns up joins the conversation about what this sprint will contain, rather than being mentioned in passing. |
| v5.8.1 | The chained chat stops deadlocking on the chat that launched it. No new file, no card line, and the LOCK BLOCK IS UNCHANGED (a stamp-only refresh at the next lock-freshness check). Applicable at the next maintenance touch on any project whose card is not `Chaining: off`. **(1) Regenerate `scripts/keel-continue`:** it now writes its own session PID into the launch receipt right after claiming it, and runs `scripts/keel-handoff-verify --release` immediately BEFORE firing — the order is the fix, since the window it opens checks the lane within seconds. **(2) Regenerate `scripts/keel-handoff-verify`:** add `--release` (frees the lane only if this session is the recorded owner, prints one line either way, exits 0), add the BATON — a live lane holder whose PID is the one recorded in THIS hand-off's launch receipt is a session that has already closed out, so the lane passes instead of stopping — and make the busy-lane `STOP` name the holder's PID, its start time and how to clear it. **(3) The close-out releases the lane as its LAST act, on EVERY path** (`off` and `prefill` included, blocked hand-off included): the rule used to say the holder releases "on exit", which is a moment in the PROCESS that a SESSION never reaches — a chat sits alive and idle in its window for as long as the window is open, so the lane it finished with blocks the next chat whether the chain opened it or the user did. Measured on a real project: the chain did not fork, it deadlocked at link one with every guard behaving exactly as written. **(4) On a project not yet regenerated**, a lane stuck this way is cleared by closing the previous chat's window, or by running `scripts/keel-handoff-verify --release` in it. |
| v5.9.0 | The autonomous cycle stops being interrupted, from outside by a permission dialog and from inside by its own stop rule. No new file, no card line, and the LOCK BLOCK IS UNCHANGED (a stamp-only refresh at the next lock-freshness check). **(1) Merge the new `permissions.allow` block into `.claude/settings.local.json`** on any project where Keel wrote that file — union, never overwrite, never reordering or dropping a rule the user put there — covering the forge's conversational surface only: `gh issue view/list/comment/create/edit/close/reopen`, `gh pr view/list/diff/checks/create/comment/edit/merge`, `gh label`. `defaultMode: "auto"` was never sufficient by itself: it removes the static-matching problem, but the classifier it hands the command to reads a comment on a client's issue as an outward-facing action to confirm, so the cycle stopped to ask permission to answer the reporter it had just built a fix for. Say it in one line when the file is merged. **(2) What is left OUT is part of the change** — no `gh repo delete`, no `gh release create`, no blanket `gh api`. Publishing a release is a Phase 7 decision the user approves by name and it stays a dialog. **(3)** The `develop` → `main` rule is unchanged in substance; `references/keel-maintenance.md` now additionally records that a prefix rule cannot enforce it (the target branch is not in the command line) and what a `PreToolUse` hook would have to resolve to make it mechanical. Keel does not write that hook; it is the user's own machine configuration. **(4) A stop is scoped to the ITEM, never to the SESSION** — every "When to stop and ask" row says one piece of work cannot proceed on the assistant's judgment, not that there is nothing else to do; the item is parked (question asked, notification sent, state recorded) and the session moves to the next queue item independent of the answer. Waiting is correct only when nothing independent remains, established by looking rather than assumed. No per-project action: the rule lives in `SKILL.md`, which the lock's item 1 already requires reading in full every session. **(5) "Finish the queue" holds mid-block**, not only when a block of work ends — that framing was why the mid-flight "then wait" won. **(6) `Handover: blocked` describes the SESSION**: a hand-off with parked items and buildable work left is `clean` and carries the parked list as open items, so one unanswered question no longer costs the whole next session by suppressing the chain. **(7) The language of a forge message follows who owns the thread**: an issue Keel OPENS is always English (a permanent public artifact), and every reply on an issue somebody else opened is written in THAT issue's language, read from the reporter's own text and never from the repo, the conversation, a name or a locale — mixed thread → the reporter's last used language, undecidable → English. The language contract covered "everything Keel creates" and "the conversation", and a forge issue is neither; read as the first, it answers a Spanish bug report in English. No per-project action. **(8) NEW CARD VALUE — ask Question 2b once on every existing project that has a forge and record `Issue capture: on/off` on the `Autonomy:` line** (`n/a no forge` where there is no forge, and nothing else changes). With `on`, a problem the user reports becomes a forge issue OPENED BEFORE the work starts — so an interrupted session leaves the report standing rather than only in a chat log — commented as the work advances, deduplicated into an existing open issue rather than filed twice, and closed by the user, never by Keel. It is asked and never assumed: a tracker is a shared public surface. |
| v5.10.0 | Automatic mode stops writing a hand-off nobody opens. No new file, no new card line, no migration, and the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). **(1) `Chaining:` stops having a fixed recommended value.** It used to be recommended `off` on every project, whatever the card said about autonomy — so the standard automatic-mode setup was a session that wrote `docs/continuation-prompt.md`, showed it, and stopped, with nobody there to open the next chat. That is a stop, and automatic mode exists precisely so the work does not stop for a person who is not there. From v5.10.0 the recommendation FOLLOWS `Autonomy:`: on `Autonomy: automatic` the recommended value is the MAXIMUM tier this project's gates allow — `start` where the four requirements are met (single-lane lock, macOS-verified open action, allow-list entry, `claude` on PATH), `prefill` otherwise — and outside automatic mode it stays `off`. **(2) The gates do not bend and nothing is selected silently.** A recommendation can never promote `start` on a project that has not earned it, the Phase 1 step 0a question is still asked with its full warning attached (that `start` moves development to CLI sessions, and that it removes the only participant supervising), and `off` remains a valid answer on any card. **(3) Per-project action, one question, only where it applies:** on an existing project whose card says `Autonomy: automatic` and `Chaining: off`, re-ask the chaining question once with the new recommendation and record whatever the user answers — including `off` again. Projects that are not in automatic mode, and projects already on `prefill` or `start`, need nothing. |
| v5.10.1 | The chain stops refusing to fire on a project that is configured to chain. No new file, no card line, no migration, and the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Measured on a project already at `Chaining: start` with every gate satisfied: the close-out printed the continuation prompt instead of firing `scripts/keel-continue`, and then asked the user whether it should fire it — an automatic close that ends in a question is a stop. **(1) What may stop a chain is now a CLOSED list of four** — blocked hand-off, failed gate on the card's tier, missing `scripts/keel-continue`, or the script's own answers (receipt claimed, lane busy, breaker tripped). Everything else resolves to FIRE, and a session that does not chain names the entry that stopped it, by number. **(2) An unexercised mechanism is not a blocked one.** The card in question carried a note saying the end-to-end chain had never actually fired there — pieces tested, launch not — and it was read as a prohibition when it says the opposite: it asks the next real close to BE the evidence. A note about a project's history is a fact and never a veto; where prose must record one, it says what is missing and what unblocks it, because a bare "unverified" reads as a warning and will be obeyed as one. **(3) The close-out NEVER asks permission to chain** — the card's `Chaining:` value IS that permission, asked once at Phase 1 with its full warning, and a visible window is what `start` means rather than a new decision. **No per-project action:** all three live in `SKILL.md` and `references/project-state.md`, which every session already reads, so a project picks them up as soon as its copy is current. |
| v5.10.2 | Whether to chain is decided by the SCRIPT, never by the session. v5.10.1 closed the specific misreading but left the session still weighing the four stop conditions in prose before deciding whether to call `scripts/keel-continue` — the same kind of judgment call, made under a near-full context with nobody watching, that produced the incident. **(1) The session's role at close is now exactly two mechanical steps:** write the hand-off, then run `scripts/keel-continue` whenever `Chaining:` is `prefill`/`start` — unconditionally, clean or blocked — and relay what it printed. Its only remaining judgment call is whether the script exists. **(2) Every stop condition now lives inside the script, re-verified live on every run:** `Handover:`, the tool-tier registry probe, and a NEW check — `command -v claude` re-confirmed immediately before every `start` fire, rather than trusted from the one-time Phase 1 preflight (a PATH that changed since would otherwise fire an empty launch, silently). **(3) New eval scenario `tests/evals/scenarios.md` E7**, covering the clean-fire path (no permission asked, the card's history note never read as a blocker) and the blocked path (the script's own refusal, not the session's paraphrase) — closing a real gap: chain close-out is the single most field-fragile mechanism in the skill (a fork, the v5.8.1 deadlock, the v5.10.1 misread veto) and had zero regression coverage. **No per-project action:** the rules live in `SKILL.md` and `references/project-state.md`, which every session already reads, so a project picks them up as soon as its copy is current. |
| v5.10.3 | The `scripts/keel-continue` contract closes four real gaps found on a project's macOS `start` launch: a coin-flip narrative aside became an enforceable rule, and a canonical `env.PATH` template stopped hiding `claude` from its own detector. No new file, no card line, no migration, and the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). **(1) New numbered contract points 6a/6b** (`references/project-state.md`): 6a requires the Terminal command be written to a real, `chmod +x`'d script file executed BY PATH — never an interpolated string across the shell/AppleScript/Terminal-shell quoting layers — and names the BSD `mktemp` trap (a literal suffix after the `X` run is not randomized, so a template like `...XXXXXX.sh` collides on the SECOND run, not the first); 6b requires passing `claude` a short instruction to read the hand-off file, never the file's raw content, because the hand-off's own `---` freshness header reads as an unrecognized CLI option regardless of correct shell quoting. **(2) Point 8 now says explicitly that a `start` action's exit code proves a window opened, not that the intended command ran** — `osascript` exits 0 on the broken launches exactly as readily as the working one, which is how three bugs hid behind each other in the same incident; verify by reading back the new window's terminal history while developing or changing the script. **(3) `env.PATH`** (`references/keel-maintenance.md`, "Permission mode") now always includes the user's own per-user installer directory (`~/.local/bin`, expanded to its literal absolute value, never the string `$HOME`) alongside the MacPorts/Homebrew/system list — Claude Code's native installer places its binary there, and its absence made `scripts/keel-continue`'s own live `command -v claude` re-check (point 5a) report "not on PATH" on a machine actively running Claude Code. **No per-project action:** the rules live in files every session reads in full (`references/project-state.md`, `references/keel-maintenance.md`), so a project picks them up at its next `scripts/keel-continue` regeneration or maintenance touch; an existing project's own `.claude/settings.local.json` is fixed the same way, in one line, the next time `env.PATH` is touched. |
| v5.11.0 | Tests stop being written after the code they check. One new card line, one new test-point column, no migration and no new file; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). **(1) Ask the test-first question once and add the `Test-first policy:` card line** (`references/phase-2-functional-spec.md` §4e): `pure-logic` is the default and the value to propose — pure functions of their inputs (signatures, parsers, validators, state machines, money, entitlements) get their test written and seen FAILING before their code; `pure-logic + acceptance` also opens each slice with a failing check derived from its `AC-nn`, and may be scoped to part of the codebase in the plan's `## Testing`; `none` takes a `docs/decisions.md` entry. A blank line is not an answer. On an existing project this is not retroactive: no test already written is rewritten, and the policy governs from the next slice onward. **(2) Add the `Red first` column to `docs/05-test-points.md`** — one of the five values in `references/phase-5-development.md` (`observed`, `n/a — policy`, `n/a — out of scope`, `n/a — predates`, `n/a — delegated`). Existing rows take `n/a — predates` — the value that exists for exactly this migration, so a row that WOULD have been in scope is never confused with one the policy does not cover; new rows carry the real value, and an `observed` row's failure line goes in its evidence cell. **(3) Extend `scripts/keel-verify` with the three red checks:** FAIL a row whose `Red first` cell is empty or is not one of the five values (the same enum check `Coverage` already gets); FAIL a row claiming `observed` with no failure output beside it; and REPORT — never fail — every row whose `Red first` is neither `observed` nor `n/a — delegated`, listed at the sprint close (on a `none` project, one line naming the policy and its decision entry instead of a row list). The asymmetry is deliberate: the first two are mechanical facts about a cell, the third is a judgment a person should make over a greppable list — and it is blunt on purpose, because every value other than `observed` and `delegated` carries a judgment no script can make. **(4) From the next bug fixed, on ANY policy value including `none`, the fix starts from a failing reproduction test** — write it, run it, watch it fail on the bug, then fix; that test stays as the regression test. This applies to hotfixes too, where the pressure to skip it is highest and the cost of skipping it shows up three versions later as the same report from the same customer. **(5) Standing rule, every policy value: a test derived from an `AC-nn` or a reproduced bug is never edited to make it pass** — the requirement is what is wrong, and that is a decision entry or a Design Request. Renaming, moving, improving a failure message or fixing the test's own scaffolding are outside the rule; changing what would pass it is inside it. Full policy in `references/test-automation.md` ("When the test is written"); the two new traps are 12c and 12d in `references/anti-patterns.md`. |
| v5.12.0 | Websites stop coming out looking like the previous one. One new reference, one new conditional handoff artifact, one machine-local file, no migration and no card line; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Applies to website projects only, from the next site or the next redesign onward: no site already built is revisited. **(1) Run `references/phase-8-art-direction.md` BEFORE the design brief**, after the section catalogue and before `references/phase-8-design-direction.md` is filled in. It has eight steps and its own definition of done, checked item by item like every Keel gate: declare the Design Read in one sentence (site type, audience, archetype, named aesthetic family); set `LAYOUT_VARIANCE`, `VISUAL_DENSITY`, `MOTION_INTENSITY` and `EVIDENCE_RATIO` with a one-line reason each, arguing with the archetype preset rather than inheriting it; read the ledger; run the divergence round; consolidate signature elements; apply the blacklist; answer the self-critique; record and close. **(2) The divergence round is the engine and it is not skippable for time.** Design produces THREE directions for ONE section (hero, product card, or docs index by archetype), each from a differently NAMED aesthetic family, at least one a deliberate risk, each carrying its tokens and a fit rationale. The user picks one or consolidates two; the choice goes into `docs/decisions.md` with the rejected alternatives named, and that CLOSES the subject: the normal no-deviation regime resumes and later divergence is a Design Request. A waiver is itself a recorded decision entry naming what was traded away. **(3) New machine-local ledger at `~/.keel/art-ledger.md`** — outside every repository by construction, never committed, never shipped with the skill. The last three entries are a live constraint: primary typeface and pairing, palette family and accent hue, hero paradigm, grid family and every signature element are unavailable to the next site. Missing or unparsable ledger is an absent constraint stated in one line, never a block and never reconstructed from memory. The entry is APPENDED BY THE ASSISTANT at the close of the art direction, from the direction actually chosen — a ledger somebody has to remember to update promises a guarantee it does not deliver. **(4) New conditional handoff artifact `SPEC/art-direction.md`** (`references/handoff-contract.md`, "Website projects add two files to `SPEC/`"), carrying the read, the dials with reasons, the chosen direction with its rejected alternatives and decision entry, every signature element with its recurrence points and CSS treatment, blacklist overrides, and both self-critique answers. The Phase 4 completeness gate fails a website handoff without it and fails any signature element named there and absent from the delivered CSS. **(5) `phase-8-design-direction.md` §2 no longer asks for tone in adjectives** — it carries the recorded art-direction output verbatim instead; `design-brief-template.md` gains §2b for the same content, skipped by non-website projects. **(6) The blacklist is literal and vanilla-CSS-shaped** (layout, type, colour and material, content, punctuation) so that every item is auditable; its em-dash rule is binary and greppable: `—` and separator `–` are forbidden in EVERY string visible on the built site, and one occurrence fails the check. Scope is the rendered site only — Keel's own `docs/`, briefs and changelog are untouched. **(7) Two verification points:** the launch checklist verifies signature elements present and recurring, dials recognisable in what shipped, blacklist holding, em-dash grep at zero, and the ledger entry written; anti-patterns trap 24 and self-audit question 24 carry the same check into every sprint close and Phase 7 gate. |
| v5.13.0 | The unattended chain stops being a contract nobody executes. One new script, one new card line, one new hand-off header field, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Applies to projects whose card is not `Chaining: off`. The chain's every field failure had one shape: a piece of a prose contract was silently unmet for days, and the mechanism said so exactly once — at the close, with the context spent and nobody watching. **(1) Generate `scripts/keel-chain-check` at the next scaffold or maintenance touch on any project that chains**, and add its ONE allow-list entry `Bash(./scripts/keel-chain-check:*)`. It is the whole chaining contract made executable: twelve structural rows — launcher and verifier present and executable, both allow-list entries, `permissions.defaultMode` actually `auto`, `env.PATH` expanded/absolute with the system and per-user directories, hand-off gitignored, `claude` resolvable **under the DECLARED `env.PATH` and not merely under the current shell's**, lane directory reachable, the four measured v5.10.3 launch bugs absent from the launcher by grep, and the `Chain verified:` checksum still matching the launcher on disk — printing one line each plus `VERDICT: READY\|NOT READY\|N/A`, with `--json`. **(2) Run `--smoke` once, and again whenever `scripts/keel-continue` is regenerated or `Chaining:` is raised a tier.** It fires the real launch action with a harmless nonce prompt, in a dedicated receipt namespace that never touches the real hand-off, the lane or the breaker, then **reads the new window's own terminal history back** to prove the command RAN — because a launcher's exit code proves a window opened and never more than that, which is how four bugs hid behind each other in one incident — and closes the window. No read-back available → `INCONCLUSIVE`, which is a third state and never the good one. **(3) New card line `Chain verified:`** — date, tier, Keel version and the launcher's checksum, written only by `--smoke`. This is the load-bearing part: editing `scripts/keel-continue` invalidates the proof automatically, so nobody has to remember that a careful-looking edit un-proved the launcher. **(3b) New card line `Chaining model:`, asked at Phase 1 in the same breath as `Chaining:`, and a new contract point 5b: the launcher passes `claude --model <value>` on every fire.** A new `claude` process inherits nothing from the one that launched it — not its directory (known since v5.5.2) and not its model. Claude Code's precedence is `/model`, then `--model`, then `ANTHROPIC_MODEL`, then the settings `model` field, and the flag applies only to the session launched with it, so a bare `claude '<prompt>'` resolves the whole order from scratch. Measured: every link of a chain came up on Sonnet while the session launching it was on Opus — working perfectly, at a capability and a price nobody chose, reporting nothing, because from the launcher's side nothing went wrong. Row 10a of `keel-chain-check` catches both halves by grep. **(4) New `Mode:` field in the hand-off freshness header**, carrying `Autonomy:` and `Chaining:` so the arriving session applies the mode from the courier instead of from a card it has not read yet — the failure that looks least like a chaining failure and is the most common: the chain fires perfectly and the next session stops on the first permission dialog because it came up in `manual`. Carried, not checked; the card stays the authority. **(5) Two run points, neither of them a veto:** at every session START on a chaining project, beside `keel-doctor --check`, where a failing row still has hours in which to be fixed; and immediately BEFORE the close-out fire, so a chain that does not continue names the row that broke instead of leaving a printed prompt and a mystery. `keel-chain-check` adds NOTHING to the closed list of four things that may stop a chain — turning a diagnostic into a gate would re-open the exact judgment call v5.10.2 removed. |
| v5.14.0 | A procedure a session walks by hand is a procedure that gets skipped. Two new artifacts, no card line, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Measured: a session wrote the hand-off, kept working, committed twice more and never regenerated it — the header pointed at a commit two behind `HEAD`, `scripts/keel-continue` refused exactly as its contract requires, and the work sat still for **nine hours**. The rule it broke was already written here, already emphatic, already UNBREAKABLE. **(1) Generate `scripts/keel-close` at the next scaffold or maintenance touch, on EVERY project**, and add its ONE allow-list entry `Bash(./scripts/keel-close:*)`. It runs the mechanical tail of the close in a fixed order — commit, `keel-verify`, merge and push, write the hand-off with `Commit:`/`Tree:` read from git AT THE INSTANT OF WRITING, `keel-chain-check`, `keel-continue`, `--release` — and prints every step with its evidence. **The printed list is the checklist, and it is an OUTPUT rather than an input**: a checklist a session ticks itself is the same class of artifact as the prose it replaces. The sprint-close judgments (PROGRESS.md, the sprint file, lessons, the issue sweep, the self-audit) stay the session's and run BEFORE it. **(2) Install `.githooks/post-commit` and set `core.hooksPath` on EVERY project** — unconditionally, unlike the `pre-commit` confidential-data gate. The hook deletes `docs/continuation-prompt.md` if it exists, and that is all: committing removes the hand-off, so a hand-off that exists is newer than the last commit by construction and the stale case is removed from the space of possible states rather than caught later. This is only safe because Keel already treats an absent hand-off as normal ("a missing file is not an error — it means resume normally from PROGRESS.md"); an absent hand-off costs one read of PROGRESS.md, a lying one cost nine hours. Verify it fires on a real commit, not merely that the file exists. **(3) New row 10b in `keel-chain-check`:** hook present and executable, `core.hooksPath` pointing at it, and any hand-off on disk matching `HEAD` — the third is redundant while the hook works, which is exactly why it is the one that proves it does. A fresh clone has no `core.hooksPath` (local config, never committed), so this row is what catches the machine where every guarantee above quietly stopped applying. **(4) Keel's own scaffolding in a bad state stops ending chains.** Every stop condition is sorted by one question — is a CAPABILITY missing, or is a KEEL ARTIFACT in a bad state? Missing capability (no `claude` on PATH, no verified action, no launcher, no `PROGRESS.md` to fall back to) stays TERMINAL. A Keel artifact in a bad state (hand-off missing, stale, unreadable or `blocked`; `Chaining model:` unfilled) now DEGRADES: the courier loses its authority, the launch falls back to `claude "Read docs/PROGRESS.md and continue"`, and the script reports the degradation. **Identity and concurrency never degrade** — containment, `Repo:`, dirty tree, claimed receipt, busy lane, tripped breaker stay terminal without exception. The chain used to treat a MISSING hand-off as "resume from PROGRESS.md" and a STALE one as a permanent stop, with identical evidence available in both. Contract point 2 now reads the verifier row by row instead of as one verdict; point 3 degrades instead of printing. Full fourteen-row table in `references/project-state.md`. **(5) New notification trigger:** on a chaining card, a `scripts/keel-continue` that prints instead of launching notifies through the recorded channel with `keel-chain-check`'s failing rows beside it. The launcher refusing correctly into a window nobody is watching is a stall, and it behaving exactly as designed is what makes it easy to miss. |
| v5.15.0 | Every mechanism in v5.14.0 is reached by a session that DECIDES it is finished. A turn that just ENDS reaches none of them. Measured: zero chain fires across a seventeen-hour window containing two full working sessions, ten hours dead, nothing malfunctioning. One new artifact, no card line, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh). **(1) Generate `scripts/keel-stop-hook` at the next scaffold or maintenance touch, on EVERY project**, add its ONE allow-list entry `Bash(./scripts/keel-stop-hook:*)`, and **register it as a `Stop` hook** in every capable accepted tool's settings. It blocks the stop on the three UNBREAKABLE-broken states, blocks once more on a non-empty queue, never spins (a block that changed nothing is the last one, plus a hard cap per hour), hands the queue on through `scripts/keel-continue` before going quiet, and notifies through the recorded channel. Any internal error exits 0: it runs on every turn, so a hook that can break a session is worse than none. **Verify it FIRES** by ending a turn with a deliberately dirty tree — present and registered is not firing, the same rule the smoke run applies to the launcher. **(2) Re-read `references/anti-patterns.md`:** eight new universal entries, 12e-12l, all measured, seven sharing one shape — a command succeeded while reality did not change the way the session believed. They cover a second session writing into a checkout somebody else is working in; a copy in context read as if it were the repository; a repair whose effect was never checked; `git add -A` in a tree you did not author; an identifier chosen from memory; a network call inside an unconditional hook, and a protection that fails OPEN; a matcher that only sees the start of the command; and a control that passes by answering a different question. Self-audit rows 17a-17c. **(3) Two new SKILL.md operating principles apply immediately, no artifact needed:** never write into a repository another session is working in (reading is always safe; writing is what needs the check), and stage explicit paths while verifying a repair by its effect rather than its exit code. **(4) The context-discipline principle gains two exceptions:** re-read from DISK when the session is working ON the repository a file in context came from, and when an external process may have touched the tree. |
| v5.15.1 | The stop hook stops blocking the session that did not make the mess. No new card line, no migration; one new artifact that most projects already compute without naming, and the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Measured two days after v5.15.0 shipped: session A blocked naming two files session B had modified eight seconds earlier and was still editing, offered "commit to `develop`" as the remedy — `git add -A` in a tree it did not author — and stayed blocked eight turns, because rule 3's fingerprint covered the whole tree and B's keystrokes renewed it every turn. Not a block that expires; a block renewed by somebody else's work. **(1) Regenerate `scripts/keel-stop-hook`.** Its uncommitted-work rule is now scoped to the SESSION: it establishes whether another session is live in this checkout (`git status --porcelain` plus the modification times of what it lists, and `claude agents --json --cwd <path>` where available) and, where one is, CEDES — allows the stop and says so, naming the session it ceded to and why. Ceding drops no guarantee: the work belongs to a session carrying the same hook, which will be blocked by it under its own message. Unpushed commits and a stale hand-off still block either way, being facts about `HEAD`. **(2) The block log is keyed by repository AND session**, and the no-change fingerprint covers only what the block itself named (`HEAD`, the unpushed count, the hand-off identity, the status entries for the paths that block listed). Keyed by directory, two sessions shared one anti-spin history and one session's block could satisfy the other's "nothing changed" test — releasing a stop that should have been blocked. Same root cause, opposite sign. **(3) Generate `scripts/keel-session-pid.sh`** — one sourced file, one function `keel_session_pid`, this session's identity derived once from its own process (PID + start time) and read by the lane, the receipt, the baton and the hook, so "who am I" has one answer per project instead of one per caller. No allow-list entry: it is sourced, never executed. **(4) Nothing is loosened.** The three UNBREAKABLE-broken states, the queue block, the hourly cap, `stop_hook_active` and rule 5 (any internal error exits 0, no `ERR` trap) are unchanged, and a probe that cannot answer means "not established", which BLOCKS — a lone session with a dirty tree still blocks exactly as in v5.15.0. **Concurrency is detected; authorship is never attributed:** git records that a path changed and never who changed it, so per-file attribution is a guess and this skill does not ship guesses as checks. **The single lane is not this bug** and must not be re-keyed — it serialises a TREE, so the tree is the right key. **(5) Verify BOTH directions and verify it for real:** dirty tree with another session live allows and says why; dirty tree with no other session still blocks; two sessions' block logs are independent; the cap and `stop_hook_active` still behave — then observe the fixed hook FIRING after a session restart, because fixtures can describe two sessions and only a real turn proves the hook reads them. A guard taught to cede is one edit from a guard that never blocks, and both failures are silent. **(6) Re-read `references/anti-patterns.md` 12m** — a guard keyed to the directory enforcing a duty that belongs to the session — and self-audit row 17d. |
| v5.15.2 | v5.15.1 fixed the rule that was reported and left its sibling three lines below it untouched. No new artifact, no card line, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Measured on the next project that ran it: a session completed `scripts/keel-close` in full — commit, verify, push, hand-off, chain-check, `keel-continue` firing the successor, lane released — and was then blocked by rule 2 in a checkout that by then belonged to that successor, where this skill's own write rule forbids it to touch anything. Its brake could not engage either, because the shared fingerprint carried `HEAD` and the hand-off's `Generated:`, both of which change at every commit. On a real project the queue is never empty, so the net effect was: every committing session blocked at every stop, indefinitely. **(1) Regenerate `scripts/keel-stop-hook`. Rule 2 CEDES on the same condition rule 1 does** — another live session in the checkout → allow, printed as a cede, naming to whom. Rule 2's premise is "there is work left AND this session can do it", and in a checkout owned by somebody else the second half is false. A block whose remedy the blocked party is forbidden to perform is not a brake, it is a trap. **(2) A completed close-out DISCHARGES the queue block**, on evidence the hook reads from recorded state rather than from the session's word: a launch receipt claimed for THIS hand-off carrying this session's `launcher-pid`, the lane released, and `scripts/keel-handoff-verify` returning `CONTINUE` on a hand-off that describes `HEAD`. All three → allow, naming the receipt. Any one missing → block, because a partial close-out is the case the hook exists for. **(3) The queue is counted in `docs/PROGRESS.md`'s `## Open items` and nowhere else**, excluding items parked on the user (a stop is scoped to the item, so a person's silence is not this session's block). Unspecified, it was invented per generator: the one measured grepped a 4,000-line living document and counted 28, most of them historical bullets from closed sprints. **(4) Each rule's no-change fingerprint contains that rule's OWN inputs and nothing else** — uncommitted → the status entries for the paths that block listed; unpushed → the unpushed count and the upstream ref; stale hand-off → the hand-off's `Commit:` and `HEAD`; queue → the `## Open items` identifiers that block named. v5.15.1's enumeration was the union of every rule's inputs, which is v5.15.0's whole-tree fingerprint one rule to the right. **(5) Verify BOTH directions for EVERY rule that can block**, not only the one that was reported: cedes to a live session; a completed close-out discharges the queue block; three commits in a row do not re-block the session that made them; and — the direction that keeps the guarantee — a lone session with a non-empty `## Open items` and no close-out still BLOCKS. Then observe it firing for real after a session restart. **(6) New SKILL.md operating principle, applicable immediately:** fix the class, not the instance that was reported — the release that generalises a defect sweeps every instance of the class it can reach and names where it looked, and says what is left when the sweep cannot be completed. Anti-pattern 12m gains that closing rule; self-audit row 17e makes it auditable; eval scenario E14 covers it. |
| v5.16.0 | CI stops firing on the assistant's own commit loop, and WHEN it fires becomes the user's recorded decision. ONE new card line, no new artifact, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). **(1) NEW CARD LINE — ask the question once on every existing project that has a forge with CI and an accepted config package, and record `CI runs on:`**: `main` (the DEFAULT — push to `main`, version tags, and pull requests TARGETING `main`), `main+develop`, `all-branches`, or `n/a` where there is no forge CI or the package was declined. Asked at Phase 1 step 0a in the same batch as the assistant-config package, since that batch is already where CI is decided at all. Projects with no CI need nothing. **(2) The default is argued, not assumed.** In automatic mode Keel commits to `develop` many times per sprint and pushes each one, so a workflow keyed to every push re-runs the full suite dozens of times a day and produces a stream of green checks nobody reads — and a check nobody reads has stopped being evidence (`references/anti-patterns.md`, 12l). It is also a duplicate at that moment: the session drove the whole suite locally at the test point and ran `scripts/keel-verify` before the commit, seconds earlier, on the same code. The merge to `main` is different in kind — the one act in the flow that is always the user's, and the moment the work reaches users. **(3) What the default COSTS is recorded beside it:** a break only CI's environment surfaces — another OS, a clean checkout, a dependency present on the developer's machine and nowhere else — is found at the merge instead of at the commit that caused it. That is precisely what the wider values buy, which is why this is a question rather than a decision, and why the answer is never inferred from the repository, from how busy the forge looks, or from `Autonomy:` — a fully automatic project can still want `develop` green for a human collaborator. **(4) Version tags fire on EVERY value**, `main` included: the tag is what builds and publishes the release, and it is never traded away for a trigger preference. The setting governs which ORDINARY commits are checked, nothing else. **(5) Regenerate the `on:` block of an existing `.github/workflows/ci.yml`** (or the forge's equivalent) to match the recorded value at the next maintenance touch; changing it later is an ordinary maintenance change — one edit to the workflow, one to the card line, one D-entry saying why. **A PR trigger with no branch filter is the case to look for**: it reads as "on pull request" and fires on pull requests to every branch, so a repo whose pushes are already `main`-only can still be running CI on the integration branch without anyone having chosen that. |
| v5.17.0 | The sprint plan becomes a plan instead of a directory, and a project may publish an end-to-end result. Four new artifacts (two always, two conditional), TWO new card lines, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). **(1) The sprint plan, at the next sprint kickoff.** `docs/sprints/sprint-<N>.md` gains YAML frontmatter as its DATA (`schema: keel.sprint/1`, sprint, goal, status, and per slice `id`/`title`/`status`/`hours`/`depends_on`/`criteria`) with the prose staying in the body; `docs/sprints/deferred.md` is created as the ONE backlog file, sharing an id namespace with the slices so promoting an item is a move that keeps its id; and `docs/.keel/plan.json` plus the human index are GENERATED from both and never hand-edited. **(2) Percentages are computed from hours and never stored.** The plan is explicitly modifiable, so a hand-written `%` is wrong the first time anything moves, and wrong in silence. The hours are Keel's existing unit — AI working time plus supervision — so the plan's per-slice hours ARE the Phase 5 partida and `docs/estimate.md`'s Phase 5 line is their sum; contingency is excluded from the percentage or the plan never reaches 100%. Deferred hours are what the NEXT version would cost and stay out of the total. **(3) `scripts/keel-verify` gains the plan checks:** closed status enum; no duplicate id across sprints and deferred; every `depends_on` resolving to the same sprint or an earlier one and never to a deferred item; `plan.json` matching its sources; the estimate agreeing with the plan's hours; and a slice that left a sprint being in `deferred.md` or `dropped` with its D-entry rather than deleted. **(4) NEW CARD LINES `E2E:` and optional `E2E env:` — ABSENT IS THE DEFAULT and absent means nothing happens at all.** Where a project declares an end-to-end command, its newest result is published to `docs/.keel/e2e-status.json` (gitignored by default) and optionally appended to `docs/.keel/e2e-history.jsonl`, and the Phase 7 gate passes only on `result: "pass"` with `commit` equal to `HEAD` — a missing file, a stale one, an `error` and a `fail` all block and each says which of the four it was. A waiver is a `docs/decisions.md` entry written first; the hotfix path alone may reduce the scope, naming the reduction in that entry. `scripts/keel-doctor` verifies the declared command is RUNNABLE, not merely present. **What is optional is the publication, never the verification:** the Phase 7 full driven-suite re-run is untouched and stays unconditional, and updating the skill changes no existing project's behaviour — removing the line returns a project to its previous state with no residue. Keel installs no browser, runtime or test framework, and depends on no reader of these files. **(5) One convention for every machine-readable artifact** (`references/project-state.md`): location `docs/.keel/<name>.json`, a mandatory self-identifying `schema` of the form `keel.<name>/<n>`, readers tolerating unknown fields and REFUSING an unrecognised schema, closed enums wherever a program branches, and derived files regenerated rather than edited with drift failing the run. A new machine-readable artifact that ignores this is the class 12m describes. |
| v5.18.0 | Two related mechanisms stop assuming one assistant's contract or action applies to every accepted tool. No new artifact, no card line, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Measured on the same project, the same week: (a) `.codex/hooks.json` pointed at the identical `scripts/keel-stop-hook` Claude Code runs; Codex rejected its `hookSpecificOutput`/`decision: "block"` output with "invalid stop hook JSON output," ending the turn the hook exists to keep open. (b) `scripts/keel-continue` opened a Claude Code Terminal window at the close of a CODEX session — the CLI row's action fired under a different tool's detection. **(1) The container matrix (`references/assistant-config.md`) gains a "Stop hook" row.** `scripts/keel-stop-hook`'s output — Claude Code's own schema — is UNCHANGED and never rewritten to serve another tool; it is registered ONLY where that exact schema is CONFIRMED against the target tool's own current documentation — today, Claude Code alone. Every other cell is `—`, exactly like an allow-list cell with no committable mechanism: the duty stays with the session, never a guessed schema shipped as if verified. This is the opposite case from `.githooks/pre-commit`, portable because GIT invokes it, never the assistant. **(2) `scripts/keel-continue`'s contract gains point 4a (UNBREAKABLE): the action fired is ALWAYS the DETECTED tool's own row — never a different tool's action substituted as a fallback.** An unmatched or unverified row prints; printing is not a lesser outcome to route around, it is the correct one, and it costs nothing a person cannot recover from by reading the prompt. **(3) The tool registry gains a real, sourced Codex `start` action** — `codex --cd '<repo>' --model '<Chaining model:>' --ask-for-approval never --sandbox workspace-write '<prompt>'`, Codex's own documented working-directory, model and auto-approval flags (never the sandbox-erasing `--yolo`), marked **DOCUMENTED, UNTESTED** like the row it replaces — no evidence tier is upgraded without an actual observed firing. **(4) `Chaining model:`'s card description is generalised** from "passed as `claude --model <value>`" to "passed via the DETECTED tool's own model flag" — the mechanism was already tool-agnostic in intent; only the one example in its own description was Claude-only. **(5) Two new universal anti-patterns, 12p and 12q** — a hook's trigger existing is not evidence its contract matches another tool's; a launcher's proven row is not licence to fire it for a tool it was never proven for — both measured on the same incident report, both closing self-audit rows (17f, 17g). **(6) The `CI runs on:` default (v5.16.0) gains its concrete reason.** GitHub Actions' included minutes on the Free/Team tier are billed PER ACCOUNT, pooled across every private repository — never per repository — and exhausting them does not fail loudly: a run is simply never scheduled, nothing to read a failure from. Measured on this skill's own repository: a confirmed, correctly-formed tag push produced no workflow run at all, discovered only because the expected release never appeared. Where the forge is GitHub and the repo is private, this is now named explicitly alongside the signal-to-noise argument (`references/assistant-config.md`, `references/phase-1-discovery.md`), checking visibility with the forge's own tool where available rather than assuming it. New universal anti-pattern 12r and self-audit row 17h. |
| v5.19.0 | Three additions from one two-day failure, plus one card value. No new artifact and no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). **(1) `docs/decisions.md` gains a `Not checked:` field, REQUIRED on any entry that asserts an impossibility** — at least one avenue that was NOT examined. Measured: a project recorded "there is no channel into an already-running session" after checking exactly two interfaces; the product had an official feature doing precisely that, and the entry cost two days of the wrong architecture while being cited back at the owner three times. Add the matching `scripts/keel-verify` check (grep `docs/decisions.md` for `cannot` / `there is no` / `not possible` / `impossible` / `does not support` and fail any such entry with no `Not checked:` line) at the next maintenance touch. **(2) When the USER contradicts a recorded negative, RE-MEASURE — never restate the conclusion.** This is the one counterpart to the append-only rule: that rule protects decisions that were CHOSEN; a negative finding is a claim about the world, and the user's contradiction is fresh evidence about that world. **(3) New card value `Chaining: supervised — <what supervises it>`**, for a project whose continuation is handled by something OUTSIDE it (an external supervisor, a scheduler, a CI runner, a person). `off` was conflating "we chose not to chain" with "somebody else continues this", and Keel's own chaining fights an external supervisor by opening a sibling session it cannot reach. It counts as `off` at every chaining gate — no `scripts/keel-continue`, no `scripts/keel-chain-check`, `Chaining model:` and `Chain verified:` `n/a` — while the hand-off is written and shown exactly as on every other value. Change an existing card from `off` to `supervised` ONLY where that is genuinely true, naming the supervisor; it is a record correction, not a behaviour change. **(4) Three new universal anti-patterns and self-audit rows:** 12s (the negative result recorded without its scope, 17i), 12t (the test that asserts an assumption instead of a requirement — a test NAME that states a mechanism rather than an outcome, measured as a green test certifying a swallowed argument as correct behaviour, 17j). |
| v5.19.1 | Wording only, in the two places the chaining question is actually put. No new artifact, no card line, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). **The `supervised` option now names an instance alongside the categories** — *an external supervisor (SessionPort, for example), a scheduler, a CI runner, or a person*. Measured on the first project asked the question: the owner had built the supervisor, it was managing that very repository, and they still could not tell which option was theirs, because the list named four abstractions and no example. Keel still never learns the supervisor's name and the card still records whatever the user answers; an example is not an endorsement and not a dependency. |
| v5.19.2 | The stop hook stops reading a rename as "nothing happened". No new artifact, no card line, no migration; the LOCK BLOCK IS UNCHANGED in substance (a stamp-only refresh at the next lock-freshness check). Measured: a session was blocked on `UNBREAKABLE-broken: uncommitted work` over a single STAGED RENAME it had not made, and offered as its remedy a commit in a checkout that was not its own — v5.15.1's cede was present, correct and complete, and unreachable. The cede needs two facts, another session live AND something in the dirty tree touched recently, and the second was computed by slicing `git status --porcelain` from the fourth character. For a rename that line is `R  old -> new`, so the slice produced a string that is not a file, `stat` skipped it, and "nothing was touched recently" came back as if it were a measurement. **(1) Regenerate `scripts/keel-stop-hook`** on any project that carries one: its mtime probe now PARSES the porcelain line — split on ` -> ` and keep the DESTINATION, unquote and unescape a quoted path — and reports a path it cannot stat (a deletion, a rename's source) as NOT ESTABLISHED rather than as quiet, so where no entry can be stat-ed the cede rests on `claude agents` alone instead of on a silence read as evidence. Verify it against a rename and against a path with a space; a fixture produces both in four commands. **(2) The same parsing rule binds the UNBREAKABLE write rule**, which runs the identical probe by hand before the first write into a repository this session did not start — SKILL.md, no artifact needed. **(3) Re-read `references/anti-patterns.md` 12u** — the probe whose parser drops every input, so it answers with the default that disables the guard — and self-audit row 17k. Nothing is loosened: a lone session with a dirty tree still blocks, and a probe that cannot answer still means "not established". |

## Maintenance (part of EVERY release — no exceptions)

- Every file touched by a release updates its "Last changed in" row; a new file gets a new row; a removed file loses its row (noted in the changelog).
- If the release changes what a project must contain (new required file/directory, new project-card line, a condition change), Table 1 is updated in the same release — and Table 3 gets the release's action row (or an explicit "None structural").
- The header line of this manifest carries the skill version and is one of the FOUR synced version locations (frontmatter, SKILL.md heading, CHANGELOG.md, MANIFEST.md header) governed by the version change policy in `references/keel-maintenance.md` — never bumped without explicit user instruction. The repository `README.md` version line and the canonical lock-block stamp in `references/project-state.md` are kept in sync as part of the same release hygiene.
- Verifying this manifest against the release's actual diff is part of release hygiene for the skill itself: a stale manifest defeats its purpose. Run `python3 tests/lint-release.py` before tagging — CI runs it again on every tag.
