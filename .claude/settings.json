{
  "_comment": [
    "Klytos CMS — committed permission allow-list (Sprint 1, slice 9).",
    "ONLY the verified tooling and playground commands from docs/03-technical-plan.md",
    "and docs/playground.md. Exact commands or tight prefixes; never a catch-all.",
    "Deliberately absent, and they must stay absent: installer/install.php and",
    "cli.php build (NEW-04 — both are destructive to a checkout), and git push",
    "(the user has instructed that nothing is pushed until the project is stable).",
    "Personal, machine-local overrides belong in .claude/settings.local.json,",
    "which is gitignored."
  ],
  "permissions": {
    "allow": [
      "Bash(composer install:*)",
      "Bash(composer audit -d installer)",
      "Bash(vendor/bin/phpunit:*)",
      "Bash(XDEBUG_MODE=off vendor/bin/phpunit:*)",
      "Bash(vendor/bin/phpcs --standard=phpcs.xml:*)",
      "Bash(vendor/bin/phpcbf --standard=phpcs.xml:*)",
      "Bash(php scripts/keel-verify)",
      "Bash(php scripts/dev/seed-playground.php:*)",
      "Bash(XDEBUG_MODE=off php scripts/dev/seed-playground.php:*)",
      "Bash(bash scripts/dev/upgrade-test.sh)",
      "Bash(XDEBUG_MODE=off bash scripts/dev/upgrade-test.sh)",
      "Bash(php -l:*)",
      "Bash(php -S 127.0.0.1:*)",
      "Bash(nc -z 127.0.0.1:*)",
      "Bash(curl -D - http://127.0.0.1:*)",
      "Bash(curl -s -o /dev/null http://127.0.0.1:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(git ls-files:*)",
      "Bash(git check-attr:*)",
      "Bash(git check-ignore:*)",
      "Bash(git tag --sort:*)",
      "Bash(git archive:*)"
    ]
  }
}
