# PROGRESS — Klytos CMS

> Living state. Read this FIRST in every session. Keep current and compact.

## Project card
- Name / one-line purpose: **Klytos CMS** — self-hosted AI-first CMS: build and manage websites entirely with AI through the Model Context Protocol (MCP), generating a static site.
- Project type: **Web app (self-hosted PHP application)** / **MCP server** (spans both — stricter rule wins on conflict)
- Stack & target platform(s): PHP 8.1+, no framework (custom microframework), Apache + mod_rewrite, pluggable storage (flat-file JSON or MySQL/MariaDB), vanilla CSS/JS, static-site build output. See `docs/03-technical-plan.md`.
- License: GPL-3.0-or-later
- Docs language: English (D-008)
- Security profile: `references/security/web-app.md` + `references/security/mcp-server.md` (D-003)
- Accessibility: **WCAG 2.2 AA + European Accessibility Act**, admin panel *and* generated output (D-007) — measured baseline ~20–25% / ~15%, see `docs/04-adoption-audit.md`
- i18n: multi — base English, 20 locales (ca, da, de, el, en, es, eu, fi, fr, gl, it, ja, nb, nl, pl, pt, ru, sv, tr, zh), custom JSON key-based mechanism (`installer/core/i18n.php`), no gettext (D-006)
- Installed base: **yes — released publicly** (GitHub Releases + self-updater + standalone installer repo); production installs with stored, encrypted data. Migrations / backward compatibility are mandatory.
- Design system: **existing**, token-based (`installer/admin/assets/css/klytos-tokens.css`, 118 `--klytos-*`; frontend tokens generated by `theme-manager.php`); no design source files. UI predates Keel — no design contract, current look is the baseline (D-009)
- Keel portability: lock + embedded v3.4.0 (both trees: `.claude/skills/keel/` + `.agents/skills/keel/`) (D-002)
- Assistant config: **full** (tools: claude, codex, copilot, cursor, gemini, windsurf) — per references/assistant-config.md (D-010). Gemini via `.gemini/settings.json` `context.fileName` → `AGENTS.md`, no `GEMINI.md` mirror.
- Models: orchestrator = session model (strongest available) / reviewer = mid tier / mechanical = cheapest tier — concrete names live only in each tool's agent config (D-011)
- Keel baseline: v3.4.0
- Website intent: **yes** — klytos.io; Phase 8 deferred until requested (D-012)
- Client budget: **no** — own project (D-013)
- User guide: n/a until Phase 6
- Docs theme: n/a until Phase 6

## Phase status
| Phase | Status | Key artifacts |
|-------|--------|---------------|
| 1 Discovery | **adopted (as-built)** | docs/00-competitive-landscape.md, docs/01-discovery.md |
| 2 Functional spec | **adopted (as-built)** | docs/02-functional-spec.md, docs/03-technical-plan.md, docs/api/INDEX.md |
| 3 Design handoff | n/a — UI predates Keel, no design contract (D-009) | — |
| 4 Faithful build | n/a — see Phase 3 | — |
| 5 Development | **in progress** — Sprint 1 CLOSED (all 10 slices); Sprint 2 CLOSED (MCP tool authorization, all 4 slices — audit NEW-02 closed); Sprint 3 CLOSED (vendor-ai CVE remediation + the AI stack fails safe — both slices; audit NEW-05 and NEW-06 CLOSED); **Sprint 4 CLOSED** (the hook mutation contract + owner recovery — both slices; audit NEW-03, NEW-36 and NEW-08 CLOSED) | docs/sprints/sprint-1.md … sprint-4.md, docs/05-test-points.md, docs/estimate.md, docs/flows/ |
| 6 Documentation | pending — progressive backfill of per-surface docs is in force | docs/architecture.md, docs/api/, docs/usage/, docs/reference/ |
| 7 Release | pending — the next release runs the FULL Phase 7 | docs/07-release.md |
| 8 Website | deferred (intent = yes) | docs/site/ |

## Current position
- Phase: **5 Development — SPRINT 2 CLOSED 2026-07-24 (MCP tool authorization, all 4 slices; audit NEW-02 CLOSED). Sprint 1 CLOSED (all 10 slices, 2026-07-20). The project has a playground, a test harness whose own guards have now been proven rather than assumed, an auditable dependency tree, an identity that fails closed, one capability matrix, a central default-deny authorization gate over every admin surface (66 at slice 4, **64** since slice 7 deleted one and its map entry) with `scripts/keel-verify` keeping it honest, a named regression test standing behind every escalation the audit reported, and — as of slice 6 — one SSRF-resistant fetcher applied at every call site whose URL an untrusted party influences, and — as of slice 7 — its first genuinely anonymous write surface, deliberately built OUTSIDE the admin tree, and — as of slice 8 — one place that decides response security headers and one place that sends them, covering all 64 admin surfaces including the refusals. Sprint 2 planning done; slice 1 CLOSED 2026-07-22 (MCP actor resolution). **Slice 2 CLOSED 2026-07-22** — the gate: `installer/core/mcp/tool-capabilities.php` maps the 169 live core tools (absent = deny, `null` = audited exception, `mcp.tool_capabilities` filter); a typed `PermissionDeniedException`; ONE `denialReason()` default-deny gate in `ToolRegistry::call()` **above** the `mcp.handle_tool` filter; `setActor()` carries the credential's identity per request (server.php after auth, chat-engine from the session); `listTools()` filtered by the same decision; `server.php` catch → JSON-RPC error object + **HTTP 403**; a `mcp.access_denied` audit action; keel-verify **check 10** (9→10). **Slice 3 CLOSED 2026-07-23** — coverage completeness: the tool loader **fails loudly** (extracted `registerToolFile()` + typed `ToolRegistrationException`, D-049/L-007); `integrity-tools.php` wired in as the 34th loader file with its 3 tools mapped `site.configure` (live count 169→**172**, check 10 now 172); the two shipped MCP plugins declare capabilities via `mcp.tool_capabilities` (`klytos-forms` 16→`forms.manage`, `klytos-importer` 10→`site.configure`) and are activated in the seed; `chat-engine` `getAvailableTools()` default-denies unknown roles + closes the `function_exists`/null fail-opens; and **NEW-30 RESOLVED** (user-confirmed, **D-050**) — `exists()` = registered OR mapped so filter-injected tools (x402, the plugins) are callable over HTTP AND gated. Suite 171→**185 tests/805 assertions**; every new test proven to FAIL against unfixed code (stash+reseed: 2 errors/9 failures); **both review subagents clean, no blocking** (non-blocking fixes applied: the broad `RuntimeException` catch narrowed to a typed `ToolNotFoundException` + a mapped-but-unhandled pin test, keel-verify stale comments corrected); upgrade from real v0.30.1 passes; all D-025 baselines held; live playground owner `tools/list` = **206** served (172 core + 8 x402 + 16 forms + 10 importer), NEW-30 live proof (owner `klytos_forms_list` over HTTP → 200). **Slice 4 CLOSED 2026-07-24 — and with it SPRINT 2.** The reconciliation slice: the counts were **re-measured live, not carried forward** (the plan's 177/169/3 were all stale after slice 3) — **172** core loader tools across 34 files + **8** filter-injected x402 = **180 served by a default install**, **206** with both shipped MCP plugins active (the playground, and what `docs/api/INDEX.md` records as tools on disk), **0** dead; the `authorization.md` forward reference is **closed** (it claimed 172 ungated tools and a `klytos_require_permission()` reuse — both false) and now points at `reference/mcp-authorization.md`, which gained "How many MCP tools there are" and "**Adding a new MCP tool — the checklist**"; the client-facing refusal is **translated in all 20 catalogues** (`mcp.permission_denied`, resolved through the I18n *service* — the global `__()` does not exist on the MCP path, NEW-18 — naming the tool and the fix, never the role or capability); `docs/playground.md` gained §3a with a runnable `tools/call` curl and the **measured** per-role table; **8 skills** updated; and **`ai.use` widened to `editor`** (**D-051**, user-confirmed, superseding D-035 at its own recorded trigger — the chat can no longer amplify a role because every tool call carries the caller's own one). Suite 185→**190 tests/951 assertions**; both changes proven to FAIL against unfixed code (4/4 i18n red against reverted catalogues; 2 red against the un-widened matrix, one of them over real HTTP); keel-verify 10 checks; upgrade from real v0.30.1 PASS; all five D-025 baselines held exactly. **L-018** recorded — a non-disclosure test written as a word blocklist failed on correct English.**
- **Slice 0 CLOSED 2026-07-18** — the playground exists and works: `scripts/dev/seed-playground.php` + `scripts/dev/router.php` + `docs/playground.md`. Gate zero met (baseline-locked lint per D-025; app boots; MCP `tools/list` → 177 tools). Evidence in `docs/05-test-points.md`.
- **Slice 1 CLOSED 2026-07-19** — the test harness exists and both tiers work: `tests/Unit` (no App; temp dir per test; runs on a bare checkout) and `tests/Integration` (real App on the playground; `actingAs( $role )` assigns `$_SESSION`) — the seam slices 3–5 assert refusals through. Dev-only `composer.json` (D-022) with PHPUnit pinned and `composer.lock` now tracked (D-027). Test point PASS: `composer install` clean, 9 tests / 37 assertions green, `phpcs` clean, D-025 baseline unchanged at 204/488. Proven (not assumed) that the integration tier **skips** rather than passing when the playground is absent. Evidence in `docs/05-test-points.md`.
- **Slice 2 CLOSED 2026-07-19** — `installer/vendor-ai/` is auditable: `installer/composer.json` + tracked `installer/composer.lock` pin all **16 packages** (not 9 — the original H-04 count was wrong) at exactly the vendored versions, derived from Composer's own `installed.json`, not guessed. `composer audit -d installer` runs. Test point PASS: 0 resolution deltas, `vendor-ai/` unmutated, suite 12 tests / 57 assertions green, D-025 baseline unchanged at 204/488. Drift guard `tests/Unit/VendorAiManifestTest.php` proven to fail on injected drift before being trusted. Audit H-04 **closed**. Evidence in `docs/05-test-points.md`.
- **NEW-05 CLOSED 2026-07-25 (Sprint 3 slice 1).** The 11 advisories in the vendored HTTP stack are gone: `composer audit -d installer` reports **zero**, measured both sides. guzzle 7.10.0→**7.15.1**, psr7 2.9.0→**2.13.0**, promises 2.3.0→**2.5.1**, plus a new **`symfony/polyfill-php80`** required by both (16→**17** packages, 482→**509** tracked files, a **95-file** diff). **D-029's recorded floors would have left 6 of 11 open** — the criterion "re-audit to zero" won over the derivation "≥7.12.1/≥2.12.1" (L-014), a user decision recorded as **D-052**. The reachability assessment was **redone from scratch**: no cookie jar exists and Guzzle's default is `false` (now asserted by a test), nothing sets a proxy, redirects strip `Referer`, and — the question this slice was told not to inherit — `safe-http.php` and `http-client.php` contain **zero** `GuzzleHttp`/`Psr\Http` references, so the two HTTP stacks never share a URL and the differential-parsing precondition for the host-confusion CVEs **does not exist here**; NEW-15 is untouched by them. **None of the 11 had a demonstrated exploitation path in Klytos** — the bump closed a standing obligation on a released product, not a live hole. **L-020** recorded: the drift guard's root-package skip was hardcoded to `'__root__'` and had never been exercised, because it was built against a tree vendored elsewhere and never regenerated here.
- **Two further findings from the same slice, both recorded and unpatched:** **NEW-06** — the vendored AI stack requires PHP 8.3 (`soukicz/llm`) while Klytos declares 8.1+ (D-004), with no guard at the load point, so the failure mode is a fatal inside a vendored library rather than a disabled feature; **NEW-07** — `soukicz/llm` and `phplang/scope-exit` (both BSD) ship no licence text, and the compensating `LICENSE-THIRD-PARTY.md` listed 14 of 16 packages and misattributed `soukicz/llm`. The notice itself is fixed and now guarded by a test; the packaging half (it is stripped from release archives by the blanket `*.md export-ignore`) belongs to H-02 and Phase 7.
- **Slice 3 CLOSED 2026-07-19** — the authorization axis now has a single matrix and an identity that fails closed. `klytos_current_user()` no longer promotes an unidentified session to `owner` (NEW-01/D-021 — the escalation that defeated every gate the sprint adds); the 22-entry capability matrix exists once, in `UserManager::hasPermission()`, with `klytos_has_permission()` delegating (S-04, resolved the **opposite way** to the audit's note and recorded); `UserManager::hasPermission()` also denies a record with no usable role instead of defaulting it to `viewer`; and the boot crash on a failing v1.x migration is contained (D-031 — a user decision that overrode the assistant's proposal to defer it). Test point PASS: 32 tests/219 assertions, the 4 refusal tests **proven to fail** against the unfixed code, upgrade from **real v0.30.1** passing 17/17, D-025 baseline **improved** to 201/488. Evidence in `docs/05-test-points.md`.
- **Slice 3's prerequisite was resolved first: storage isolation for the integration tier (D-030)** — snapshot/restore of `installer/config/` + `installer/data/` around every integration test, proven to fail with isolation off before anything relied on it. It is what makes the migration test safe, since that test deletes the seeded owner.
- **Two findings from slice 3, recorded and unfixed:** **NEW-08** — there is no supported way to recreate a missing owner anywhere in the product (the CLI's `users` command only lists), so the contained crash degrades to a diagnosable, fail-closed denial but does **not** restore access; and a sharpening of **NEW-02** — `ai/chat-engine.php:401-421` filters the AI tool list only for exactly `viewer`/`editor`, so any unrecognized role falls through unfiltered (fail-open), which Sprint 2's enforcement point must fix by default-denying unknown roles.
- **Slice 4 CLOSED 2026-07-19** — the admin panel is gated, systemically and by default. S-07 is closed by **inversion** rather than by the audit's suggested shape: instead of adding a gate call to 51 files (which leaves file 67 free to forget), the decision moved to `admin/bootstrap.php` — verified mechanically to be required by all 66 admin files — driven by a **gate map** in `installer/core/admin-gate.php` where an ABSENT entry is a REFUSAL. Coverage went 15/66 → **65/66 mapped**, the 66th being `bootstrap.php` itself, deliberately unmapped so a direct request for it default-denies. `klytos_require_permission()` / `klytos_deny()` / `klytos_current_surface()` are the reusable helpers Sprint 2 consumes; the denial shape was **promoted** from `router.php:438-447`, not reinvented. API surfaces now answer **401/403 JSON** instead of a 302 to HTML. Five capabilities were added to the ONE matrix (S-04 preserved). Test point PASS: 45 tests/270 assertions, 5 of the 6 HTTP tests **proven to fail** against the unfixed code, `keel-verify` **proven to fail** on a removed gate then restored, a full **65×4 = 260-request role walk** confirming no lockout, upgrade from real v0.30.1 still 17/17, D-025 baseline unchanged at 201/488. Evidence in `docs/05-test-points.md`. Decisions **D-032…D-035**; lessons **L-007**, **L-008**.
- **Bugs found by slice 4's survey and fixed IN-PATH (D-033), per D-031's narrowing:** **NEW-10** — `setup-wizard.php` required authentication but checked no role, so on a fresh install any authenticated user could complete setup and **mint themselves an MCP application password**; **NEW-12** — `api/download-identity.php` carried **three** stacked defects, each masking the next: `Auth::isLoggedIn()` (no such method, so the endpoint has been dead in production for everyone), then `Logger::log()` (no such method either, reached for the first time once the first was fixed), plus a hand-rolled owner check. The hand-rolled `in_array( $role, ['owner','admin'] )` in `security.php` was replaced with the matrix on **five** sites — four POST branches and, caught by `code-reviewer`, the section's visibility check that the first pass missed.
- **NEW-09 was fixed and then DELIBERATELY REVERTED inside the slice (D-036) — the most important thing that happened here.** Adding `api/webauthn-challenge.php` to bootstrap's pre-auth exemption, to repair passkey second-factor login, opens a **full account-takeover primitive**: `is2faPending()` is true after a correct **password alone**, that endpoint gates all four of its actions on it, and `TwoFactor::completePasskeyRegistration()` enrols a new authenticator and sets `enabled = true` **without checking any existing factor** — so a stolen password would defeat 2FA permanently. Verified against source, not taken on the reviewer's word. The exemption also bought **nothing**: passkey login cannot complete regardless, because `verifyPasskeyAssertion()` has zero call sites and `login.php`'s 2FA dispatcher has no `passkey` branch. NEW-09 stays **open**, with its fix shape written down, bound to the NEW-11 slice.
- **NEW-11 found and deliberately NOT fixed:** `Auth::login()` (`core/auth.php:99-102`) validates **only** against `config['admin_user']` and never calls the fully-implemented `UserManager::authenticate()`, so `admin`, `editor` and `viewer` accounts **cannot log into the admin panel at all** — verified live against the seeded playground. This is authentication, not authorization, and is an adjacent subsystem in D-031's sense. It is very likely *why* S-07 survived: with one usable account, ungated surfaces never misbehave. Trigger: Sprint 2 planning. Stated plainly: **Sprint 1 gates a multi-role system that, in production, only one role can currently enter.**
- **Standing warning for slice 7:** `api/comment-submit.php` is the next file due to join `$preAuthScripts` (S-09). An entry there removes the only authentication check in front of whatever that file does internally — it gets the same scrutiny NEW-09 just received, or it repeats it.
- **Slice 5 CLOSED 2026-07-19** — every named escalation now has a named test, and two defects the central gate could not reach are closed. S-01, S-02, S-03 and S-05 were already shut by slice 4's gate and are now each pinned by a test that fails if they regress, every one of them asserting the POSITIVE case too (L-008). **S-06 had a live residue:** `api/tasks.php` never re-gated `update`/`complete` at `tasks.manage` although its page twin does (`tasks.php:38`), so an editor was refused task completion through the interface and allowed it through the endpoint the interface calls — an asymmetry the gate map cannot see, because both files legitimately sit at the same floor. **S-12 is closed:** the RSA private-key export now requires POST and CSRF, and `security.php`'s form posts straight to it — the old 302 redirect *was* the reason a state-writing secret export answered GET, and a redirect cannot carry a POST. Its docblock's three false claims (re-auth, 2FA, owner notification) are corrected rather than implemented; the missing protections are **NEW-13**, bound to the authentication slice (D-040). Test point PASS: **60 tests / 336 assertions**, 3 new tests **proven to FAIL** against the unfixed code, `keel-verify` 2/2, upgrade from real v0.30.1 passing, D-025 baseline **improved** to 199/488. Decisions **D-038…D-040**; lesson **L-010**.
- **The most consequential finding of slice 5 was in the harness, not the product.** `PlaygroundState::assertConfigNotMutated()` — D-030's "honest half", the guard that fails loudly where a file restore cannot reach — **had been inert since slice 3**. It runs after `restorePlayground()` and re-read the config file live, so every run compared the restored file against the snapshot it had just been restored from. Slices 3 and 4 were both signed off with it counted as evidence. Proven inert with a probe (a marker key written into core config, test passed green), then repaired to compare **decrypted** content minus `scheduler_last_run` — a byte hash could not work, because the file is encrypted and `ActionScheduler` rewrites it on every boot. Pinned permanently by `tests/Integration/PlaygroundGuardTest.php`, both directions. It paid for itself the same session: it fired on the S-12 tests against the unfixed code, independently confirming the GET wrote config, and went quiet once fixed (D-039, **L-010**).
- **Keel v3.3.0 → v3.4.0 post-update reconciliation done 2026-07-19** (user chose the full run): both embedded trees replaced and verified file-for-file, lock blocks re-stamped in `CLAUDE.md` + `AGENTS.md`, project card baseline updated, `.gemini/settings.json` corrected to stop listing the deliberately-absent `GEMINI.md` (D-010). The v3.4.0 build-assets contract is **overridden** by D-038: all 68 tracked `*.min.*` are third-party vendor distributions and Klytos ships no minified first-party asset, so the source↔minified drift the contract prevents cannot occur here; adoption is bound to Phase 7. The MANIFEST Table 1 parity check found three real gaps — `docs/flows/`, the four permission allow-lists and the CI workflow — all now tracked as deferred items with fired triggers.
- **NEW-14 found by slice 5's `security-auditor` pass, verified independently, recorded and NOT fixed:** `Auth::sendSecurityHeaders()` has six call sites repo-wide and **none** of them is an admin API endpoint — **0 of 24** send security headers, while every admin *page* gets them via `templates/header.php`. Distinct from S-11 (a missing HSTS directive *inside* that function); fixing S-11 would change nothing here. Same shape as S-07 and it wants the same answer — one enforcement point rather than 24 remembered calls. Bound to **slice 8**, which already opens that function.
- **Slice 6 CLOSED 2026-07-19** — outbound requests to URLs someone else chose now go through one class. `Klytos\Core\SafeHttp` was **promoted** from `ImportValidator::validateUrl()` (the product's only working SSRF control, reachable only from the importer plugin) and applied at the five influenced call sites: `admin/api/oembed.php`, `webhook-manager.php` (create **and** delivery — a subscription is long-lived, so a host that resolved publicly when stored can resolve privately when it fires), `integrity-checker.php::registerDeveloperKey()`, and `ImportValidator` itself, which now **delegates** so one implementation exists rather than two. **S-08 was wider than the audit recorded, in two ways found only by reading the file for the fix:** the oEmbed endpoint *discovered inside* the fetched page is attacker-controlled too and, unlike the page fetch, **its response is echoed back to the caller** — a full read-SSRF primitive; and pre-flight validation was never sufficient, because `CURLOPT_REDIR_PROTOCOLS` is set **nowhere** in the repo, the `stream_context_create` sites inherit PHP's default of **20** hops, and only `PageFetcher` re-validated after a redirect at all — and only the FINAL url, after the body had already been fetched. SafeHttp turns the transport's redirect handling off and walks the chain itself. Refusals answer as the endpoint's generic `Invalid URL`, never as an internal-network oracle. Test point PASS: **107 tests / 472 assertions**, **6 of the 8** endpoint tests proven to FAIL against the unfixed code (404 — what the endpoint returns *after* fetching), the old transport demonstrated following a 302 to `169.254.169.254` via `CURLINFO_EFFECTIVE_URL`, `keel-verify` 2/2, upgrade from real v0.30.1 passing, D-025 baseline **improved** to 193/488 and plugins to **113**/109. Decisions **D-041** (amended after its own review cycle), **D-042**; lessons **L-011**, **L-012**, **L-013**.
- **Slice 7 CLOSED 2026-07-20** — the product has a public comment endpoint that works, and it is deliberately **not** in the admin tree. S-09 is closed by **relocation**, not by the remediation the audit recorded: "add it to `$preAuthScripts`" fails the slice's own criterion 4 by construction, because the handler sat inside the randomized admin directory and any form posting to it would publish that name on every generated page (`helpers.php:192-197`). **The D-036 question was asked before acting and it changed the design** — `admin/bootstrap.php` runs the cron manager and the action scheduler on every request (`bootstrap.php:184-196`), so exempting an anonymous endpoint would have handed every passer-by a scheduler trigger on top of a comment box; `installer/index.php` does neither. The handler is now `installer/public/comment-submit.php`, copied to the **web root** by `BuildEngine::syncCommentEndpoint()` (the x402 placement precedent — but deliberately not its bootstrap, which requires a `core/bootstrap-minimal.php` that does not exist in this repo). The old admin file is **deleted** and its gate-map entry removed, so `keel-verify` now reports 64 surfaces and would have failed the build had the entry been left behind. **The per-session rate limit was not weak — it was inert:** `Auth::startSession()` scopes the cookie `path=<base>/admin/` with `SameSite=Strict`, so a form on the static site can never send it and every submission arrived with a brand-new session (confirmed live). It now uses the existing persistent, IP-keyed `MCP\RateLimiter`, **reused rather than forked**. Test point PASS: **116 tests / 541 assertions**, every new test **proven to FAIL** against the unfixed code by seven named probes, the four criteria walked for real in the playground, `keel-verify` 2/2, upgrade from real v0.30.1 passing, D-025 baselines unchanged at 193/488 and 113/109. Decision **D-043** (amended after its own review cycle); lesson **L-014**.
- **The named finding was the shallowest of three, which is the transferable part of slice 7.** Under S-09 sat **NEW-16**: `SiteConfig::setValue()` **did not exist**, although `core/mcp/tools/comment-tools.php:136-148` calls it four times — so `klytos_set_comment_settings`, the only supported way to switch comments on, had fataled for its entire life, and its sibling `set()` silently drops `comments_enabled` through a hardcoded allow-list. Fixed in path, **in scope by necessity**: the test point requires an anonymous submission to succeed, which is unreachable while the feature cannot be enabled. Under *that* sits the third layer, still true and deliberately so: **no comment form exists anywhere in the generated output** and none ever did — no template, part or build step emits one, and `renderCommentsHtml()` returns early on zero approved comments. Form emission is bound to the theme-package sprint (D-023), and `docs/reference/public-comments.md` says so in plain words rather than implying the feature is complete. Recorded as **L-014**: closing a finding is not the same as making the feature work.
- **The security review returned a blocking finding AFTER the suite was already green, and it restructured the slice.** The rate limit ran after `App::boot()` — which decrypts config, builds ~25 managers and runs every plugin's `init.php` — at a fixed, scannable URL on every install; and worse, **the honeypot ran before the rate limit**, so a flood setting `_honeypot` on every request took the cheap success branch and was never counted at all. A non-filterable flood ceiling (10/60s) now runs **pre-boot**, needing only a directory path, and the filterable policy (2/60s) runs after boot deriving its count from `getRemainingRequests()` so it costs no second write. The honeypot also answered `200` where a real submission answers `201`, while the comment beside it claimed it responded "exactly as a success would" — a bot comparing statuses once would have learned the trap; it now answers `201` with a decoy id. The `code-reviewer` returned six non-blocking items, four taken — including that **`installer/public/` was outside the phpcs scan set entirely**, so no public entry point in the product had ever been linted (now scanned, at 0/0).
- **Four findings recorded and deliberately NOT fixed:** **NEW-17** (behind a non-loopback proxy `getClientIp()` collapses every visitor into one bucket, turning the limit into a site-wide throttle — the remedy is trusted-proxy config, which changes MCP and OAuth too); **NEW-18** (the global `__()` is declared only in `admin/bootstrap.php:28` while `app.php:775-793` declares a *namespaced* one, so **no public entry point can translate** — which is why `t.php` and `x402-gate.php` hardcode English; the new endpoint calls the I18n **service** through a closure instead of adding a third shim, and its 11 keys are in all 20 catalogues); **NEW-19** (`RateLimiter` rewrites its whole file per request and its identifier set grows monotonically); **NEW-20** (`check()` is a read-decide-write with no lock spanning it — carried as **plausible and unproven**, because the concurrency test that would settle it was not run, and saying otherwise would overstate the evidence).
- **Two defects fixed in-path (D-031's narrowing), neither in the audit:** `HttpClient::requestWithStream()` accepted `follow_redirects` at `request()` and then **dropped it**, hardcoding `follow_location => 1` — so on any host without `ext-curl` a caller that switched redirects off got them anyway, which is precisely the bypass SafeHttp exists to close, on the hosts least likely to notice. And **the integration tier had never reset hooks** while the unit tier always did: `Hooks` is static and `App::boot()` runs once per process, so a filter registered by one test leaked into every later test. Invisible for five slices because **no integration test had ever registered a hook**; slice 6 is the first, and its `allowed_schemes` test was silently permitting `ftp://` downstream. Caught by asserting on the refusal REASON rather than just the refusal — the tier now records a post-boot hook baseline and fails any test that hands it back larger, proven in both directions with a probe before being trusted (D-042, **L-012**).
- **NEW-15 recorded and deliberately NOT fixed:** DNS rebinding survives S-08's fix — the address is resolved to validate and resolved again by the transport to connect, so a hostile nameserver with a short TTL can answer public then private. The remedy pins the validated address (`CURLOPT_RESOLVE`), which changes `HttpClient`'s contract for every caller and needs a resolver the test controls. Severity LOW (needs attacker-controlled authoritative DNS; the reachable surfaces are authenticated or operator-initiated). Stated in `docs/reference/safe-http.md` in these words rather than implied away: **slice 6 raises the cost of SSRF substantially, it does not make it impossible.** Trigger: the authentication slice, or the first unauthenticated surface to gain an outbound fetch.
- **`klytos_http_get()` / `klytos_http_post()` deliberately NOT made safe by default** — it would break any plugin legitimately calling a LAN or localhost service, which is a breaking change needing its own decision and release note (the D-034 precedent), not a quiet inclusion in a security slice. Plugin authors fetching untrusted URLs call `klytos_safe_http()`.
- **Slice 8 CLOSED 2026-07-20** — every admin response now carries security headers, decided in one place and sent from one place. S-11, the CSP fail-open and NEW-14 are closed by the **S-07 answer**: ONE call in `admin/bootstrap.php`, which all **64** admin entry points require, rather than 25 remembered calls. HSTS is `max-age=31536000`, **HTTPS-only** and **without `includeSubDomains`** (a browser caches that directive for a year and it would force HTTPS onto subdomains belonging to the operator, not to Klytos — a user decision, D-044). The CSP now **fails closed**: no nonce means `script-src 'self'`, not the old silent `'unsafe-inline'`. Test point PASS: **138 tests / 603 assertions**, five named probes proving every test fails against the unfixed code, headers asserted on **real** responses including the 401 and 403 refusals, headless Chrome clean on `login.php` and a CSP sweep clean across 18 admin pages, `keel-verify` 2/2, upgrade from real v0.30.1 passing, all four D-025 baselines held. Decision **D-044**.
- **The finding's own numbers were wrong in two ways, and re-validation caught both — the L-014 discipline paying off again.** NEW-14 says "all 24 files in `admin/api/`": there are **23** (slice 7 deleted one). It also says "every admin PAGE gets them, because they all include `templates/header.php`" — **false for five pages**, and of those **`login.php` and `logout.php` called nothing at all**. So the login form — the most security-sensitive page in the product — was being served with **no CSP, no `nosniff` and no `X-Frame-Options`**. The real gap was **25 surfaces including the login form**, not 24 JSON endpoints, and the finding's own reasoning ("pages are fine, endpoints are not") is what hid it.
- **Placement was the load-bearing part and is bounded on both sides.** It cannot go later: the auth guard's 401 JSON, `klytos_deny()`'s 403 document and three redirects all emit below it — all four now carry headers on the wire, which is the ordering proof. It cannot go earlier: `registerAutoloader()` is **Step 1 of `App::boot()`**, so `Auth` does not resolve before boot — **probed, not inferred** (L-006's rule applied before writing the line rather than after debugging it). The residue is recorded as **NEW-22** rather than papered over: the boot-failure page and two pre-boot redirects still send nothing.
- **The CSP fail-open was load-bearing, not cosmetic, and this is the transferable part.** `login.php` carries an inline `<script>` — the 2FA method switcher — and had no CSP at all. Extending header coverage to it without nonce-ing that block would have **broken two-factor login**: a regression shipped *by* a security fix. Nonce-ing it was in scope by necessity, the NEW-16 shape from slice 7.
- **A catch that would have broken every generated site, found by checking rather than assuming.** Failing the CSP closed at `installer/index.php` would have silently disabled the **GDPR consent banner** on every generated page: `Router::dispatch()` `readfile()`s pre-generated HTML and `build-engine.php:881` writes `<script>ConsentManager.init(...)</script>` into it inline, and a build-time file cannot carry a per-request nonce. That call site now states an **explicit** policy keeping `unsafe-inline`, written as a literal so the weakening shows up in a diff — which is exactly what the implicit fallback did not do. Recorded as **NEW-23**, bound to D-023.
- **Two false passes were caught in this slice's own test infrastructure — L-010 arriving a third time.** (1) The first unit tests drove `sendSecurityHeaders()` and read `headers_list()`; under the **CLI SAPI `header()` is a no-op and `headers_list()` returns empty**, so all three "the header is ABSENT" assertions passed against an empty string and would have passed against code setting no headers at all. Repaired by splitting out a pure `Auth::buildSecurityHeaders()` — the unit tier asserts the policy, the integration tier asserts it reaches the wire. (2) An integration assertion searching the whole body for the nonce **still passed** with the nonce stripped from the `<script>` tag, because `login.php`'s `<style>` block carries one too.
- **`style-src` keeps `'unsafe-inline'` deliberately (S-10 stays out of scope), and a test asserts that weakness on purpose.** Per CSP Level 3 a nonce source in `style-src` makes browsers **ignore** `'unsafe-inline'`, so "tidying" the policy the way scripts were tidied would break all **349** inline `style=` attributes at once. The 12 `<style>` blocks now carry nonce attributes that do nothing yet and are ready for the slice that removes it.
- **Three findings recorded and deliberately NOT fixed:** **NEW-21** (`page-editor.php` sets its own CSP with `script-src 'unsafe-inline'` and 7 inline blocks — an *explicit* opt-out, distinct from the implicit fallback that was closed; user decision, bound to the S-10 sprint); **NEW-22** (the boot-failure page and two pre-boot redirects send no headers, because the autoloader does not exist on those paths); **NEW-23** (the public site's CSP cannot use nonces while its HTML is pre-generated).
- **Slice 9 CLOSED 2026-07-20 — and with it SPRINT 1.** `scripts/keel-verify` goes from 2 checks to **9**, and every new one was **proven to FAIL on an injected violation and pass once reverted** (10 probes, all fired, tree left clean) — the discipline that is the whole point of a checks slice, since a check that has never fired is indistinguishable from one that cannot. Settled with the user: a **VERIFIER, not a regenerator** for `docs/api/INDEX.md` (a bespoke extractor's silent mis-extraction would rewrite a correct file and the verifier would then agree with the extractor rather than with reality). A new **WARN** tier reports a genuinely-broken property whose fix another phase owns, printing full evidence without failing the build — the treatment `sprint-1.md` had already scoped for H-01. Pinned by `tests/Unit/KeelVerifyTest.php`, itself proven in three directions. Test point PASS: **142 tests / 617 assertions**, upgrade from real v0.30.1 passing, all four D-025 baselines held with `scripts` recorded fresh at 0/2. Decision **D-045**; lesson **L-016**.
- **Building the "no placeholder copy in distributable surfaces" check honestly is what found two production defects, because it forced an honest definition of "distributable" — `git check-attr export-ignore`, the same authority that builds the release archive, rather than a hand-written list.** **NEW-27 (HIGH):** all **16** in-product guides under `installer/core/guides/` are stripped from every release by the blanket `*.md export-ignore`; the directory ships EMPTY. They back `klytos_list_guides` / `klytos_get_guide`, whose own tool descriptions declare five of them **REQUIRED** before creating page content — so the AI-first CMS ships without the instructions it tells the AI to read first. Verified it reaches real installs, not just `git archive`: v0.30.1 has **no attached assets**, so the updater falls back to GitHub's zipball, which honours `export-ignore`. **NEW-28 (MEDIUM):** `scripts/` is not export-ignored either, so it ships to the web root, and the root `.htaccess` serves any existing file — `router.php` **executed over HTTP** returning an internal disclosure page (admin path, MCP endpoint, BuildEngine internals), and `upgrade-assert.php` executed with HTTP 200. Both now carry SAPI guards (fixed in path per D-031); the packaging half is Phase 7's, with NEW-27 and H-02 — all three are one `.gitattributes` review.
- **Three deferred items whose trigger had fired are now done.** The **CI workflow** exists and runs the same commands the test points run — and it **seeds the playground**, which is load-bearing rather than convenience: 91 of 138 tests are the integration tier, which *skips* rather than fails without one, so an un-seeded CI would have reported green having executed 34% of the suite and omitted every authorization test. A skip is promoted to a hard CI failure. PHP 8.2/8.3 run the suite; 8.1 gets a syntax-only job because PHPUnit 11 needs 8.2+ (D-027 stated, not papered over). All **four permission allow-lists** written from the verified command set. **`docs/flows/`** created with three targeted journeys (MCP tool call, admin authentication, public comment) — the ones Sprint 2 actually needs a failure branch for.
- **`scripts/` had never been linted at all** — the slice-7 shape a second time (that slice found `installer/public/` outside the scan set). Now scanned, with `scripts/keel-verify` named explicitly because it has no `.php` extension and a directory entry alone would have silently skipped the one file this slice writes. Fresh baseline 0 errors / 2 warnings; `keel-verify` itself 0/0.
- **L-016 is the sharpest lesson of the sprint and it is about this session's own instruments: three separate measurements produced a false PASS.** A shell fallback invented "0 ERRORS AND 0 WARNINGS" for the project's largest lint baseline; a guard probe passed against the guard *not firing*, because the unguarded file answered 404 for that path too and only the body SIZE (468 bytes of disclosure vs 19 bytes of guard) separated them; and an archive fixture built with `git write-tree` tested HEAD instead of the working tree. In the slice whose entire subject is replacing eyeball-checking with machine-checking, the instruments lied three times. **A broken check goes quiet; a broken measurement goes green.**
- **Sprint 2 IN PROGRESS — MCP tool authorization (NEW-02, D-020).** Planned and approved 2026-07-22 in plan mode; the Phase 5 §0 kickoff re-validation (session 13, re-confirmed this session) produced the five source-verified corrections now recorded as **D-046…D-049**. The recorded one-liner (D-020: reuse `klytos_require_permission()` at the ToolRegistry point) would deny 100% of MCP traffic because there is **no identity on the MCP path** — so identity is built from the credential first. Planning artifacts written: `docs/sprints/sprint-2.md`, decisions **D-046…D-049**, **Estimate v2**. **Slice 1 CLOSED 2026-07-22** — MCP actor resolution: `TokenAuth::validate()` builds a `{user_id, role}` actor from the credential and `getActor()` surfaces it (bearer role stamped on the record; app-pw/OAuth roles resolved from the user record — **D-047 amended**, DRY and NEW-11-ready, so only bearer tokens are stamped); idempotent boot migration (`Auth::migrateCredentialRoles()`, Step 10b-2) stamps role-less bearer tokens → owner, **proven on a real v0.30.1 upgrade**. 156 tests/643 assertions; the fail-closed `null`-role default proven load-bearing by a TEMP-BREAK cycle; **L-017** (a `$x['k'] ?? [] as &$ref` migration footgun that returned a count while persisting nothing — caught by asserting the persisted role) and **NEW-29** (the same footgun pre-exists in `validateAppPassword`'s `last_used`, not fixed). **Next action: slice 2 — the gate in `ToolRegistry::call()` above the `mcp.handle_tool` filter + central `tool-capabilities.php` map + `tools/list` filter + keel-verify check 10** (reusing the admin-gate/`AdminHttpTestCase` patterns; HTTP :8105 denial proof), then slice 3 (loader fail-loud, wire `integrity-tools`, plugins declare caps, `chat-engine` default-deny), slice 4 (reconciliation + D-035 widening + docs/skills/i18n + count truth). The vendor-ai CVE slice (NEW-05/D-029) and the NEW-03 slice remain queued behind it.
- **Two production bugs found by the playground's first boot, recorded and deferred by D-026:** NEW-03 (`Hooks::doAction()` copies arguments, so by-reference listeners never bind — warns on every page create, x402 injection silently dead) and NEW-04 (`build` writes into the repository root, overwriting the tracked `.htaccess`). Neither is fixed; both have triggers. **Never run `installer/install.php` or `installer/cli.php build` in a checkout.**
- Sprint 1 scope: the audit's fix-now bucket (S-01…S-09, T-01) **plus** NEW-01 (prerequisite — it defeats every gate), T-02, H-04 (D-022), S-11/S-12 and the CSP fail-open. 10 slices.
- Kickoff re-validation done (Phase 5 §0): three audit claims corrected (S-04, S-07, S-12) and two CRITICAL findings added (NEW-01, NEW-02) — all recorded in `docs/04-adoption-audit.md`.
- **Scope change registered (2026-07-18, not yet started):** the frontend theme model is redesigned as an installable, validated **theme package** with an authoring skill for external design agents (D-023) plus a package-scoped, preview-only MCP authoring surface so a design agent can connect to the site and iterate (D-024) — full spec in `docs/theme-package-model.md`. Designed only; the user explicitly sequenced it **after Sprint 1**. It becomes its own Phase 5 sprint and runs through Keel Phases 3–4 (first redesign to do so). **Not costed in `docs/estimate.md` v1**, which deliberately covers Sprint 1 only — it gets its own estimate version when its sprint is planned.

## Open items
- Unresolved user questions: none
- Open Design Requests: none
- Unverified external steps/assets: none
- Forge issues in progress: none open — see `docs/issues.md` (S-01/S-02/S-03/S-05/S-07/S-12 must NOT be filed as public issues before they are fixed; the repo is public — use a private security advisory or fix-then-disclose)
### Deferred items (consciously postponed work)
- ~~**Storage isolation for the integration tier**~~ — **RESOLVED 2026-07-19 (D-030)**, before slice 3 as its trigger required. `tests/PlaygroundState.php` snapshots `installer/config/` + `installer/data/` before every integration test and restores them after, ON by default. Chosen over per-test fixtures because slice 3's own test point must delete the seeded owner to prove migration idempotency — a record a fixture does not own. Proven by `tests/Integration/PlaygroundIsolationTest.php`, which was demonstrated to FAIL with isolation disabled before being trusted. Four in-memory caches survive a file restore (`App::$config` has no invalidation path at all); the primitive names them and fails loudly rather than pretending to cover them.
- `playground-qa` subagent + `docs/playground.md` + the playground itself — severity HIGH — review trigger: Phase 5 scaffold (Sprint 1).
- ~~**Permission allow-lists per tool, CI workflow**~~ — **RESOLVED 2026-07-20, slice 9.** All four containers written (`.claude/settings.json`, `.codex/rules/klytos-tooling.rules`, `.cursor/cli.json`, `tools.allowed` in `.gemini/settings.json`) from the verified command set, with `install.php`, `cli.php build` (NEW-04) and `git push` deliberately excluded. `.github/workflows/ci.yml` written, seeding the playground and promoting a skip to a hard failure. **MCP dev-server registration remains genuinely n/a.** Original note follows. — trigger had FIRED (found by the v3.4.0 reconciliation's MANIFEST Table 1 parity check, 2026-07-19). The blocker they waited on is gone: the playground exists (slice 0) and the verified commands exist (`composer install`; `XDEBUG_MODE=off vendor/bin/phpunit`; `vendor/bin/phpcs --standard=phpcs.xml`; `php scripts/keel-verify`; `XDEBUG_MODE=off bash scripts/dev/upgrade-test.sh`). Still missing: **all four** allow-list containers (`.claude/settings.json`, `.codex/rules/`, `tools.allowed` in `.gemini/settings.json`, `.cursor/cli.json` — only the personal, gitignored `.claude/settings.local.json` exists) and the **CI workflow** (`.github/workflows/` does not exist; the forge is GitHub, so CI is available). MCP dev-server registration remains genuinely n/a — the technical plan describes Klytos's *own* MCP server as a product surface, not dev servers to register. New review trigger: **Sprint 1 close**, with slice 9 (which already owns `scripts/keel-verify`) as the natural home for the CI workflow, since CI must run the same commands the test points run.
- ~~**`docs/flows/` does not exist**~~ — **RESOLVED 2026-07-20, slice 9**: three targeted flows created (`mcp-tool-call.md`, `admin-authentication.md`, `public-comment.md`), chosen as the journeys Sprint 2 needs a documented failure/recovery branch for. A full retroactive flow set was rejected with the user as an as-built documentation exercise competing with security work. Original note follows. — **`docs/flows/` did not exist** — found by the same parity check. Unconditional from Phase 2 in Keel's MANIFEST Table 1, and the project is at Phase 5. Phase 2 was adopted as-built (D-001), which is *why* it is absent: adoption reconstructed `01`/`02`/`03` and `docs/api/INDEX.md` but never split the journeys into per-flow files. Severity LOW while Sprint 1 runs — Phase 5 §2's test point wants "the failure/recovery branch from its flow file", and slices 0–4 substituted the audit findings for that. Review trigger: **Sprint 1 close**, or the first slice whose acceptance genuinely needs a documented failure/recovery branch.
- **`.cursor/agents/` is NOT a gap** — verified against D-011, which records that Cursor reads `.claude/agents/` natively. The parity check flagged it; the decision governs. Noted here so the next check does not re-raise it.
- `launch-verifier` subagent — review trigger: Phase 8 start.
- `guide-qa` subagent + `guide/` — review trigger: Phase 6.
- `design-fidelity-auditor` — not generated: no BUILD-SPEC exists (D-009). Review trigger: the first redesign that goes through Phases 3–4.
- `docs/estimate.md` — **v1 (Sprint 1) 2026-07-18; v2 (Sprint 2) 2026-07-22**. Still deferred: an estimate covering the theme-package sprint (D-023) and the vendor-ai CVE re-vendor (D-029). Review trigger: when each of those is planned.
- Copilot subagent `model:` binding — omitted, not guessed: the accepted values are undocumented (D-011). Severity LOW — the agents run on the session model meanwhile. Review trigger: when GitHub documents the accepted identifiers.
- Collaborator setup line for the pre-commit gate (`git config core.hooksPath .githooks`) not yet documented in the repo's development notes (D-015). Severity MEDIUM in a public repo — contributors' clones have no gate. Review trigger: audit H-bucket triage.
- ~~**Sprint 2 — MCP tool authorization**~~ (NEW-02; D-020) — **CLOSED 2026-07-24, all 4 slices** (D-046…D-051, Estimate v2, `sprints/sprint-2.md` with its close-out table filled). Actor resolution from the credential → the default-deny gate in `ToolRegistry::call()` + central capability map + `tools/list` filter + keel-verify check 10 → coverage completeness (loader fails loudly, integrity + x402 + both shipped plugins gated, NEW-30 resolved) → reconciliation (count truth, the closed forward reference, the refusal translated ×20, 8 skills, `ai.use` widened to editor per D-051). **Audit NEW-02 is CLOSED.**
- ~~**`vendor-ai/` CVE remediation slice**~~ (NEW-05, D-029) — **RESOLVED 2026-07-25, Sprint 3 slice 1.** `composer audit -d installer` reports **zero**, measured both sides (11 → 0). guzzle 7.10.0→**7.15.1**, psr7 2.9.0→**2.13.0**, promises 2.3.0→**2.5.1**, plus a new **`symfony/polyfill-php80`** required by both (16→**17** packages, 482→**509** tracked files). **D-029's recorded floors would have left 6 of 11 open** — its two halves ("bump to ≥7.12.1/≥2.12.1" and "re-audit to zero") had come apart as upstream published more advisories, and the criterion won over the derivation (L-014), a user decision recorded as **D-052**. **The reachability assessment was redone from scratch and retires the NEW-15 worry**: `safe-http.php` and `http-client.php` contain **zero** `GuzzleHttp`/`Psr\Http` references, so the two HTTP stacks never share a URL and the differential-parsing precondition for the host-confusion CVEs does not exist here. Stated plainly: **none of the 11 had a demonstrated exploitation path in Klytos** — the bump closed a standing obligation, not a live hole. Two corrections of record: the change is **95 files**, not the 482 D-029 implied (that was the tree size), and the drift guard's root-package skip was hardcoded to `'__root__'` and had **never been exercised**, because the guard was built against a tree vendored elsewhere and never regenerated here (**L-020**). Estimate **v3** written.
- **NEW-27 / NEW-28 / H-02 — one `.gitattributes` review** (found 2026-07-20, slice 9). The blanket `*.md export-ignore` strips all 16 in-product guides (a live MCP surface), plus `README.md` and `INSTALL.md`; `scripts/` is not export-ignored so dev scripts ship to the web root. Both are made **detectable now** by keel-verify WARN checks rather than fixed. Severity HIGH for NEW-27 (a released install has no guides at all). Review trigger: **Phase 7**.
- **NEW-31 — the AI chat's panels had no permission check of their own** — **FIXED 2026-07-24 in slice 4** (panel→capability map in `ai-chat.php`, absent = deny; `get_providers` gated at `site.configure`). Recorded here because the *class* of defect recurs: a privileged partial trusting the file that included it. No open work.
- **NEW-32 — the authorization audit hooks have no listener** (`mcp.access_denied`, `auth.access_denied` fire; nothing subscribes, so refusals write nothing to the log). Severity MEDIUM — an authorization system that refuses silently cannot be operated, and the sprint's own "paste the debug log" hand-off returns nothing for exactly the failures it built. NOT fixed at close: "log every refusal by default" carries real volume and content decisions (an anonymous caller can drive refusals; the entry names a role and a capability). Review trigger: **the next slice touching logging or the audit trail, or the NEW-11 authentication slice**.
- **NEW-33 — the terminal/CLI's strings are hardcoded Spanish** (`terminal-executor.php`), against the recorded English-base rule (D-006). Severity LOW. Review trigger: the next slice touching `terminal-executor.php`, or the NEW-18 resolution that owns the underlying `__()` reachability problem.
- **NEW-35 — MCP tool input schemas are advertised but never enforced** (`ToolRegistry::call()` runs the authorization gate, the plugin filter and the handler, and never validates `$params` against `inputSchema`), **and a second URL interpolation rides on that** (`ai-image-generator.php:62`, reachable over MCP via `klytos_generate_image` at `assets.manage`, with a declared `enum` that looks like it prevents exactly this). Severity LOW/MEDIUM. The systemic half matters more than the instance: fixing the interpolation alone leaves every other tool's published contract unenforced — the by-omission shape S-07 and NEW-02 both exist to close, on a third axis. NOT fixed: enforcing schemas is a **behaviour change for every MCP client** and needs the D-034 treatment (recorded decision + release note). Review trigger: the next slice touching `ToolRegistry::call()` or the MCP tool surface, or the NEW-34 slice — same defect class, should close both together.
- **NEW-34 — the AI chat's `model` parameter is unvalidated and reaches the provider URL** (`admin/api/ai-chat.php:168` → the Gemini URL's path segment). Severity LOW: measured, not reasoned about — a `#` pushes `key={apiKey}` into the URI fragment so the request leaves with no API key (401), a `?` truncates the path, and the **host never moves in any spelling**; CRLF is percent-encoded. Denial-of-function, not a boundary crossing, and the population is **editor and above** (`ai.use` per D-051). Fix shape recorded so it need not be re-derived. Review trigger: the next slice touching `installer/admin/api/ai-chat.php`, or the NEW-11 authentication slice.
- **`README.md`'s per-module tool table is stale** — 34 rows summing to **177** (the pre-slice-3 figure) with row labels that no longer map to the loader's files. The one plainly-false number ("33 tool modules") was fixed in slice 4; regenerating the table is an editorial pass on the public README belonging to **Phase 6 / D-017**. The measured per-file counts are recorded in `docs/04-adoption-audit.md` D-05 so it need not be re-derived.
- ~~**NEW-06 — no PHP-version guard on the AI stack**~~ — **RESOLVED 2026-07-25, Sprint 3 slice 2 (D-053)**, in the D-029 remediation sprint exactly as this item predicted. `App::getChatEngine()` now refuses **above** the vendored `require_once` with a typed, translated `Klytos\Core\Ai\UnsupportedRuntimeException`, instead of letting Composer's `platform_check.php` send HTTP 500, echo third-party text into the response body and throw from inside vendored code. The **ordering** is the load-bearing part and is pinned by a test that reads the source, because on a supported host no runtime test can reach that branch — proven by relocating the guard below the require and watching the test fail. The policy is a pure `App::aiRuntimeUnsupportedReason( int )` (the D-044 split) since PHP cannot be downgraded in the suite; the floor is written once and a test asserts it matches what Composer generated. `ai.runtime_unsupported` is an **action** with **no core listener**, said in those words per L-019. **It does NOT raise the product's PHP floor** — reconciling the four floors this codebase asserts stays with D-027's trigger.

Last updated: 2026-07-25 — **SPRINT 4 CLOSED — slice 1 CLOSED; audit NEW-03 CLOSED and NEW-36 opened-and-closed in the same slice.** Actions are now fire-and-forget **by enforcement**: `Hooks::addAction()`/`addFilter()` refuse a callback declaring a by-reference parameter with a typed `Klytos\Core\HookContractException` naming the hook, the parameter and the callback's `file:line`; the one real listener (x402, the only one repo-wide) became a filter on the new **`page.save_data`**, applied by `PageManager` **above** the `page.before_save` action, which stays as an observer (**D-054**). **The obvious fix was refuted by measurement, not preference** — `mixed &...$args` binds correctly and then makes PHP reject every non-variable argument with a fatal `Error`, and **36+ call sites** pass a literal, a `??`, a ternary or an array literal, starting with `PageManager::create()`'s own `'create'` literal; an undefined array key passed by reference is also silently *created*. **D-026's "301 registered actions" does not reproduce and is wrong in kind** — a stale copy of INDEX's `| Actions |` row at commit `622d54c`; nothing *registers* 301 anything. Re-measured three ways (the last comment-stripped and multi-line-aware): **308** action names, **363** fire sites, **23** shipped registrations, **120** filter names. The real blast radius was **one listener**, and the inflated figure is part of why it was deferred twice (L-015). **The first two passes disagreed with each other**, and reconciling them found `x402.should_protect` fired in code with no INDEX row — a pre-existing gap from adoption's line-based extraction, now added (Filters **120**, total **961**). **A second, independent defect was found by driving the FEATURE rather than the finding (L-014) and fixed in path: NEW-36** — `admin/post-type-edit.php` applies the `admin.post_type_edit.update_data` filter and hands the result to `PostTypeManager::update()`, whose hardcoded 7-field allow-list **silently drops it**, so x402's post-type checkbox has **never** persisted: six sites read the value, nothing could write it. Closed by making `$updatable` filterable through a new **`post_type.updatable_fields`** where **omission still denies** (the `admin.gate_map`/`mcp.tool_capabilities` shape). The separation was proven by a precondition assertion that fired against the unfixed tree, not deduced. `phpunit.xml` now sets **`failOnWarning="true"`** — the trigger that file had written for itself — so the warning's *absence* is asserted across all 220 tests. Suite **206 → 221 tests / 1007 → 1029 assertions**; keel-verify 10 checks exit 0; upgrade from real v0.30.1 PASS; lint baselines held with core+admin **improved** 193 → **192**. New `docs/reference/hooks.md` (the engine had no reference doc and no test of any kind before this slice). Verified live on the real request path: a page created over MCP HTTP now carries `x402_enabled` and the server log is clean. **Both reviews returned NO blocking findings — and they DISAGREED with each other**, which is the transferable part: the `code-reviewer` called the missing reserved-key denylist on `post_type.updatable_fields` a real gap, the `security-auditor` returned no finding on the same code having traced that the storage key is the `$id` parameter and that `delete()` checks a literal. Re-derived independently (L-013): `$pt['builtin']` is read only for sort order and menu display — **the auditor was right and the reviewer overstated it**, so the guard is kept and described as defence in depth, not a vulnerability fix. The auditor's best finding: the new interlock test scanned `installer/core/` while **`installer/admin/bootstrap.php` registers five hooks outside PluginLoader's catch, outside its own try/catch, and outside every test** — it was guarding the safer half. Scan widened and proven by planting an offender the old one missed. **L-023** recorded — two of my own measurements disagreed, and reconciling them surfaced `x402.should_protect` missing from INDEX since adoption. **SLICE 2 CLOSED 2026-07-25 — audit NEW-08 CLOSED (D-055).** `owner:repair --email=<address>` writes the missing `admin_email` into config and runs the product's **own** `UserManager::migrateFromV1Config()`, rebuilding the owner record from `config['admin_user']` and `config['admin_pass_hash']` — **the operator's existing password still applies and the command sets no credentials.** Reachable from the CLI with **no session**, which the state requires; `dispatch()` runs no permission check and `cli.php` calls it directly, so the declared `users.manage` gates the **web** terminal only. Refusals **THROW**, because `dispatch()` reports success for anything that returns and `cli.php` derives its exit code from it. **The first design was REFUTED by the slice's own code-reviewer and rebuilt (L-024):** it took `--username`/`--password` and called `UserManager::create()`, but `Auth::login()` validates against `config['admin_user']` and `config['admin_pass_hash']`, never the record — so it minted an owner nobody could log in as, and `findOwner()` returning non-null then made the command refuse forever. **The test written to prevent exactly that quoted L-014 in its docblock and then asserted `UserManager::authenticate()` instead of `Auth::login()`** — it passed against a command that restored nothing. The reference doc contained its own disproof three paragraphs below its headline. Now proven through the **real gate**, with the wrong password asserted first. **D-039's config guard fired on the new tests and was right** — the repair writes `config.json.enc`; resolved by passing the email config already holds so the write is net-zero, rather than suppressing a guard the whole tier depends on. The `security-auditor` separately found that `execute()` persisted typed commands into the `terminal` collection (**absent from ENCRYPTED_PATHS at every level**) and the audit log, readable by `admin` via `logs.php` at `site.configure` — fixed with a general `redactSecrets()` at that one choke point, kept even though the redesign means the command no longer takes a password. **NEW-33 deliberately NOT closed** (user decision); trigger re-bound. Suite **221 → 227 tests / 1029 → 1059 assertions**; keel-verify 10 checks exit 0; lint held. New `docs/reference/owner-recovery.md`. **Next = sprint close** (docs-verifier, playground-QA, Estimate v4, try-it script, continuation prompt). **Previously — SPRINT 3 CLOSED, slice 1 audit NEW-05 CLOSED.** The vendored AI HTTP stack is no longer known-vulnerable: **11 advisories → 0**, measured both sides. guzzle→7.15.1, psr7→2.13.0, promises→2.5.1, plus a new `symfony/polyfill-php80` (16→**17** packages, 482→**509** tracked files, **95** files changed — not the 482 D-029 implied, which was the tree size). **D-029's version floors were stale and its own "audit to zero" criterion decided it** (D-052, user decision). **The reachability assessment was redone from scratch and retires the NEW-15 worry** — `safe-http.php`/`http-client.php` carry zero Guzzle/psr7 references, so the host-confusion CVEs cannot reach the SSRF control; **none of the 11 had a demonstrated exploitation path**. `VendorAiManifestTest` was observed **RED on all three methods** before the four records were reconciled — no injected fault needed — and it exposed **L-020**: its root-package skip was hardcoded to `'__root__'` and had never been exercised, because the guard was built against a tree vendored elsewhere and never regenerated here. `installer/composer.json` gains `"version": "1.0.0"` so the tracked generated `installed.php` stops embedding the branch name and commit sha. New `VendorAiCompatibilityTest` (3 tests) proves the vendored API surface the AI stack imports still resolves, proven to fail in three directions. Suite **195 tests / 986 assertions**; keel-verify 10 checks exit 0; upgrade from real v0.30.1 PASS; all five lint baselines held exactly. **Slice 2 CLOSED 2026-07-25 — audit NEW-06 CLOSED.** `App::getChatEngine()` refuses **above** the vendored `require_once` with a typed, translated `Klytos\Core\Ai\UnsupportedRuntimeException`; the policy is a pure `App::aiRuntimeUnsupportedReason( int )` (the D-044 split, because PHP cannot be downgraded in the suite); the floor is written once and a test asserts it matches what Composer generated; `ai.runtime_unsupported` is an **action** with **no core listener**, said in those words (L-019). Plus a non-blocking `vendor-advisories` CI job, since the advisory count grew 5→11 unobserved for five days. **The refusal branch WAS driven end to end** by raising the floor — proving `__()` resolves in the throwing path (the sharpest risk: NEW-18 means the *global* `__()` exists only under `admin/` and one caller is an MCP tool), that the action fires before the throw, and that `GuzzleHttp\Client` is never loaded. **The `code-reviewer` returned a BLOCKING finding that was correct and was this slice's own subject turned against it:** the new tests would have turned CI's PHP 8.2 leg red for behaviour that is *correct* there. Simulating that leg found **8 tests in 3 classes** broken — 2 from slice 2, 3 from slice 1, and **3 latent since Sprint 2 slice 3**. Fixed with `#[Group('ai-runtime')]` excluded on the 8.2 leg (skipping alone would trip D-045's skip-is-a-hard-failure rule, which must keep meaning "the playground did not seed") plus `IntegrationTestCase::requireAiRuntime()` for local 8.1/8.2 runs. **L-022 — CI has never actually run**, not once: the workflow was written in Sprint 1 slice 9 and all 29 commits since are unpushed, so its 8.2 leg had been broken for two sprints invisibly. **L-021** — the first per-role playground walk was answered by a leftover server from the previous session that every L-011 tell agreed with. Also recorded, not fixed: **NEW-34** (unvalidated `model` reaches the provider URL) and **NEW-35** (`ToolRegistry::call()` never validates `$params` against `inputSchema`, so every published tool contract is advisory — plus a second interpolation riding on it). Suite **206 tests / 1007 assertions**; keel-verify 10 checks; upgrade PASS; all five lint baselines held. **SPRINT 3 CLOSED 2026-07-25** — `docs-verifier` **PASS, no blockers** (INDEX parity exact, 957 = 957 re-counted per section; every new surface documented with its examples checked against source; the key confirmed in all 20 catalogues). The fresh-context **playground-QA** ran ~60 commands and returned *"largely accurate but does not work end to end"*: **both areas this sprint edited came back clean** (the `$RPORT` bind check fires both ways, both kill-by-port Stop recipes work, and the L-021 `pkill` claim is empirically true), and it re-confirmed §3a's 5×4 table byte-for-byte — but it found **11 document defects, the two worst created by this sprint**: the vendored-dependency section still documented **11 advisories and 16 packages** four hours after slice 1 made both false, and a STOP-box `pkill -f "127.0.0.1:8099"` that could never match anything (§1 forbids `RPORT=8099`, and the pattern misses `-d`-flagged servers — two independent reasons). All 11 fixed and each re-run verbatim, including a `klytos_delete_page` example that was **only accidentally safe** and a token-cleanup command whose first version silently revoked nothing. **Next action = the NEW-03 slice** (`Hooks::doAction` by-reference) **or the theme-package sprint** (D-023/D-024, still un-estimated) — the user's call. Estimate **v3** written. **Previously — SPRINT 2 CLOSED (all 4 slices; audit NEW-02 CLOSED).** **Slice 4** reconciled the counts by MEASURING them (172 core + 8 x402 = **180** default install, **206** with both MCP plugins, **0** dead — the plan's 177/169/3 were stale), closed the `authorization.md` forward reference, wrote "Adding a new MCP tool — the checklist", translated the client-facing refusal into **all 20** catalogues (`mcp.permission_denied`), updated **8** skills, and widened **`ai.use` to `editor`** (**D-051**, user-confirmed, superseding D-035). Suite **192 tests/961 assertions**; keel-verify 10 checks; upgrade from real v0.30.1 PASS; all five lint baselines held exactly. **The sprint-close reviews are what made this slice worth its cost, for the ninth consecutive slice.** The `security-auditor` found **NEW-31** — `admin/ai-chat.php` includes `partials/ai-panel-{$panel}.php` for a caller-supplied `?panel=`, and those four partials had **zero** permission checks: `ai-panel-users.php` creates users and changes ANY account's password behind CSRF alone (owner-only work), `ai-panel-settings.php` writes site/SMTP settings, and `api/ai-chat.php`'s `get_providers` leaked 6+4 characters of every AI provider key. **Pre-existing and live for `admin` today**, invisible because `ai.use` and those capabilities were the same owner+admin set — D-051 is what would have widened it. **Fixed in path** (panel→capability map, absent = deny, before `header.php`; `get_providers` gated), proven both directions (editor got **200** on `?panel=users` before the fix). The fresh-context **playground-QA** pass returned §3a **flawless, every cell matching**, and **8 defects elsewhere in `docs/playground.md`**, all fixed — including that the document's own section-1 server collided with `upgrade-test.sh`'s port 8099 and fabricated an upgrade failure, and that the debug-log section promised entries **nothing writes** (**NEW-32**: `mcp.access_denied` and `auth.access_denied` have no core listener — a seam, not a sink; the "went to the audit log" wording is corrected in code and reference). Also recorded: **NEW-33** (the terminal/CLI's strings are hardcoded Spanish, against D-006) and **NEW-05's advisory count has grown 5 → 11**, which the queued D-029 slice must start from. **L-018** (a non-disclosure test written as a word blocklist failed on correct English) and **L-019** (a seam is not a sink) recorded. **Previously — slice 3 CLOSED** (coverage completeness). **Slice 3:** the tool loader **fails loudly** — the per-file logic is extracted into a public `ToolRegistry::registerToolFile()` that throws a typed `Klytos\Core\MCP\ToolRegistrationException` on a missing file or a file that registers nothing (D-049/L-007), keeping the `$toolFiles` literal intact for check 10; `integrity-tools.php` is wired in as the **34th** loader file and its 3 tools mapped `site.configure` (mirroring admin `system-integrity.php`), so live core count **169→172** and keel-verify check 10 now reads 172; the two shipped MCP plugins declare capabilities via `mcp.tool_capabilities` (`klytos-forms` 16→`forms.manage`, `klytos-importer` 10→`site.configure`) and are **activated in the seed** (state written directly, side-effect-free); `chat-engine` `getAvailableTools()` **default-denies unknown roles** to an empty list + closes the `function_exists`/null fail-opens (advisory-only now, slice 2 neutralized the teeth); and **NEW-30 RESOLVED** (user-confirmed, **D-050**) — `ToolRegistry::exists()` = registered OR mapped so filter-injected tools (x402 + the plugins) are **callable over HTTP AND gated** (`handleToolsCall()` catches a typed `ToolNotFoundException` for a mapped-but-unhandled tool → "Unknown tool", a narrow catch that never masks a real error). Suite 171→**185 tests/805 assertions** (+14: 4 loader + 3 gate + 3 chat-engine + 3 HTTP + 1 mapped-but-unhandled pin from the review), each proven to FAIL against unfixed code via a stash+reseed cycle (2 errors/9 failures on the unfixed tree); upgrade from real v0.30.1 PASS; all D-025 baselines held **exactly** (193/488, 113/109, 0/0, 0/2, 0/0); live playground owner `tools/list` = **206** served (172 core + 8 x402 + 16 forms + 10 importer), NEW-30 live (owner `klytos_forms_list` over HTTP → **200**, was "Unknown tool"); INDEX +2 classes → **955**; keel-verify 10 checks + INDEX parity green. **D-050** records the NEW-30 resolution + the capability assignments + the seed activation. **Both review subagents ran on the finished diff (L-015) and returned NO blocking findings** — the `security-auditor` traced the default-deny invariant end to end (the gate always runs before dispatch for filter-injected tools; catch order keeps a 403 from degrading to "Unknown tool"); the `code-reviewer` flagged that the `RuntimeException` catch was broader than needed (could mask a future handler's error), so it was **narrowed to a typed `ToolNotFoundException`** (`handleToolsCall()` now maps only that back to "Unknown tool"), pinned by a new test, and the stale keel-verify comments were corrected. **Skills (4) + the full count-truth reconciliation (177/206 served vs 172 live vs 3 dead) + the `authorization.md:217-221` forward reference + refusal i18n × 20 + D-035 `ai.use` widening (CONFIRM with the user) are slice 4.** **Previously — Slice 2 (the gate):** `installer/core/mcp/tool-capabilities.php` maps the **169 live core tools** (absent = deny, `null` = audited exception, `mcp.tool_capabilities` filter); a typed `PermissionDeniedException`; ONE `denialReason()` default-deny gate in `ToolRegistry::call()` **above** the `mcp.handle_tool` filter; `setActor()` carries the credential's identity per request (server.php after auth, chat-engine from the session); `listTools()` filtered by the same decision; `server.php` catch → JSON-RPC error object + **HTTP 403**; a `mcp.access_denied` audit action; keel-verify **check 10** (9→**10**, proven to FAIL both directions on an injected typo then reverted). Suite 156→**169 tests/671 assertions**; every denial proven to FAIL against ungated code (9 red on the `denialReason()` TEMP-BREAK, 4 positive controls green); real HTTP :8105 + a live playground walk (viewer `klytos_delete_page` → 403 JSON-RPC error, owner allowed 200, viewer `tools/list` = 19 tools/no destructive, owner = 169); upgrade from real v0.30.1 still passes; all D-025 baselines held (whole-scope **306/599** = exact sum of the recorded baselines). **D-046 amended** (setActor nullable — bearer has null user_id; ONE `denialReason()`; new `mcp.access_denied` action). **`code-reviewer` caught one BLOCKING finding — a gap in the PLAN, not just the code:** the map omitted the 8 `klytos_x402_*` tools, which the **core** x402 module (loaded unconditionally at boot, NOT one of the two deferred plugins) injects via `mcp.tools_list`/`mcp.handle_tool`, so default-deny made them unusable by every role including owner (owner's list had dropped 177→169). FIXED — `x402-mcp-tools.php` now declares its capabilities via `mcp.tool_capabilities` (reads → `x402.view`, writes → `x402.manage`); 2 x402 tests, proven to FAIL against the un-declared code; static core map stays 169 so check 10 is unaffected. `security-auditor` no blocking findings. **NEW-30** recorded (filter-injected tools unreachable via the HTTP `exists()` path — pre-existing, fails closed, slice-3 reconciliation). Skills deferred to **slice 4** per the plan. Next action = **slice 3** (loader fail-loud + wire `integrity-tools.php` gated + `klytos-forms`/`klytos-importer` declare capabilities + `chat-engine` `getAvailableTools()` default-deny). **Previously — slice 1 (MCP actor resolution):** `TokenAuth::validate()` builds a `{user_id, role}` actor from the credential — the MCP path has no session, so identity cannot come from `klytos_current_user()`; bearer tokens carry a stamped role (`createBearerToken()` optional role), app-pw/OAuth roles resolve from the user record (**D-047 amended** — DRY, NEW-11-ready), an idempotent boot migration (Step 10b-2) stamps role-less bearer tokens → owner, **proven on a real v0.30.1 upgrade** (a v0.30.1 bearer token stamped owner post-boot); 156 tests/643 assertions, the fail-closed `null`-role default proven load-bearing; **L-017** (a `$x['k'] ?? [] as &$ref` migration footgun that returned a count while persisting nothing) + **NEW-29** (same footgun pre-exists in `validateAppPassword` `last_used`, not fixed); next = slice 2 (the gate). **Planning:** kickoff re-validation confirmed the five source-verified corrections — the recorded D-020 one-liner would deny **100% of MCP traffic** because there is no identity on the MCP path (`Auth::startSession()` runs only in the admin path, so `klytos_current_user()` returns null on every `tools/call`). Wrote `docs/sprints/sprint-2.md`, decisions **D-046…D-049** (enforcement point in `ToolRegistry::call()` **above** the `mcp.handle_tool` filter; credential→role model + idempotent migration existing app-pw + bearer → owner; central `installer/core/mcp/tool-capabilities.php` map with keel-verify **check 10**; `integrity-tools.php` wired in + loader fail-loud), **Estimate v2**, and this update; the two carried Sprint-1 doc edits (`sprint-1.md` verdict row, `token-ledger.md` row 13) go into the first Sprint-2 commit. **Next = slice 1 (MCP actor resolution).** Keel maintenance: all copies + both lock stamps v3.4.0, update-check stamp refreshed. Previously: **SPRINT 1 CLOSED (all 10 slices)**; slice 9 took `scripts/keel-verify` from 2 checks to **9**, every new one proven to FAIL on an injected violation and both WARNs proven to go quiet when fixed; a **verifier not a regenerator** for INDEX (user decision); **two production defects found by defining "distributable" honestly** — **NEW-27** (all 16 in-product guides stripped from every release, so `klytos_get_guide` returns nothing on a real install although the tool declares five of them REQUIRED) and **NEW-28** (dev scripts ship to the web root and **executed over HTTP**, `router.php` returning an internal disclosure page; SAPI guards fixed in path); **CI workflow** written that seeds the playground because 91 of 138 tests would otherwise silently skip; all **four permission allow-lists** and **`docs/flows/`** done; `scripts/` added to the phpcs scan set having never been linted; suite 138→**142** tests / 603→**617** assertions; all baselines held plus `scripts` 0/2 fresh; decision **D-045**; lesson **L-016** — **three measurements produced a false PASS in one session**, including a guard test that passed against the guard not firing; next action = **Sprint 2 (MCP tool authorization, NEW-02/D-020)**

Previously: **slice 8 closed** (security headers get ONE enforcement point in `admin/bootstrap.php` — the S-07 answer applied to headers; **S-11**, the **CSP fail-open** and **NEW-14** all CLOSED); the finding's own numbers wrong twice over — `admin/api/` holds **23** files not 24, and "every admin page gets them via `templates/header.php`" is **false for five pages**, of which **`login.php` and `logout.php` sent nothing at all**, so the login form had no CSP, no `nosniff` and no `X-Frame-Options`; **the CSP fail-open proved load-bearing** — giving `login.php` a CSP without nonce-ing its inline 2FA switcher would have broken two-factor login, a regression shipped *by* a security fix; **a catch that would have broken every generated site** — failing closed at `installer/index.php` would have silently disabled the GDPR consent banner, since a build-time HTML file cannot carry a per-request nonce (**NEW-23**, now an explicit literal policy so the weakening appears in a diff); HSTS is HTTPS-only and **without `includeSubDomains`** (user decision); placement proven bounded on both sides by probe — `registerAutoloader()` is Step 1 of `boot()` — with the residue recorded as **NEW-22**; **two false passes caught in this slice's own tests (L-010, a third time)** — `header()` is a no-op under the CLI SAPI so three unit assertions could never fail, and an integration assertion passed with the nonce stripped because a `<style>` block also carried one; `style-src` keeps `'unsafe-inline'` deliberately with a test asserting that weakness on purpose, because a nonce source there makes browsers IGNORE it and would break all 349 inline attributes at once; suite 116→**138** tests / 541→**603** assertions; all four lint baselines held; decision **D-044**; **NEW-21**/**NEW-22**/**NEW-23** recorded and not fixed; next action = Sprint 1 slice 9 (`keel-verify` + regenerable INDEX + the CI workflow)

Previously: **slice 7 closed** (public comment submission relocated OUT of the admin tree to the web root; S-09 CLOSED, and closed the **opposite way** to the audit's recorded remediation, because that remediation failed the slice's own criterion 4 and would have granted anonymous scheduler triggers); the per-session rate limit found **structurally inert** (admin-scoped `SameSite=Strict` cookie) and replaced with the existing persistent IP-keyed limiter; **NEW-16** found and fixed in path (`SiteConfig::setValue()` never existed, so comments could never be enabled at all — the L-009 shape a third time); the security review's blocking finding restructured the endpoint's ordering after the suite was already green (flood ceiling moved before `App::boot()`; honeypot moved after the rate limit, where a `_honeypot` flood had been uncounted); `installer/public/` found to be outside the phpcs scan set entirely and now scanned at 0/0; suite 107→**116** tests / 472→**541** assertions; lint baselines held at 193/488 and 113/109; decision D-043 (amended post-review); lesson **L-014** (the audit's recorded fix was refuted by the audit's own recorded test point, and the feature was broken at three independent layers); NEW-17…NEW-20 recorded and not fixed; next action = Sprint 1 slice 8 (HSTS + CSP fail-open + NEW-14)

Previously: **slice 5 closed** (named escalations, one test each: S-01/02/03/05/06/12; S-06 residue closed in `api/tasks.php`; S-12 closed with POST + CSRF and the caller retargeted; the false docblock corrected and its missing protections recorded as NEW-13); the **inert `assertConfigNotMutated()` guard repaired** after being proven to check nothing since slice 3 (D-039, L-010); HTTP harness extracted to `tests/AdminHttpTestCase.php`; Keel v3.4.0 reconciliation completed with the build-assets contract overridden (D-038); decisions D-038…D-040; lesson L-010; suite 50→60 tests, lint baseline improved 201→199 errors; next action = Sprint 1 slice 6 (`SafeHttp` + SSRF, S-08)

Previously: slice 4 closed (`klytos_require_permission()` + central default-deny admin gate, S-07 CLOSED; `scripts/keel-verify` created and proven to fail on a removed gate; 5 capabilities added to the one matrix); bugs fixed in-path per D-033 (NEW-10 setup-wizard escalation, NEW-12's three stacked fatals in download-identity); NEW-09's fix REVERTED as unsafe (D-036); NEW-11 found and deferred (only `config['admin_user']` can log in); decisions D-032…D-036 (D-036 reverses part of D-033 after the security audit); lessons L-007, L-008, L-009; next action = Sprint 1 slice 5 (named escalations, one test each)
