# PROGRESS — Klytos CMS

> Living state. Read this FIRST in every session. Keep current and compact.

## Project card
- Name / one-line purpose: **Klytos CMS** — self-hosted AI-first CMS: build and manage websites entirely with AI through the Model Context Protocol (MCP), generating a static site.
- Project type: **Web app (self-hosted PHP application)** / **MCP server** (spans both — stricter rule wins on conflict)
- Stack & target platform(s): PHP 8.1+, no framework (custom microframework), Apache + mod_rewrite, pluggable storage (flat-file JSON or MySQL/MariaDB), vanilla CSS/JS, static-site build output. See `docs/03-technical-plan.md`.
- License: GPL-3.0-or-later
- Docs language: English (D-008)
- Security profile: `references/security/web-app.md` + `references/security/mcp-server.md` (D-003)
- Accessibility: **WCAG 2.2 AA + European Accessibility Act**, admin panel *and* generated output (D-007) — measured baseline ~20–25% / ~15%, see `docs/04-adoption-audit.md`
- i18n: multi — base English, 20 locales (ca, da, de, el, en, es, eu, fi, fr, gl, it, ja, nb, nl, pl, pt, ru, sv, tr, zh), custom JSON key-based mechanism (`installer/core/i18n.php`), no gettext (D-006)
- Installed base: **yes — released publicly** (GitHub Releases + self-updater + standalone installer repo); production installs with stored, encrypted data. Migrations / backward compatibility are mandatory.
- Design system: **a design contract now exists (D-065, superseding D-009)** — the Claude Design project *Klytos CMS Redesign* delivered 41 admin screens, a PackDesk-derived token set and the SVG logo family. The in-repo system (`installer/admin/assets/css/klytos-tokens.css`, 118 `--klytos-*`; frontend tokens generated by `theme-manager.php`) is the CURRENT state, not the target. The delivery lives at `docs/design/design-handoff/` (contract rule 10). Handoff gate: **PASSED 2026-07-29 (D-069)** — the DR-001 re-delivery is installed at `docs/design/design-handoff/` (123 files), the previous delivery archived at `docs/old/design-handoff/DR-001/`, and the evidence is `docs/BUILD-SPEC.md` §1c
- Keel portability: lock + embedded **v5.12.0** (both trees: `.claude/skills/keel/` + `.agents/skills/keel/`) (D-002; replaced by D-067, D-080; **both trees re-copied from the already-current user-level install and verified `diff -rq` clean, and both lock stamps refreshed v5.3.0 → v5.12.0 on 2026-08-09 (D-081)** — CLAUDE.md and AGENTS.md lock blocks rewritten in full from the new canonical copy, byte-identical to each other.
- Assistant config: **full** (tools: claude, codex, copilot, cursor, gemini, windsurf) — per references/assistant-config.md (D-010). Gemini via `.gemini/settings.json` `context.fileName` → `AGENTS.md`, no `GEMINI.md` mirror.
- Models: orchestrator = session model (strongest available) / reviewer = mid tier / mechanical = cheapest tier — concrete names live only in each tool's agent config (D-011)
- Keel baseline: **v5.13.0** (reconciled 2026-08-09, **D-082** — v5.12.0 → v5.13.0: `scripts/keel-chain-check` generated (twelve rows + `--json` + `--smoke`); `scripts/keel-continue` gained contract point 5b (`--model` on every `start` fire); new card lines `Chaining model:` (**claude-opus-5**) and `Chain verified:` (**not yet proven**); both lock blocks stamp-bumped v5.12.0 → v5.13.0 (content unchanged in substance per the changelog). `--smoke` was NOT run for real this session — it fires a live, billed `claude` process per the literal contract, and this implementation instead smoke-tests only the `osascript`/Terminal.app mechanism with a harmless `echo`, which is a deliberate scope reduction stated here rather than silently passed off as the full contract. Superseded by nothing; **D-081 below is the prior v5.3.0 → v5.12.0 reconciliation, unchanged.**)
- Keel baseline (prior): **v5.12.0** (reconciled 2026-08-09, **D-081** — v5.3.1…v5.12.0. Actions applied: `.claude/settings.local.json` gets `defaultMode: auto` + the forge conversational allow-list + `env.PATH` (pending user approval of the file edit — auto-mode classifier blocks direct edits to permission files); project card gains `Autonomy:`, `Durability:`, `Branches:`, `Notify:`, `Test-first policy:` lines (all asked/confirmed 2026-08-09); `scripts/keel-handoff-verify` extended with the single-lane lock + `--release` (tested: take/busy/release/idempotent-release, all pass in an isolated scratch repo); `scripts/keel-continue` generated per the full v5.3.0…v5.10.3 contract (verify-before-fire, Handover check, tool detection, tier downgrade, live PATH re-check, atomic receipt claim, circuit breaker, lane release before firing, mktemp launch-script + `do script` by path, non-`---`-prefixed instruction) — tested through the print-fallback path (claude hidden from PATH) in a scratch repo; **the actual osascript firing itself is UNEXERCISED — the first real close-out that chains is its own evidence**, per the skill's own "an unexercised mechanism is not a blocked one." `docs/05-test-points.md` and `scripts/keel-verify`'s Red-first / test-first checks (v5.11.0) and `docs/keel-conformance.md`'s new-row sweep are **NOT yet done — carried to the next work block**, named so a fresh session does not skip them silently.)
- Autonomy: **automatic** — chosen 2026-08-09. `defaultMode: auto` in `.claude/settings.local.json` (edit pending user approval, see Keel baseline note); Keel merges to `develop` and pushes without asking; never merges to `main` or publishes a release without explicit instruction.
- Durability: **covered** — `origin` remote at `https://github.com/joseconti/klytos.git`, both `develop` and `main` tracked. Verified 2026-08-09.
- Branches: **git-flow** — `develop` integrates, `main` is the user's; both exist and are tracked at `origin`.
- Notify: **PushNotification tool** (desktop notification in this session's terminal; also pushes to phone if Remote Control is connected). Recorded 2026-08-09.
- Issue sweep interval: **24h**, duty accepted 2026-08-09 — sweep `docs/issues.md` freshness at every sprint kickoff and close.
- Issue capture: **off** — a problem the user reports in conversation stays in conversation/`docs/history.md`; it does not automatically become a public GitHub issue. Klytos's tracker already reserves some findings for private disclosure pre-fix (`docs/issues.md`), which is the reason recorded 2026-08-09.
- Test-first policy: **pure-logic** (default) — chosen 2026-08-09. Pure functions of their inputs (validators, parsers, money, entitlements, state machines) get their test written and seen FAILING before the code; markup/framework-glue/config exempt. Every bug fix, at any policy value, starts from a failing reproduction test (unconditional).
- Chaining: **start** — chosen 2026-08-09 (D-081), the maximum tier `Autonomy: automatic` recommends and this project's gates allow (macOS, `claude` on PATH verified, single-lane lock now implemented). `scripts/keel-continue` generated; the end-to-end fire is unexercised (see Keel baseline note above).
- Chaining model: **claude-opus-5** — chosen 2026-08-09 (D-082, Keel v5.13.0). Every chained `start` launch passes `--model claude-opus-5`; a chained chat never inherits the launching session's model.
- Chain verified: **not yet proven** — no `--smoke` run has been recorded, and that single row is the ONLY thing standing between this project and `VERDICT: READY`. As of 2026-08-09 `scripts/keel-chain-check` reports **11 of 12 rows OK**; B5 is the outstanding one. It certifies that the launcher has been *observed* to run, which no amount of reading can establish — see D-082.
- Domain rubric (Phase 2 §6a): **API ergonomics** — `docs/rubrics/api-ergonomics.md`, applied by `code-reviewer` check 8 (D-066)
- Website intent: **yes** — klytos.io; Phase 8 deferred until requested (D-012)
- Client budget: **no** — own project (D-013)
- User guide: n/a until Phase 6
- Docs theme: n/a until Phase 6

## Phase status
| Phase | Status | Key artifacts |
|-------|--------|---------------|
| 1 Discovery | **adopted (as-built)** | docs/00-competitive-landscape.md, docs/01-discovery.md |
| 2 Functional spec | **adopted (as-built)** | docs/02-functional-spec.md, docs/03-technical-plan.md, docs/api/INDEX.md |
| 3 Design handoff | **LIVE since 2026-07-27 (D-065)** — a real handoff arrived for the admin (41 screens, tokens, logo set) | Claude Design `Klytos CMS Redesign`; `docs/BUILD-SPEC.md` |
| 4 Faithful build | **IN PROGRESS — gate PASSED 2026-07-29 (D-069); Step 2 DONE 2026-07-29 (D-070).** Step 1 ran twice: FAILED 2026-07-27 (ten gaps → DR-001), PASSED on the re-delivery after a wholesale swap, a byte-stability diff (`screens/**` untouched; six token files differ in comments only) and an **independent recomputation of all 72 contrast ratios (72/72 agree)**. **DR-001 RESOLVED.** Step 2 consolidated `BUILD-SPEC.md` **§5** (11 subsections) and **found four open items** — one of them a real gap: the **Dashboard (`index.php`) has no design**, drafted as **DR-002** (not sent). **NEXT: the user's decisions on §5.11, then Step 4 (build).** No file under `installer/admin/` touched yet | docs/BUILD-SPEC.md (§1c, §5), docs/design/design-requests/DR-001.md + DR-002.md, docs/design/design-handoff/, docs/design/DESIGN-BRIEF.md |
| 5 Development | **in progress** — Sprint 1 CLOSED (all 10 slices); Sprint 2 CLOSED (MCP tool authorization, all 4 slices — audit NEW-02 closed); Sprint 3 CLOSED (vendor-ai CVE remediation + the AI stack fails safe — both slices; audit NEW-05 and NEW-06 CLOSED); Sprint 4 CLOSED (the hook mutation contract + owner recovery — both slices; audit NEW-03, NEW-36 and NEW-08 CLOSED); Sprint 5 CLOSED (authentication, both slices — NEW-11 + NEW-37 + NEW-39 + NEW-09; D-056…D-058; **user verdict PASS 2026-07-26**); **Sprint 6 IN PROGRESS** (hardening — **slice 1 CLOSED 2026-07-26**: NEW-40 + NEW-20 + NEW-44; **slice 2 CLOSED 2026-07-26**: NEW-41; **slice 4 CLOSED 2026-07-27**: NEW-47 + NEW-26 + NEW-50 + NEW-51 + NEW-52, pulled forward by D-061; **slice 3 CLOSED 2026-07-27**: NEW-42, so ALL FOUR SLICES ARE CLOSED and only the sprint close itself remains; D-059, D-060, D-061, D-063) — plus **`docs/roadmap.md`**, the ordered route to v1 (D-062) | docs/sprints/sprint-1.md … sprint-6.md, docs/05-test-points.md, docs/estimate.md, docs/flows/ |
| 6 Documentation | pending — progressive backfill of per-surface docs is in force | docs/architecture.md, docs/api/, docs/usage/, docs/reference/ |
| 7 Release | pending — the next release runs the FULL Phase 7 | docs/07-release.md |
| 8 Website | deferred (intent = yes) | docs/site/ |


## Current position

- **Phase 5 — Development. Sprint 6 (hardening) has all four slices CLOSED**; what remains is the
  sprint close itself. Audit findings closed by this sprint: **NEW-40**, **NEW-20**, **NEW-44**
  (slice 1, D-059); **NEW-41** (slice 2, D-060); **NEW-47**, **NEW-26**, **NEW-50**, **NEW-51**,
  **NEW-52** (slice 4, D-061); **NEW-42** (slice 3, D-063).
- **Tree state (re-measured 2026-07-29, after DR-003):** suite **284 tests / 1393 assertions**, 0
  skips (+1 assertion: check 16's row in `KeelVerifyTest::EXPECTED_CHECKS`);
  `keel-verify` **17 check(s) run: 13 passed, 4 warning(s)** — **check 16 added 2026-07-29 (D-074,
  L-030)**: every `#ks-*` the admin references resolves to a sprite `<symbol>`, proven to FAIL on a
  planted bad id and to pass after restore. The four warnings are unchanged and each is owned
  elsewhere (version touchpoints and the guide export-ignore → Phase 7; the README link backlog →
  D-017; the conformance gap → Phases 6/7/8). `keel-doctor --check` green, 14 rows, all blocking
  satisfied; upgrade from real v0.30.1 PASS. Lint baselines (D-025): core+admin **191/488**, plugins
  113/109, tests 0/0 (46 files), installer/public 0/0, scripts 0/2 — **unchanged**; `scripts/keel-verify`
  is extensionless and phpcs does not lint it, so the baseline could not have grown from this change.
  First-party lint suppressions: **0** (the 119 counted in a first pass are all vendored — D-067).
  `docs/api/INDEX.md` total **970** (stage 1's `admin.design_tokens` row; the 969 here was stale).
- **Keel reconciled v3.5.0 → v5.0.0 on 2026-07-28 (D-067).** New artifacts a session should know
  exist: `docs/keel-conformance.md` (the sweep + the anti-patterns self-audit),
  `docs/threat-model.md`, `docs/rubrics/api-ergonomics.md`, `docs/design/DESIGN-BRIEF.md`,
  `scripts/keel-doctor`, and the `test-driver` subagent. **Two behavioural rules now bind every
  session:** run `./scripts/keel-doctor --check` before trusting any test result, and **the user is
  not the test runner** — anything a machine can drive, the assistant drives, and a delegation
  without one of the eight tags is a defect. Sprints 1–5 archived to `docs/old/`.
- **NEXT ACTION — finish the Sprint 6 close.** Done: `docs-verifier` (PASS, counts re-derived
  independently); the state-file split (**D-064**, this change). Owed: the fresh-context
  playground-QA pass, and **the user's own recorded test verdict** on the sprint's flows — the
  sprint does not close without it.
- **Phase 4 — Step 2 is DONE (2026-07-29, D-070): `docs/BUILD-SPEC.md` §5 is the build contract.**
  Eleven subsections: the five build rules that override intuition · the 43-entry resolved screen
  list + the 11-template reuse map · the exact token table (colour, neutrals, the Klytos
  accessibility layer, type, spacing/motion, inherited-and-unused, the seven rules) · a state matrix
  **per template** (43 screens' deltas are referenced, never copied — forking the manifest is L-004
  at 43×) · a 41-row per-screen ledger · the accessibility contract · the interaction table · the
  asset map · external setup and external assets (both genuinely n/a) · the integration plan with
  its code-side adaptations log · the faithfulness checklist.
  - **Step 2 found FOUR open items (§5.11). Item 1 is CLOSED; 2–4 are open and are the user's:**
    **(1)** ~~the Dashboard has no design~~ — **CLOSED 2026-07-29 by DR-002 (D-071)**: it is now
    manifest **entry 44**, `index.php` · overview-stats · H1 "Dashboard", specified as deltas with
    no twelfth template and no new glyph. One consequence rides along and is **not** cosmetic:
    indexing moves off the Dashboard to **Settings → Advanced** (checkbox + Save, gated at
    `site.configure`), and the Dashboard only warns while the site is blocked — `index.php` carries
    that toggle today. **(2)** the Setup wizard's seven steps are
    **`installer/install.php`** (three steps today), not `admin/setup-wizard.php` — product scope,
    plus NEW-04 makes `install.php` destructive in a checkout. **(3)** Verify, Comments and Health
    are **new product surfaces**, not redesigns. **(4)** the i18n cost is invisible in the design:
    41 screens of new copy × 20 catalogues.
  - **Two code-side adaptations logged up front:** `tokens/fonts.css`'s two `url()`s may move
    (authorised by `assets-index.md` §2), and **entry-point filenames stay as they are in the tree**
    — Klytos is released, a filename is a URL, and the manifest's names are recorded as a mapping.
  - **THE FOUR §5.11 QUESTIONS ARE ANSWERED — 2026-07-29, D-072. Step 4 is authorised.**
    **(1)** the indexing move is **CONFIRMED** by the user: `open-questions.md` items 13–15 are
    genuinely theirs, so entries 44 and 9 are built as DR-002 specifies (toggle to Settings →
    Advanced gated at `site.configure`; the Dashboard warns only, with no toggle).
    **(2)** the **Setup wizard is DEFERRED** to its own Phase 5 slice — product scope, and NEW-04
    makes `install.php` destructive in a checkout. **Consequence: `template-wizard.md` is not built
    in Phase 4 either** (entry 17 is its only consumer).
    **(3)** **Verify, Comments and Health are DEFERRED** to their own Phase 5 slices, with Verify
    **paired with NEW-38**.
    **(4)** the **i18n rule is UNCHANGED** — every new key in all 20 catalogues in the same slice;
    no slice passes its test point with a key in English only.
    All four deferrals are carried into `docs/roadmap.md` §0b so a decided deferral cannot become a
    forgotten one. They stay manifest entries and stay in §5.1: **the redesign is not reportable as
    complete while they are outstanding.**
  - **STEP 4 IS UNDER WAY. Scope: 40 of the 44 entries** (44 minus 11, 14, 17, 22), in the
    delivery's own order: **tokens → shell → components → list screens → form screens → the
    specialised** (editor, terminal, AI chat, preview — the wizard leaves with entry 17).
    `docs/BUILD-SPEC.md` §5 is 44 entries / 42 product screens; **Phase 4 builds 40 / 38.**
  - **Stage 1 of 6 — TOKENS — DONE 2026-07-29.** The first files under `installer/admin/` have now
    been touched. What landed, all of it verified rather than asserted:
    - **74 delivered files placed and each one diffed byte-for-byte against the delivery**: 9 token
      stylesheets → `assets/css/tokens/`, 3 font files → `assets/fonts/`, the 67-symbol sprite →
      `assets/icons/`, 13 brand SVGs → `assets/images/brand/`, 48 PNGs → `.../brand/png/`. The four
      `*-text.svg` editing sources are deliberately **not** shipped (§5.7).
    - **One file edited, under written authorisation**: `tokens/fonts.css`'s font paths →
      `../../fonts/`. **It is four `url()` lines, not two** — each `@font-face` lists its file twice
      (variations hint + fallback). Changing only the two `src:` lines and missing the continuations
      was **caught by diffing against the delivery** and would have shipped a 404 on the fallback.
    - **The chain is verified from rendered output, not from source** (Step 4a): playground on
      `KPORT=8123`, bind confirmed, owning PID checked (L-021), no `Server:` header (L-011), logged
      in as owner, and the browser receives the eight token files **in the SPEC's declared order
      with `klytos-admin.css` last and `klytos-components.css` immediately after**. All 8 CSS, 3
      fonts, sprite, brand SVG and PNG answer **200**.
    - **`platform.css` is placed but deliberately NOT linked** — the delivery lists it under
      *inherited and not used*; implementing it would be the deviation (adaptation 4).
    - **Build rule 1 re-verified against the bytes**: `typography.css` shadows `--type-body` and
      `--type-caption`; the Klytos block at lines 91/95 wins, so the effective values are
      **13px/17px** and **11px/16px** — not the 18/14 a first grep match returns.
    - New public surface, documented in the same slice: filter **`admin.design_tokens`** →
      `docs/reference/design-tokens.md` + its `docs/api/INDEX.md` row (**Filters 122 → 123, total
      969 → 970**; the parity check passes).
    - **Adaptations 3, 4 and 5 logged** in §5.9. **Adaptation 5 is temporary and tracked as such:**
      Font Awesome is still loaded because every shipped screen still draws from it; it is retired
      when the last consumer is ported, and the redesign is not complete while that row stands.
    - Tree state after the stage: suite **284 tests / 1392 assertions**, 0 skips; `keel-verify`
      **12 passed / 4 warnings** (unchanged); `header.php` lint **0 errors / 5 warnings —
      identical to HEAD**, so the D-025 baseline did not grow.
  - **Stage 2 of 6 — THE SHELL — STOPPED BEFORE ITS FIRST LINE, 2026-07-29 (D-073).** No file under
    `installer/admin/templates/` has been touched. Two gaps found by reading the delivery against
    itself block the sidebar, and Phase 4 Step 4's rule for mid-build discovery is to stop, not to
    improvise. **DR-003 is DRAFTED, not sent** (`docs/design/design-requests/DR-003.md`).
    - **Gap 1 — 19 of the 35 glyphs the sidebar draws are not in the delivered sprite.** Computed:
      the union of every prototype's `navGroups` is 35 Material Symbols names; 16 are present as
      `ks-*`, **19 are absent** (`account_circle` `category` `checklist` `cookie` `dynamic_form`
      `forum` `group` `menu_book` `monitor_heart` `monitoring` `palette` `perm_media` `policy`
      `preview` `receipt_long` `shield` `space_dashboard` `translate` `workspace_premium`). The
      contradiction is **inside the delivery**: `assets-index.md` §3 calls the sprite "one `<symbol>`
      per glyph the design uses" and names it as the sidebar's source at 18px. Three shell controls
      also have no glyph named anywhere: the <900 drawer button, the rail's "Expand navigation"
      button, and the theme toggle.
    - **Gap 2 — the sidebar navigation is never specified.** The README names the eight groups in one
      sentence and stops; `template-shell.md` specifies the sidebar's *states*, not its contents. No
      file maps the 44 entries onto the groups. Each prototype carries a different partial
      `navGroups`, no file shows all eight groups, and the union is **not self-consistent**: `Blocks`
      is under **Design** in four files and **Content** in two, and `Guides` (count 16) is a nav item
      for a screen that is not a manifest entry and has no admin surface here at all.
    - **Why the Step 1 gate missed both:** it verified the sprite exists, is described, and is
      byte-identical — all true. It never asked whether a glyph a screen *draws* is one of the 67,
      and `template-shell.md` is complete for what it claims to cover. `open-questions.md` item 18
      ran exactly the right check for the Dashboard and it was never run for the shell. **A
      per-screen glyph-presence check is added to the gate when DR-003 resolves.**
  - **DR-003 RESOLVED the same day (D-074) — stage 2 is UNBLOCKED.** The re-delivery arrived at
    `docs/klytos-cms-redesign/`, was audited before installation, installed as a wholesale swap
    (previous delivery **moved** to `docs/old/design-handoff/DR-003/`, 123 files; new one 124), and
    re-gated. Evidence: `docs/BUILD-SPEC.md` **§1e**. What the build now has that it did not:
    - Sprite **87 symbols** (67 + the 19 + `ks-menu`), 0 removed, geometry identical across all 87.
      Re-placed in the tree and byte-verified; the other 73 stage-1 files re-checked, 0 differing.
    - **`SPEC/navigation.md` is a new normative input** and wins over any prototype `navGroups`:
      eight groups in fixed order, 34 items with label/glyph/entry/count, plugin capability→group
      mapping with a five-item bound and `ks-extension` fallback, capability-gated items **hidden**
      (not disabled), and §8's shell controls — drawer `ks-menu`, "Expand navigation"
      `ks-chevron_right`, **theme toggle text-only with no glyph**.
    - Five nav labels differ from their `<h1>` on purpose (Overview, Assets, Theme, MCP, Agent
      payments) and three prototype counts were removed with reasons (Transactions, Logs, Options).
  - **Stage 2 of 6 — THE SHELL — DONE 2026-07-29 (D-075, D-076).** Built against
    `template-shell.md` (behaviour) **and** `SPEC/navigation.md` (contents), and **verified by
    driving a running install**, not by reading source.
    - **Files:** `templates/header.php` + `sidebar.php` + `footer.php` rewritten; **new**
      `core/admin-nav.php` (the nav model), `assets/css/klytos-shell.css`,
      `assets/js/klytos-shell.js`, `admin/api/theme.php`. `klytos-sidebar.css` left the chain —
      it styled markup that no longer exists.
    - **The two user decisions taken BEFORE the first line, not discovered inside it:**
      **(1)** Comments (14) and Health (22) are **OMITTED** from the nav until their Phase 5
      slices — rendering them would 404 the primary navigation of every screen. They stay in the
      definition marked `'deferred' => true`, so restoring each is deleting one line.
      **(2)** the shipped extras are triaged **"design wins, hooks preserved"**: all 30 hooks keep
      firing (released plugins use them), the recovery banner and dev bar stay, and the JS search
      filter / collapse-to-60px / hover tooltips are replaced by the design's own ⌘K and 56px rail.
    - **Driven evidence (playground `KPORT=8137`, bind + owning PID + no `Server:` header checked,
      reseeded `--reset`):** all **39** shell-using screens driven as owner with **0 PHP errors, 0
      fatals, 0 product-log errors** (the 8 non-200s each checked: 4 are the screens' own
      "no id → list" redirect present at HEAD, plus login 302, bootstrap 403, plugin-page 400) ·
      the capability rule driven as **all four roles** — owner 8 groups/35 items, admin 8/30,
      editor 6/12, viewer 4/4, **0 `disabled` markers at any role**, empty groups vanishing,
      **Overview present for all four** (checked against the real matrix: `pages.view` is held by
      every role, so §7 is not a widening) · 8 groups in the fixed order, `Blocks` under **Design**,
      `Guides` absent, Settings last · landmark uniqueness 1/1/1 and **no `tabindex` above 0** ·
      `page-editor.php` highlights **Pages** (§5 parentage) · the theme toggle flips
      `<html data-theme>` **server-side from the cookie with no JS**, no-CSRF **403**, GET **405**,
      invalid value ignored, open-redirect refused.
    - **Three things the driving caught that reading would not have:** a fatal from
      `klytos_kses()` called with one argument where it takes two (the suite found it on
      `security.php`); **"1 post types"** — this i18n mechanism has no plural forms, so all 20
      catalogues' count phrases were rewritten to `navigation.md`'s own number-neutral wording; and
      **`keel-verify` check 16 was passing on ZERO references** while the shell plainly drew icons,
      because it scanned only `installer/admin/` for a literal `#ks-*` and the ids live in the core
      nav model as quoted strings. **That is L-030's failure mode reappearing inside the check
      written to close it.** Fixed, now sees **36 ids all resolving**, and **re-proven to FAIL** on
      a planted bad id before being trusted again.
    - **i18n: 64 new keys × 20 catalogues**, pure additions (74 lines per file, 0 deletions),
      parity **PASS** at 729 keys each.
    - **Tree state (re-measured 2026-07-29 after D-077):** PHP suite **304 tests / 1526
      assertions**, 0 skips · **browser tier `tests/E2E/` — 16 tests, all passing** (new), trace and
      video recorded for every one of them under the gitignored `tests/E2E/artifacts/` ·
      `keel-doctor --check` green with **Playwright and axe-core now OK/blocking** rather than
      MISSING/optional (the doctor promotes them itself once the E2E tier exists) ·
      `keel-verify` **17 checks: 13 pass, 4 warnings** (unchanged, all owned elsewhere),
      `keel-doctor --check` green, lint baseline **188/485 — it went DOWN**, never up (D-025 records
      191/488). `docs/api/INDEX.md` **986** rows, Summary and data rows both 986.
    - **NOT verified, and it needs the user's word:** everything JavaScript-dependent — the command
      palette's open/close and combobox keyboard behaviour, the drawer's focus trap, the rail's
      Expand button, the offline status. **Playwright is absent** (`keel-doctor` marks it MISSING,
      optional) and installing it is not a call the assistant makes unilaterally. The server-rendered
      half is fully driven; the JS half is `⚠ unverified` until Playwright is installed.
    - **One pre-existing defect recorded and deliberately NOT fixed here:** `ai-chat.php` prints
      **two** `<h1>`s against `accessibility.md` §4.2. It belongs to the conversation template in
      stage 6; folding the fix into a shell slice would hide it from the stage that owns it.
      `$pageEmitsOwnH1` stops the shell adding a third.
  - **Stage 2's `⚠ unverified` list is CLOSED — 2026-07-29 (D-077).** The user authorised installing
    **Playwright** (1.62.0 + chromium 151.0.7922.34, verified by launching it, not by reading the
    package tree) and `@axe-core/playwright` 4.12.1. A new `tests/E2E/` tier exists —
    `fixtures.js` (real-form login + the read-back duty), `smoke.spec.js`, `shell.spec.js` — and
    **16 tests pass** over the palette, the drawer, the rail and the offline state.
    - **It found TWO REAL DEFECTS in code stage 2 had already closed.** (1) **The command palette
      was a keyboard trap** (WCAG 2.1.2): the search field opens it on focus, `closePalette()`
      returns focus to its opener, so Escape closed it and the search field immediately re-opened
      it — **no way out with the keyboard alone.** Fixed with a `restoringFocus` guard.
      (2) **The rail's "Expand navigation" button could never be shown at any viewport** —
      `.k-rail-expand { display: grid }` sat inside the 900–1199 media query and the base
      `display: none` sat AFTER it, so source order won. **This is build rule 1's exact shape**
      (`typography.css` shadowing `--type-body`) arriving in a layout rule. Fixed by moving the
      base block above the media query, with the reason written into the stylesheet.
    - **Two of the four first-run failures were TEST defects, not product defects** —
      `locator.focus()` does not complete the palette hand-off that a real click does, and the
      drawer-scrim click at (5, 5) landed on the brand link inside the drawer. Separating them
      from the real ones was the work.
    - **Both product fixes were proven by watching their test go red → green**, and the read-back
      fixture was proven to FAIL on a planted `console.error` before being trusted (L-030's rule).
    - **Recorded as L-031:** the unverified fraction is never a random fraction — it is the
      fraction the tools could not reach, and defects concentrate there for the same reason.
    - **`@axe-core/playwright` is installed and deliberately NOT yet wired into a test.** The
      per-state accessibility pass lands with the component layer. Present-and-unused is stated,
      not counted as coverage.
  - **DR-004 IS SENT — 2026-07-29 (D-077).** `docs/design/design-requests/DR-004.md` is
    `Status: sent`; the ready-to-paste prompt is in its last section. It blocks **one control** —
    the sidebar search form's JS-off destination — and nothing else. Stages 3–6 proceed.
  - **Stage 3 of 6 — THE COMPONENT LAYER — DONE 2026-07-29 (D-078).**
    `assets/css/klytos-components.css` is now the redesign's `.k-*` layer: button, badge, chip,
    card, table, field, switch, checkbox, radio, stat card, progress, code block, empty/error
    state, status line and error summary. **Ten of accessibility.md §5's twelve components** —
    the palette was built in stage 2 and the copilot dock belongs to stage 6.
    - **Adaptation 9, and it is the stage's one structural call.** The file the contract names is
      also the live stylesheet of the screens the redesign has not reached: **up to 40 admin PHP
      files** draw `.btn` / `.card` / `.alert` / `.form-control` from it. Its previous 991 lines
      moved **unmodified** to `klytos-legacy-components.css`, loading immediately before the new
      layer — body `diff`ed clean BEFORE the original was replaced. **Temporary and tracked like
      adaptation 5**: deleted when its last consumer is ported. Verified on six real admin screens
      as owner — both sheets load in order, `.card` keeps 8px, `.k-card` gets 10px, zero errors.
    - **A test-only SPECIMEN makes the layer verifiable per STATE.** Manifest entry 16 is
      explicitly "a catalogue — not a screen" with no entry point, so nothing was built as a
      product URL: `tests/E2E/fixtures/components-specimen.html` (under the export-ignored
      `tests/`) is served by route interception at an `/installer/admin/` path, giving it the
      playground's real origin and the REAL stylesheet chain. The router was not touched.
    - **`tests/E2E/components.spec.js` — 64 tests, all passing** (whole tier now **80**).
      axe-core at WCAG 2.2 AA scoped to **each of 12 sections × both themes**, plus geometry read
      back from the browser, the table's grid-and-roles in one run, the states, and four
      breakpoints. `KPORT=8143`, bind + owning PID + no `Server:` header checked, `--reset` seeded.
    - **FOUR REAL DEFECTS, none of which reading found.** (1) A **WCAG 1.4.10** failure: the page
      scrolled 346px at 320 CSS px because `.k-sr` — a 1 × 1 invisible span in §2.1's own table
      markup — is `position:absolute` with no positioned ancestor, so it escaped both the scroll
      container and the card's `overflow:hidden`. **Every containment reading said 280-inside-320
      while the page really scrolled.** (2) A cascade loss painting a record's name at 4.11:1:
      `.k-card a:not(…)` is `(0,4,1)` and beat `.k-table tbody th a` at `(0,1,3)` — **build rule
      1's shape a third time, and NOT source order this time** (L-032). Fixed structurally with
      `:where()` at zero specificity after two numeric attempts failed. (3) A whole class of link
      had **no colour rule anywhere in the admin** and fell to the UA's `#0000EE` — 1.48:1 in
      dark. (4) Stacked error-summary links under the 2.5.8 target size.
    - **TWO FALSE FAILURES separated from the real ones, which was the work.** axe's
      `aria-required-children` fires on a legal `<caption>` inside an explicitly-roled table —
      refuted by reading Chromium's actual tree, which is exactly what §2.1 describes; the rule is
      disabled **only on the two table sections** and what it covered is pinned by two tests, each
      proven against a planted defect. And a contrast reading taken **mid-`transition`** reported
      a button at 2.59:1 that is 4.86:1 — the fixture now bakes `data-theme` in before load, which
      is how the product itself works (D-075).
    - **Three failing pairs FIXED in the build** because `klytos-admin.css` already states the
      rule: selected chip → `--sobre-tinte-acento` (4.11 → 5.02), tinted log lines →
      `--sobre-tinte-*` (3.82 → 4.53), read-only field → `--texto-primario` (4.46 → 14.79). The
      secondary button's border is the same shape, logged as **adaptation 10**.
    - **Tree state after the stage:** PHP suite **304 / 1526**, 0 skips (unchanged) · E2E **80
      tests, all passing** · `keel-verify` **17 checks: 13 pass, 4 warnings** (unchanged) ·
      `keel-doctor --check` green, 14 rows · lint baseline **188/484 — it went DOWN again** (D-025
      records 191/488; last session 188/485) · i18n **untouched — this stage added no
      user-facing string**, so the 20 catalogues stay at 729 keys each, parity PASS.
    - **`docs/05-test-points.md` gains a Phase 4 section** — stage 3 is the FIRST row to carry its
      trace paths, as Keel v5.1.0 requires. Stages 1 and 2 predate the rule and that is stated
      rather than backfilled.
  - **Stage 4 of 6 — THE LIST SCREENS — BATCH A ONLY, 2026-07-29 (D-079). TWELVE OF THIRTEEN
    SURFACES ARE BLOCKED ON DR-006.**
    - **What blocked them.** `template-list-table.md` §1 delegates `grid-template-columns` to the
      consuming screen; `accessibility.md` §2.1 forwards column widths to "the README's Table
      entry"; the README's Table entry writes `grid-template-columns:…` — an ellipsis. The manifest
      records the value for **entry 1 alone**. Twelve surfaces have columns, filters, empty sentence
      and deltas — the exact four things §1 asks for — and no widths; **three of them (28 Licence's
      activated domains, 35 Updates' plugin updates and history) have no column list either**. The
      prototypes cannot substitute: their tables have DIFFERENT COLUMNS, not the same columns at
      other widths (Users has no checkbox and no 2FA and gives email its own column; Plugins carries
      a Licence column the manifest omits; Pages draws SIX tracks where the manifest gives SEVEN).
      **DR-006 is SENT.** L-030's shape a third time — the gate proved every template exists and
      never asked whether the value it hands the screen was ever written down.
    - **BUILT: the shared table machinery + manifest entry 1 (Pages), fully specified and complete.**
      The under-900 stacked record cards (stage 3 deferred them here), the scroll container, the
      sort/pagination/bulk-bar chrome, and `pages.php` rewritten against the template — chips,
      search, caption with count and page position, the seven-column grid quoted from the manifest,
      all six states, the disabled home-delete with its reason in its accessible name.
    - **FIVE REAL DEFECTS, four of them in code earlier stages had already closed:**
      **(1)** the redesign's link layer had been **losing to the pre-redesign `klytos-base.css`
      since stage 3** — `:where()` at (0,0,0) loses to a bare `a` at (0,0,1), so every plain link in
      a card rendered the old `#5B8DEF` at **4.31:1** in dark. **The specimen could not show it: it
      does not load that sheet** (L-033). **This is build rule 1's FOURTH mechanism, introduced by
      the fix for the third.** **(2)** the bulk bar was **visible on load** — `[hidden]` lost to
      `.k-bulkbar{display:flex}` on source order. **(3)** the content area never gained its 48px —
      `.k-has-bulkbar` lost to `.k-main`'s shorthand in a later sheet. **(4)** `.k-table-scroll` was
      written in CSS and **never rendered**, so 320 CSS px scrolled **349px** (WCAG 1.4.10) and the
      table and stacked cards were both present under 900. **(5)** `--opacidad-desactivado` did not
      exist — invented from habit; §1.4's 3:1 floor on `--texto-deshabilitado` is the rule.
      **Every fix was proven by planting the defect back and watching its test go red**, then
      restoring byte-identically.
    - **Four product calls taken WITH THE USER, not inside the build:** the trash view and custom
      statuses **stay** as further chips (removing shipped behaviour is not a fidelity decision);
      the designed **Private chip is NOT rendered** — Klytos has no private status, so it would be a
      filter that can never match (DR-006 item 4); **sort and search live in the screen**, not in
      `PageManager::list()`, leaving a released signature untouched; the bulk set maps onto existing
      operations and **change template** was built new.
    - **DR-005 gained an ADDENDUM**: the filter row sits on `--fondo-ventana`, outside any card, and
      both chip states measure **4.46:1** in light while both pass on a card. The unselected chip's
      pair is **identical to gap 2's**, so a fix scoped to the code panel would leave thirteen
      filter rows failing.
    - **i18n: 26 new keys × 20 catalogues**, pure additions, parity **PASS** at **754 keys** each.
    - **Tree state:** PHP suite **304 / 1526**, 0 skips · browser tier **111 tests, all passing**
      (80 + 31 new), **0 skipped** — both tests that first skipped on a thin seed now build their
      own population through the real PageManager · `keel-verify` **17 checks: 13 pass, 4 warnings**
      (unchanged), check 16 now sees **41** ids all resolving · `keel-doctor --check` green ·
      lint baseline **182/482 — it went DOWN again** (D-025 records 191/488; last session 188/484),
      and `pages.php` itself went **6 errors → 0**.
    - **One transient worth recording rather than hiding:** the first PHP run of the block reported
      297/1480 with an error. It was **L-025 exactly** — the suite ran while the playground server
      was live and the integration tier shares it. Green at 304/1526 with the server stopped.
  - **NEXT ACTION — stage 4 batch B, which needs DR-006 answered first.** With the widths in hand
    the remaining twelve are width-substitutions over machinery that is now built and driven:
    5 Users · 15 Plugins · 24 Webhooks · 27 Profile · 28 Licence · 30 Options · 32 Taxonomies ·
    33 Scheduled · 34 System integrity · 35 Updates · 36 Transactions (41 Logs needs nothing from
    DR-006 — console-stream has no grid). **Every one of them re-opens L-033's question**: the
    legacy sheets are still loaded, so each ported screen measures its own colours on a real page.
    Then stage 5 (form screens) and stage 6 (the specialised four).
- **Phase 4 — the gate PASSED on 2026-07-29 (D-069) and the build is authorised.** The re-delivery
  was downloaded by the user, installed as a wholesale swap, diffed against the archived delivery,
  and re-audited with evidence in `docs/BUILD-SPEC.md` §1c. `screens/**` byte-identical; six token
  files changed in comment headers only; **all 72 contrast ratios recomputed independently, 72/72
  agree**; `open-questions.md` at zero. **DR-001 RESOLVED — zero open Design Requests.**
  - **Still no file under `installer/admin/` has been touched.** Passing the gate authorises the
    build; it is not the build. Step 2 (above) wrote the contract it will not deviate from.
  - Two facts already fixed, so they are not rediscovered mid-build: `typography.css` **shadows**
    `--type-body` and `--type-caption` (the Klytos block at the foot wins — never take the first grep
    match), and tables are **real `<table>` elements carrying the full explicit role set**, spelled
    out element by element in `SPEC/accessibility.md` §2.1.
  - **Staging copy `docs/klytos-cms-redesign/` (167 files) DELETED 2026-07-29**, on the user's
    instruction, after a file-by-file check — and the check is the part worth keeping, because an
    earlier entry had already claimed this deletion had happened when it had not. **The "verified
    byte-identical" that entry rested on covered only the export's
    `project/design_handoff_klytos_admin/` subtree** (`diff -rq` clean, re-confirmed before
    deleting) — **not the export's project root**, which held its own copies of the nine `*.dc.html`
    prototypes. **Eight of those nine differed from the delivery's**, always by the same single line:
    the ⌘K hint's `top:calc(50% - 5px)` versus the delivery's `top:50%`. Everything else at that root
    was accounted for first: the `_ds` PackDesk bundle and the three support files byte-identical,
    nine of thirteen brand SVGs identical, and the other four the superseded live-text marks,
    preserved byte-for-byte inside the delivery as `*-text.svg`. All of it is recorded in
    `docs/BUILD-SPEC.md` §1c item 3 **before** the bytes were destroyed. After deleting: the
    delivery is intact at 123 files and both archives (`DR-001` 49, `DR-002` 123) are untouched.
    Two files that existed
    only in the export and are **not** part of the delivery were recorded in §1c first:
    `css/klytos-components.css` (the file the BUILD writes — taking it would be a principle-8
    violation) and the prototypes' Font Awesome CDN link (scaffolding; the delivered sprite is the
    icon source).
- **After Sprint 6**, the recorded route is `docs/roadmap.md` (D-062): **NEW-27** first (the
  `.gitattributes` review), then the rest of the hardening — with **NEW-17** early, because
  NEW-46 and NEW-49 cannot be reasoned about honestly until the client address is trustworthy —
  then accessibility, the theme package, and Phases 6/7/8.

> **The narrative record — every slice, every review finding, every measurement — moved to**
> **`docs/history.md`** on 2026-07-27 (**D-064**). It is not part of the session-start read. This
> section states where the project IS; `docs/history.md` says how it got here.

## Open items
- Unresolved user questions: **none open** — the four `BUILD-SPEC.md` §5.11 questions were answered 2026-07-29 (**D-072**). *(The 2026-07-25 "todas las guías, en inglés y en español" instruction was scoped with the user the same day — see the deferred item below.)*
- Open Design Requests: **THREE.**
  **DR-006 — SENT 2026-07-29 (D-079)**, raised by stage 4 before its first line.
  `grid-template-columns` is delegated to the consuming screen by `template-list-table.md` §1 and
  recorded in `SPEC/manifest.md` for **one** of the thirteen list surfaces. Twelve have no widths;
  **three of those have no column list at all** (28 Licence, 35 Updates ×2) and cannot be built in
  any form. The prototypes cannot fill it — their tables carry different COLUMNS, and on Pages the
  drawing and the manifest disagree (six tracks vs seven). **Blocks twelve of the thirteen list
  surfaces**; entry 1 and the shared layer are complete and shipped. Ready-to-paste prompt: the
  last section of `docs/design/design-requests/DR-006.md`.
  **DR-005 — SENT 2026-07-29**, raised by stage 3's per-state accessibility
  pass. Three colour pairs the delivery **itself specifies** measure below WCAG 2.2 AA, in
  compositions the delivered 72-pair audit does not include: a semantic badge inside a selected
  table row (**4.44:1 light / 3.08:1 dark**), the code panel's specified `--texto-secundario` and
  `--color-acento` on `--fondo-ventana` (**4.46:1 / 4.23:1** light), and `--color-peligro` on
  `--fondo-elevado` in dark (**4.32:1**), which hits every error message and destructive button
  inside a card. **This is not the audit being wrong** — all 72 declared pairs were recomputed at
  the gate and 72/72 agreed; it is L-030 one layer down, a gate proving each pair correct without
  asking whether the set covers what the screens compose. The build invented nothing: the three
  elements are excluded **by selector** from the axe pass (each carrying its measured ratio and the
  DR number), every other element in those sections is still checked, and the rule itself is never
  disabled. Blocks nothing structural. **The user chose to send it at the start of stage 4**, before
  the first list screen was built, because stage 4 renders gap 1 on every one of them; the
  ready-to-paste prompt is the last section of `docs/design/design-requests/DR-005.md`.
  **DR-004 — SENT 2026-07-29 (D-077)**, raised by D-076. `template-shell.md` §1
  says the sidebar's search field "also works as a plain search submit with JS off", and **no file
  in the delivery names the screen that submit goes to**; this codebase has no admin-wide search
  surface either. Choosing one is inventing product behaviour (Phase 4 rule 2), so it is registered
  rather than guessed. It blocks **one control**: the specified half is built and verified, and the
  form carries no `action`. Three answers are offered, one of which is simply withdrawing the
  clause. **The user chose to send it**, and the ready-to-paste prompt is the last section of
  `docs/design/design-requests/DR-004.md`. It blocks one control; stages 3–6 proceed meanwhile.
  DR-003 is **RESOLVED 2026-07-29 (D-074)** — the sprite went
  **67 → 87 `<symbol>`s** (the 19 missing nav glyphs + `ks-menu`, 0 removed, geometry identical) and
  the sidebar's contents are now normative in the new **`SPEC/navigation.md`** (eight groups, 34
  items, glyphs, targets, counts, plugin placement, capability rule, and §8's three shell controls).
  `Blocks` → **Design**; `Guides` → **dropped**, so the manifest stays at 44. Exactly five files
  differ and one is added; `tokens/**`, `screens/**`, `accessibility.md`, `color-contrast-audit.md`,
  `design-tokens.md` and `manifest.md` are byte-identical, so §1c's 72/72 contrast recomputation
  stands. Evidence: `docs/BUILD-SPEC.md` §1e. **Stage 2 is unblocked.** DR-001 and DR-002 are
  RESOLVED — history below. DR-002 (the Dashboard) is **RESOLVED 2026-07-29 (D-071)** — Design specified it as **entry 44** (`index.php` · overview-stats · H1 "Dashboard"), the bundle was installed as a wholesale swap, diffed and re-audited, and the gate **PASSES** (`docs/BUILD-SPEC.md` §1d). Exactly four files differ from the archived delivery; `tokens/`, `assets/`, `screens/` and the four other SPEC files are **byte-identical**, so §1c's 72/72 contrast recomputation stands. **No twelfth template, no new glyph.** Two notes kept because they are the record: the first export placed at the staging path contained **nothing new** (byte-identical to what was installed — nothing was archived or swapped), and **one item still needs the user's word** — `open-questions.md` items 13–15 are attributed to the client, and item 15 moves the indexing toggle off the Dashboard into Settings → Advanced, a behavioural change to shipped code. DR-001 is RESOLVED 2026-07-29 (D-069) — see the register. It blocks `index.php` alone; the other 40 screens are unblocked. DR-001 is **RESOLVED 2026-07-29** (D-069) — see the register. History kept below because the gate's rejections are part of the record:
  - ~~**DR-001 — complete the handoff for the Klytos Admin redesign**~~ (sent 2026-07-27, `docs/design/design-requests/DR-001.md`). Two blocking gaps: `SPEC/accessibility.md` absent entirely, and the type scale contradicts itself between the README and the delivered tokens. Eight further gaps. **The build does not start until it is resolved** — no file under `installer/admin/` has been touched.
  **2026-07-28 — a RE-DELIVERY EXISTS and has not yet been audited.** Read over MCP from the Claude Design project (`6916aa0a-…`), `design_handoff_klytos_admin/` now carries `SPEC/accessibility.md`, `SPEC/color-contrast-audit.md`, `SPEC/design-tokens.md`, `SPEC/manifest.md`, `SPEC/assets-index.md`, `SPEC/open-questions.md` (declaring zero unresolved), eleven `SPEC/screens/template-*.md`, `assets/fonts/` with both `.woff2` **and** `OFL.txt`, `assets/icons/klytos-ui-icons.svg` (67 `<symbol>`s), 48 PNGs under `assets/png/`, and a new Klytos-owned `tokens/klytos-admin.css`. A size-level byte-stability pre-check against the local old bundle found **nothing changed outside the named gaps**: all 20 files under `screens/**` identical, the icon/favicon/seal SVGs identical, wordmark and lockup replaced by outlined versions with the live-text originals preserved byte-for-byte as `*-text.svg`, and the eight `tokens/*.css` grown consistently with added origin headers. **The real byte diff of `tokens/*.css` is still owed and needs the bytes locally.**
  **BLOCKED ON ONE THING:** the MCP `read_file` returns entity-escaped text and cannot carry the 48 PNGs or the two `.woff2` faithfully, so the delivery must be **downloaded by the user** from claude.ai/design. Then, in order: archive the current delivery to `docs/old/design-handoff/DR-001/` (move, never delete), install the new one whole at `docs/design/design-handoff/` (contract rule 10 — wholesale swap, never a file-by-file merge), diff it against the archived one, re-run the Phase 4 Step 1 gate with mechanical evidence into `docs/BUILD-SPEC.md` §1, recompute every contrast pair rather than trusting the delivered audit, and only then set DR-001 `Status: resolved`.
- Unverified external steps/assets: none
- Forge issues in progress: none open — see `docs/issues.md` (S-01/S-02/S-03/S-05/S-07/S-12 must NOT be filed as public issues before they are fixed; the repo is public — use a private security advisory or fix-then-disclose)
### Deferred items (consciously postponed work)
- **The four manifest entries Phase 4 does not build (D-072, 2026-07-29)** — **17 Setup wizard**
  (restructure `installer/install.php` 3 → 7 steps; product scope; NEW-04 makes it destructive in a
  checkout, so the slice designs a non-destructive driving strategy first; carries
  `template-wizard.md`, whose only consumer it is), **11 Verify** (split the pending-2FA branch out
  of `login.php`; **paired with NEW-38** so the OAuth consent 2FA gap closes in the same area),
  **14 Comments** (a new admin screen over the working `core/comment-manager.php`; L-014 records
  what this feature actually costs here), **22 Health** (no backing surface exists — the slice
  builds a data source before a screen). Each is **fully specified** — this is scheduling, not a
  gap, and no Design Request follows. They remain manifest entries and remain in
  `docs/BUILD-SPEC.md` §5.1; **the redesign is not reportable as complete while they are open.**
  Shape and reasons: `docs/roadmap.md` §0b. Review trigger: **the close of the Phase 4 build.**
- ~~**Storage isolation for the integration tier**~~ — **RESOLVED 2026-07-19 (D-030)**, before slice 3 as its trigger required. `tests/PlaygroundState.php` snapshots `installer/config/` + `installer/data/` before every integration test and restores them after, ON by default. Chosen over per-test fixtures because slice 3's own test point must delete the seeded owner to prove migration idempotency — a record a fixture does not own. Proven by `tests/Integration/PlaygroundIsolationTest.php`, which was demonstrated to FAIL with isolation disabled before being trusted. Four in-memory caches survive a file restore (`App::$config` has no invalidation path at all); the primitive names them and fails loudly rather than pretending to cover them.
- `playground-qa` subagent + `docs/playground.md` + the playground itself — severity HIGH — review trigger: Phase 5 scaffold (Sprint 1).
- ~~**Permission allow-lists per tool, CI workflow**~~ — **RESOLVED 2026-07-20, slice 9.** All four containers written (`.claude/settings.json`, `.codex/rules/klytos-tooling.rules`, `.cursor/cli.json`, `tools.allowed` in `.gemini/settings.json`) from the verified command set, with `install.php`, `cli.php build` (NEW-04) and `git push` deliberately excluded. `.github/workflows/ci.yml` written, seeding the playground and promoting a skip to a hard failure. **MCP dev-server registration remains genuinely n/a.** Original note follows. — trigger had FIRED (found by the v3.4.0 reconciliation's MANIFEST Table 1 parity check, 2026-07-19). The blocker they waited on is gone: the playground exists (slice 0) and the verified commands exist (`composer install`; `XDEBUG_MODE=off vendor/bin/phpunit`; `vendor/bin/phpcs --standard=phpcs.xml`; `php scripts/keel-verify`; `XDEBUG_MODE=off bash scripts/dev/upgrade-test.sh`). Still missing: **all four** allow-list containers (`.claude/settings.json`, `.codex/rules/`, `tools.allowed` in `.gemini/settings.json`, `.cursor/cli.json` — only the personal, gitignored `.claude/settings.local.json` exists) and the **CI workflow** (`.github/workflows/` does not exist; the forge is GitHub, so CI is available). MCP dev-server registration remains genuinely n/a — the technical plan describes Klytos's *own* MCP server as a product surface, not dev servers to register. New review trigger: **Sprint 1 close**, with slice 9 (which already owns `scripts/keel-verify`) as the natural home for the CI workflow, since CI must run the same commands the test points run.
- ~~**`docs/flows/` does not exist**~~ — **RESOLVED 2026-07-20, slice 9**: three targeted flows created (`mcp-tool-call.md`, `admin-authentication.md`, `public-comment.md`), chosen as the journeys Sprint 2 needs a documented failure/recovery branch for. A full retroactive flow set was rejected with the user as an as-built documentation exercise competing with security work. Original note follows. — **`docs/flows/` did not exist** — found by the same parity check. Unconditional from Phase 2 in Keel's MANIFEST Table 1, and the project is at Phase 5. Phase 2 was adopted as-built (D-001), which is *why* it is absent: adoption reconstructed `01`/`02`/`03` and `docs/api/INDEX.md` but never split the journeys into per-flow files. Severity LOW while Sprint 1 runs — Phase 5 §2's test point wants "the failure/recovery branch from its flow file", and slices 0–4 substituted the audit findings for that. Review trigger: **Sprint 1 close**, or the first slice whose acceptance genuinely needs a documented failure/recovery branch.
- **`.cursor/agents/` is NOT a gap** — verified against D-011, which records that Cursor reads `.claude/agents/` natively. The parity check flagged it; the decision governs. Noted here so the next check does not re-raise it.
- `launch-verifier` subagent — review trigger: Phase 8 start.
- `guide-qa` subagent + `guide/` — review trigger: Phase 6.
- `design-fidelity-auditor` — not generated: no BUILD-SPEC exists (D-009). Review trigger: the first redesign that goes through Phases 3–4.
- `docs/estimate.md` — **v1 (Sprint 1) 2026-07-18; v2 (Sprint 2) 2026-07-22**. Still deferred: an estimate covering the theme-package sprint (D-023) and the vendor-ai CVE re-vendor (D-029). Review trigger: when each of those is planned.
- Copilot subagent `model:` binding — omitted, not guessed: the accepted values are undocumented (D-011). Severity LOW — the agents run on the session model meanwhile. Review trigger: when GitHub documents the accepted identifiers.
- Collaborator setup line for the pre-commit gate (`git config core.hooksPath .githooks`) not yet documented in the repo's development notes (D-015). Severity MEDIUM in a public repo — contributors' clones have no gate. Review trigger: audit H-bucket triage.
- ~~**Sprint 2 — MCP tool authorization**~~ (NEW-02; D-020) — **CLOSED 2026-07-24, all 4 slices** (D-046…D-051, Estimate v2, `sprints/sprint-2.md` with its close-out table filled). Actor resolution from the credential → the default-deny gate in `ToolRegistry::call()` + central capability map + `tools/list` filter + keel-verify check 10 → coverage completeness (loader fails loudly, integrity + x402 + both shipped plugins gated, NEW-30 resolved) → reconciliation (count truth, the closed forward reference, the refusal translated ×20, 8 skills, `ai.use` widened to editor per D-051). **Audit NEW-02 is CLOSED.**
- ~~**`vendor-ai/` CVE remediation slice**~~ (NEW-05, D-029) — **RESOLVED 2026-07-25, Sprint 3 slice 1.** `composer audit -d installer` reports **zero**, measured both sides (11 → 0). guzzle 7.10.0→**7.15.1**, psr7 2.9.0→**2.13.0**, promises 2.3.0→**2.5.1**, plus a new **`symfony/polyfill-php80`** required by both (16→**17** packages, 482→**509** tracked files). **D-029's recorded floors would have left 6 of 11 open** — its two halves ("bump to ≥7.12.1/≥2.12.1" and "re-audit to zero") had come apart as upstream published more advisories, and the criterion won over the derivation (L-014), a user decision recorded as **D-052**. **The reachability assessment was redone from scratch and retires the NEW-15 worry**: `safe-http.php` and `http-client.php` contain **zero** `GuzzleHttp`/`Psr\Http` references, so the two HTTP stacks never share a URL and the differential-parsing precondition for the host-confusion CVEs does not exist here. Stated plainly: **none of the 11 had a demonstrated exploitation path in Klytos** — the bump closed a standing obligation, not a live hole. Two corrections of record: the change is **95 files**, not the 482 D-029 implied (that was the tree size), and the drift guard's root-package skip was hardcoded to `'__root__'` and had **never been exercised**, because the guard was built against a tree vendored elsewhere and never regenerated here (**L-020**). Estimate **v3** written.
- **NEW-27 / NEW-28 / H-02 — one `.gitattributes` review** (found 2026-07-20, slice 9). The blanket `*.md export-ignore` strips all 16 in-product guides (a live MCP surface), plus `README.md` and `INSTALL.md`; `scripts/` is not export-ignored so dev scripts ship to the web root. Both are made **detectable now** by keel-verify WARN checks rather than fixed. Severity HIGH for NEW-27 (a released install has no guides at all). Review trigger: **Phase 7**.
- **NEW-31 — the AI chat's panels had no permission check of their own** — **FIXED 2026-07-24 in slice 4** (panel→capability map in `ai-chat.php`, absent = deny; `get_providers` gated at `site.configure`). Recorded here because the *class* of defect recurs: a privileged partial trusting the file that included it. No open work.
- **NEW-32 — the authorization audit hooks have no listener** (`mcp.access_denied`, `auth.access_denied` fire; nothing subscribes, so refusals write nothing to the log). Severity MEDIUM — an authorization system that refuses silently cannot be operated, and the sprint's own "paste the debug log" hand-off returns nothing for exactly the failures it built. NOT fixed at close: "log every refusal by default" carries real volume and content decisions (an anonymous caller can drive refusals; the entry names a role and a capability). Review trigger: **the next slice touching logging or the audit trail** — the NEW-11 half of that trigger **fired and was answered NO** at the Sprint 5 kickoff (**D-057**, user decision): logging every refusal by default carries volume and content decisions that belong to a slice of their own, not to a rider on an authentication sprint. Stated rather than left implicit, because Sprint 5 produces exactly the refusals nobody can currently read.
- **The 16 in-product guides must ship in English AND Spanish** — **user instruction, 2026-07-25, scope confirmed with the user the same day** (the alternative reading, the Phase 6 end-user guide, was ruled out; that one is created bilingual when Phase 6 runs). Scope: `installer/core/guides/` — 16 files, currently English-only, read at runtime by `klytos_list_guides` / `klytos_get_guide`, whose own tool descriptions declare five of them REQUIRED before creating page content. **Not started, and it is a scope change with its own decision entry and estimate version.** Three things must be settled by whoever plans it, stated now so they are not discovered mid-slice: (1) **the loader takes no locale** — there is no language-selection mechanism for guides today, so this is new behaviour on an MCP surface, not a translation chore; (2) **NEW-27 comes first or the work ships nothing** — the blanket `*.md export-ignore` strips all 16 from every release archive, so on a real install both language sets would be absent, and translating before fixing that duplicates content nobody receives; (3) content parity has to be mechanically checkable or the two sets drift, which is the L-004 defect at 16× — the locale-catalogue parity check in `scripts/keel-verify` is the obvious model. Does **not** conflict with D-006/D-008: those fix English as the base language of the product and of `docs/`, and say nothing against shipping translated guide CONTENT, which is product output. Review trigger: **with the Phase 7 `.gitattributes` review that owns NEW-27**, or sooner on the user's word.
- **NEW-38 — the OAuth consent screen cannot complete a 2FA login** (`core/mcp/oauth-authorize-view.php:91` is the second of the two `Auth::login()` callers repo-wide and has no second-factor branch of any kind; on the 2FA path `success` is `true`, so its only check is not taken and the screen selector — which asks `isAuthenticated()`, false while pending — silently re-renders the login form with no error). Severity MEDIUM. **Pre-existing; Sprint 5 slice 1 widens its population** from "a 2FA-enabled owner" to any 2FA-enabled account, which is stated in the audit entry rather than implied. Fix shape recorded (reuse `login.php`'s dispatcher via a `redirect_to` back to the consent URL rather than forking a second 2FA UI). Review trigger: the next slice touching the OAuth authorization flow, or the first report of an MCP client that cannot be authorized.
- **NEW-33 — the terminal/CLI's strings are hardcoded Spanish** (`terminal-executor.php`), against the recorded English-base rule (D-006). Severity LOW. Review trigger: the next slice touching `terminal-executor.php`, or the NEW-18 resolution that owns the underlying `__()` reachability problem.
- **NEW-35 — MCP tool input schemas are advertised but never enforced** (`ToolRegistry::call()` runs the authorization gate, the plugin filter and the handler, and never validates `$params` against `inputSchema`), **and a second URL interpolation rides on that** (`ai-image-generator.php:62`, reachable over MCP via `klytos_generate_image` at `assets.manage`, with a declared `enum` that looks like it prevents exactly this). Severity LOW/MEDIUM. The systemic half matters more than the instance: fixing the interpolation alone leaves every other tool's published contract unenforced — the by-omission shape S-07 and NEW-02 both exist to close, on a third axis. NOT fixed: enforcing schemas is a **behaviour change for every MCP client** and needs the D-034 treatment (recorded decision + release note). Review trigger: the next slice touching `ToolRegistry::call()` or the MCP tool surface, or the NEW-34 slice — same defect class, should close both together.
- **NEW-34 — the AI chat's `model` parameter is unvalidated and reaches the provider URL** (`admin/api/ai-chat.php:168` → the Gemini URL's path segment). Severity LOW: measured, not reasoned about — a `#` pushes `key={apiKey}` into the URI fragment so the request leaves with no API key (401), a `?` truncates the path, and the **host never moves in any spelling**; CRLF is percent-encoded. Denial-of-function, not a boundary crossing, and the population is **editor and above** (`ai.use` per D-051). Fix shape recorded so it need not be re-derived. Review trigger: the next slice touching `installer/admin/api/ai-chat.php` — the NEW-11 half **fired and was answered NO** (D-057): Sprint 5 does not open `ai-chat.php`, and NEW-34 should close with NEW-35, which is the same defect class.
- **`README.md`'s per-module tool table is stale** — 34 rows summing to **177** (the pre-slice-3 figure) with row labels that no longer map to the loader's files. The one plainly-false number ("33 tool modules") was fixed in slice 4; regenerating the table is an editorial pass on the public README belonging to **Phase 6 / D-017**. The measured per-file counts are recorded in `docs/04-adoption-audit.md` D-05 so it need not be re-derived.
- ~~**NEW-06 — no PHP-version guard on the AI stack**~~ — **RESOLVED 2026-07-25, Sprint 3 slice 2 (D-053)**, in the D-029 remediation sprint exactly as this item predicted. `App::getChatEngine()` now refuses **above** the vendored `require_once` with a typed, translated `Klytos\Core\Ai\UnsupportedRuntimeException`, instead of letting Composer's `platform_check.php` send HTTP 500, echo third-party text into the response body and throw from inside vendored code. The **ordering** is the load-bearing part and is pinned by a test that reads the source, because on a supported host no runtime test can reach that branch — proven by relocating the guard below the require and watching the test fail. The policy is a pure `App::aiRuntimeUnsupportedReason( int )` (the D-044 split) since PHP cannot be downgraded in the suite; the floor is written once and a test asserts it matches what Composer generated. `ai.runtime_unsupported` is an **action** with **no core listener**, said in those words per L-019. **It does NOT raise the product's PHP floor** — reconciling the four floors this codebase asserts stays with D-027's trigger.
